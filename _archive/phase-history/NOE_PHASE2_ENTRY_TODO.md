# Noe 阶段2 准入 TODO（显式登记，防 advisory 长期悬空）

> 本文件只登记"阶段1判定为非阻塞、但进入阶段2前必须收口"的事项。
> 不覆盖 `NOE_PHASE1_目标契约_CANONICAL.md`（目标事实源）与 `NOE_BAILONGMA_ARCH_AUDIT.md`（阶段2待复核草稿）。
> 来源：集群协同对方成员（codex）上一轮 3 条 suggestions 的逐条收口。

## 阶段1 判定回执（已达标，下列仅为非阻塞遗留）

- `node NOE_PHASE1_GATE.mjs` → 合成终判 **PASS**（verify:phase1=13/13 · m1=8/8）。
- C13 目标复述卡 SHA-256 冻结锁未漂移；端口 51835/51735 物理隔离已活体实测。

## 必须在进入阶段2"逐章复核审计稿"之前收口的 TODO

- [ ] **T1 — 数据库 schema 字段级核对（对方 suggestion #1）**
  审计稿 §3 对 `focus_absorbed` / `merged_into` / `embedding BLOB` / `memories_fts(trigram)`
  等字段目前是**基于阅读 `BaiLongma-audit/src/db.js` 的推断**，C10 只验证了 12 个模块"实存"
  而非"逐字段准确"。复核时必须打开镜像 `de78c6f` 的 db.js 做字段级核对，
  **不得把草稿描述当既成事实写进 Noe 设计**。验收：核对结论回写审计稿对应小节并标注行号。

- [ ] **T2 — F13/F14 审计稿卫生 advisory 收口（对方 suggestion #2）**
  verify:phase1 C8 中 F13/F14（审计稿卫生热点）当前为 advisory、不 gate 阶段1。
  进入阶段2前显式收口：要么修复热点，要么在本 TODO 标注"接受现状 + 理由"，
  避免"非阻塞"无限悬空。验收：C8 输出里 `待修=` 清空，或本条标注 accepted。

- [ ] **T3 — 审计稿基线快照锁（判定方 suggestion #2：防多成员并发覆盖导致复核基线漂移）**
  本轮实测此文件正被并发实时覆盖（同一窗口内 437 行 → 98 行），是历轮 gate 抖动的真根因。
  阶段2"逐章复核"必须钉一个基线，复核结论以该基线为准，不被中途覆盖污染。
  **进入阶段2前第一动作**：重算并登记基线，复核期间任一成员改写审计稿须先比对此 SHA、
  在本条追加变更记录，禁止静默覆盖。
  - 基线时间：2026-06-01（Claude/xike-builder 现场实测）
  - 基线行数/字节：**98 行 / 7050 B**（行数仅供参考，已非 gate 判据，见下）
  - 基线 SHA-256：`1fa04463628230ee9ccc6e7218e3c77cf3bcbca601f4c532812171534a9374ea`
  - 复现：`shasum -a 256 NOE_BAILONGMA_ARCH_AUDIT.md`（若与上值不同 = 已被覆盖，复核前先对齐）
  验收：阶段2开工时此 SHA 与磁盘一致，或差异已在本条留痕说明谁、何时、为何改。

- [x] **T4 — 根因修复：C4 解耦审计稿行数（本轮 Claude/xike-builder 实测复发后修复）**
  实测 gate 因审计稿被并发覆盖到 98 行 < C4 旧阈值 `lines>=100` 而 **FAIL(12/13)**——
  证明"≥100 行"仍耦合到热点文件。已把 C4 改为对覆盖鲁棒的双判据
  **字节≥2000 且 markdown 标题≥3**（空壳截断仍 FAIL，437/98 行真实草稿均稳定 PASS）。
  修复后连跑验证 gate 恢复确定性 PASS（13/13）。这是 5 轮"都说 PASS 却不收敛"的真根因之一。

## 已完成（无需再做）

- [x] **对方 suggestion #3 — M1 从文档门槛升级为真实启停活体证据**
  `NOE_M1_ISOLATION_SMOKE.mjs` 已实跑 8/8：51835 真 LISTEN(PID 自校验) → HTTP 200 →
  owner-token 守卫 401 → 干净停回归还端口；51735 PID 13768 起测前/运行中/停止后三采样全不变。
