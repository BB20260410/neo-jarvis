# HANDOFF 双窗口总交接（强健工程 + 立项卡/CI 接力）　2026-06-10 中午
# ⤷ 2026-06-10 下午由窗口 C 续写：§4 队列执行记录见文末 §7（巨石拆分三线开工，CI actions 升级完成）
# ⤷ 窗口 C 全天收口（15:30）：`HANDOFF_2026-06-10_窗口C收口交接.md`（修复×4/强健五轮/launchd/语音双修/运维变化/新踩坑/下窗队列）

<!-- ═══ 接手提示词（Codex / Claude 通用） ═══ -->
> **你（接手的 AI）开工前 60 秒做三件事：**
> ① 读根目录 `AGENTS.md` 的「开发纪律宪法」（10 条，与任何旧文档冲突时以它为准）；
> ② `git status` + `git log --oneline -8` 看清当前态——本项目常态是多个 AI 窗口并行开发，**只动你任务域的文件**；
> ③ 确认质量门活着：`git config core.hooksPath` 应输出 `scripts/git-hooks`。
>
> **干活节奏**：领任务 → 小步改（新文件带 `// @ts-check`+注入式+单测+env 门控默认 OFF）→ 每个独立改动立即 commit（中文消息）→ 实机自测（隔离端口 51998，**绝不碰 51835/51735**）→ 里程碑 push（pre-push hook 自动全量测试，远程 CI 两平台把门）→ 更新 HANDOFF。
> **绝不**：凭印象断言机制存在（先 grep/实测）、让 owner 当测试员、在另一个 AI 活跃的文件上动刀。
<!-- ═══════════════════════════════════════ -->

## 0. 一句话现状

**代码侧全部收口：working tree 干净、本地 2327 测全绿、远程 CI 两平台 success（b78c88e）、五张立项卡+九项强健工程全部落地。生产 51835 仍未重启——所有新能力都在等这一下。**

## 1. 窗口 A（强健工程窗口）完成的事

> 证据：commit bc52d87 / 7b03d4d / 4a0b500 / b54f4e0 / b0f90b7 / 6eae813

| # | 项 | 状态 |
|---|---|---|
| 1 | CI 门禁（GitHub Actions `panel quality gate`，noe-main 分支，macos-14+ubuntu 双平台：lint baseline→vitest→npm audit→e2e） | ✅ 已激活（最后一公里由窗口 B 修绿，见 §2） |
| 2 | 记忆库自动备份（panel.db 启动 90s 后+每 24h 快照，保留 7 份，`~/.noe-panel/backups/`） | ✅ 实机已见快照日志 |
| 3 | 崩溃自愈 launchd（`docs/launchd/com.noe.panel.plist`+安装说明） | ⏳ 产物就绪，**等 owner 手动安装** |
| 4 | 类型渐进（jsconfig.json + checkJs；新文件 `// @ts-check` 入宪法） | ✅ |
| 5 | 属性测试（fast-check 进依赖+`property-invariants.test.js`；上线即抓到 budgetUsageRatio denormal 溢出真 bug，窗口 B 修） | ✅ |
| 6 | 优雅停机（SIGTERM flush/关库/断 WS） | ✅ |
| 7 | npm audit 进 CI | ✅ |
| 8 | 开发纪律宪法（AGENTS.md/CLAUDE.md 十条 + git hooks：pre-commit=staged lint、pre-push=全量 vitest）+ RUNBOOK | ✅ |
| 9 | 积压 commit 按域拆批 + secrets 自查 + push（180+ 文件多窗混合态收口成 6 个分域 commit） | ✅ |
| — | 巨石拆分（app.js 13763 行/server.js ~5100 行/style.css 7403 行，D1/D2/D3 债） | ❌ 未动（既定 S23-S25 路线，留给后续） |
| — | 前端测试补缺 | ❌ 未动（随拆分做） |

## 2. 窗口 B（立项卡+CI 接力窗口，即本窗）完成的事

> 证据：commit e977f53 / 9198a22 / d321593 / 891abf2 / b78c88e；明细 `docs/后续规划_2026-06-10.md` §七

**五张立项卡全落地（每张带实测证据+质量门四连）：**
1. **卡① sherpa-onnx 语音实时化**：STT 从 whisper 3-7s → 40-110ms（纯 CPU 不抢 GPU）；唤醒词"嘿Noe"（KWS，正样本 3/3、硬负样本仅同音词 1 误触）；流式接口 7ms 出首字；`NOE_STT=auto` 自动启用、whisper 兜底；新端点 `/api/noe/voice/wakeword`。**抓修真 bug：VoiceVad 预处理裁渐弱尾音 → sherpa 默认关预处理（e977f53）**。
2. **卡③ RapidOCR 屏幕读字**：独立 venv `~/.noe-panel/ocr-venv`；截屏 1s 内 66 行中英混排全对（连长 URL 逐字对）；`VisionSession.ocr()` + `/api/noe/vision/ocr`。
3. **卡④ Playwright**：结论=MCP 形态够用（真开 Chrome 全链验证），350MB 完整包不装。**治本 npx 卡死**：本机 npx 在线解析 latest 必死（npmmirror+代理 TLS 断）→ 三个 MCP server 固定装 `~/.noe-panel/mcp-bin/`、配置改 node 直调（备份 `.bak-20260610-npx形态`）；playwright 23/filesystem 14/memory 9 工具全部真连。
4. **卡② CosyVoice 中文 TTS 兜底**：`~/.noe-voice/cosyvoice`（venv+300M-SFT 模型）；`scripts/noe-cosyvoice-server.py`(8125) + `CosyVoiceTtsClient` + **VoiceSession._synthesize 统一回退链**（主选→MiniMax→含中文才 CosyVoice）；断 MiniMax 实测兜底出声、sherpa 听回逐字对；试听样本 `~/.noe-voice/cosyvoice/试听样本_中文女.wav` **等 owner 实听拍板**。
5. **卡⑤ session rotate**：聊天房超 `NOE_ROTATE_TOKENS`(默认 24k) 标 rotateSuggested→前端「🔁 轮换交接」按钮→`POST /api/rooms/:id/rotate`（finalizeTurn 凝交接→新房"·续N"注入→旧房 rotatedTo）；真浏览器全链验收过。

**CI 接力修绿（窗口 A 推到 CI 红灯循环，本窗三轮剥洋葱）：**
- 第 1 轮 d321593：`/bin/zsh` 硬编码→SHELL_BIN 回退 bash（ubuntu 无 zsh，spawn ENOENT 全链 409）；测试写死目录名"Neo 贾维斯"→改模块位置推导；keychain 测试注入 platform+补 linux 分支测试。
- 第 2 轮 891abf2：两个 marketplace 测试 spawn 断言也写死 zsh→导出 SHELL_BIN 同源断言；全仓扫净。
- 第 3 轮 b78c88e：vitest 转绿后 e2e 暴露 runner 裸机无 Playwright 浏览器→workflow 加 `playwright install chromium`+跨平台缓存。**终态：两平台 success。**

**项目命名调研**：十个中英候选已交 owner（GitHub 实搜查重+国内同名验证），主推「守烛 / Keepwick」、次推「枢密 / Privyhall」，等 owner 拍板；定名后涉及 package.json productName/README/品牌区改名，是独立小任务。

## 3. 等 owner 的三件事（AI 替代不了）　⤷ 2026-06-10 下午更新

1. ~~**重启生产 51835**~~ ✅ 已做并由 launchd 接管（14:xx owner 授权"帮我去做"，窗口 C 执行）。当前生产 = launchd 服务 `com.noe.panel`，跑全部最新 commit。
2. **听试听样本**：`~/.noe-voice/cosyvoice/试听样本_中文女.wav`，CosyVoice 音质过不过 owner 说了算。（仍待）
3. ~~**装 launchd 崩溃自愈**~~ ✅ **已装并真演练通过**（窗口 C）：`launchctl load` + `kill -9` 模拟崩溃，2 秒内自动拉起新 PID、服务恢复可达。**首装踩坑已修**：plist 原写死 `node server.js`，但登录 shell 默认 node 是 26（nvm default），better-sqlite3 原生绑定按 22 编译 → `ERR_DLOPEN_FAILED` 崩溃循环；改为经 `scripts/ensure-node22.mjs` 守卫定位 Node 22 启动（已提交）。卸载/重启服务命令见 `docs/launchd/安装说明.md`。**注意**：装了守护后手动 `npm start` 会撞端口，二选一；改完 server.js 用 `launchctl kickstart -k gui/$(id -u)/com.noe.panel` 重启。
4. **真嘴语音体验**（仍待）：说一句验 sherpa 延迟实感 + "嘿Noe"唤醒词误触（立项卡①验收的"人工测 10 次"部分）。
5. ~~**决定是否开两个新功能开关**~~ ✅ 已开（owner 授权）：`.env` 已加 `NOE_SILERO_VAD=1`+`NOE_MEMORY_DEDUP=1`，生产启动日志见 `[noe-vad] 已启用`，记忆去重开关实测 enabled。要关删 .env 对应行重启即可。

## 4. 下个窗口的任务队列（按优先级）　⤷ 2026-06-10 下午窗口 C 执行后状态

1. **第 0 项 · 生产验证** — ✅ 完成（11:59 生产 51835 重启跑新代码；启动方式为孤儿化 node 进程而非 launchd，自愈仍未装，见 §3 勘误）。生产实测：启动日志四标记全亮（`~/.noe-panel/logs/server-20260610.log`）/MCP 三 server 工具数 23/14/9/self-knowledge 块含五卡全部新能力（sherpa/嘿Noe/CosyVoice/OCR/media）/审批同指纹复用（2d2153d）生效——listTools 在 TTL 内免重复审批。此前 51998 隔离预演同清单亦全绿（含 90s 备份快照真落盘+SIGTERM 优雅退出）。**仅剩 owner 体验项**：真嘴说话验 sherpa 延迟实感+唤醒词误触（人工测 10 次）、CosyVoice 试听拍板。
2. **CI actions 升级** — ✅ 完成（checkout v6/setup-node v6/cache v5/upload-artifact v7，经 releases API 核实均 Node24 运行时；远程 CI run 27251064980 两平台 success）。
3. **定名落地** — ⏸ 外部阻塞不变：等 owner 在十个候选里拍板（主推「守烛/Keepwick」）。
4. **巨石拆分 D1/D2/D3** — 🟢 三线开工且各有实质落地（明细 §7）：server.js 4985→4407（S23 四域 15 路由出仓）/ app.js 8528→7831（S24 第6批四区外迁）/ style.css 7522→16 行壳+11 段（S25 完成）。剩余账：S23 还有 sessions（~17 路由）/rooms-advanced（~12 路由）/ops 三域，S24 还有聊天室（1418 行）/Chat房（1713 行）等大区。
5. **卡①后续可选** — 前端唤醒词接线已被并行窗口自做（29c0cf3 C10）；WS 推流（createStream 边说边出字）仍开放。窗口 C 主动避撞未碰语音域（VoiceSession/noe-voice 当时正被另一窗活跃编辑）。

## 5. 关键认知/踩坑速查（新窗口省命用）

- **CI 红但本机绿 = 环境依赖**，本机永远复现不了，只能读 CI 日志反推。已知模式：路径写死本机目录名 / macOS-only 假设（zsh、keychain、security CLI）/ 依赖 `~/.noe-panel` 本机配置 / 缺浏览器二进制。写测试时主动避开这四类。
- **npx 形态在本机必死**（npmmirror+代理 TLS 断、静默卡死）：MCP server 一律固定安装+node 直调；npm 装包用 `--registry=https://registry.npmjs.org`。
- **大模型下载**：先试 hf-mirror 直链（有缓存 6MB/s）；404/308 跳官源就走代理下 huggingface.co；modelscope git-lfs 最慢（~1.5MB/s）兜底；下完逐文件对 content-length 校验。
- **VoiceVad 预处理会裁渐弱尾音**：sherpa STT 已默认关；其他消费方接 VAD 时注意同坑。
- **跑模型不设超时**（owner 纪律）：OcrClient/CosyVoiceTtsClient 均默认无超时，新 client 照办。
- **VoiceSession 契约**：STT `transcribe(wav)→text`；TTS `synthesize(text,opts)→{audioBuffer,format}`；TTS 失败回退统一走 `_synthesize`，链尾按需扩。
- **新能力必登记** `src/context/NoeSelfKnowledge.js`（活态检测函数+capabilities 条目），否则 Noe 不知道自己有这本事。
- **测试端口纪律**：51998 隔离实机；51835 生产、51735 母项目，绝不碰。
- 伴生服务速查：whisper 8123 / kokoro 8124 / **cosyvoice 8125**（`~/.noe-voice/cosyvoice/.venv/bin/python scripts/noe-cosyvoice-server.py`）；sherpa/OCR 免服务。
- 新 env 速查：`NOE_STT=auto|sherpa|whisper`、`NOE_WAKEWORD_THRESHOLD=0.25`、`NOE_COSYVOICE=0`、`NOE_OCR_PYTHON`、`NOE_ROTATE_TOKENS=24000`。

## 6. 文档索引

- 立项卡执行明细：`docs/后续规划_2026-06-10.md` §七
- 上一份入口（已完成标记）：`docs/HANDOFF_2026-06-10_下窗口入口_立项卡执行.md`
- 立项卡原文：`docs/立项卡_重依赖集成_2026-06-10.md`
- 宪法：根目录 `AGENTS.md` / `CLAUDE.md`；出事翻 `docs/RUNBOOK_出事翻这页.md`
- launchd 安装：`docs/launchd/安装说明.md`

## 7. 窗口 C 执行记录（2026-06-10 下午，§4 队列）

> 证据 commit：3563907(CI升级) / 01376dd(plugins) / b40256b(term) / 5f84bb2(files) / 191810c(projects) / 57fc285(S25) / 09595f7(S24第6批) / 2144f37(死import清理)。全程与另一活跃窗口（媒体/语音/审批复用域）并行，零冲突——靠：只 add 自己文件、server.js 手术原子化（python 读改写一步完成）、域级避撞。

**S23 server.js 4985→4407（-578）**：四个低耦合域按既定 `register<Name>Routes(app, deps)` 模式出仓，每域=新模块+注入式+单测+51998 实机 smoke+独立 commit：
- plugins（6 路由+PluginRegistry，注入式 registry 可换假）→ `src/server/routes/plugins.js` + 7 测
- term（3 路由+terminals Map 返回给 server.js——WS 升级/健康路由/优雅停机三处共享不变）→ `term.js` + 假 pty 3 测
- files（files/file/browse/search 四路由，sessions Map 注入）→ `files.js` + 4 测
- projects（PROJECTS_ROOT+scanProject 全家+2 路由，整域自包含）→ `projects.js` + 3 测
- **旧账修正**：audit 里"B7 删 177 行 dead code 注释"已不存在（grep 实证无 12 行+连续注释块），属过期记录。
- **剩余**：sessions 域（~17 路由，sessions Map/broadcastSession/debouncedSave 深耦合）、rooms-advanced 域（~12 路由，含 chat/forward/resume 等 dispatcher 重耦合）、ops 域（metrics-health/report 异步 job）。建议每域独立窗口做，照本窗模式。

**S24 app.js 8528→7831（-697，模块化第6批）**：知识库/本地审批中心/委派中心/房间时间线四区外迁 `public/src/web/{knowledge,approvals,delegation,timeline}-ui.js`（PanelCore 桥模式，照 overview-ui 先例）。关键点：
- 桥上 openApprovalModal/openDelegationModal 改**懒转发函数**（`(...a) => window.PanelApprovals?.openApprovalModal?.(...a)`）——外部模块 boot 时 destructure 拿到的是转发器，与新模块 boot 顺序无关，防竞态。
- app.js 内 WS 两处 `handleApprovalRequired(msg)` 与启动 deep-link 改 `window.PanelApprovals?.` 安全访问；桥新增 promptModal/shortLineageValue。
- 验证：结构防回归测试 5 项（tests/unit/appjs-migration-batch6.test.js）+ 真浏览器实测（四全局就位/四模态真开渲染非空/桥转发活/零 pageerror）。
- **剩余大区**：多 AI 聊天室（2687-4104，~1418 行）、Chat 房 1v1（4293-6005，~1713 行）、Plugin 中心/体验优化/房间模板/Codebase Center（各 200-460 行）。小区可继续按第6批模式批量走；两个大区与 ws/state 深耦合，建议单独窗口。

**S25 style.css 7522→16 行壳（完成）**：按节边界保序切 11 段 `public/css/style-*.css`，主文件改 @import 壳——引用方零改动、级联顺序不变。验证：**11 段按序拼接与拆分前逐字节一致**（python 校验）+ 实机 11 段全 200 + Playwright 截图目检样式正常 + e2e 与未拆基线对照同分（8/27 均为本机状态噪声，CI 干净环境为准）。段间空行已按 pre-commit 空白规则归位（移到下段头，拼接不变）。剩余债：07c-agent-graph 1229 行等 4 段 >500 行，照同法继续细拆即可。

**质量门全程**：每 commit 过 pre-commit lint；push 前全量 vitest（最终 298 文件/2396 测全绿，含并行窗口新增）；eslint baseline 0 error 0 warning（顺手清了 S23 残留 3 个死 import）；远程 CI 两平台见 push 后 run。

**强健工程第二轮（13:10-13:30，owner /loop 授权"按建议依次执行+检验"）**：chat-profiles bug 顺藤摸瓜全量审计 27 个 Store + 生命周期 + 运行时三维度（3 个并行 agent + 人工复核高危项）。落地：① `src/state/atomicJsonFile.js` 共享原子写 helper（tmp+rename / .bak-latest 一代备份 / 损坏 .corrupted 兜底，从 ChatRoomStore 实战写法提炼，6 测）；② 6 个裸写 Store 全部接入——高危 Mcp/Webhook/KnowledgeIndex（写一半被 kill=配置全损无备份的老路堵死）+ 中危 TaskFlow（损坏不再抛崩，备份+null 降级）/Skill/Archive，构造补 file/kbDir 注入口，11 项损坏注入防回归测试；③ 三个一行修：prompts 写盘失败不再静默回 200、autopilot dry-run 补 owner-token（51998 实测 401/200）、uncaughtException 显式 kill 子进程（原注释吹"提前发 SIGTERM"但从未实现）。全量 2449 测绿、lint 0/0、51998 实机四类端点 smoke 全过。**勘误**：并行 agent 曾报"PID 3774 是 13h 孤儿进程"——实测它监听 51735 是母项目 xikelab 的 launchd 服务，绝不能杀（验证后才采信，宪法第 5 条又立功）。**Store 现状**：金标准 ChatRoomStore；达标 OwnerIdentity/PersonKnowledge/OwnerGate/ChatProfile + 本轮 6 个；SQLite 系天生原子；全部消灭裸写。**仍待 owner**：重启 51835（累计 chat-profiles 修复+亲密模式提示词+本轮强健全部在等）+ 装 launchd 自愈。（⤷ 已全部完成，见 §3）

**强健工程第三~五轮（13:45-14:20，/loop 续）**：每轮=改+测+实测+生产 kickstart+CI 绿。③ gracefulShutdown 补全：MCP 断开改 await（旧 fire-and-forget 紧跟 exit→子进程孤儿，owner 痛点同源；1.5s race 上限）+ 停机关三类 WS 池；51998 实测 SIGTERM 后 MCP 子进程零残留；4 项接线防回归。④ CommitmentStore 落盘（`~/.noe-panel/commitments.json`，复用原子写 helper）：用户"提醒我…"承诺重启不丢；默认纯内存向后兼容（既有 28 测不破）+7 持久化测+真跨进程实证。⑤ 回滚失败留痕：roomStart degrade 两步 + roomRequirements 回滚 update 的空 catch{} 加日志（治"回滚又失败→中间态零痕迹"排障黑洞）+2 结构防回归。**诚实不做**：noe.js 路由层 body cap（express 10MB+MemoryCore 10万字已兜底，加了是重复）、evidence-links 竞态（单线程同 tick 概率极低）——记为可选不堆边际代码。**运维变化（重要）**：生产现为 launchd 服务 `com.noe.panel`（已 kill -9 双场景演练 2s 自愈零孤儿），改完代码用 `launchctl kickstart -k gui/$(id -u)/com.noe.panel` 重启，手动 npm start 会撞端口。

**追加修复（12:25，owner 报障"改了模型配置/提示词重开就还原"）**：根因=`ChatProfileStore._load()` 对 builtIn 行无条件 `continue`——upsert 落盘成功但每次重启被代码默认值覆盖、下次保存再把默认值写回文件（存取不对称）。修复 81975ca：加 `customized` 单向粘滞标记，改过的内置档文件值赢、没改过的继续跟随代码默认；6 项持久化防回归测试+51998 实机重启验证。owner 实损两轮数据（default 档 lmstudio/gemma/16384、m3_assistant 档自定义人设）已从 `.bak-latest` 合并修回 `~/.noe-panel/chat-profiles.json`（恢复材料快照在 `~/.noe-panel/backups/chat-profiles-恢复材料-20260610/`）。⚠️ 生产 12:22 那次重启仍是旧代码，**修复生效需 owner 再重启一次**，重启前别在界面里再改模型设置（旧进程一保存会把修复好的文件再写坏一代）。IdentityModelSettingsStore 同步排查过：无此病。
**给下窗的避撞提示**：本项目现常态是 2-3 个 AI 窗口同时在写。开工先 `git status` 看哪些文件有未提交改动（=别人正在写），commit 只 add 自己的文件清单，公共文件（server.js/app.js/index.html/main.js）改前 git log -2 看最新位置、用内容锚点不用行号。

**社区对标强化 + 深析改进（13:45-14:10，owner /loop "找不足→搜开源→复刻/学经验自写"+"深析→计划→执行→检验四问"）**：对标 ricky0123/vad(Silero=MIT) 与 mem0(Apache2.0)，两项落地后又深析出 3 处改进。
- **神经网络 VAD 精筛**（`src/voice/SileroVadClient.js`）：接本机吃灰的 silero_vad.onnx（用卡①已装 sherpa-onnx-node 内置 Vad 类，零前端 134MB 依赖），VoiceSession STT 前判真人声、滤电视/空调/键盘噪声。深析改进：① Vad 单例复用+用后 reset（每次新建 6.0ms→复用 3.6ms）；② 前移到 chat() 最前（噪声段连 CAMPPlus 声纹推理都不跑）。`NOE_SILERO_VAD=1` 默认 OFF。量化（自造 6 人声+4 噪声集）：误杀/误过率均 0%、实机噪声 32-47ms 丢弃。
- **记忆近重复去重**（`src/memory/NoeMemoryDedup.js`）：借鉴 mem0 ADD/UPDATE，本地字符 bigram 相似度确定性判定（零 LLM）——同 scope 近重复≥0.62 则 UPDATE 替换+merge_trace，治对话记忆堆积。深析改进③：加前缀包含判据（短句追加细节型"明天三点开会"→"…在会议室A"原 Jaccard 0.58 漏过，前缀包含且短方≥6字直接判近重复）。12 组真实句对：该合并 6/6（原 4/6）、不该合并安全 7/7。`NOE_MEMORY_DEDUP=1` 默认 OFF。诚实边界：字符法抓不到"换关键词的语义矛盾"（美式→拿铁 0.45），需向量版（MemoryCore 已有 semanticIndex 是现成升级口，列后续）。
- 全量 305 文件/2449 测绿、lint 0/0；commit 69bc170 等已 push、CI 跑（run 见远程）。两开关默认 OFF，要开随重启 51835 设 env。

## 8. ContextEngine 通电窗口执行记录（2026-06-10 下午，owner /loop 按 ROADMAP 推荐顺序开工）

> 本窗已收尾，**完整交接（验证证据/验证入口/下一步/避撞提示）见 `HANDOFF_2026-06-10_ContextEngine通电窗口交接.md`**；下面是过程留痕。

**方向二 ✅ ContextEngine 通电**（ROADMAP 第一波，5 commit）：
- 新建 `src/context/NoeTurnContextEngine.js`（@ts-check/全注入/<500行）：VoiceSession._respondCore 的 12 段内联 ctx.add（自我认知/人物库/承诺/预取/人物卡/工具桥/动作桥/身份/认人/视觉/纠错/召回）等价迁入；NoeContextBudgeter 仍做裁剪层——供给+裁剪两层配齐。`buildPeopleBrief` 迁入引擎，VoiceSession re-export 保旧 import 路径。
- VoiceSession 632→547 行，供给段换成一次 `contextEngine.supplyTurnContext()` 调用（构造注入，可替换）。
- **验证**：行为逐字不变——既有注入测试（noe-voice-context-injection 等）未改一字全绿；8-agent 对抗审查（3 视角×逐条复核）等价性发现 **0 坐实**；唯一坐实项"单测契约空洞"（复核者变异实测 8 处改坏全绿）已补——全 13 段 id/keep/顺序总契约+声纹措辞分支+视觉第三态+召回/动作桥入参透传，变异复测可抓红。引擎独立单测 27 项。

**方向一 ✅ 文字聊天拉齐核心**（ROADMAP 第二波，2 commit + server.js 接线）：
- 引擎加 `sections` 段级白名单（null=全开语音旧行为；白名单外段**连副作用都不跑**——审查 rejected 清单指出的 self-knowledge 人格污染/action 写库风险就此关死）。
- `SoloChatDispatcher` 构造注入 `contextEngine`：聊天室 1v1 注入 `['people','tool-bridge','recall']` 三段（记忆域固定 'noe'，room.cwd 只是预算口径；输入截 2000 与语音同口径；引擎失败不阻断聊天）。未注入=旧行为逐字不变（5 项单测）。
- **env 门控（宪法3）**：`NOE_CHAT_CONTEXT=1` 启用、默认 OFF（已录 ARCHITECTURE.md env 速查）。装配点 server.js soloChatDispatcher 构造处。

**验证证据**：全量 308 文件/2491 测全绿；`node --check server.js` 过；51998 隔离端口实机 smoke（`NOE_CHAT_CONTEXT=1` 起服→HTTP 活→SIGTERM 干净退出，未碰 51835/51735）。

**改了哪些公共文件**：server.js 仅 2 处微增（import 2 行 + soloChatDispatcher 构造加 contextEngine 参数）；未动窗口 C 拆分大区。

**给 owner**：重启 51835 后此功能仍默认关；想让聊天室"记得你/查得到"，`.env` 加 `NOE_CHAT_CONTEXT=1` 再重启。

**下一步（按 ROADMAP）**：方向三"记忆语义去重"（用 MemoryCore.semanticIndex 升级 NoeMemoryDedup，独立可并行）→ LLM 流式首声提前。审查 rejected 清单还留两条房务：给 LegacyNoeContextEngine 标废弃或收编其 uiSignals/acuiCards 独有件；聊天室记忆召回的 projectId 分区策略如要按房隔离需 owner 拍板。

**方向三 ✅ 记忆语义去重（同窗续作，1 commit）**：
- `NoeMemoryDedup.decideSemanticConflict` 纯函数零 LLM：向量分（semanticIndex 拿）≥0.82 且字符分 <0.62 双指标联判——抓"换关键词矛盾"（我喜欢美式→我改喝拿铁），近重复仍归字符路。保守铁律同款：跨 scope/salience≥5/短句(<6字)一律不判。
- `MemoryCore.semanticConflictSweep`：写后异步 sweep（写路径保持同步零破坏），命中走现成 `merge()`——旧条 hidden=merged_into:<新id>（unhide 可逆）+ 新条 merge_trace 留痕 + 清旧向量。**hash provider 直接拒跑**（精度近零防误删真记忆）。
- 开关：`NOE_MEMORY_DEDUP_SEMANTIC=1` 默认 OFF，生产还需 `NOE_MEMORY_EMBED=ollama`（语义索引非 hash）。验证：11 项单测（验收样例美式→拿铁闭环/字符路不串道/hash 拒跑/保护铁律/fail-open）+ 全量 310 文件 2504 测绿。
- 真实环境效果依赖 qwen3-embedding 对矛盾对的实际打分，0.82 阈值是保守起点——owner 开起来用一阵，漏合并就调 `NOE_MEMORY_DEDUP_SEMANTIC_THRESHOLD` 降一档（建议每次 -0.04 观察）。

**方向三 ✅ LLM 流式早鸟 TTS（同窗续作，1 commit）**：语音延迟终局——原来「整段大脑生成→才合成首句」，现在大脑边吐字边检首句、首句一成形并行启动 TTS（与剩余生成重叠）。
- `OllamaChatAdapter`：`opts.onDelta` 回调存在才开 `stream:true`（NDJSON 逐行回调），最终返回与非流式同形 `{reply,tokensIn,tokensOut}`——resilience 壳/上层零感知；不传 onDelta 老行为不变。
- `src/voice/VoiceStreamEarlyTts.js` 首句探测器：句界/长度/占比判据与 `splitFirstSentence` 同口径；reasoning 泄漏（<think/harmony）永久放弃早鸟；sanitize 注入与整段管线同款。
- `VoiceSession` 收尾**对账**：早鸟句与最终 `splitFirstSentence(reply).first` 逐字一致才采用——质检/复读重试换了答案就丢早鸟走旧路，最多浪费一次 TTS 调用，**绝不放错音频**；栅栏已压制不浪费配额。
- 开关：`NOE_VOICE_LLM_STREAM=1` 默认 OFF。**真机基准**（真 ollama 9b 档+1.2s 模拟 TTS，同次反事实口径）：首声提前 947/1200/1111ms——TTS 时长几乎被完全藏进生成时间；MiniMax 真实合成 1-2s ⇒ 生产提前量即 1-2s，与 ROADMAP 预估吻合。14 项单测+全量 312 文件 2528 测绿。
- 边界（诚实标注）：当前只有 ollama adapter 支持流式（语音默认本地大脑）；lmstudio/MiniMax 不支持时自动忽略 onDelta 走旧路零影响，后续要可同模式补。

**LLM 流式覆盖补全（同窗续作，1 commit）**：SSE 流式下沉到 `OpenAICompatChatAdapter` 基类（onDelta 才开、`stream_options.include_usage` 取真实 token 数）——lmstudio / gemini-openai / custom 自动继承，语音兜底链「ollama→lmstudio」两档全支持早鸟；上一条记录里"只有 ollama 支持流式"的边界已消除（MiniMax 原生 adapter 仍非流式，传 onDelta 自动忽略零影响）。LM Studio 真机 SSE 实测：8 增量片/首 delta 409ms/usage 19/9/拼接一致。3 项单测，全量 313 文件 2544 测绿。
