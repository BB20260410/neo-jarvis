# Execution Record · P0/P1 Qwen Main Brain Noe100 2026-06-12

执行窗口：2026-06-12，repo root `/Users/hxx/Desktop/Neo 贾维斯`。

## Repo 入口核验

- `pwd` -> `/Users/hxx/Desktop/Neo 贾维斯`
- `git rev-parse --show-toplevel` -> `/Users/hxx/Desktop/Neo 贾维斯`
- `git log -5 --oneline` 顶部为 `145e642 本地模型: 收敛单主脑 Gemma`
- 未 stage、未 commit、未 push、未 reset。

## P0 状态表

| 项 | 状态 | 证据 |
|---|---|---|
| benchmark | done | `scripts/noe-main-brain-candidate-benchmark.mjs` 保留为 manual benchmark / explicit experiment；无 `--ack-manual-benchmark` 或 `NOE_ACK_MANUAL_BENCHMARK=1` 时拒跑；benchmark 不写回自动默认。脚本新增 `--loaded-only` no-load 模式，可在不改变 LM Studio loaded models 的情况下只测当前已加载模型。历史已执行 `node scripts/noe-main-brain-candidate-benchmark.mjs --ack-manual-benchmark --loaded-only --model gemma-4-26b-a4b-it-qat-mlx`，报告 `output/main-brain-candidate-benchmark-20260612/20260612T030810Z/results.json`：21 tasks，quality `72/82` (`87.8%`)，score `89.6`，speed `87.7 tok/s`，JSON `21/21`，errors `0`；loaded 前后均为 `gemma-4-26b-a4b-it-qat-mlx`。该报告只是当时已 loaded Gemma 的 no-load smoke，不代表当前默认。当前默认以 `src/model/NoeLocalModelPolicy.js` 为准：Q35-6 Main Brain、Q27-4 Review Brain、G26-4 Fallback Brain。`scripts/lib/noe-manual-benchmark-gate.mjs` 同步接入 `scripts/noe-gemma-family-benchmark.mjs`、`scripts/noe-new-model-benchmark.mjs`。 |
| live 51835 | done | `51835` 已 direct restart。P0 核验 PID `8570`；P2 runtime restart recovery drill 曾到 PID `55627`；mind proof 上线后最新 runtime restart recovery drill 当前核验 PID `52411`，cwd `/Users/hxx/Desktop/Neo 贾维斯`，命令 `/Users/hxx/.nvm/versions/node/v22.22.2/bin/node server.js`。最终 `GET /health` -> 200 `{ok:true,status:"ok",service:"noe-panel",port:51835}`；最终 `GET /api/noe/readiness` -> `status:"passed"`，loop/memory/fileIndex passed，counts: memoryVisible `501`、focusDepth `4`、total `9`、enabled `9`、pendingApprovals `50`、pendingActs `0`；`npm run verify:noe:freedom-live` 4/4 PASS。 |
| self_learning | done | 最新完成目标已随 live 后台推进到 `ad49f1c9-f741-424b-a7de-b2ad2dfe4689`，标题 `自主学习：让 Noe 的主观思考更接地，避免 echo trap`，状态 `done`。plan 9 个 step，7 个 action step。`npm run verify:noe:action-evidence-spine` 报告 `output/noe-action-evidence-spine/action-evidence-spine-1781243786322.json`，稳定入口 `output/noe-action-evidence-spine/latest.json`，证明 6 个 done action step 有 checkpoint -> event -> act -> actionEvidence，另 1 个 recovered action step 有 `step_recovered` checkpoint；`actionStepsSatisfied=7`，blockers `[]`。关键 successful evidenceRefs 为 `sqlite:events/38735`、`38758`、`38772`、`38824`、`38842`、`38868`；step 4 的三次 failed observe 尝试保留在 rawEvidenceBlockers，不伪装为成功。 |
| 知识库/联网学习 | done | 本地 wiki、联网 search/research、项目 `rg` evidence 三类已在 P0 记录中证明：wiki 命中 `wiki/karpathy-llm-wiki-method.md`；联网 search/research 200 且 `mockSearch:false`；项目 rg 命中 `NoeMemoryConflictPolicy`、`FactExtractor`、`NoeKnowledgeGraph`、`NoeNightlyReflection`、`ExpectationLedger/Brier`。 |
| 自动模型变量 | done | shell env 与 launchctl 检查 `NOE_REFLECT_MODEL`、`NOE_INNER_MODEL`、`NOE_VLM_MODEL`、`NOE_VLM_FALLBACK_MODEL`、`NOE_REFLECTION_MODEL`、`NOE_FACT_MODEL`、`NOE_LMSTUDIO_MODEL`：均未设置，未发现覆盖三角色默认的旧 env override。代码默认统一由 `src/model/NoeLocalModelPolicy.js` 输出：`NOE_MAIN_BRAIN_MODEL=qwen/qwen3.6-35b-a3b`、Review Brain `qwen/qwen3.6-27b`、Fallback Brain `gemma-4-26b-a4b-it-qat-mlx`。未把 secret 原值写入记录。 |

## P1 状态表

| 项 | 状态 | 证据 |
|---|---|---|
| Noe100 统一证明门 | done | `docs/NOE_100_ACCEPTANCE_MATRIX.md`、`scripts/noe-100-readiness.mjs`、`npm run verify:noe:100-readiness` 已落地并已同步 natural live expectation 口径。最新稳定入口 `output/noe-100-readiness/latest.json`：`score=94`，`passed=false`，`readyFor100=false`，35/37 checks passed。真实 blockers：`not_enough_soak_evidence`（activeDays `3`，requiredDays `7`）和 `expectation_settlements_below_20`（natural live resolved `4/20`；controlled drill `20/20` 只证明机制，不满足长期 live calibration）。 |
| Goal/action checkpoint | done | `src/cognition/NoeGoalCheckpoints.js` 为每条 checkpoint payload 写入 `workflow.schemaVersion/idempotencyKey/resumeCursor/sideEffectFingerprint/rollbackEvidence`。`scripts/noe-goal-checkpoint-workflow-backfill.mjs --apply` 已给 live DB 历史 checkpoint 补 workflow；最新 Noe100 读数为 `noe_goal_checkpoints total=759`，action checkpoint workflow coverage `612/612`，act evidence sideEffectFingerprint `151/151`，rollbackRequiredMissing `0`。本轮补齐 report 稳定入口：preview `npm run noe:goal-checkpoint:backfill-workflow` 写入 `output/noe-goal-checkpoint-workflow/goal-checkpoint-workflow-backfill-1781243227339.json` 和 `output/noe-goal-checkpoint-workflow/latest.json`，二者一致；扫描 `1036`，updates `0`，未新增 live DB 写入。 |
| Action Evidence Spine | done | `npm run verify:noe:action-evidence-spine` PASS，报告 `output/noe-action-evidence-spine/action-evidence-spine-1781243786322.json`，稳定入口 `output/noe-action-evidence-spine/latest.json`。最新完成 self_learning 目标 `ad49f1c9-f741-424b-a7de-b2ad2dfe4689`，checkpointCount `46`，actionEvidenceCheckpoints `9`，actionStepsWithValidEvidence `6`，actionStepsRecovered `1`，actionStepsSatisfied `7`，durableWorkflowReady `true`，durableWorkflowGaps `[]`，blockers `[]`。脚本已区分 `done` 与 `recovered`：recovered step 要有恢复 checkpoint，失败尝试只作为 rawEvidenceBlockers。 |
| 思考接地与记忆评测 | done | `src/loop/InnerMonologue.js` 增加低接地 deterministic experience anchor，且相似 anchor 重复时强制使用最佳真实经历锚点而不是落回低接地原文；`scripts/noe-thought-grounding-repair.mjs --apply` 对历史低接地 inner_monologue 做带 `meta.groundingRepair` 的可审计修复，共修复 `203 + 2 + 3` 条。本轮补齐 repair report 稳定入口：preview `npm run noe:thought-grounding:repair` 写入 `output/noe-thought-grounding-repair/thought-grounding-repair-1781243227385.json` 和 `output/noe-thought-grounding-repair/latest.json`，二者一致；当前扫描低接地待修复 `0`、repaired `0`，未新增 live DB 写入。`npm run verify:noe:thought-memory` PASS，最新报告 `output/noe-thought-memory-eval/thought-memory-eval-1781241356477.json`，稳定入口 `output/noe-thought-memory-eval/latest.json`：sampleCount `818`，avgScore `0.74`，passRate `1`，rewriteCount `500`，refKeyCount `818`，memory live queries 3/3，conflict fixtures 3/3，blockers `[]`。 |
| mind panel 100 proof | done | 新增 `GET /api/noe/mind/proof` 和 `/mind.html` proof 区块，展示 Noe100 score、blockers、last tick、last thought、last action、last recovery。live API 验证 200：score `97`、passed `false`、blockers `["not_enough_soak_evidence"]`、lastRecovery `output/noe-runtime-restart-recovery-drill/20260612T021744Z/report.json`；当前最新 Noe100 reportPath 为 `output/noe-100-readiness/noe-100-readiness-1781243686431.json`，稳定入口 `output/noe-100-readiness/latest.json`。浏览器后台打开 `/mind.html` 验证：proof 区块渲染 `97%`、blocker 可见、console errors `[]`、URL token 自举后清除；390px 手机视口 `overflowX=false`。 |
| NoeSelfKnowledge verified/declared | done | `src/context/NoeSelfKnowledge.js` 现在为每个能力返回 `status: verified|declared` 与短 evidenceRefs；prompt 行前缀 `[verified]`/`[declared]`，说明 verified 才代表 smoke/eval/live 已证实，declared 只是代码/配置/模型存在或待验证。verified 只来自最新 Noe100/growth 报告检查：memory、dream、self-evolution、heartbeat、stream-v2、expectations、workspace、goals、self-improve；voiceprint/face/voice 等未跑烟测能力保持 declared。`buildNoeSelfKnowledgeBlock()` 长度 `3958`，不含 `output/noe-` 路径。 |

## P2 状态表

| 项 | 状态 | 证据 |
|---|---|---|
| 受控真实副作用 + rollback 样本 | done | 新增 `scripts/noe-controlled-side-effect-drill.mjs` 和 `npm run verify:noe:side-effect-drill`。已执行 `npm run verify:noe:side-effect-drill -- --apply`，生成 `output/noe-controlled-side-effect-drill/20260612T010417Z/report.json`：`dryRunOnly=false`，本地文件写入 hash verified，随后删除并 `artifactAbsent=true`，`rollback.verified=true`，actionEvidence validation `ok=true`。这是受控本地文件副作用样本，不冒充社交平台/公网发布。 |
| model health daily / provider unavailable | done | 新增 `scripts/noe-model-health-snapshot.mjs` 和 `npm run verify:noe:model-health`，现已补齐 `output/noe-model-health/latest.json` 稳定入口。最新报告 `output/noe-model-health/model-health-1781244538774.json` 与 `output/noe-model-health/latest.json` 一致：LM Studio `/v1/models` 200、modelCount `10`、loadedModels 为 `qwen/qwen3.6-35b-a3b`；Ollama `fetch failed`；MiniMax/Xiaomi configured 且 reachable/authOk；Gemini/OpenAI/Anthropic secret_unconfigured。报告声明 `readOnly=true`、`noChatCompletionCalls=true`、`lmStudioLoadUnloadChanged=false`、`secretValuesReturned=false`。 |
| expectation settlement drill | done | 新增 `scripts/noe-expectation-settlement-drill.mjs`、`tests/unit/noe-expectation-settlement-drill.test.js` 和 `npm run verify:noe:expectation-settlement-drill`。正式报告 `output/noe-expectation-settlement-drill/20260612T012857Z/report.json`：隔离 SQLite，`sampleCount=20`、`resolvedCount=20`、`unresolvedCount=0`、Brier `0.117`、`liveDbMutated=false`。当前 live expectation 为 `total=26/resolvedScored=4/open=22`，受控 drill 只证明结算机制，不冒充长期真实校准。单测已改用临时输出目录，避免 fake/test report 抢占正式 evidence。 |
| live expectation calibration snapshot | done | 新增 `scripts/noe-expectation-calibration-snapshot.mjs`、`tests/unit/noe-expectation-calibration-snapshot.test.js` 和 `npm run verify:noe:expectation-calibration`。正式报告 `output/noe-expectation-calibration/2026-06-12/report.json`：只读 live `noe_expectations`，不写 DB、不调用模型、不读 owner token、不输出 claim 正文、不改变 LM Studio。当前 live total `26`、open `22`、openWithDueAt/dueOpen `22`、dueNowOpen/overdueOpen `0`、futureOpenWithDueAt `22`、dueWithin24h `2`、dueWithin7d `22`、resolvedScored `4/20`、Brier `0.038125`、nextOpenDueAt `1781324968708`、hoursUntilNextOpenDue `22.34`、resolverActionableNow `false`；controlled drill `20/20` 作为机制证据单独列出，liveCalibrationReady `false`，blocker `live_expectation_resolved_below_20`。 |
| model_unloaded recovery drill | done | 新增 `scripts/noe-model-unload-recovery-drill.mjs`、`tests/unit/noe-model-unload-recovery-drill.test.js` 和 `npm run verify:noe:model-unload-recovery-drill`。正式报告 `output/noe-model-unload-recovery-drill/20260612T012857Z/report.json`：fake provider 返回 `model_unloaded`，主脑 `lmstudio:gemma-4-26b-a4b-it-qat-mlx` 被标记 unavailable，backup `lmstudio:benchv3-gemma-4-31b-qat` 接管，quorum `availableCount=2/threshold=2/approvedCount=2`。LM Studio 状态只读前后快照一致，均为 `gemma-4-26b-a4b-it-qat-mlx`，`lmStudioLoadUnloadCommandsIssued=false`、`lmStudioLoadUnloadChanged=false`。这不是实际 `lms unload/load`。Noe100 只接受真实 `lmstudio:/api/v0/models` 快照来源，跳过单测 fake snapshot。 |
| runtime restart recovery drill | done | 新增 `scripts/noe-runtime-restart-recovery-drill.mjs`、`tests/unit/noe-runtime-restart-recovery-drill.test.js` 和 `npm run verify:noe:runtime-restart-drill`。mind proof 上线后复跑 `npm run verify:noe:runtime-restart-drill -- --apply`，最新正式报告 `output/noe-runtime-restart-recovery-drill/20260612T021744Z/report.json`：真实 `51835` 旧 PID `55627` SIGTERM 后退出，新 PID `52411` 由 Node22 direct restart，cwd `/Users/hxx/Desktop/Neo 贾维斯`；`/health` ok、`/api/noe/readiness` passed、`verify:noe:freedom-live` 4/4；`51735` PID `4773`/cwd `/Users/hxx/Desktop/00_项目/05_Claude可视化面板` 前后一致；LM Studio loadedModels 前后一致，均为 `benchv4-gemma-4-26b-a4b-qat-4bit` 与 `gemma-4-26b-a4b-it-qat-mlx`。 |
| act failure / approval wait recovery drill | done | 新增 `scripts/noe-act-recovery-drill.mjs`、`tests/unit/noe-act-recovery-drill.test.js` 和 `npm run verify:noe:act-recovery-drill`。正式报告 `output/noe-act-recovery-drill/20260612T015219Z/report.json`：隔离 SQLite，`liveDbMutated=false`；failed act 首次 `failed`、重启模拟后 retry 到 `completed`、executorAttempts `2`、executedEventCount `1`、actionEvidenceValid `true`；approval wait 首次 `awaiting_approval`，重启模拟后仍同一 approval，批准前 executorCalls `0`，批准后 `completed` 且 finalExecutorCalls `1`。Noe100 已纳入该检查。 |
| 7 天 soak 日报快照 | done | 新增 `scripts/noe-soak-daily-snapshot.mjs`、`tests/unit/noe-soak-daily-snapshot.test.js` 和 `npm run verify:noe:soak-snapshot`。最新写入 `output/noe-soak-daily/2026-06-12/report.json` 与 `output/noe-soak-daily/latest.json`：脚本刷新 Noe100、采样 live health/readiness；`readOnly=true`、`noDbWrites=true`、`noModelCalls=true`、`lmStudioLoadUnloadChanged=false`、`doesNotBypassSoak=true`。当前 `soak.status="pending"`、activeDays `3/7`、daysRemaining `4`、blocker `not_enough_soak_evidence`；expectations 显示 natural live resolved `4/20`、controlled drill `20/20` 只证明机制、`longTermReady=false`。 |
| verifier owner-token 边界 | done | `scripts/noe-full-current-verify.mjs` 默认不再跑 live `phase5` / cognitive owner-token 路径；只有 `--include-live` / `--include-cognitive-live` 且同时带 `--ack-read-owner-token` 或 `NOE_ACK_READ_OWNER_TOKEN=1` 才进入 live token 验证。`scripts/noe-phase5-runtime-verify.mjs`、`scripts/noe-cognitive-verify.mjs`、`scripts/noe-cognitive-runtime-verify.mjs`、`scripts/noe-real-use-replay.mjs`、`scripts/noe-freedom-live-smoke.mjs`、`scripts/noe-capture-external-evidence.mjs`、`scripts/noe-voice-ear-acceptance.mjs`、`scripts/noe-social-dom-live-probe.mjs` 的 live 模式均增加 policy gate，默认只写 policy-blocked report，不读取 owner-token、不输出 secret、不访问受保护 live API。policy 证据包括 `output/noe-phase5-runtime/phase5-runtime-1781246058361.json`、`output/noe-real-use-replay/real-use-replay-1781246058388.json`、`output/noe-cognitive-verify/cognitive-verify-1781246058622.json`、`output/noe-cognitive-runtime/cognitive-runtime-1781246058633.json`、`output/noe-freedom-live/freedom-live-1781246831741.json`、`output/noe-external-evidence/capture-report-1781246831804.json`、`output/noe-voice-ear-acceptance/voice-ear-2026-06-12T06-46-36-264Z/report.json`；均 `tokenPolicy.policyBlocked=true`、`secretValueReturned=false` 或 `ownerTokenPrinted=false`。安全 full-current 证据：`npm run verify:noe:full-current -- --include-managed` PASS，最新报告 `output/noe-full-current/full-current-1781246962402.json`，11/11，`includeLive=false`、`includeCognitiveLive=false`。`scripts/noe-handoff-consistency.mjs` 已把这些规则加入门禁，`npm run verify:handoff` PASS 49/49。 |

## P3 状态表

| 项 | 状态 | 证据 |
|---|---|---|
| growth readiness proof | done | 新增 `scripts/noe-growth-readiness.mjs`、`tests/unit/noe-growth-readiness.test.js` 和 `npm run verify:noe:growth-readiness`。最新正式报告 `output/noe-growth-readiness/20260612T053921Z/report.json`，稳定入口 `output/noe-growth-readiness/latest.json`：隔离 SQLite，`liveDbMutated=false`；sleep/reflection 写入 1 条 insight 并复核 1 条 prior insight；dream consolidation 合并重复记忆且保护 salience=5 身份记忆；SkillExtractor 生成 disabled skill candidate `noe-recovery-evidence-drill`，SkillCurator dry-run 给出 active/archive_candidate 且 destructive=false；automatic curriculum 生成 4 个 replaySafe 任务；autonomy regression gate 调用 `scripts/noe-self-evolution-plan-verify.mjs`，198/198 PASS。 |
| personality SFT/LoRA dataset readiness | done | 新增 `scripts/noe-personality-dataset-readiness.mjs`、`tests/unit/noe-personality-dataset-readiness.test.js` 和 `npm run verify:noe:personality-dataset`。正式报告 `output/noe-personality-dataset-readiness/personality-dataset-readiness-1781232973321.json`：只读、无训练、无模型调用、不导出语料正文、不改变 LM Studio。当前 `~/.noe-panel/sft` 有效训练对 `100/500`，invalid `0`，sensitive `0`，unique `100`，smokeDatasetReady `true`，formalDatasetReady `false`；live DB 可见记忆 `502`、insight `7`、fact `74`、highSalience `21`、episodes `984`、innerMonologue `695`、activeDays `3`；owner voice/face ready，people `4`，personality/narrative snapshot 存在。训练/采用仍 blocked：`not_enough_sft_pairs_for_formal_training`、`owner_training_plan_required`，旧 LoRA gate report FAIL `2/12`，不得训练或采用。 |

## Live / DB 触碰

- `51835`: 已触碰并重启。P0 direct restart 最终核验曾为 PID `8570`；P2 runtime restart recovery drill 曾到 PID `55627`；mind proof 上线后最新 runtime restart recovery drill 当前 PID `52411`，cwd `/Users/hxx/Desktop/Neo 贾维斯`。
- 2026-06-12 14:48 CST 复核：`51835` 当前已在线，PID `89417`，cwd `/Users/hxx/Desktop/Neo 贾维斯`，命令 `/Users/hxx/.nvm/versions/node/v22.22.2/bin/node server.js`；本轮没有 kill/restart/takeover，只做只读观察。`GET /health` -> 200 `{ok:true,status:"ok",service:"noe-panel",port:51835}`；`GET /api/noe/readiness` -> 200，`readiness.status="passed"`，counts memoryVisible `512`、focusDepth `4`、total `9`、enabled `9`、pendingApprovals `50`、pendingActs `0`。受保护 `verify:noe:freedom-live` 默认不再跑 live dry-run，因无 `--ack-read-owner-token` 预期 exit 2 并写 `output/noe-freedom-live/freedom-live-1781246831741.json`，`tokenPolicy.policyBlocked=true`、`secretValueReturned=false`。
- `51735`: 只观察，PID `4773`，cwd `/Users/hxx/Desktop/00_项目/05_Claude可视化面板`；未 kill、未 restart、未接管。
- LM Studio loaded models: 本轮 verifier/live 复核未执行 `lms load/unload`。2026-06-12 14:48 CST 只读 `lms ps` 显示 `qwen/qwen3.6-35b-a3b` / `qwen/qwen3.6-35b-a3b@6bit` IDLE，context `262144`，parallel `1`。前一段 8bit loaded-model 漂移来自另一个 benchmark 进程，已结束；当前三角色策略仍以 Q35-6 主脑、Q27-4 复核、G26-4 兜底为准。
- live DB `/Users/hxx/.noe-panel/panel.db`: 已写入 checkpoint workflow backfill 和 thought grounding repair；两者均有 `output/` 报告。
- 2026-06-12 15:43 CST standing autonomy grant / 自我授权落地：新增本机授权文件 `/Users/hxx/.noe-panel/autonomy-grant.json`，权限 `-rw-------`，只保存授权范围、边界和过期策略，不保存 owner-token/API key/cookie/OAuth 原值。`npm run noe:autonomy:grant` 写入 max-autonomy grant，`npm run noe:autonomy:check -- --scope restart-51835:repair` 与 `npm run noe:autonomy:check -- --scope freedom-live:run` 均 PASS，输出 `source:"standing_autonomy_grant"`、`secretValuesReturned:false`。无需 `--ack-read-owner-token` 的 live 验证已通过：`node scripts/restart-panel.mjs --check-only` PASS，PID `61331`、cwd `/Users/hxx/Desktop/Neo 贾维斯`，只做 check 未 restart；`npm run verify:noe:freedom-live` PASS 5/5；`npm run verify:noe:phase5` PASS 9/9，报告 `output/noe-phase5-runtime/phase5-runtime-1781249622412.json`，`tokenPolicy.authorization.mode:"standing_grant"`；`npm run verify:noe:full-current -- --include-live --include-managed` PASS，报告 `output/noe-full-current/full-current-1781250183078.json`，`ownerTokenAuthorization.mode:"standing_grant"`。`GET /health` 与 `GET /api/noe/readiness` 均 200/passed；`51835` 未 kill/restart/takeover，只做 check-only 和 live API 验证。`51735` 只读观察 PID `4773`，未触碰。`lms ps` 只读显示仍只有 `qwen/qwen3.6-35b-a3b@6bit`，未改变 LM Studio loaded models。
- 本地 output: 已写入并验证 `output/main-brain-candidate-benchmark-20260612/20260612T030810Z/results.json`、`output/noe-controlled-side-effect-drill/20260612T010417Z/report.json`、`output/noe-expectation-settlement-drill/20260612T012857Z/report.json`、`output/noe-expectation-calibration/2026-06-12/report.json`、`output/noe-model-unload-recovery-drill/20260612T012857Z/report.json`、`output/noe-runtime-restart-recovery-drill/20260612T021744Z/report.json`、`output/noe-act-recovery-drill/20260612T015219Z/report.json`、`output/noe-growth-readiness/20260612T053921Z/report.json`、`output/noe-growth-readiness/latest.json`、`output/noe-action-evidence-spine/action-evidence-spine-1781243786322.json`、`output/noe-action-evidence-spine/latest.json`、`output/noe-goal-checkpoint-workflow/goal-checkpoint-workflow-backfill-1781243227339.json`、`output/noe-goal-checkpoint-workflow/latest.json`、`output/noe-thought-grounding-repair/thought-grounding-repair-1781243227385.json`、`output/noe-thought-grounding-repair/latest.json`、`output/noe-model-health/model-health-1781244538774.json`、`output/noe-model-health/latest.json`、`output/noe-100-readiness/noe-100-readiness-1781244549302.json`、`output/noe-100-readiness/latest.json`、`output/noe-soak-daily/2026-06-12/report.json`、`output/noe-soak-daily/latest.json`、`output/noe-personality-dataset-readiness/personality-dataset-readiness-1781232973321.json`、`output/noe-personality-dataset-readiness/latest.json`、`output/noe-thought-memory-eval/thought-memory-eval-1781241356477.json`、`output/noe-thought-memory-eval/latest.json`；临时 side-effect artifact 已删除并验证不存在。

## 验证结果

- `node --check`：`src/server/routes/noeMind.js`、`public/mind.js`、`src/context/NoeSelfKnowledge.js`、`src/cognition/NoeGoalCheckpoints.js`、`src/cognition/NoeWorkspace.js`、`src/loop/InnerMonologue.js`、`scripts/noe-action-evidence-spine.mjs`、`scripts/noe-100-readiness.mjs`、`scripts/noe-goal-checkpoint-workflow-backfill.mjs`、`scripts/noe-thought-grounding-repair.mjs`、`scripts/noe-expectation-settlement-drill.mjs`、`scripts/noe-model-unload-recovery-drill.mjs`、`scripts/noe-runtime-restart-recovery-drill.mjs`、`scripts/noe-act-recovery-drill.mjs`、`scripts/noe-growth-readiness.mjs` PASS。
- `node --check scripts/noe-main-brain-candidate-benchmark.mjs` PASS。
- `node scripts/noe-main-brain-candidate-benchmark.mjs --loaded-only --task-limit=1` without ack correctly exits 2 and prints manual benchmark / explicit experiment gate.
- `node scripts/noe-main-brain-candidate-benchmark.mjs --ack-manual-benchmark --loaded-only --model gemma-4-26b-a4b-it-qat-mlx` PASS，生成 `output/main-brain-candidate-benchmark-20260612/20260612T030810Z/results.json`；21 tasks，quality `72/82`，score `89.6`，JSON `21/21`，errors `0`，loaded 前后均为 `gemma-4-26b-a4b-it-qat-mlx`。这是历史 loaded-only smoke，不代表当前默认；当前默认是 Q35-6 Main Brain。
- `npm run verify:noe:freedom-live` PASS，4/4。
- `npm run verify:noe:side-effect-drill -- --apply` PASS，生成 rollback verified 的受控本地副作用样本。
- `npm run verify:noe:model-health` PASS，生成只读 model/provider health 快照 `output/noe-model-health/model-health-1781233848605.json`，LM Studio loadedModels 为 `gemma-4-26b-a4b-it-qat-mlx`，脚本未 load/unload。
- `npm run verify:noe:expectation-settlement-drill` PASS，生成 `output/noe-expectation-settlement-drill/20260612T012857Z/report.json`，20 条隔离期望结算样本，live DB 未变更。
- `npm run verify:noe:expectation-calibration` PASS，生成 `output/noe-expectation-calibration/2026-06-12/report.json` 和 `output/noe-expectation-calibration/latest.json`；live resolved `4/20`，dueNowOpen `0`，nextOpenDueAt `1781324968708`，hoursUntilNextOpenDue `22.61`，controlled drill `20/20` 分开记录，长期校准仍未 ready。
- `npm run verify:noe:model-unload-recovery-drill` PASS，生成 `output/noe-model-unload-recovery-drill/20260612T012857Z/report.json`，fake provider `model_unloaded` 恢复样本通过，LM Studio loaded models 前后一致。
- `npm run verify:noe:runtime-restart-drill -- --apply` PASS，最新生成 `output/noe-runtime-restart-recovery-drill/20260612T021744Z/report.json`，真实 51835 restart 后 health/readiness/freedom-live 均通过，51735 和 LM Studio loaded models 未变。
- `npm run verify:noe:act-recovery-drill` PASS，生成 `output/noe-act-recovery-drill/20260612T015219Z/report.json`，failed act / approval wait 恢复样本通过，live DB 未变。
- `npm test -- tests/unit/noe-growth-readiness.test.js` PASS，1 file / 1 test；验证 `latest.json` 与 timestamp report 一致。
- `npm run verify:noe:growth-readiness` PASS，最新生成 `output/noe-growth-readiness/20260612T053921Z/report.json` 与 `output/noe-growth-readiness/latest.json`，sleep/skill/curriculum/self-evolution regression gate 均通过，live DB 未变。
- `npm test -- tests/unit/noe-goal-checkpoint-workflow-backfill-report.test.js tests/unit/noe-thought-grounding-repair-report.test.js` PASS，2 files / 2 tests；均使用临时 SQLite 和临时输出目录，验证 timestamp report 与 `latest.json` 一致。
- `npm run noe:goal-checkpoint:backfill-workflow` PASS，preview 扫描 live checkpoint `1036`，updates `0`，写入 `output/noe-goal-checkpoint-workflow/goal-checkpoint-workflow-backfill-1781243227339.json` 和 `output/noe-goal-checkpoint-workflow/latest.json`。
- `npm run noe:thought-grounding:repair` PASS，preview 扫描低接地待修复 `0`，repaired `0`，写入 `output/noe-thought-grounding-repair/thought-grounding-repair-1781243227385.json` 和 `output/noe-thought-grounding-repair/latest.json`。
- `npm run verify:noe:soak-snapshot` PASS，生成 `output/noe-soak-daily/2026-06-12/report.json` 和 `output/noe-soak-daily/latest.json`；刷新 Noe100 到 `output/noe-100-readiness/noe-100-readiness-1781233859998.json`，仍 `score=97`、`passed=false`、activeDays `3/7`，不绕过 `not_enough_soak_evidence`。
- `npm run verify:noe:personality-dataset` PASS，生成 `output/noe-personality-dataset-readiness/personality-dataset-readiness-1781232973321.json` 和 `output/noe-personality-dataset-readiness/latest.json`；只读扫描显示 SFT `100/500`，invalid `0`，sensitive `0`，toolingReady `true`，formal training blocked。
- `npm run verify:noe:100-readiness` runs and honestly returns `passed:false`, score `97`, 1 blocker: `not_enough_soak_evidence`；最新报告 `output/noe-100-readiness/noe-100-readiness-1781243686431.json`。
- `npm run verify:noe:action-evidence-spine` PASS，生成 `output/noe-action-evidence-spine/action-evidence-spine-1781243786322.json` 和 `output/noe-action-evidence-spine/latest.json`；最新 self_learning 目标 `ad49f1c9-f741-424b-a7de-b2ad2dfe4689`，6 个成功 action evidence + 1 个 recovered step，actionStepsSatisfied `7/7`。
- `GET /api/noe/mind/proof` with owner token PASS：200，score `97`，lastRecovery `output/noe-runtime-restart-recovery-drill/20260612T021744Z/report.json`；当前最新 Noe100 reportPath 为 `output/noe-100-readiness/noe-100-readiness-1781233859998.json`。
- Browser check `/mind.html` PASS：proof block renders score/blocker/last tick/thought/action/recovery, console errors `[]`，390px viewport `overflowX=false`。
- `npm test -- tests/unit/noe-chat-profile-selfknowledge.test.js tests/unit/noe-context-engine.test.js tests/unit/noe-turn-context-engine.test.js tests/unit/noe-context-budgeter.test.js tests/unit/solo-chat-context-engine.test.js` PASS，5 files / 61 tests.
- `npm run verify:noe:action-evidence-spine` PASS.
- `npm run verify:noe:thought-memory` PASS，最新报告 `output/noe-thought-memory-eval/thought-memory-eval-1781241356477.json`，稳定入口 `output/noe-thought-memory-eval/latest.json`。
- `npm run verify:noe:self-evolution` PASS，198/198.
- `npm run test:p0:unit` PASS，105 files / 740 tests.
- `npm test -- tests/unit/routes/noe-mind-routes.test.js` PASS，1 file / 8 tests.
- `npm test -- tests/unit/noe-soak-daily-snapshot.test.js` PASS，1 file / 3 tests.
- `npm test -- tests/unit/noe-personality-dataset-readiness.test.js` PASS，1 file / 3 tests.
- `npm test -- tests/unit/noe-expectation-calibration-snapshot.test.js` PASS，1 file / 3 tests.
- `npm run verify:handoff` PASS，27/27.
- `git diff --check -- ':!games/cartoon-apocalypse/**'` PASS.
- `npm test` PASS，418 files / 3354 tests.
- 2026-06-12 14:10 文档一致性修正后复验：`npm run verify:handoff` PASS，27/27；`npm run test:p0:unit` PASS，105 files / 740 tests；`git diff --check -- ':!games/cartoon-apocalypse/**'` PASS。修正范围只涉及交接/设计/基准文档里的历史 Gemma 单脑口径标注与当前三角色路由指针；未触碰 `51735`、`51835`、LM Studio loaded models 或 live DB。
- 2026-06-12 14:09 CST P2 只读日报刷新：`npm run verify:noe:model-health` PASS，报告 `output/noe-model-health/model-health-1781244538774.json`；`npm run verify:noe:soak-snapshot` PASS，刷新 Noe100 到 `output/noe-100-readiness/noe-100-readiness-1781244549302.json`，仍 `score=97`、activeDays `3/7`；`npm run verify:noe:expectation-calibration` PASS，live resolved `4/20`、dueNowOpen `0`、hoursUntilNextOpenDue `22.34`。边界复核：`51735` PID `4773`、`51835` PID `36587` 只观察未重启；`lms ps` 仍只有 `qwen/qwen3.6-35b-a3b`，context `262144`、parallel `1`。
- 2026-06-12 14:23 CST 入口规则收敛：`AGENTS.md`、`CLAUDE.md` 已覆盖旧“硬边界作废 / 可读取密钥 / 自动 commit / 可触碰 51735”口径，改为当前硬边界和三角色模型路由；`AGENTS.md` 接手必读已把 2026-06-12 handoff、三角色模型执行记录、P0/P1/Noe100 执行记录和 Noe100 matrix 放到前五；`scripts/noe-handoff-consistency.mjs` 新增当前入口规则、read order、当前 handoff、三角色模型执行记录、Noe100 matrix 断言。验证：`node --check scripts/noe-handoff-consistency.mjs` PASS；`npm run verify:handoff` PASS，40/40；`npm run test:p0:unit` PASS，105 files / 740 tests；`git diff --check -- ':!games/cartoon-apocalypse/**'` PASS。未触碰 `51735`/`51835`/LM Studio loaded models/live DB。
- 2026-06-12 14:36 CST verifier owner-token 边界收敛：`node --check` PASS for `scripts/noe-full-current-verify.mjs`、`scripts/noe-phase5-runtime-verify.mjs`、`scripts/noe-cognitive-verify.mjs`、`scripts/noe-cognitive-runtime-verify.mjs`、`scripts/noe-real-use-replay.mjs`、`scripts/noe-handoff-consistency.mjs`。默认 live verifier policy gate 验证：`npm run verify:noe:phase5`、`npm run verify:noe:real-use-replay`、`npm run verify:cognitive`、`npm run verify:noe:cognitive-runtime` 均预期 exit 2，并写出 `tokenPolicy.policyBlocked=true`、`secretValueReturned=false` 的 report。安全 full-current：`npm run verify:noe:full-current -- --include-managed` PASS，报告 `output/noe-full-current/full-current-1781246085004.json`，11/11，未包含 live/cognitive token step。最低验证复跑：`npm run verify:handoff` PASS 45/45；`npm run test:p0:unit` PASS 105 files / 740 tests；`npm run verify:noe:self-evolution` PASS 198/198；`git diff --check -- ':!games/cartoon-apocalypse/**'` PASS。
- 2026-06-12 14:49 CST live/no-secret 复核：新增 gate 后 `npm run verify:noe:freedom-live`、`npm run verify:noe:capture-evidence -- --skip-delegate`、`npm run verify:noe:voice-ear -- --long-rounds 0 --bracket-rounds 0 --wake-rounds 0 --no-doc`、`npm run verify:noe:social-dom-live-probe` 均预期 policy-blocked，不读取 owner-token；`node --check` for these scripts PASS。`51835` 当前 PID `89417`，cwd 真实仓库；public `/health` 与 `/api/noe/readiness` 200/passed。只读日报刷新：`npm run verify:noe:model-health` PASS，报告 `output/noe-model-health/model-health-1781246930282.json`，loadedModels `["qwen/qwen3.6-35b-a3b"]`，Ollama now reachable，MiniMax/Xiaomi reachable，Gemini/OpenAI/Anthropic unconfigured；`npm run verify:noe:soak-snapshot` PASS，报告 `output/noe-soak-daily/2026-06-12/report.json`，Noe100 刷新到 `output/noe-100-readiness/noe-100-readiness-1781246930421.json`；`npm run verify:noe:expectation-calibration` PASS，live resolved `4/20`、dueNowOpen `0`、hoursUntilNextOpenDue `21.68`；`npm run verify:noe:100-readiness` 诚实返回 `score=97`、`passed=false`、唯一 blocker `not_enough_soak_evidence`，最新报告 `output/noe-100-readiness/noe-100-readiness-1781246930332.json`。最低验证复跑：`npm run verify:noe:full-current -- --include-managed` PASS 11/11，报告 `output/noe-full-current/full-current-1781246962402.json`；`npm run test:p0:unit` PASS 105 files / 740 tests；`npm run verify:noe:self-evolution` PASS 198/198；`npm run verify:handoff` PASS 49/49；`git diff --check -- ':!games/cartoon-apocalypse/**'` PASS。
- 2026-06-12 14:58 CST 剩余 owner-token 读取入口收敛：`scripts/restart-panel.mjs` 增加 `--ack-read-owner-token` / `NOE_ACK_READ_OWNER_TOKEN=1` gate；默认 `node scripts/restart-panel.mjs --check-only` 预期 exit 2，`restartMethod:"policy-blocked"`，`tokenPolicy.policyBlocked=true`，`secretValueReturned=false`，未执行 restart 或 protected API check。`scripts/perf-check.mjs` 默认不读 owner-token；`node scripts/perf-check.mjs` 对公开静态资源返回 200，对受保护 API 返回 401；owner 本轮明确授权后，`node scripts/perf-check.mjs --ack-read-owner-token` 验证受保护 API 均 200，输出未包含 secret 原值。`tests/e2e/noe-brain-ui-p0.e2e.mjs`、`tests/e2e/noe-freedom-stage-summary.e2e.mjs`、`tests/e2e/panel-ui-walkthrough.mjs` 改为默认只接受 `OWNER_TOKEN` 注入；读取 live `~/.noe-panel/owner-token.txt` 必须显式 `--ack-read-owner-token`，且裸跑保留端口 `51735/51835` 需要 `NOE_E2E_ALLOW_RESERVED_PORT=1`。`scripts/noe-handoff-consistency.mjs` 新增三条门禁：`restart_panel_token_requires_ack`、`perf_check_token_requires_ack`、`raw_e2e_token_requires_ack_or_injected_owner_token`。`node --check` 对以上 6 个脚本全部 PASS。剩余 `scripts/e2e-with-server.mjs` / `ce12-*` 读取的是隔离 HOME 生成的测试 token；`scripts/打开面板.command` 是用户显式打开面板 launcher，未在本轮运行或输出 token。
- 2026-06-12 15:03 CST 本轮最终验证：`npm run verify:handoff` PASS 52/52；`npm run test:p0:unit` PASS 105 files / 740 tests；`npm run verify:noe:self-evolution` PASS 198/198；`git diff --check -- ':!games/cartoon-apocalypse/**'` PASS。隔离 e2e：`node scripts/e2e-with-server.mjs` 在随机端口 `64460` / 隔离 HOME 下 PASS 18/18，端口已清理；`npm run test:e2e:freedom-stage` 在随机端口 `64522` / 隔离 HOME 下 PASS 21/21，端口已清理。Raw e2e 默认拒绝直连保留端口：`tests/e2e/noe-brain-ui-p0.e2e.mjs`、`tests/e2e/noe-freedom-stage-summary.e2e.mjs`、`tests/e2e/panel-ui-walkthrough.mjs` 均在默认 `51835` 下 exit 1 且先报 reserved-port refusal。`NOE_E2E_TARGET=tests/e2e/panel-ui-walkthrough.mjs node scripts/e2e-with-server.mjs` 未适用：wrapper 明确只接受 `tests/e2e/<name>.e2e.mjs`，没有进入测试执行。
- 2026-06-12 15:06 CST live 51835 恢复复核：只读端口检查发现 `51835` 一度无监听；按 owner 明确授权执行 `PANEL_RESTART_FORCE_DIRECT=1 node scripts/restart-panel.mjs --repair --ack-read-owner-token`，direct repair 启动 PID `61331`，cwd `/Users/hxx/Desktop/Neo 贾维斯`，`restartMethod:"repair-direct"`，`health.status:"passed"`，`readiness.status:"passed"`，warnings `[]`。随后 public `GET /health` -> 200 `{ok:true,status:"ok",service:"noe-panel",port:51835}`；public `GET /api/noe/readiness` -> 200，`readiness.status:"passed"`，counts memoryVisible `512`、focusDepth `4`、total `9`、enabled `9`、pendingApprovals `50`、pendingActs `0`。`npm run verify:noe:freedom-live -- --ack-read-owner-token` PASS 5/5：missing priorStageEvidence 被 409 阻断，有效 priorStageEvidence 200 通过，social DOM recipe probe 无 secret leak。`npm run verify:noe:full-current -- --include-managed` PASS 11/11，报告 `output/noe-full-current/full-current-1781247955718.json`。`npm run verify:noe:model-health` PASS，报告 `output/noe-model-health/model-health-1781247955880.json`，LM Studio loadedModels `["qwen/qwen3.6-35b-a3b"]`，`lmStudioLoadUnloadChanged=false`，未手动执行 `lms load/unload`。`npm run verify:noe:100-readiness` 仍 `score=97`、`passed=false`，唯一 blocker `not_enough_soak_evidence`，最新报告 `output/noe-100-readiness/noe-100-readiness-1781247983851.json`。`51735` 只读观察 PID `4773`，未触碰。
- 2026-06-12 15:43 CST 自我授权代码验证：`node --check` PASS for `scripts/lib/noe-standing-autonomy-grant.mjs`、`scripts/noe-standing-autonomy-grant.mjs`、`scripts/noe-freedom-live-smoke.mjs`、`scripts/noe-full-current-verify.mjs`、`scripts/noe-phase5-runtime-verify.mjs`、`scripts/noe-real-use-replay.mjs`、`scripts/noe-cognitive-verify.mjs`、`scripts/noe-cognitive-runtime-verify.mjs`、`scripts/noe-capture-external-evidence.mjs`、`scripts/noe-voice-ear-acceptance.mjs`、`scripts/lib/noe-social-dom-live-probe-utils.mjs`、`scripts/lib/noe-social-dom-live-probe-runner.mjs`、`scripts/restart-panel.mjs`、`scripts/perf-check.mjs`、`tests/e2e/noe-brain-ui-p0.e2e.mjs`、`tests/e2e/noe-freedom-stage-summary.e2e.mjs`、`tests/e2e/panel-ui-walkthrough.mjs`。`npm test -- tests/unit/noe-standing-autonomy-grant.test.js` PASS 5/5；`npm run test:e2e:raw` PASS 136/136；`npm run test:p0:unit` PASS 106 files / 745 tests；`npm run verify:noe:self-evolution` PASS 198/198；`npm run verify:handoff` PASS 71/71；`git diff --check -- ':!games/cartoon-apocalypse/**'` PASS。
- 2026-06-12 15:57 CST 连续主动节奏加速：自由档默认从保守间隔改为高频连续泵，`NOE_INNER_INTERVAL_MS=10000`、`NOE_GROWTH_INNER_INTERVAL_MS=10000`、`NOE_IDLE_INNER_INTERVAL_MS=30000`、`NOE_PROACTIVE_TICK_MS=15000`、`NOE_PROACTIVE_COOLDOWN_MS=300000`、`NOE_AFFECT_TICK_MS=15000`、`NOE_EXPECTATION_RESOLVE_MS=600000`、`NOE_WORKSPACE_DELIBERATIONS_PER_DAY=96`、`NOE_AUTONOMOUS_LEARNING_INTERVAL_MS=300000`、`NOE_AUTONOMOUS_LEARNING_CONTINUOUS=1`。`NoeGoalSystem` 已支持 self_learning 完成后立即滚动到下一主题，不再被创建时间间隔硬卡住；`proactiveTick` 增加 `in_flight` 守卫，允许 15s 高频判断但不并发叠加开口。验证：`node --check` for `server.js`、`src/cognition/NoeAdaptiveRhythm.js`、`src/cognition/NoeGoalSystem.js`、`src/cognition/NoeWorkspace.js`、`src/loop/proactiveTick.js` PASS；定向单测 5 files / 57 tests PASS；`npm run test:p0:unit` PASS 106 files / 745 tests；`npm run verify:noe:self-evolution` PASS 198/198。live 已 direct 重启到 PID `52991`，cwd `/Users/hxx/Desktop/Neo 贾维斯`，`/health` 200，`/api/noe/readiness` passed，`npm run verify:noe:freedom-live` PASS 5/5。SQLite `noe_tick_cursor` 只读核验：`meso=10000`、`micro=15000`、`proactive=15000`、`expectation=600000`。`51735` 只读观察 PID `4773`，未触碰；LM Studio loaded models 未改变，仍为 `qwen/qwen3.6-35b-a3b@6bit`。
- 2026-06-12 16:13 CST 连续自主运行验收：新增 `scripts/noe-continuous-autonomy-snapshot.mjs`、`tests/unit/noe-continuous-autonomy-snapshot.test.js`、`npm run verify:noe:continuous-autonomy`，并接入 `scripts/noe-handoff-consistency.mjs` 与 `test:p0:unit`。首次执行快照时 `51835` 无监听，诚实失败并写出 `output/noe-continuous-autonomy/continuous-autonomy-20260612T081235166Z.json`，blockers 为 `live_health_not_ok`、`live_readiness_not_ok`；DB 证据仍显示 `meso=10000`、`micro=15000`、`proactive=15000`、`expectation=600000`，且 10 分钟内有 done tick。因本任务需要 live 验收，执行 `PANEL_RESTART_FORCE_DIRECT=1 node scripts/restart-panel.mjs` direct 恢复 `51835`，脚本 startedPid `33980`；最终只读复核当前监听 PID `41764`，cwd `/Users/hxx/Desktop/Neo 贾维斯`，`/health` 200 `{ok:true,status:"ok"}`，`/api/noe/readiness` 200 `status:"passed"`。复跑 `npm run verify:noe:continuous-autonomy` PASS，最新报告 `output/noe-continuous-autonomy/continuous-autonomy-20260612T081620168Z.json`，稳定入口 `output/noe-continuous-autonomy/latest.json`；live health/readiness ok，四类游标全达标，10 分钟窗口内 `meso/micro/proactive` 均有 done tick，tick counts 包含 expectation done，`self_learning` total `29`、done `28`、active `1`，最新 active 目标 `7e06a02c-ce15-434d-89d4-f0bb889bce90`。本轮误写到 `/Users/hxx/Documents/Neo 2` 的两个临时文件已删除；真实仓库新增文件只在 `/Users/hxx/Desktop/Neo 贾维斯`。`51735` 只读观察 PID `4773`，未触碰；LM Studio loaded models 未改变，仍为 `qwen/qwen3.6-35b-a3b@6bit`。
- 2026-06-12 16:25 CST 连续自主节奏二次加速：自由档默认更新为 `NOE_INNER_INTERVAL_MS=5000`、`NOE_GROWTH_INNER_INTERVAL_MS=5000`、`NOE_IDLE_INNER_INTERVAL_MS=15000`、`NOE_PROACTIVE_TICK_MS=10000`、`NOE_PROACTIVE_COOLDOWN_MS=120000`、`NOE_AFFECT_TICK_MS=10000`、`NOE_EXPECTATION_RESOLVE_MS=600000`、`NOE_WORKSPACE_DELIBERATIONS_PER_DAY=192`、`NOE_AUTONOMOUS_LEARNING_INTERVAL_MS=60000`、`NOE_AUTONOMOUS_LEARNING_CONTINUOUS=1`。`scripts/noe-continuous-autonomy-snapshot.mjs` 的验收阈值同步收紧为 `meso=5000`、`micro=10000`、`proactive=10000`、`expectation=600000`；`NoeAdaptiveRhythm` 默认成长焦点 `5s`、空闲焦点 `15s`。验证：`node --check server.js` PASS；`node --check src/cognition/NoeAdaptiveRhythm.js` PASS；`node --check scripts/noe-continuous-autonomy-snapshot.mjs` PASS；`node --check scripts/noe-handoff-consistency.mjs` PASS；`npm test -- tests/unit/noe-adaptive-rhythm.test.js tests/unit/noe-continuous-autonomy-snapshot.test.js tests/unit/noe-goal-system.test.js tests/unit/noe-proactive-tick.test.js` PASS 4 files / 50 tests；`npm run verify:handoff` PASS 79/79。首次 live 复核 `npm run verify:noe:continuous-autonomy` 失败并定位到旧 `micro` 下限：`cadence_micro_too_slow_or_missing`，DB cursor 为 `micro=15000`；已修 `server.js` 为 `Math.max(10_000, Number(process.env.NOE_AFFECT_TICK_MS))` 并加 `server_defaults_match_fast_autonomy_cadence` 门禁。随后 direct 重启 `51835` 到 PID `51610`，cwd `/Users/hxx/Desktop/Neo 贾维斯`，`/health` 200 ok，`/api/noe/readiness` passed；复跑 `npm run verify:noe:continuous-autonomy` PASS，报告 `output/noe-continuous-autonomy/continuous-autonomy-20260612T083054683Z.json`，游标 `meso=5000`、`micro=10000`、`proactive=10000`、`expectation=600000`，10 分钟窗口内 `meso/micro/proactive` 均有 done tick。收尾验证：`npm run test:p0:unit` PASS 107 files / 754 tests；`npm run verify:noe:self-evolution` PASS 198/198；`npm run verify:handoff` PASS 79/79；`npm run verify:noe:freedom-live` PASS 5/5；`git diff --check -- ':!games/cartoon-apocalypse/**'` PASS。`51735` 只读观察 PID `4773` 未触碰；`lms ps` 只读显示 `qwen/qwen3.6-35b-a3b@6bit`，未执行 load/unload。
- 2026-06-12 16:39 CST 长期校准证据门硬化：`scripts/noe-expectation-calibration-snapshot.mjs` 改为只按自然 live `noe_expectations` resolved scored rows 判定 `liveCalibrationReady` 和 `readyForLongTermCalibration`；controlled/synthetic/fixture/test/drill 来源只计入 `controlledLiveRows`、`controlledLiveResolvedScored`、`brierControlledLive` 和 warning，不满足长期 live calibration。新增单测覆盖 20 条 controlled rows + 4 条自然 rows 不能凑够 `20/20`，以及自然 rows 达 20 时才 ready；`scripts/noe-handoff-consistency.mjs` 新增 `expectation_calibration_excludes_controlled_live_rows` 门禁，`docs/NOE_100_ACCEPTANCE_MATRIX.md` 同步口径。验证：`node --check scripts/noe-expectation-calibration-snapshot.mjs` PASS；`node --check scripts/noe-handoff-consistency.mjs` PASS；`npm test -- tests/unit/noe-expectation-calibration-snapshot.test.js` PASS 5/5；`npm run verify:noe:expectation-calibration` PASS，报告 `output/noe-expectation-calibration/2026-06-12/report.json`，total `140`、natural live resolved `4/20`、controlledLiveRows `0`、dueNowOpen `0`、nextOpenDueAt `1781296263545`、hoursUntilNextOpenDue `11.87`，长期校准仍 blocked：`live_expectation_resolved_below_20`。
- 2026-06-12 16:45 CST Noe100 readiness 口径同步硬化：`scripts/noe-100-readiness.mjs` 改为只按 natural live expectation rows 判定 `expectation_settlements_below_20`；controlled isolated drill 继续作为机制证据写进 details，但不让该 check 通过。`scripts/noe-soak-daily-snapshot.mjs` 同步输出 `naturalLiveResolved`、`controlledLiveResolved`、`controlledMechanismReady`、`longTermReady`。`scripts/noe-handoff-consistency.mjs` 新增 Noe100/matrix/soak 口径门禁。验证：`node --check scripts/noe-100-readiness.mjs` PASS；`node --check scripts/noe-soak-daily-snapshot.mjs` PASS；`npm test -- tests/unit/noe-100-readiness.test.js tests/unit/noe-soak-daily-snapshot.test.js` PASS 2 files / 5 tests；`npm run verify:noe:100-readiness` PASS as command，报告 `output/noe-100-readiness/latest.json`：`passed=false`、`score=94`、35/37、blockers `not_enough_soak_evidence` 与 `expectation_settlements_below_20`；`npm run verify:noe:soak-snapshot` PASS，`output/noe-soak-daily/2026-06-12/report.json` 显示 natural live resolved `4/20`、controlled drill `20/20`、`longTermReady=false`。受保护 `GET /api/noe/mind/proof` 使用本机 owner-token 读取验证，未打印 token：HTTP 200，`readiness.score=94`、双 blocker 可见。

## 剩余真实 blocker

- Noe100 不能宣称 100：activeDays=3，未满足 7 天 soak；natural live expectation resolved `4/20`，未满足长期 live calibration；`verify:noe:soak-snapshot` 已提供日报快照入口，当前 day `2026-06-12`、snapshotDayCount `1`。
- live expectation natural resolved 仍为 4/20；最新 `verify:noe:expectation-calibration` 显示当前 dueNowOpen `0`。受控隔离 drill 20/20 只证明机制，不替代长期真实校准，不能硬结算未来项。
- 模型恢复已做受控 `model_unloaded` fake provider drill；本轮按边界未改变 LM Studio loaded models，真实 `lms unload/load` 仍未执行。benchmark 只跑了 `--loaded-only` 当前主脑 no-load smoke，不是全候选 load/unload 对照。
- 社交平台/公网真实发布样本仍未做；当前只证明受控本地文件副作用 + rollback。
- 人格 SFT/LoRA 不能训练或采用：SFT 100/500，owner training plan 未批准，旧 gate report FAIL 2/12。

## 下一步建议

1. 每天跑一次 `npm run verify:noe:soak-snapshot` 留存 soak 日报；连续运行到 activeDays >= 7 后复跑 `npm run verify:noe:100-readiness`。
2. 每天跑一次 `npm run verify:noe:expectation-calibration` 留存 live 校准快照；继续让 live expectation 自然到期并由 resolver/owner 裁决，累计真实 resolved >=20 后复核 Brier 和 unknown ratio。
3. 若 owner 允许改变 loaded models，再另开窗口做真实 LM Studio `lms unload/load` 恢复 drill；当前已完成的是 no-state-change fake provider 恢复 drill。
4. 若要证明公网/社交外部动作，另开 owner 明确授权的真实发布/删除/隐藏样本，不要把本地文件 drill 冒充为社交发布。
5. 继续累积 SFT 到 >=500 对后再跑 `npm run verify:noe:personality-dataset`；只有 owner 明确训练计划、正式数据达标、训练后 `scripts/noe-lora-gate.mjs` 全 PASS，才允许考虑采用。
