# HANDOFF 2026-06-12 - 任务回报自修复与语音兜底

## 真实仓库

- Repo root: `/Users/hxx/Desktop/Neo 贾维斯`
- 不使用 `/Users/hxx/Documents/Neo 贾维斯` 或 `/Users/hxx/Documents/Neo 2`
- 不触碰 `51735`
- 不读取或输出 `.env`、token、API key、cookie、OAuth、`room-adapters.json`

## 本轮已完成

- 已提交 `bc1a0df 任务回报: 接通故障自修复和语音兜底`
- 新增 `src/cognition/NoeIncidentEscalator.js`
- 接入 `server.js`、`NoeWorkspace`、`NoeTaskReportbackQueue`、`src/server/routes/noe.js`、`public/cognitive.html`
- 覆盖源：
  - `inner_monologue`
  - `failed_action`
  - `task_reportback play_failed`
- 故障升级路径：
  - 识别系统故障
  - 去重
  - 创建 `source=system_repair` 目标
  - 自动生成 `act shell.exec` 诊断步骤
  - 自动跑相关测试
  - 前端任务卡持续显示 accepted/running/done
  - 任务终态语音回报
  - 浏览器播放失败时写回 `speechFailedAt/speechError`
  - macOS `/usr/bin/say` 系统语音兜底，并写回 `systemSpeechFallback`

## 验证证据

- `npm test -- tests/unit/noe-incident-escalator.test.js tests/unit/noe-task-reportback-queue.test.js tests/unit/routes/noe-routes.test.js tests/unit/noe-task-reportback-wiring.test.js`
  - 4 files / 24 tests passed
- `npm run test:p0:unit`
  - 107 files / 757 tests passed
- `npm run verify:noe:self-evolution`
  - 198 passed / 0 failed
- `npm run verify:handoff`
  - 83 passed / 0 failed
- `git diff --check -- ':!games/cartoon-apocalypse/**'`
  - passed
- live `51835` 已重启验证，最后观察 PID `71554`，cwd `/Users/hxx/Desktop/Neo 贾维斯`
- `51735` 只观察未触碰，最后观察 PID `4773`

## Live 实机结果

- 注入“语音断了”内心事件后，自动创建并完成：
  - `系统自修复：语音链路`
- 任务语音播放失败后，自动创建并完成：
  - `系统自修复：任务回报链路`
- 认知页能持续显示任务卡，不再只有“我去查”但无后续。
- 在当前 in-app browser 环境，浏览器音频仍会出现 `play_start_timeout`。
- 后端已记录失败并兜底：
  - `speechFailedAt`
  - `speechError: play_start_timeout`
  - `systemSpeechFallback: { attempted: true, command: "say" }`

## 剩余方向

1. 降低误触发自修复风暴：当前内心里提到 `play_start_timeout` 也可能触发额外 `system_repair`。下一步应给 incident escalator 加来源权重和冷却审计视图，只让真实失败 ack / failed action 默认升级，普通内心提及走 shadow。
2. 把浏览器音频确认做成更强 telemetry：区分 `tts_synthesized`、`play_started`、`played_to_user_confirmed`、`play_start_timeout`、`play_confirm_timeout`。
3. 给系统语音兜底加 UI 标记：任务卡上显示“浏览器播放失败，已用系统语音兜底”，避免 owner 不知道为什么声音来自系统。
4. 做一次真实用户点击后的 Safari/Electron 对照测试：当前 Codex in-app browser 仍会挡播放，不代表 Electron/Safari 一定失败。
5. 对 live probe 生成的测试任务做归档/过滤：测试任务不应长期挤占认知页最近任务视图。

## 2026-06-12 后续修复：任务回报语音风暴收口

- 已处理 owner 反馈的偶发系统播报：
  - “浏览器任务语音播放失败是因为超时了……系统语音兜底……自修复目标自动收口”
- 根因确认：
  - 前端 `public/cognitive.html` 在任务回报音频 15 秒内未触发播放开始时，会回写 `play_start_timeout`。
  - 后端 `/api/noe/tasks/reportbacks/speech-ack` 过去默认启用 `NOE_TASK_REPORTBACK_SYSTEM_TTS=1`，因此播放失败会再调用 macOS `say` 或 `afplay` 兜底。
  - `play_start_timeout` 相关文本又会进入 incident escalator / 内心反刍，形成“播放失败 -> 系统自修复 -> 完成回报 -> 再播放”的风暴。
- 已落地修复：
  - `src/server/routes/noe.js`：任务回报系统语音兜底默认改为关闭；只有显式 `NOE_TASK_REPORTBACK_SYSTEM_TTS=1` 才启用。
  - `src/cognition/NoeIncidentEscalator.js`：`task_reportback` / `inner_monologue` 中的 `play_start_timeout`、`play_confirm_timeout`、浏览器任务语音播放失败不再升级为自修复目标。
  - `src/cognition/NoeTaskReportbackQueue.js`：`系统自修复：*` 任务回报默认 `speak=false`，仍显示在 UI，但不自动播报。
  - 历史队列 `~/.noe-panel/task-reportbacks.json` 已备份并标记旧失败为已处理：
    - 备份：`~/.noe-panel/task-reportbacks.json.bak-20260612T155050Z-speech-storm`
    - 旧 live probe 残留静音备份：`~/.noe-panel/task-reportbacks.json.bak-20260612T155600Z-live-probe-speech`
    - 静默系统自修复回报：19 条
    - 标记播放超时已处理：33 条
    - 最终计数：`speakTrue=0`、`systemRepairSpeakTrue=0`、`playbackTimeoutUnhandled=0`
- live 收口：
  - 51835 已用 `npm run restart:panel -- --ack-read-owner-token` 重启并加载当前代码。
  - 当前 listener：PID 44668，cwd `/Users/hxx/Desktop/Neo 贾维斯`。
  - `/health` 与 `/api/noe/readiness` 均通过；`verify:noe:freedom-live -- --ack-read-owner-token` 通过。
  - 51735 未重启、未请求业务接口；仅查询监听状态确认其仍由 PID 4773 占用。
- 定向验证：
  - `npm test -- tests/unit/noe-incident-escalator.test.js tests/unit/noe-task-reportback-queue.test.js tests/unit/routes/noe-routes.test.js tests/unit/noe-task-reportback-wiring.test.js`
  - 4 files / 28 tests passed
  - `npm run test:p0:unit`：107 files / 759 tests passed
  - `npm run verify:noe:self-evolution`：198 passed / 0 failed
  - `npm run verify:handoff`：83 passed / 0 failed
  - `git diff --check -- ':!games/cartoon-apocalypse/**'`：passed

## 接手提示

```text
继续 `/Users/hxx/Desktop/Neo 贾维斯`。先读 AGENTS.md、CLAUDE.md 和 docs/HANDOFF_2026-06-12_任务回报自修复语音兜底.md。
不要碰 51735，不要读取 secret。
本轮已完成任务回报语音风暴收口，但当前工作区仍有未提交改动；不要自动 commit，除非 owner 明确要求。
下一步优先做：任务卡显示 systemSpeechFallback 状态 + Electron/Safari 真实音频确认对照 + P6 owner-perceived delivery 的真实用户样本。
验证最少跑：
npm test -- tests/unit/noe-incident-escalator.test.js tests/unit/noe-task-reportback-queue.test.js tests/unit/routes/noe-routes.test.js tests/unit/noe-task-reportback-wiring.test.js
npm run test:p0:unit
npm run verify:noe:self-evolution
npm run verify:handoff
git diff --check -- ':!games/cartoon-apocalypse/**'
```
