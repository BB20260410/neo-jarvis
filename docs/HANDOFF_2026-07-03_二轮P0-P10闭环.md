# HANDOFF 2026-07-03 · 二轮 P0–P10 闭环（接手从这里开始）

> owner /goal 升级为闭环:一轮 P0–P10 全解决点火后,再体检出新 P 表,**再全部解决**。本文档是二轮唯一入口;一轮见 `HANDOFF_2026-07-02_P0-P10大修与总点火.md`。

## 基线（2026-07-03 实测）
- 全量 `npm test`:**866 文件 / 8096 测试**(一轮 8083→二轮 +13);`npm run test:e2e` 18/18;生产 51835 HTTP 200
- 生产已恢复飞轮点火(`NOE_SELF_EVOLUTION=1`,二轮期间曾用 plist=0 停防双写,收尾恢复);RSS ~739MB(观察项,含 2GB sqlite mmap 热页)
- 公开仓 neo-jarvis:PII 历史已压单 commit 重写(author 全 Neo Maintainer),CI 对新历史绿,v0.1.0 在

## 二轮 commit（noe-main,本地未 push,owner 惯例）
- R2-P0 `f6ec353` **社媒重复发布幂等洞(唯一不可逆副作用级)**:点击前预落 attempt 标记,去重闸认 attempted|performed,落盘失败绝不点击(从「发布后失败」增强为「发布前拦截」)
- R2-P1 `7964d9c` 自愈资源债:autopilot schedule 无上界 do-while 改一步跳算(停机数天+1s 间隔旧版迭代百万次阻塞事件循环)+日志轮转+清 79 个 PPID=1 孤儿进程
- R2-P2 `b4089d1`+`52ec19e` ActPipeline 真实执行标 executing 非 dry_run(卡死可 retry)+隔离泵 timer 清理+空 catch 留痕
- R2-P3 `3d2329d` NoePromptPrefix 接 resolve(NOE_PROMPT_PREFIX OFF)
- R2-P4 `54ddc51` FuzzyPatchMatcher 接 apply 未命中回退(NOE_FUZZY_PATCH OFF,唯一+相似度双核,verify/rollback 全照走)
- R2-P5 `e0fad7b` 心跳 onOverdue→heartbeat_overdue 事件 + `09daa82` 棘轮基线校准 3712→3717
- R2-P6 `eca3c37` 前端 WS 永续重连+innerHTML 转义+深链 hash+轮询生命周期
- R2-P7 `817a9a3` 删 log-ring 孤儿
- R2-P9 `52378c2` 拆 NoeFreedomAdapters 2146→193 行(5 子模块,逐字节一致)
- 收编 `291207e` 飞轮 toSafeMessages 对象形态边界守卫

## 二轮新 flag（默认 OFF,待 owner 点火,见 scratchpad/ignition_list.md）
- `NOE_PROMPT_PREFIX=1` — prefix cache 稳定化(收益前提:system prompt 含每轮变易变行;host 已启动缓存本就稳定,预留)
- `NOE_FUZZY_PATCH=1` — apply 精确 from 未命中时容漂移唯一匹配回退(强制走 verify;抬飞轮 apply 成功率)

## 二轮明确降级/未做（诚实清单）
- **R2-P8 deps 升级(undici→8/otel→2/ajv→8/express→5/electron→43)降级专门窗口**:非 bug(现版本正常)+ 各有 breaking change + 本轮已改 20+ 文件,叠加高风险升级放大回归面违隔离半径最小。**作战计划在 scratchpad/deps_upgrade_plan.md**(按风险升序 undici→otel→ajv→express→electron,每步单升+全量+生产验证)。
- **R2-P5 Consistency→doctor / IntegrationMetric→metrics 降级**:两模块已写好+有测试(能力存在),接入属增量观测 dashboard 且碰核心诊断链,按变更纪律留专门窗口。onOverdue 是 R2-P5 实质交付。
- **NoeLaneQueue 未删**:体检称孤儿但被 NoeDoctor 存在性哨兵清单(NoeDoctor.js:248)引用,删它要动审计清单(牵一发),保守保留待专门窗口连哨兵一起摘。
- **NoeCuaDriver 保留待接线**:体检建议接 freedom 工具表作本机 GUI 直驱,但写类操作需 owner TCC 授权才完整,属能力扩展非清理。

## owner 事项
1. 二轮新 flag(NOE_PROMPT_PREFIX/NOE_FUZZY_PATCH)是否点火:FUZZY_PATCH 值得点(抬 apply 成功率、有双核安全兜底);PROMPT_PREFIX 可缓(收益前提未满足)。
2. 孤儿进程根因:chrome-devtools-mcp-safe-server 未随父 Codex 退出而清,79 个 PPID=1 已清,但根因(safe-server 无父死自清)待长期修——建议给 safe-server 加 `process.on('disconnect')`/父进程存活探测自退。
3. GitHub profile 显示名改笔名(防 PII 复发,一轮已提)。
4. deps 升级挑独立窗口按 scratchpad/deps_upgrade_plan.md 做。

## 观察项
- RSS 739MB(boot 后爬升,heapUsed 健康,大概率 mmap 稳态)——建议 30-60 分钟复查趋势
- agent_runs 49.6 万行,retention 30 天默认清 0(行都在窗口内);若日增显著可 `NOE_AGENT_RUNS_RETENTION_DAYS=14` 收紧
- 飞轮恢复后首轮 verify_not_green 回滚是防作弊闸正常代谢;evolution_outcome.reason(一轮 P2)现可归因
