# ROADMAP P0–P15 代码引用校对表（P0-D 交付物）

> 生成：2026-06-15 · 方法：函数名/关键词 grep 核（**行号随 commit 漂、函数名稳**，故漂移用函数名定位即可）
> 图例：✅ 准确 / ⚠️ 微漂移（指向同一逻辑块，可接受）/ ❌ 错误（已修）

| 引用对象 | ROADMAP 标注 | grep 实测 | 结论 |
|---|---|---|---|
| judge 证据构造工厂 | `NoeExpectationResolver.js:1186` | `:1186 export function buildEventsEvidence` | ✅ |
| judge 不查向量库 | `NoeExpectationResolver.js` 全文 0 embedding | `grep -nE 'embedding\|cosine\|vectorIndex\|semanticSearch\|recallFused'`=0（149 处 `semantic*` 经追证均 bigram 词面） | ✅ |
| 维度黑洞 SQL 过滤 | `VectorIndex.js:85` | `:85 const conds = ['dim = ?']`（`:121` 同，semanticSearchVectors） | ✅ |
| 黑洞真因比对点 | （v3 新引）`VectorIndex.js:47` | `:47 const sameDim = rows.find(r => Number(r.dim) === qvLen)` | ✅ |
| owner 预测落空触发 | `NoeOwnerBehaviorPredictor.js:148` | `:148 FOLLOWUP_FAIL_RE.test(raw)`（`:39` 定义，`:154` outcome=1，确认仅 owner 明确否定 followup 才落空、topic/沉默不落空） | ✅ |
| 自驱链硬传 confirmOwner | `NoeSelfEvolutionExecutors.js:87` | `:87 runNoePatchApply({... confirmOwner: true ...})`（`:103/:172` rollback 同传 true） | ✅ |
| owner 预测门控 | `server.js:2024` | `:2024 if (noeExpectationLedger && process.env.NOE_OWNER_PREDICTION === '1')` | ✅ |
| selfEvolve 心跳 job | `server.js:2078` | register 实际在 `:2080`（`:2078` 是块起始/注释） | ⚠️ 微漂移 |
| integration 心跳 job | `server.js:2129` | register 实际在 `:2130` | ⚠️ 微漂移 |
| free 自主档默认表 | `server.js:32-66` | NOE_AUTONOMY_DEFAULTS 区间 | ✅ |

**结论**：ROADMAP 核心代码引用**全部准确**；2 处 server.js register 行号差 1–2 行（指向同逻辑块，已标注，建议引用 `register('selfEvolve'`/`register('integration'` 函数名而非裸行号）；断点表 file:line 在 ROADMAP v2 已校正（断点1 `:1186`、VectorIndex `src/embeddings/`）。

**自我校对纪律（R5 整改）**：改任何模块前先 `grep` 既有测试覆盖——本次发现 `tests/unit/noe-vector-index-dim-warn.test.js`（69 行）已覆盖维度告警 setup，P0-B 应**扩展该文件**而非并行新建。
