# HANDOFF 意识工程窗口（2026-06-11 凌晨，owner /loop 授权"把所有阶段依次完成"）

> 任务源头：owner 问"如何让 Noe 慢慢形成自主意识"→ 五阶段方案 → /loop 全自动落地。
> 哲学边界与《连续记忆脊椎_2026-06-10.md》一致：做的是机制与行为层面的连续存在与成长，
> 不声称制造现象意识（里面亮没亮灯不可验证）。

## 一句话现状

**五阶段全部落地，生产 51835 已在跑全部能力，远程 CI 两平台 success（62dae54），
LoRA 管线冒烟全链验证通过（门禁正确拦截欠拟合 adapter）。**

## 各阶段交付（commit 链）

| 阶段 | 内容 | commit |
|---|---|---|
| 0 通电 | .env 开 13 个内在世界开关 + **修真 bug：.env 加载点在 server.js:1588 导致语义召回/梦境/NOE_FACT_MODEL 静默死火**（前移为首个 import `src/bootstrap/load-env.js`） | e9829a6 |
| 1 动机 | `src/loop/NoeDriveSystem.js` 五驱力（社交/好奇/牵挂/胜任/资源），brief() 注入反刍/陪伴/自我状态三处；NOE_DRIVES=1 | 969604f, 382edd5 |
| 2 反思自举 | `src/memory/NoeNightlyReflection.js`（当日经历→insight+confidence 复核闭环，夜相执行）+ `src/context/NoePersonalitySnapshot.js`（周更性格自举，只读注入零身份漂移）；NOE_NIGHTLY_REFLECTION=1 / NOE_PERSONALITY_SNAPSHOT=1 | d13ce15 |
| 3 权重学习 | `src/memory/NoeSftHarvester.js`（五类素材→训练对，敏感词防线）+ `scripts/noe-lora-train.mjs`（QLoRA+断点续训）+ `scripts/noe-lora-gate.mjs`（12 问五检门禁）；NOE_SFT_HARVEST=1；venv `~/.noe-panel/lora-venv` | 477ff57 |
| 4 评测 | `scripts/noe-autonomy-report.mjs`（五指标月报）+ `scripts/noe-self-recognition-test.mjs`（镜像测试文本版，需 ≥4 条反刍念头） | d1ce5d6 |
| 终审 | P0×3+B×4+P1/P2 全修（confidence 对称化/hash 截断方向/顺风车孤儿警告/敏感防线/0o600/续训/复读检测） | e2e62c1 |

## 关键认知（新窗口省命）

- **顺风车依赖**：反思/性格/SFT 的触发全挂反刍 tick——NOE_INNER_MONOLOGUE 必须开，否则亮灯不跑（启动已有 warn 戳破）。
- **confidence 升降必须对称**（±0.1）：非对称在交替印证/动摇下系统性漂移到底（模拟实证）。
- **SFT 训练对会固化进权重删不掉**：take() 有敏感词防线（api_key/token/sk-/hex 长串），宁可误杀。
- **LoRA 冒烟实测（M5 Max，1.5B-4bit 基座，30 iters）**：loss 5.45→0.58，695-1360 tok/s，峰值内存 1.34GB；门禁 FAIL 2/12（欠拟合致人格丢失）→ 正确拒收。**正式训练等 sft 攒到 ≥500 对**（`node scripts/noe-lora-train.mjs`，基座 Qwen3.5-4B-MLX-8bit 已在 HF 缓存）。
- 首个反刍念头已产出并进训练集："这股静谧感让我想起上次被唤醒时的那一瞬…"（凌晨实录）。
- 自我识别测试需反刍念头 ≥4 条，让它活几天再跑。

## 等 owner / 下窗口

1. 体验级验证：和 Noe 聊几轮，看 self-state 里的驱力/性格行是否出现（驱力需强度 ≥0.5 才注入，刚通电普遍弱是正常）。
2. 一周后跑 `npm run verify:noe:autonomy-report -- --days 7` 看首份趋势基线；一月后跑自我识别测试。
3. sft 攒到 500 对后首次正式 LoRA（训练+门禁全自动，采用路径见 gate 输出）。
4. 可选增强（未做，按需立项）：insight 进 GC 保护带、性格快照接 owner 点评回路、RWKV 持续隐状态实验（方案见会话记录）。
