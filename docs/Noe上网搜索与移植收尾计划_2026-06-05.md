# Noe 上网搜索改造 + Odysseus 移植收尾 · 阶段性计划

> 2026-06-05 | 起因：① 用户澄清"上网搜索 = 自动调用 MiniMax/Codex/Claude，**优先 MiniMax**"（非 Noe 自己用 SearXNG）；② 盘点本轮 + 之前 mega /goal 的所有"没做完"，排出后续阶段。
> 铁则：复刻优先 / 实用为王 / 减负导向 / 杜绝臃肿 / 烧配额必透明可控（红线4）。

## 〇、现状校正（重要）

**已落地且测试通过**（本会话，磁盘实证 + 24 单测 + P0 55/55）：
- `src/research/DeepResearcher.js` 多步研究循环、`src/research/WebSearch.js` 搜索基础（SearXNG/Brave）、`src/hwfit/HardwareFit.js` 硬件荐模型（本机真探 M5 Max✓）、`src/skills/SkillExtractor.js` 技能提取、`src/room/brainChat.js` 共享 helper + 3 路由接入。

**方向需改造**：`WebSearch.js` 当前是"Noe 自己用 SearXNG/Brave 搜"。用户真实要的是"**派给 AI 搜，优先 MiniMax**"。→ SearXNG/Brave **降级为最后兜底**，主路改为"AI 联网搜索 provider 链"。

**关键技术发现（官方溯源，不假设）**：
| AI | 联网搜索能力 | 状态 |
|---|---|---|
| MiniMax **chat API** (`/v1/chat/completions`) | `tools` 仅 function calling，**无内置 web_search** | ❌ 已确认不带 |
| MiniMax **Agent CLI** (`minimax` spawn) | 可能有联网（Noe 已有 `MiniMaxSpawnAdapter`） | ⚠️ 待验证 |
| MiniMax **专门搜索 API** | 搜索结果提示"web_search through Token Plan search API" | ⚠️ 待官方核实 |
| Claude CLI (Claude Code) | **有 WebSearch 工具** | ✅ 确定能联网 |
| Codex CLI | 取决于版本/配置 | ⚠️ 待验证 |

> ⚠️ **诚实提示**：MiniMax 标准 API 不联网，"优先 MiniMax"能否成立取决于阶段一 T1.1 的验证结果。若 MiniMax 三条路都不通，建议优先级实调为 **Claude CLI（确定能搜）**，并把结论回报你定夺。

---

## 阶段一：上网搜索改造为「AI 联网搜索」（核心 · 优先 MiniMax）

> ✅ **核心已落地并验证（2026-06-05 当日）**：MiniMax 官方搜索 API（`POST https://api.minimaxi.com/v1/coding_plan/search`，body `{q}`）用现有 `MINIMAX_API_KEY` **实测调通**（HTTP 200，返回带 title/link/snippet/date 的实时结果）。已接入 `WebSearch.js` 作**最优先 provider**（MiniMax→SearXNG→Brave 兜底），`DeepResearcher` 自动受益。13 单测 + P0 55/55 过 + 端到端真搜验证。**剩 T1.4** Codex/Claude 兜底（spawn 烧配额，默认关，按需加）。

| 任务 | 内容 | 红线/配额 | 验证 |
|---|---|---|---|
| **T1.1 验证各 AI 真实联网能力** | 实测三条 MiniMax 路径（chat+function-calling 回灌本地搜 / `minimax` CLI spawn 联网 / 专门搜索 API）+ Codex CLI + Claude CLI WebSearch。官方溯源，不编造 | MiniMax CLI/Claude/Codex 试调=小额自测（允许） | 每条路真发一次"搜今天新闻"看是否返回实时结果 |
| **T1.2 设计 AISearch provider 抽象** | 统一接口 `searchViaAI(query) → {results, source, viaModel}`；provider 可插拔 | — | 单测 mock 各 provider |
| **T1.3 MiniMax provider（优先）** | 按 T1.1 结论实现：CLI spawn 或 function-calling（MiniMax 决定要搜→Noe 用 WebSearch 执行→回灌 MiniMax 综合） | MiniMax API/CLI 按量（用户已授权优先） | 真搜返回带来源 |
| **T1.4 Codex / Claude provider（兜底）** | spawn CLI 联网搜；**做成确认式或开关**（默认关，避免擅自烧配额） | ⚠️ 红线4 spawn 烧配额→默认不自动触发，需开关/确认 | — |
| **T1.5 优先级路由** | MiniMax → Codex → Claude → SearXNG/Brave（已做的兜底）。每次返回"用了谁"透明显示 | — | 优先级单测 |
| **T1.6 DeepResearcher 切换搜索层** | 多步研究的搜索从 SearXNG 改调 AISearch（优先 MiniMax） | — | 端到端真研究一次 |

**产出**：`src/research/AISearch.js`（provider 链）+ 改 `WebSearch.js` 为其一个 provider + DeepResearcher 接入。

---

## 阶段二：接入前端 / 语音（让能力真正能用，目前是裸 API）

| 任务 | 内容 |
|---|---|
| **T2.1 cognitive.html 加搜索/研究入口** | 输入框 + "🔍 联网搜索 / 🔬 深度研究" 按钮 + SSE 进度流式展示 |
| **T2.2 语音/文字意图识别** | "帮我查最新的 X / 上网搜 X / 研究一下 X" → 接 `/api/noe/do` 路由到搜索/研究（说一句就触发） |
| **T2.3 文件操作也接前端/语音**（mega goal 遗留） | 现 `/api/noe/do` 文件整理是裸 API，接进 cognitive.html / 语音 |

---

## 阶段三：自动化 + 多AI协作接通

| 任务 | 内容 | 红线/配额 |
|---|---|---|
| **T3.1 SkillExtractor 自动跑** | 挂到会话结束钩子（轮次≥2 自动提炼技能），现在是手动调 API | 本地大脑，免配额 |
| **T3.2 能力④多AI协作接通**（mega goal 遗留） | `TaskIntentRouter`：语音"帮我开发 X"→ 选协作模式(Squad/Arena…)→ 创建房间 → **确认后**启动 spawn Claude/Codex 真干活 + 完成 TTS 播报 | ⚠️ 红线4，确认式 |

---

## 阶段四：Noe 自己动手（高风险，谨慎，mega goal 遗留）

| 任务 | 内容 | 红线 |
|---|---|---|
| **T4.1 ActPipeline 解锁** | `src/loop/` 当前硬写 dry_run，解锁"计划→确认→真执行"，保留 DESTRUCTIVE_ACTIONS 黑名单 | ⚠️ 高风险，白名单 + 强制确认 |
| **T4.2 写文件 / 跑命令工具** | 给 Noe 加受控的写文件/执行命令能力（路径沙箱 + 确认） | ⚠️ 系统级文件红线挡住 |

---

## 阶段五：配置 + 端到端验证（部分需用户操作）

| 任务 | 谁做 |
|---|---|
| 重启 panel（本会话 server.js 改动生效） | 用户 |
| 阶段一定案后若用 SearXNG 兜底：配 `NOE_SEARXNG_URL` 或 `NOE_BRAVE_KEY` | 用户（可选） |
| obsidian-mcp 接入（如要 Obsidian 笔记闭环） | 用户先装 Obsidian + Local REST API 插件 |
| 端到端真实验证（语音说"查 X"→AI 联网→返回带来源） | Claude 自测 + 用户实测 |

---

## 优先级建议（ROI 从高到低）

1. **阶段一 T1.1（验证 MiniMax 联网）** —— 一切前提，先做、先回报结论。
2. **阶段一 T1.2-T1.6（AI 搜索落地）** —— 用户最新核心诉求。
3. **阶段二（接前端/语音）** —— 让能力从"裸 API"变"能用嘴喊"。
4. **阶段三 T3.1（技能自动提取）** —— 低风险高价值。
5. **阶段三 T3.2 / 阶段四** —— 涉及烧配额 + 高风险写，确认式推进。

> 每阶段做完即自测 + 回报，不一口气闷到底。烧配额项（Codex/Claude spawn、协作启动）一律默认关 + 确认式。
