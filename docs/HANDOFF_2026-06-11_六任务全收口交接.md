# HANDOFF 六任务全收口交接　2026-06-11 凌晨

> 接续 `HANDOFF_2026-06-10_全窗口总交接.md` §5 的任务队列，本窗（多 agent 工作流编排）把队列里 AI 可做的任务**全部依次完成**。
> 接手提示词同前份（AGENTS.md 宪法 / git status / hooksPath 三件事），不再重复。

## 0. 一句话现状

**任务队列清零：S23 server.js 三域拆完（2581 行）、S24 app.js 房间域拆完（4627 行）、S25b css 全部 <500 行、e2e walkthrough 修到 150/150 满分、ContextEngine 死代码标废弃、内在世界七支柱全部落地（5 个新 env 开关默认 OFF）。全部已 push（HEAD=7bc21cf 后另有本文档 commit），本地 334 文件 / 2726 测全绿，远程 CI 双平台绿。生产 51835 仍跑旧代码，一切新能力等 owner kickstart + 开关拍板。**

## 1. 本窗六条线总账

| 线 | 做了什么 | commit |
|---|---|---|
| ① S23 收尾 | server.js 拆出 sessions(19 路由)/rooms-advanced(14)/ops(4) 三域共 37 条 → 6 个新模块（sessions/sessionsContinuum/roomsAdvanced/roomsForward/roomsMedia/ops.js），4475→2581 行；37/37 对账保序、51998 实机三域读写 7/7 | 79e5cb4 / e14b46f / 2839464 |
| ② ContextEngine 房务 | LegacyNoeContextEngine 查证运行时零接线、"可收编件"为空集（uiSignals/acuiCards 实为共享 builder 转调）→ 标废弃（@deprecated 块+误导注释清理）；删除留 owner 确认 | 6ea2963 |
| ③ S24 房间域 | app.js 六批次外迁聊天室+Squad+Chat 房整个房间域（batch7-12）：chatMedia/cluster 工具/cluster 实时渲染/房间核心+PanelCore 桥改造/辩论+Squad/handleRoomEvent 总分发+操作绑定，7831→4627 行，10 个 rooms-*.js 模块；100/100 符号对账、桥 54 键完整、真浏览器深 smoke 13/13 | e646d29→2861b35 六连 |
| ④ S25b css | 9 个 ≥500 行分域文件细拆成 23 子段（全部 <500，最大 415），拼接逐字节 IDENTICAL，壳平铺 25 段级联顺序不变 | 5db7c1e |
| ⑤ e2e 修复 | walkthrough 脚本对齐 UI 折叠改版：8/27→149/150；顺手查实并修掉真缺陷 noe-freedom-tools 裸开无 token 闸 → **150/150 满分**（连跑 3 遍可重复） | a246aa3 / 4bd52a5 |
| ⑥ 内在世界七支柱 | 见 §2 | 1c26a40→7bc21cf 六连 |

过程纪律：全程隔离端口 51998 + 临时库自测，生产 51835/51735 零触碰、真库零污染（终验 sqlite 实查）；每步小步 commit、pre-push 全量把门、三次里程碑 push CI 全绿。

## 2. 内在世界七支柱（连续记忆脊椎延伸，全部注入式+env 门控默认 OFF+fail-open+单测）

| 支柱 | 模块 | 干什么 |
|---|---|---|
| ① 记录覆盖扩展 | research/noeDelegation/SoloChatDispatcher 三段式接线 | 深研究(observation·4)/派活(milestone·4)/聊天室(observation·2)也写进自传体时间线；绝不用 interaction 防污染心境推断 |
| ⑦ 时间节律 | src/loop/NoeCircadian.js | 一天四相+23:00-08:00 静默：反刍×4 降频、主动陪伴夜里闭嘴（承诺顺延早 8 点）、自我状态带 timeOfDay、深夜心境"夜深了，安静守着" |
| ④ mood 模型化 | src/context/NoeMoodAnalyzer.js | 心境从启发式升级本地 gemma 评 ≤10 字短语；异步缓存同步读（snapshot 绝不等模型），挂掉自动回启发式 |
| ② 梦境升华 | src/memory/NoeEpisodeSublimation.js | 90 天前旧情景按周压成第一人称摘要进长期语义记忆（水位线防重复、salience≤4 永不进身份带、夜里跑）；EpisodicTimeline 新增 aged() |
| ③⑥ 反刍升华 | src/loop/NoeThoughtSublimation.js | 反刍念头命中"想跟主人说/惦记"模式 → 自生承诺入店（前缀"Noe 心声："、上限 2 条、dedupe），经既有 proactiveTick 通道说出口，零新说话通道 |
| ⑤ 叙事自我 | src/context/NoeNarrativeSelf.js | 日更把时间线压成 2-3 句"我的故事"注入自我状态；**只读注入块零人格漂移**（反哺 identity 层未做，需 owner 明示才做）；持久化 narrative-self.json |

全部已登记 src/context/NoeSelfKnowledge.js（6 个 detect*Status 活态检测）。终验全开关实机：两轮对话跨轮记住"星尘27号"、反刍夜间 360s=90s×4 精确实证、叙事 gemma 真产出、梦境无陈年情景安全 no-op。

## 3. env 开关全表（新增 5 个 + 既有，生产全部待 owner 拍板）

| env | 作用 | 建议 | 拍板点 |
|---|---|---|---|
| `NOE_CONTINUITY=1` | 连续记忆+自我状态注入对话（上窗已建，本窗扩展了三路径记录） | **开** | 无 |
| `NOE_INNER_MONOLOGUE=1` | 后台内心反刍（烧本地 gemma 不花钱） | **开** | 无 |
| `NOE_CIRCADIAN=1` | 时间节律（夜间静默全家） | **开**（实测降频精确） | 接受夜里完全不出声、提醒顺延早上？ |
| `NOE_MOOD_MODEL=1` | 心境本地模型化（TTL 20min，挂了回启发式） | 开（搭 LM Studio 常开） | 1234 是否常驻 |
| `NOE_DREAM_EPISODES=1` | 梦境升华（默认 6h 一轮，无陈年情景零开销） | **开**（实测 no-op 安全） | 摘要走 LLM（配 NOE_DREAM_MODEL）还是确定性拼接 |
| `NOE_INNER_SPEAK=1` | 反刍升华自生牵挂/提醒（**唯一会主动开口的支柱**） | 先观察一周再开 | 接受 Noe 主动找你说话？建议必配 NOE_CIRCADIAN |
| `NOE_NARRATIVE_SELF=1` | 叙事自我（只读，最安全） | **开**（gemma 产出质量好） | 无 |
| 上窗四开关 | NOE_CHAT_CONTEXT / NOE_MEMORY_DEDUP_SEMANTIC / NOE_VOICE_LLM_STREAM / NOE_SILERO_VAD 等 | 见上窗交接 §3 | 同前 |

调参项：`NOE_INNER_INTERVAL_MS`（反刍间隔）、`NOE_MOOD_TTL_MS`、`NOE_DREAM_EPISODES_FIRST_MS/INTERVAL_MS`、`NOE_NARRATIVE_INTERVAL_MS`（下限 1h 默认 24h）。

## 4. 等 owner（AI 替代不了）

1. **kickstart 生产**吃到本窗全部代码：`launchctl kickstart -k gui/$(id -u)/com.noe.panel`（改 .env 后也要再 kickstart）。
2. **开关拍板**：按 §3 表加 .env；最小推荐组合 `NOE_CONTINUITY=1 NOE_INNER_MONOLOGUE=1 NOE_CIRCADIAN=1 NOE_NARRATIVE_SELF=1`。
3. **LegacyNoeContextEngine 删除确认**：已标废弃零接线，owner 点头即可整体删（连带 tests/unit/noe-context-engine.test.js）。
4. 上窗遗留继续等：语音真耳验收 / CosyVoice 试听 / 定名拍板（守烛/Keepwick）/ brainRouter 副作用确认。

## 5. 下一步建议（新窗口可接）

1. ~~rooms-events-ui.js 585 行超标~~ ✅ 已收口：函数级已切完(2861b35，按 mode 切 6 子函数+dispatch，"单函数 523 行不可硬切"系过期表述)；文件级 2026-06-11 分文件达标——squad/cross_verify/cluster 三个零共享状态子函数原样搬入 rooms-events-collab-ui.js（window.PanelRoomsEventsCollab，dispatch/导出面 window 懒解析），两文件均 <500。
2. **app.js 剩余 4627 行**继续 S24 后续批次（非房间域部分：cmdk/活动/搜索/设置等区）。
3. **内在世界体验调优**：owner 开开关用几天后按真实感受调参（反刍间隔/叙事 prompt/牵挂措辞）。
4. **MCP free 层上限**：本机 4 个 MCP server 超 free 上限 3，e2e 步骤 13 按环境依赖跳过——license 已是 team 永久（上窗），确认 mcp-unlimited 生效后该步自动恢复。

## 6. 运维速查（继承上窗 §6 全部 + 本窗新增）

- 本窗所有新模块"真效果"依赖 LM Studio(1234) 在线；不在线全部静默回退不崩（单测+实测双验）。
- e2e walkthrough 基线现在是 **150/150**（PANEL_URL 指隔离服跑）；红了先怀疑回归而不是噪声——脚本已对齐 UI 现状。
- s18 smoke 套件已正式退役（CLAUDE.md 已更新），勿再用 68/68 基线。
- 多实例 EpisodicTimeline 共享同一 SQLite events 表是既定架构（server.js/noe.js/research 各持实例数据互通）。
- 自生承诺识别标记：text 前缀「Noe 心声：」；上限 2 条只数该前缀。

---

## 7. 追加：S24 续全收口（2026-06-11 凌晨第二波，同窗续作）

**app.js 巨石拆分至此全部完成：13763（原始）→ 731 行。** 本波 11 个 commit（ab4b38b→f2bf296，全部已 push）：

- **rooms-events 分文件**：摸底查实「handleRoomEvent 单函数 523 行不可硬切」系过期表述（batch12 当时已切 6 子函数+dispatch），剩余仅文件级违规 → squad/crossVerify/cluster 三个零共享子函数搬入 rooms-events-collab-ui.js（280 行），主文件 334 行，双双达标。
- **批13-21 九批外迁**（~3700 行）：plugin+房间模板 / 搜索+快捷键+prompts+通知 / Codebase 中心 / composer+overlays / 会话域四模块(core/list/stream/watcher，最重 -877) / sessions-tools+safety+projects-files / theme+cmdk+term / 审批基建+markdown 渲染。每批结构测试+51998 浏览器 smoke+walkthrough 150/150 把门。
- **收尾**：star/fork+ctx 归位 sessions-tools（445 行）；inspector 双实现去重（查证模块版等价超集→正式接线，删内联 63 行）。
- **终验**：137 符号对账、PanelCore 桥 68 键真浏览器运行时零悬空、349→350 文件/2855 测全绿、e2e 150/150、深 smoke 16/16、生产零触碰真库零污染。

**app.js 剩余 731 行 = B 类核心层**（fetch 劫持/state/$/api/toast/wrapper fallback/PanelCore 桥/钩子/启动块），按设计永久留守。**src/web 既有债 5 文件 >500 行**（agent-graph-ui 2325 / activity-ui 621 / governance-ui 603 / cognitive-people 511 / cognitive-research 502，全部为本波之前的既有状态），是下一个拆分窗口的候选。

**并行窗口备注**：本波期间另一窗在做「意识工程」线（NoeDriveSystem 驱力/夜间反思/SFT+LoRA 管线/自主性评测，阶段1-4 + 终审修复 + .env 加载点前移修复），两窗 commit 交替零冲突，其交接以该窗口文档为准。

---

## 8. 追加：第三波收口（2026-06-11 清晨，同窗续作）

**14 个 commit（56a3835→cd96a6f，全部已 push）。终验 7/7 全绿：362 文件/2949 测、e2e 150/150、深 smoke 16/16。**

- **license 真 bug 根治（最重要）**：查实 license-manager.test.js afterEach 真删 `~/.noe-panel/license.txt`——每次全量测试/pre-push 都把上窗激活的 team license 删掉回落 free 层（这才是 MCP 402 谜底，非环境依赖）。修复：LICENSE_PATH 支持 `NOE_LICENSE_PATH` 覆盖让测试指向临时目录 + 回归测试 + 重签重激活（生产 51835 现 tier=team、mcp-unlimited 在线）+ license.txt 进 NoeDbBackup 白名单 + e2e 步骤 13 恢复完整审批路径。**连跑两遍全量测试后 license 仍在 = 根治证明**。
- **src/web 债务清零**：cognitive-people(510→396)/cognitive-research(501→226)/activity(620→327)/governance(602→416)/agent-graph 大刀(2324→壳 331+五子模块 386-493)——**60+ 文件全部 <500**（batch23-27 结构测试配套）。
- **server.js 二轮拆分（批28-34）**：collectPanelRuntimeProcesses/squadFinishHook/sessions 持久化/claude-runner 子系统(584 行主矿，sendMessageToClaude 401 行单函数原文出仓+假 bin 全链路冒烟)/room adapter 工厂/autopilot 房间操作/Noe 维护循环群七连 → **2781→1656 行**。剩余=组合根/register 序列/WS 总闸/进程生命周期，裁决"真到底"。意识工程区（并行窗口代码）一行未碰。
- **顺手修**：webhooks.json 被历史 e2e 塞满 20 上限（实证先存污染致 149/150）→ 清空 + walkthrough 补尾部清理防复发（连跑两遍残留 0 实证）。遗留提示：生产 51835 进程内存仍持旧 20 条 webhook 快照，若触发 _save 可能复活垃圾条目——kickstart 重启后自然消失。
- **>500 行最终残留仅 2 个且均为既定终态**：server.js 1656（组合根）、app.js 731（壳层）。

**至此三波连续作业总账**：S23+S24+S25b+e2e 满分+内在世界七支柱+license 根治+src/web 清零+server.js 二轮，约 45 个重构/修复 commit，测试从 317 文件/2565 测涨到 **362 文件/2949 测**全绿。
