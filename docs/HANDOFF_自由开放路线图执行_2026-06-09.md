# HANDOFF：自由开放路线图执行进度　2026-06-09

> 用户 goal：「按建议依次执行（波次0-6），可碰核心和安全机制，完全以自由开放的开发者为核心思想」。
> 路线图见 `docs/后续计划_统一路线图_2026-06-09.md`。本文件记录执行进度 + 接线清单，便于跨窗口接力。

## ✅ 已完成（波次0-2，均含单测，全量回归绿）

### 波次0：解放自主能力（解枷锁，已端到端通电）
- `src/permissions/ExecPolicyStore.js` — capability 信任档（fs/exec/net/secrets 分项）+ 永远 deny 硬清单（net.upload/secrets.read.content/self.modify_policy）+ /yolo 有界过期 + .noetrust。13 测。
- `src/permissions/ExecPolicyLoader.js` — 从 `~/.noe-panel/exec-policy.json` 加载，默认 developer；`NOE_TRUST_LEVEL` env 可覆盖。
- `src/audit/PolicyAuditLog.js` — append-only `~/.noe/audit.log`，取证用。6 测。
- `src/loop/ActPipeline.js` — 接入 execPolicy/policyAudit，解 L1(blocked_safety)+L2(dry_run)；**默认无 policy 行为不变**（向后兼容）。8 测。
- `src/loop/SafeActExecutors.js` — 新增自由执行器 shell.exec/tool.execute（扩白名单 EXTENDED_ALLOWED_COMMANDS + DangerousPatternDetector 兜底）+ file.delete 走回收站（NoeSafeDelete）。L5。19 测。
- `server.js` — 注入 execPolicy（默认 developer）+ policyAudit。**⚠️ 需重启 51835 生效**。
- 解枷锁：L1✅ L2✅ L5✅。default 档封锁机制仍在（集成测试用 NOE_TRUST_LEVEL=default 验证）。

### 波次1：自给自足感知（纯模块，已生效）
- `src/context/NoeHostContext.js` — 本地感知三件套：ssh inventory / git 身份 / 桌面 / 系统硬件电量，**只读元数据非密钥内容**（format* 纯 + collect 注入 reader）。13 测。
- `src/context/NoeContextSufficiencyGatherer.js` — 解 L3：BLOCKED_SOURCE_PATTERNS 移 `ssh|known_hosts`（host 元数据可读），保留 `id_rsa|id_ed25519`（私钥内容仍拦）。
- `src/capabilities/NoeToolRouter.js` — 解 L6：加 actionLog 保活（最近用过的工具强制保留，防长任务丢工具）。keepAlive 字段。4 测。
- 解枷锁：L3✅ L6✅。

### 波次2：多渠道入站（可测核心已建）
- `src/channels/InboundChannels.js` — 消息标准化(normalizeTelegramUpdate) + mention-gating(shouldWakeAgent, Bus 层) + decorrelated jitter 退避 + Telegram 长轮询框架(createTelegramPoller, fetcher 注入)。11 测。
- 试点 Telegram（m3 定调）。**网络连接需配 bot token + server.js 接线 → 归波次6**。

## ⬜ 待做

### 波次3：长程自主基建（部分完成）
- ✅ **轨迹压缩**：`src/context/NoeTrajectoryCompactor.js`（compactTrajectory 早期摘要+保护尾部，summarize 注入）已实现并接入 `NoeContextEngine.compact()`（原为空 `return bundle`）。13 测。
- ✅ **hang_alert 心跳监控**：`src/autopilot/NoeHangAlert.js`（无心跳告警非杀+续命，呼应不设超时）。6 测。
- ✅ **turn_finalizer 预算耗尽死前总结**：`src/autopilot/NoeTurnFinalizer.js`（budgetUsageRatio / shouldFinalizeTurn / finalizeTurn，纯逻辑+注入 summarize+降级确定性交接，呼应 NoeHangAlert+budget hardStop）。12 测。接 SoloChatDispatcher/BudgetPolicyStore 的 live 路径归波次6。**波次3 已全部完成。**
- ✅ server.js 端到端 smoke 验证：developer 档真实启动 + API 响应 + execPolicy 日志（隔离端口 51999，未动生产 panel）

### 波次4：记忆召回质量（部分完成）
- ✅ **融合召回排序**：`src/memory/NoeFusionRanker.js`（RRF + 加权归一 + salience 二级权重）。7 测。接 MemoryCore embeddings 两路检索归波次6。
- ✅ **MemoryCurator GC**：`src/memory/NoeMemoryCurator.js`（classifyMemory / planMemoryGc，分桶 protected/expired/stale/low_confidence/keep；salience>=5 与 pinned 铁律保护；纯逻辑不直接删，输出 GC 计划交调用方 hide/demote）。15 测。接 MemoryCore 全量遍历 + 执行 hide 归波次6。**波次4 已全部完成。**

### 波次5：能力扩展（P3 按需）
- ✅ **本地 AI Agent 探测注册**：`src/autopilot/NoeLocalAgentProbe.js`（probeLocalAgents 纯聚合+注入探测器 / makeCliDetector which-resolve（对齐 *SpawnAdapter Z-05）/ parseVersionOutput）。13 测。**真实探测本机 claude 2.1.169 / codex 0.137.0 / minimax 3.0.41 / ollama 0.30.6 全可委托**。接 adapters 注册/委托路由归波次6。
- ⬜ MiniMax 多模态 SDK 剥壳 —— 真出图需 API key + 按量付费验证（碰红线4，需先确认）；纯剥壳代码可先写
- ⬜ geo-weather（无 key，IP定位+天气）/ key-auto-config —— 需选免费 API（open-meteo 等），纯逻辑+注入 fetch 可做
- ⬜ launchd 守护 + 审计日志 —— **脚本由用户主动装**（Noe 只产出 .plist + 安装说明，不自主 launchctl，守红线1）

### 波次6：接线收尾（让已建能力 live 通电）★关键
- **本地感知三件套** → 注入 VoiceSession/ChatProfiles 的 systemPrompt（`src/voice/ChatProfiles.js` DEFAULT_PROMPT 处；server.js 启动 collectHostContext 缓存）
- **入站 Telegram** → server.js 读 `TELEGRAM_BOT_TOKEN`，有则 createTelegramPoller，onMessage 接 ActPipeline/room
- **L4 解枷锁** → `PermissionGovernance.js:329-332` 加 ownerTrust=full 时把 .ssh deny 降 ask
- 4 模块 + 自我认知接 live loop（上一轮 A 类 backlog）

## 验证门
每波改核心后跑 `node node_modules/vitest/vitest.mjs run`（当前 2074+ 测全绿）。改 server.js 后告知重启。
