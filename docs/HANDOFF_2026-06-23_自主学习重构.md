# HANDOFF 2026-06-23 — Neo 自主学习重构 A-E 全完成 + research 三修复

> 下窗口/owner 接手从这里读起。本轮两大块全部完成并上生产：① research 三修复 ② A-E 自主学习重构（A-E 全完成 + Codex 检验 5 发现全修 + 子代理终审可投产）。

## 一、本轮做了什么

### 块1：research 三修复（治"学了没留住 / 反刍刷屏 / 重复抓网页"）
- 沉淀 `NoeResearchSediment`（摘要写记忆 + 幂等 id upsert + 无来源/低覆盖门槛）/ 反刍 `NoeRuminationThrottle`（双闸+双 Map LRU+record 延迟）/ 缓存 fetchCache（真 LRU）+ 主题池 24→48
- server.js 深思召回**分两次召回**（lesson∥research_report 不挤名额）+ episode 去造假
- 多模型 2 轮 + 子代理多轮审，发现全修

### 块2：A-E 自主学习重构（owner"全部认真做"；实证 Neo 是伪自主——self_learning 静态表刷 357 次 vs surprise 真好奇仅 4 次）
- **阶段A**：好奇回路供给端通电（STEP_EXPECTATION/WORLDMODEL/HEARTBEAT 早通电）+ 补 Goodhart 门 `NOE_CURIOSITY_ORIGIN_GATE`（REAL_APPLY=1 下防刷假 surprise）
- **阶段B'**：知识图谱写入 `NoeEntityHarvest`（research 抽技术实体→upsertEntity，让源①"反复遇到没深究→自动想学"活）；原对话/元认知源实证砍
- **阶段C**：防护闸（NoeTopicDiscovery per-source cap maxSourceRatio 0.5 + 每日预算闸 NOE_LEARNING_DAILY_BUDGET=8 **双入口**[maybeSeed+scheduler]）+ 点 `NOE_TOPIC_DISCOVERY` ✅
- **阶段D**：价值对齐（defaultPragmaticSignal owner 目标加权 ×1.0 / 其他 ×0.5 + NoeTopicCurator dynamicPriority 学 48 具体项目压过刷 6 总览页）

### Codex 检验 5 发现全修（多模型 Codex 深审 + 子代理 2 轮交叉，主线程逐条亲核）
- **P0#1**：动态 topic 无 localPaths → maybeSeedAutonomousLearning `...topic.localPaths` TypeError 静默失败 + recordVisit 先于 add 污染账本 → 修：localPaths/localPattern 安全兜底 + recordVisit 延后到 add 成功后（shouldRecordVisit 双门）
- **P0#2**：冷启动候选池截断（静态 48 concepts 在前挤掉 TopicDiscovery seeds）→ 修：NoeGoalSystem 传 curator 的 dynamicConcepts 顺序改 `[...dynamicTopics, ...collectLearningConcepts()]`
- **P1#3**：每日预算闸被 scheduler 绕过 → 修：scheduler runLearnOnce add 前也查 recentCountBySource（与 maybeSeed 同口径滚动 24h）；recentCountBySource 已导出
- **P1#4**：NoeEntityHarvest topic 匹配错（topic 传 query 文本非实体名，`===` 永假→已研究主题 description 空被源①误当缺口）+ TECH_RE 匹配普通词污染 KG → 修：isTopicEntity 改 `includes` + 实体加专有名词特征过滤（含大写/数字/符号）
- **P2**：投产链路测试缺 → 补：动态 topic 无 localPaths 立项成功 / recordVisit 延后 / 预算闸双向 / per-source cap / P0#2 顺序 / P1#4 真实 query 文本

### 额外（owner 2026-06-23 要求）：browser.open 复用当前标签防卡
- 根因：`browser.open`/`browser.open_url` 执行器（SafeActExecutors.js）用 macOS `open <url>` 每次开新 tab，自主学习反复学堆标签卡电脑。
- 修：Chrome 系浏览器改用 AppleScript `set URL of active tab of front window`（复用当前标签，仅无窗口时开 1 个）；非 Chrome / 显式 `allowNewTab` / 脚本失败回退 `open`；url 引号转义防注入。28 测覆盖（复用+回退+失败回退）+ 已重启生效。

## 二、当前状态
- **本轮存档**：commit `d05d349`（37 文件本地 commit 存档；`noe-main` ahead origin **366**、**未 push**[守 owner 惯例]；含 A-E + research 三修复 + browser.open 复用标签 + circuit breaker）
- `npm test` **769 文件 / 6543 测全绿**（本轮从 6510 净增 33 新测）
- **生产 kickstart 10 flag 全=1**（plist EnvironmentVariables；当前 PID `lsof -i:51835`）：
  `NOE_RESEARCH_PERSIST` / `RUMINATION_THROTTLE` / `FETCH_CACHE` / `KG_INGEST` / `CURIOSITY_ORIGIN_GATE` / `EFE_CURIOSITY` / `TOPIC_DYNAMIC_PRIORITY` / `DYNAMIC_TOPICS` / `TOPIC_DISCOVERY` / `LEARNING_DAILY_BUDGET=8`
  早通电：`STEP_EXPECTATION_RESOLVE` / `WORLDMODEL_CONFLICT` / `HEARTBEAT` / `SELF_EVOLUTION`(+REAL_APPLY+EXECUTORS)
- 服务全起：Neo 51835 / LM Studio 1234(qwen3.6-35b) / Ollama 11434(KEEP_ALIVE=-1) / SearXNG noe-searxng(colima)
- **子代理终审：A-E 自主学习闭环可投产，10 flag 安全，预算闸=8 双入口封 self_learning 失控（实证 7天230次）**

## 三、子代理指出的 3 个遗漏（非阻断，下窗口确认）
1. **.env 与 plist flag 不一致**：生产靠 plist（launchd 注入）跑对，但 `.env` 这些 flag 未设。`launchctl bootstrap` 启动正确；若改用 `node server.js` 直跑且走 dotenv，flag 会偏离——换启动方式需注意加载优先级。
2. **scheduler 入口不查 activeCountBySource 重入闸**（只 maybeSeed 查）：理论上 maybeSeed + scheduler 同窗口各立一个 self_learning，预算闸=8 已兜底总量，双重立项待下轮确认。
3. **P2 测试回归保护**：P0#2 截断顺序 + P1#4 真实 query + 动态 topic 立项 + 预算闸已补；recordVisit add-fail 反向 probe 未补（修复正确，回归网薄）。

## 四、观察指标（验"真自主"落地的硬证据，跑 1-2 天后看）
```
sqlite3 ~/.noe-panel/panel.db "SELECT count(*) FROM noe_kg_entity"   # research 跑后应从 0 涨(NoeEntityHarvest)
sqlite3 ~/.noe-panel/panel.db "SELECT source,count(*) FROM noe_goals WHERE created_at>strftime('%s','now','-1 days')*1000 GROUP BY source"  # surprise/dynamic 涨、self_learning ≤8(预算闸)
sqlite3 ~/.noe-panel/panel.db "SELECT count(*) FROM noe_memory WHERE source_type='research_report'"  # 沉淀真进记忆
```

## 五、关键坑
- 改 plist flag 后**必须 bootout+bootstrap**，`launchctl kickstart -k` 只重启进程、不重读改后 plist。
- 动态 topic 只有 {title,url,query}，凡用 topic 的代码勿假设有 localPaths/localPattern（P0#1 教训）。
- 加 sourceType 到召回圈定必同步改下游 filter；质量门槛与 fail-open=0 叠加须 `>0&&<min`；进程内开放 key Map 必 LRU 有界。
- 生产 env 靠 plist 非 .env；plist 含 secret 用 PlistBuddy 改（不 cat）；改前必备份（本轮备份在 plist.bak.*）。
