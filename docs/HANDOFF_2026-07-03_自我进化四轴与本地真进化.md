# HANDOFF 2026-07-03 · 自我进化四轴完善 + 本地模型真自我进化

> owner 两个连续任务:①完善自我迭代/进化/学习/改代码四轴 ②让 Neo 用本地模型做到真正自我进化并执行。本文档是这两个任务的交接。前置见 `HANDOFF_2026-07-03_二轮P0-P10闭环.md`。

## 任务2 已完整达成 + 生产铁证验证(owner 重点)

**核心发现**:implement 链路早就内置 `localFirst` 参数,本地模型走真实 implement 链(IMPLEMENTER_SYSTEM + JSON schema + noeStructuredCall)**能产可用 patch**:
- 结构化改进(补 JSDoc/改签名):`from` 逐字精确命中,可 apply
- 复杂函数内部逻辑:会编造 `from`(记不准缩进)→ FuzzyPatchMatcher 容错 + 云端兜底
- 27b 与 35b 表现一致(noeStructuredCall 兜住 JSON 格式)
- **历史为何云端 M3 先**:当年本地产"死代码"时 IMPLEMENTER_SYSTEM 未成熟、无 fuzzy;现都补齐,本地优先安全

**实现**(commit c338059 + b0c5b26):
- `room-adapters.js` 加专用 `lmstudio-code` adapter(model=NOE_SELFEVO_CODE_MODEL||27b, temp0, effort none, TTL900s 按需加载)。**必须在 applyRoomAdaptersConfig 内置白名单**(否则 rebuild 被清)。
- `makeNoeSelfEvolutionImplementer` 加 `localCodeAdapterId`(默认 lmstudio 零回归);LOCAL_FIRST=1 时 candidateIds=[lmstudio-code, 云端minimax, lmstudio]
- server 装配 LOCAL_FIRST=1 自动用 lmstudio-code

**已点火 + 铁证**:`NOE_SELFEVO_LOCAL_FIRST=1` 生产点火;**飞轮 01:47 真用 lmstudio-code(本地27b)产出 patch**(为 ErrorReporter.js 补测试)。本地模型真正参与自我进化 implement 环节 = 生产端到端验证通过。

**本质=本地优先+云端兜底**:结构化进化本地独立做(真 local-first + 快 + 省云端额度),复杂目标本地失败→fuzzy救→降级 minimax。review 保持不必摘 m3。

## 任务1 已完善:轴1 + 轴2元层

- **轴1 自改代码**:任务2 本地化是最大完善(从依赖云端 M3 → 本地优先)
- **轴2 元层(G6, commit 8734067)**:MetaEvolution outcomeStats 按 applied 分层(logicChanged 只算真保留,原混入回滚致误判"健康")+ 加高回滚率报警(>60% warn「算力浪费」)。治"68% 回滚仍判进化健康"的度量盲。advisory-only 不变。

**诚实纠正**(核实推翻调研代理两处误判,避免"点亮已亮的灯"):
- 我上轮 P2 的 reason 归因**已通电**(23:43 点火后 evolution_outcome reason 100% 覆盖;代理看的 838 条是点火前历史)
- `NOE_EVOLUTION_LOGIC=1`、`NOE_CODE_IMPROVEMENT_SIGNALS=1` **都已开**(代理误判为 OFF)

## 轴3(学习)/轴4(完善)已落地(2026-07-03 补做)

owner 追加"接着把这两轴写成代码,做到真正能自主进化和迭代",已 TDD 落地:
- **轴4 真自主学习(commit 7cec2a2)**:verify/self_repair 反复失败被 stuck-drop 的 goal(占多数的失败,此前只有 post_review reject 学习)现在也提炼失败教训(reason=stuck_repeated_failure + objective)喂回学习库。治「370 回滚仅 1-2 条教训、飞轮反复撞同类改不动目标」。复用 REJECT_LEARNING=1(已点火)、recordFailureLesson 已注入 → 恢复飞轮即生效。这是"真正迭代"的核心:不再重复犯同类错、攒出自我能力地图。
- **轴3 能力自举接失败信号(commit f5247ca)**:capabilityTick 原只扫 goal 标题(自进化 goal 标题都是「改进X」永不匹配→0 落地),改扫 goal 标题+why + 最近失败/学习教训里 Neo 表达的能力缺口(scanForCapabilityNeed 纯函数可测)。命中走完整安全链(搜/评估/standing-grant capability:acquire 硬门/提议)。**安全**:`capability:acquire` 未在 grant scopes → 只搜不装,owner 加 scope 才真装。
- 全量 8108 绿,生产恢复 SELF_EVOLUTION=1/LOCAL_FIRST=1/REJECT_LEARNING=1/CAPABILITY=1,心跳 capabilityTick/selfEvolve/failureLessonSignal 注册,无 crash。

## 轴2调参:本轮取舍(仍守 owner shadow 意图) + 更深下一步

本轮聚焦 owner 重点(任务2)+ 轴1/轴2元层,以下轴的深化是**清晰的下一步**(每个都要停飞轮做,有具体方案):
- **轴2 调参(ReflectiveTuner)**:regret.total=0(没喂真失败轨迹,只 3 固定 seed 场景)、ADOPT OFF。**owner 有意 shadow-only(调参碰飞轮阈值风险高),不擅自转真**。下一步须 owner 拍板是否喂真轨迹 + 开 ADOPT。
- **轴3 学习(CapabilityAcquisition)**:executor 已接线(SafeActExecutors.js:859)但触发仅靠对话正则,0 次自主装能力。下一步:接"失败信号/goal 缺口 → 主动搜装"喂入(NoeCapabilityTrigger.js:11)。
- **轴4 完善(reject 闭环)**:370 次回滚只产 1-2 条教训。根因(核实):大部分失败是 **verify_not_green**(post_review 之前),不触发 RejectLesson。下一步:让 verify_not_green 失败也提炼教训喂回 implementer(碰 applyAndVerify:346,要停飞轮 TDD)。
- **轴4 蒸馏(G9)**:100 蒸馏技能全 enabled:false。下一步:修蒸馏→启用→回流闭环。

## 生产 flag 现状(全已点火)
`NOE_SELF_EVOLUTION=1` `NOE_SELFEVO_LOCAL_FIRST=1` `NOE_FUZZY_PATCH=1` `NOE_PROMPT_PREFIX=1` `NOE_EVOLUTION_LOGIC=1` `NOE_CODE_IMPROVEMENT_SIGNALS=1` `NOE_META_EVOLUTION=1` + 一轮总点火 17 项。plist 备份 `.bak.localfirst.*`。

## 生产验证三重确认(01:47 本地 patch 走下游)
飞轮本地产的第一个 patch(补 ErrorReporter 测试)在 apply preflight 被 `patch_content_contains_secret_like_value` 拦——这**同时验证了三件事都健康**:①本地 27b 真产了 patch(implement 工作)②安全门正确拦了含 mock 假 secret 的补测试(不是 bug,是防护)③`self_evolution_preflight_blocked` 事件记录 blockers(一轮 P2 归因通电)。飞轮**没全卡**(仅此 1 个 blocked),会继续跑,遇非 secret-like 目标本地 implement→apply→verify→complete。

## 观察项
- 飞轮本地 implement 产出率:观察 lmstudio-code vs 云端兜底占比(patch-plan.json 的 adapterId),验证"本地能做的本地做"
- test_gap/补测试类目标下游易被拦(新建测试 protected + 测试含 mock secret-like):飞轮既有通病,轴4 供给质量下一步可降 test_gap 权重
- 27b 常驻 + 主脑 35b 内存压力(lmstudio-code TTL900s 按需卸载缓解,观察是否够)
- MetaEvolution 高回滚率报警是否触发(样本累积后看 advisory)
