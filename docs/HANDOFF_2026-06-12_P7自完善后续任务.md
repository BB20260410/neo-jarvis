# HANDOFF 2026-06-12 - P7 自完善后续任务

## 范围

本轮继续 P7 后续任务，保持只读/非 hot-path 优先：

- P7-A0：端到端自完善可观测基线报告。
- P7-B0：自主学习第 6 个 `capability_discovery` 主题。
- P7-C1：`NoeSelfModel` 身份层版本化存储。
- P7-C2：自我模型更新协议，先做证据化 diff proposal，不接 macro tick 热路径。
- P7-D0/D1：自我模型 proposal shadow/audit 通道，只生成提案，不应用版本。
- P7-D2：owner 明确授权后，通过显式 CLI 应用 latest proposal 并 bump self-model 版本。
- P7-E0：自主学习主题配置拆出 `NoeGoalSystem`，降低大文件冲突面。

本轮没有触碰 `51835` / `51735`，没有改 `server.js`，没有读/输出 secret。owner 后续已明确授权 apply 与 commit；提交时只纳入本轮 P7 相关文件，无关脏区继续保留。

## 已改文件

- `scripts/noe-self-maintenance-end2end-audit.mjs`
  - 新增 P7-A0 只读审计脚本。
  - 读取 SQLite / skills / SFT，只写 `output/noe-self-maintenance-end2end/*.json`。
  - 顶层输出 5 个指标：`selfLearningGoalExecCount`、`selfLearningSuccessRate`、`failureModeClusters`、`crossTopicKnowledgeReuse`、`actStepOutcomeByKind`。

- `tests/unit/noe-self-maintenance-end2end-audit.test.js`
  - 覆盖报告 shape、readiness blockers、readonly SQLite 打开参数、`latest.json` 稳定入口。

- `package.json`
  - 新增 `verify:noe:self-maintenance-end2end`。
  - 新增 `verify:noe:self-model-proposals`。
  - 新增 `noe:self-model:apply-proposal`。
  - 注意：`package.json` 之前已有其它 dirty，本轮提交只需要纳入这三条 P7 脚本新增。

- `src/cognition/NoeGoalSystem.js`
  - 新增第 6 个自主学习主题：`capability_discovery`。
  - 查询外部 MCP/tool registry/plugin marketplace 经验。
  - 本地只读扫描 `SkillStore`、tool marketplace、MCP、adapter、tool manifest 等能力发现相关代码。

- `tests/unit/noe-goal-system.test.js`
  - 新增第六轮连续自主学习命中 `capability_discovery` 的测试。

- `src/context/NoeSelfModelVersionStore.js`
  - 新增 `~/.noe-panel/self-model/vNNN.json` + `current` 指针存储。
  - 支持 `listVersions()`、`current()`、`writeNextVersion()`。
  - `current` symlink 被限制在 self-model 目录内，避免路径逃逸读取外部 JSON。
  - 核心身份字段 `name` / `relationship` / `values` 变更需要 owner 确认。

- `src/context/NoeSelfModel.js`
  - 默认只读加载版本化身份层；不存在时回退原默认身份。
  - 显式注入 `identity` 仍可覆盖版本化身份，保持测试/调用可控。
  - snapshot identity 增加 `selfModelVersion` 标记。

- `src/context/NoeSelfModelUpdateProtocol.js`
  - 新增证据化 diff proposal 协议。
  - 白名单字段：`name`、`relationship`、`disposition`、`values`。
  - 黑名单：token/secret/apiKey/cookie/password/oauth 等字段和 secret-like 值。
  - 核心字段变更必须 owner 确认后才能应用。

- `src/context/NoeSelfModelProposalAudit.js`
  - 新增 P7-D shadow/audit 通道。
  - 从 P7-A0 `self-maintenance-end2end` 报告读取聚合信号，只生成 self-model diff proposal。
  - 报告策略固定 `shadowOnly:true`、`applyAttempted:false`、`llmContextAllowed:false`。
  - 不复制 raw failure examples，避免 owner 文本或 secret-like 值进入长期审计通道。
  - proposal 已被当前 self-model disposition 吸收后，不再重复生成同一条 proposal，避免重复 bump 空版本。

- `scripts/noe-self-model-proposal-audit.mjs`
  - 新增 CLI：`npm run verify:noe:self-model-proposals`。
  - 输出 `output/noe-self-model-proposals/<timestamp>.json` 和 `latest.json`。

- `scripts/noe-self-model-proposal-apply.mjs`
  - 新增 P7-D2 显式应用 CLI：`npm run noe:self-model:apply-proposal -- --confirm-owner`。
  - 默认拒绝写入；必须带 `--confirm-owner` 才调用 `applySelfModelDiffProposal()`。
  - 支持 `--dry-run`、`--source`、`--self-model-dir`。
  - 命令摘要只输出 version/proposal/字段名/文件路径，不输出 identity 正文。
  - self-model 目录无 current 时，会用 `DEFAULT_IDENTITY` 作为基线合并 proposal patch，避免生成缺 name/relationship 的半身份版本。

- `tests/unit/noe-self-model-version-store.test.js`
  - 覆盖版本文件写入、current 指针、owner gate、路径逃逸防护、`NoeSelfModel` 读取版本化身份。

- `tests/unit/noe-self-model-update-protocol.test.js`
  - 覆盖 evidence required、字段白/黑名单、secret-like 值阻断、非核心 disposition 应用、核心身份 owner gate。

- `tests/unit/noe-self-model-proposal-audit.test.js`
  - 覆盖 shadow proposal 生成、不 apply、缺 source report 不提案、`latest.json`、不复制 secret-like 源文本。

- `tests/unit/noe-self-model-proposal-apply.test.js`
  - 覆盖 CLI 参数解析、无 owner confirmation 拒绝、owner-confirmed 应用、dry-run 不写、命令摘要不输出 identity 值。

- `src/cognition/NoeLearningTopics.js`
  - 新增自主学习主题配置模块。
  - 第 6 个主题固定为 `capability_discovery`。

- `tests/unit/noe-learning-topics.test.js`
  - 覆盖第 6 个主题和工具/MCP 文本选择规则。

## 验证结果

已通过：

```bash
node --check scripts/noe-self-maintenance-end2end-audit.mjs
node --check tests/unit/noe-self-maintenance-end2end-audit.test.js
node --check src/cognition/NoeGoalSystem.js
node --check tests/unit/noe-goal-system.test.js
node --check src/context/NoeSelfModel.js
node --check src/context/NoeSelfModelVersionStore.js
node --check src/context/NoeSelfModelUpdateProtocol.js
node --check src/context/NoeSelfModelProposalAudit.js
node --check scripts/noe-self-model-proposal-audit.mjs
node --check scripts/noe-self-model-proposal-apply.mjs
node --check src/cognition/NoeLearningTopics.js
```

定向测试：

```bash
npm test -- tests/unit/noe-self-maintenance-end2end-audit.test.js
npm test -- tests/unit/noe-goal-system.test.js
npm test -- tests/unit/noe-self-model.test.js tests/unit/noe-self-model-version-store.test.js tests/unit/noe-self-model-update-protocol.test.js tests/unit/noe-narrative-self.test.js tests/unit/noe-circadian.test.js tests/unit/noe-personality-snapshot.test.js tests/unit/noe-drives-wiring.test.js
npm test -- tests/unit/noe-self-model-proposal-audit.test.js tests/unit/noe-self-model-update-protocol.test.js tests/unit/noe-self-model-version-store.test.js
npm test -- tests/unit/noe-self-model-proposal-apply.test.js tests/unit/noe-self-model-proposal-audit.test.js tests/unit/noe-self-model-update-protocol.test.js tests/unit/noe-self-model-version-store.test.js
npm test -- tests/unit/noe-learning-topics.test.js tests/unit/noe-goal-system.test.js
```

回归门：

```bash
npm run test:p0:unit
npm run verify:noe:self-evolution
npm run verify:handoff
npm run verify:noe:self-maintenance-end2end
npm run verify:noe:self-model-proposals
npm run noe:self-model:apply-proposal -- --source output/noe-self-model-proposals/2026-06-12T15-42-45-583Z-8505b107-6100-4c5a-b621-70ba11d8b8cd.json --confirm-owner --dry-run
npm run noe:self-model:apply-proposal -- --source output/noe-self-model-proposals/2026-06-12T15-42-45-583Z-8505b107-6100-4c5a-b621-70ba11d8b8cd.json --confirm-owner
git diff --check -- ':!games/cartoon-apocalypse/**'
```

最近结果：

- P7 定向测试：7 files / 50 tests passed。
- `test:p0:unit`: 107 files / 759 tests passed。
- `verify:noe:self-evolution`: 198/198 passed。
- `verify:handoff`: 83/83 passed。
- `verify:noe:self-maintenance-end2end`: `ok:true`，`blockers:[]`，`warnings:["failure_modes_present"]`。
- `verify:noe:self-model-proposals` 在 apply 前：`ok:true`，`decision:"proposal_generated"`，`proposalStatus:"proposed"`，`applyAttempted:false`；被应用的 report 是 `output/noe-self-model-proposals/2026-06-12T15-42-45-583Z-8505b107-6100-4c5a-b621-70ba11d8b8cd.json`。
- `noe:self-model:apply-proposal -- --source output/noe-self-model-proposals/2026-06-12T15-42-45-583Z-8505b107-6100-4c5a-b621-70ba11d8b8cd.json --confirm-owner --dry-run`: `ok:true`，目标 `versionId:"v001"`，只输出 `identityFields`。
- `noe:self-model:apply-proposal -- --source output/noe-self-model-proposals/2026-06-12T15-42-45-583Z-8505b107-6100-4c5a-b621-70ba11d8b8cd.json --confirm-owner`: `ok:true`，已写入本机 `~/.noe-panel/self-model/v001.json`，`ownerConfirmed:true`，`proposalId:"9d2631e3-734f-4702-8c83-b22411318854"`。
- self-model 元数据复核：current `versionId:"v001"`，`previousVersionId:null`，`ownerConfirmed:true`，字段为 `disposition/name/relationship`，未打印 identity 正文。
- `verify:noe:self-model-proposals` 在 apply 后：`ok:true`，`decision:"no_proposal"`，`proposalStatus:null`，`applyAttempted:false`；说明当前 proposal 已被版本化 identity 吸收，latest 不会提示重复应用。
- `tests/unit/noe-failure-modes-attribution.test.js`: 3/3 passed。
- `verify:noe:failure-modes-attribution`: `ok:true`，`clusterCount:5`，`j0LiteGapSeedCount:5`，`blockers:[]`；top clusters 为 `goal_checkpoint:evidence_blocked`、`goal_checkpoint:step_recovered`、`browser_dom_host_mismatch`、`act:blocked`、`act_executor_missing`。
- P7-H0 报告输出：`output/noe-failure-modes-attribution/latest.json`，报告为只读归因，不写 DB、不触碰 live port、不调用模型；输出中 owner/goal/act id hash 化，文本脱敏，且不会进入 LLM 上下文。
- 2026-06-13 追加：P7-F0 已接入现有 Proposal Inbox。`src/runtime/NoeProposalInbox.js` 新增 `self_model` 来源，读取 `output/noe-self-model-proposals/*.json` 中的 proposed self-model diff，只展示 proposal id、字段名、evidenceRefs、reason，不输出 identity 正文；`src/runtime/NoeProposalExecutor.js` 支持把 owner-approved `self_model_diff` materialize 到 `output/noe-proposal-executions/queues/self-model-diffs.jsonl`，仍是 pending queue，不直接改 `~/.noe-panel/self-model`；`public/src/web/noe-proposals-ui.js` 增加 self model 来源筛选。定向验证：`npm test -- tests/unit/noe-proposal-inbox.test.js tests/unit/noe-proposal-executor.test.js tests/unit/routes/noe-proposals-routes.test.js tests/unit/noe-proposals-ui.test.js` PASS，4 files / 19 tests。
- 2026-06-13 追加：P7-G0 已完成。`src/cognition/NoeGoalSystem.js` 从 655 行拆到 480 行（低于 <500 目标）；`recordStepResult` 状态机抽到 `src/cognition/NoeGoalStepRecorder.js`，doing/blocked 自恢复逻辑抽到 `src/cognition/NoeGoalStepRecovery.js`。行为保持由现有目标系统 API 承载，不改目标仲裁、act/research 门控、checkpoint 写入或 live 端口。定向验证：`node --check src/cognition/NoeGoalSystem.js src/cognition/NoeGoalStepRecorder.js src/cognition/NoeGoalStepRecovery.js` PASS；`npm test -- tests/unit/noe-goal-system.test.js tests/unit/noe-learning-topics.test.js tests/unit/noe-goal-drive-weight.test.js tests/unit/noe-workspace-goals.test.js tests/unit/noe-approval-goal-resolver.test.js` PASS，5 files / 54 tests。
- 2026-06-13 追加：P7-H1 已完成。`src/server/services/noe-mind-failure-modes.js` 将 `output/noe-failure-modes-attribution/latest.json` 压缩进 `/api/noe/mind/proof.failureModes`；`public/mind-failure-modes.js` 挂在 `public/mind.html`，只读展示 cluster、severity、seed、replay level 和下一步建议，不提供 POST/派活按钮。实测 `compactFailureModes(process.cwd())` 返回 `enabled:true`、`ok:true`、`clusterCount:5`、`seedCount:5`，5 个 cluster 都有 seed、readyForJ0Lite 和 nextAction；定向覆盖在 `tests/unit/routes/noe-mind-routes.test.js` 与 `tests/unit/noe-mind-world-ui.test.js`。
- 2026-06-13 追加：P7-I0 已完成。`src/server/routes/noeProposals.js` 新增 owner-token 保护端点 `POST /api/noe/proposals/:id/self-model-apply`，只接受 `self_model/self_model_diff` proposal；dry-run 和真实 apply 都要求请求体 `confirmOwner:true`，真实 apply 还要求 proposal 已是 `approved_for_gated_apply`。`public/src/web/noe-proposals-ui.js` 为 self-model diff 详情增加 owner confirm checkbox、Self-model dry-run、Apply self-model 按钮；未勾选时不调用 apply API，只记录 UI signal。响应只返回 versionId、identityFields、report refs 等摘要，不返回 identity 正文；仍复用 `applySelfModelProposalReport()` / `applySelfModelDiffProposal()`，不接 macro tick 热路径。定向验证：`node --check src/server/routes/noeProposals.js public/src/web/noe-proposals-ui.js tests/unit/noe-proposals-ui.test.js tests/unit/routes/noe-proposals-routes.test.js` PASS；`npm test -- tests/unit/noe-proposals-ui.test.js tests/unit/routes/noe-proposals-routes.test.js tests/unit/noe-proposal-inbox.test.js tests/unit/noe-proposal-executor.test.js` PASS，4 files / 22 tests。
- 2026-06-13 追加：P8 观察窗仍不能跳过，但等待期证据已结构化。`scripts/noe-observation-status.mjs` 新增 `p8DailyObservation`，把 baseline、观察第几天、7 天窗口剩余天数、`earliestNextStageAt`、allowed work、forbidden P9/R 和 evidenceRefs 汇入 `output/noe-observation-status/latest.json`；`src/runtime/NoeWorkMapSnapshot.js`、`public/src/web/noe-work-map-ui.js`、`NoeObservationStatusReportback` 同步透出 P8 日观察状态。该改动只强化每日观察快照和续跑可见性，不改变 P8 gate、不启动 P9/R、不读 secret、不触碰 live 端口。
- 最新 P7-A0 报告：`output/noe-self-maintenance-end2end/latest.json`，被 `.gitignore` 忽略，不需要提交。
- 最新 P7-D proposal 报告：`output/noe-self-model-proposals/latest.json`，被 `.gitignore` 忽略，不需要提交。

## 当前注意点

- `src/cognition/NoeGoalSystem.js` 已从 704 行降到 480 行，P7-G0 的 <500 行冲突面目标已达成；后续若继续瘦身，优先拆空计划 bootstrap 或 autonomous learning seed，但不再是当前阻塞项。
- `NoeSelfModelUpdateProtocol` 和 `NoeSelfModelProposalAudit` 默认仍只到 shadow proposal；真实应用现在可走显式 CLI 或 Proposal Inbox UI owner confirmation，两条路径都不会接 macro tick 热路径。
- P7-A0 报告已经能作为后续 P7-B/C 改动的基线；`failure_modes_present` 已由 P7-H0/H1 归因为 5 类可审查 gap seed，并已进入 mind proof UI，但它仍是观察/诊断报告，不会自动执行修复。

## 下一步建议

1. P7-F0：已完成。self-model proposal 报告已接入 Proposal Inbox，只显示脱敏 proposal 状态、字段名和 evidenceRefs，不显示 raw identity source；owner-approved 后只进入 gated queue，不直接应用。
2. P7-G0：已完成。`NoeGoalSystem.js` 已降到 480 行，步骤记录/自恢复逻辑已抽成独立模块。
3. P7-H1：已完成。5 个 failure-mode gap seed 已通过 `/api/noe/mind/proof.failureModes` 接入 mind proof companion，只读展示，不自动派活。
4. P7-I0：已完成。Proposal Inbox 现在提供 self-model dry-run/apply owner confirmation 入口，真实 apply 需要 approved proposal + checkbox + protected POST。
5. 后续 P8：继续等待真实观察窗口；`verify:noe:p8-observation-gate` 当前外部条件仍依赖自然时间窗口。等待期内可运行 `verify:noe:observation-status` / `noe:long-task-followup` 累积每日观察证据，但不得启动 P9-A0/P9-D0/P9-G0/R line。
