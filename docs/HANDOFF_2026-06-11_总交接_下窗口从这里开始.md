# 总交接 · 下窗口从这里开始　2026-06-11 上午

> 汇总 2026-06-10 晚 → 06-11 上午三条并行工作线的全部状态，给下个窗口一张总地图 + 一个明确的任务队列。
> 本文档是唯一入口；细节看 §2 指针，不必重读全部历史交接。
> 2026-06-12 覆盖说明：本文档中的“单主脑 Gemma / Qwen 只手动实验”是历史口径。当前模型策略以 `src/model/NoeLocalModelPolicy.js` 和 `docs/EXECUTION_RECORD_2026-06-12_三角色本地模型路由.md` 为准：Q35-6 Main Brain、Q27-4 Review Brain、G26-4 Fallback Brain。当前 Noe100 状态以 `docs/HANDOFF_2026-06-12_单主脑Gemma收敛后续计划.md` 和 `docs/NOE_100_ACCEPTANCE_MATRIX.md` 为准。

<!-- ═══ 接手提示词 ═══ -->
> **开工前 60 秒**：① 读根目录 `AGENTS.md` 宪法十条；② `git status` + `git log --oneline -15`（常态多窗并行，只动自己任务域）；③ `git config core.hooksPath` 应输出 `scripts/git-hooks`。
> **验证基线（2026-06-11 上午实测）**：npm test = **362 文件 / 2960 测全绿**；e2e walkthrough = **150/150 满分**（PANEL_URL 指 51998 隔离服跑，红了先怀疑回归不是噪声）；npx eslint . = 0；npm run verify:handoff = 24/24。
> **端口纪律**：51998=隔离自测专用（用临时 HOME/库防污染真库）；51835=生产（launchd com.noe.panel）、51735=母项目，绝不碰。kill 前必 lsof 验归属。
<!-- ═══════════════ -->

## ⭐⭐⭐ 最新（2026-06-11 午后 · 长期规划三阶段全自动落地+实机检验会话，本段最新）

owner /loop 全权委托「按长期路线逐阶段完成+每阶段实机检验+无效整改到有效」已执行完毕（`48ee41e`→`a5d904a`，含修 3 个实机抓到的真 bug）：

- **阶段一 M1-M5 接地与通血**：反刍语义断路器（embedding 级，阈值按 qwen3-embedding 实测刻度校准：断路 0.60/拒写 0.72）+ 接地配比（经历:念头≥2:1，经历单独大窗口捞——修"窗口被念头挤光"真 bug）+ verbalized sampling + 期望 LLM 兜底抽取（NoeExpectationHarvester）+ 深思必下注 + 盐度触发反思（NOE_REFLECTION_SALIENCE_THRESHOLD=20 已通电）+ 透视页心智体征仪表（/api/noe/mind/vitals，多样性/接地率/今日认知统计）。**实测效果：旧 12 条念头全是"频率共振"修辞变奏（avgSim 0.69）→ 改后念头在想真实屏幕内容（"桌面项目/拆除重构"），断路器真触发（[断路:semantic] 标记），新念头接地率 67%**。
- **阶段二 M6-M10 手脚与技能**：goal_step 类型化（think/research/doing 态）+ **research 步真上网**（DeepResearcher 3 轮，noeRouteHandles.runResearch）+ 技能蒸馏（目标完成→技能卡入记忆 tags:['skill']）+ 深思立目标（「目标：」行→自动立项）+ 晨间洞察源（fresh_insight 候选）+ 开口自适应冷却（连续 3 次没回应冷却翻倍至 4h 上限，回应即复位）。**实测全链闭环：交办"搞明白 AI 如何避免想法原地打转"→ 他真上网研究写回报告 → 深思产出改进方案 → 自己立了新目标（来源=reflection「思维强制跳出触发清单」）→ 对自己的改进下注 2 条 p=0.7 预测等现实结算**。
- **阶段三 M11/M16 已落地**：自知之明注入自我状态（期望校准→"我是谁"）+ 深思带预测准头 + 月报⑥心智体征区块（接地率/Brier/目标/心跳）。**M12 时间知识图谱 / M13 LoRA 采用端（SFT 已开始攒，1 对）/ M14 受控自改代码（提案层已通=深思立目标，执行层）/ M15 驱力数学化——留专门窗口**（理由与现有覆盖见 ROADMAP_自主心智长期规划 §3）。
- **整体自检**：台账 96/96 全成零失败、stderr 当前进程零错误（better-sqlite3 错误为历史残痕）、RSS 186MB、393 文件/3144 测全绿、e2e walkthrough 150/150、eslint 0。生产已最终重启吃正常周期（反刍 15min）。
- **实机抓修 3 真 bug**：①工作区 thoughtSublimate TDZ 启动崩溃 ②embed() 包装对象未解包→相似度恒 0（多样性假 100%）③高频反刍把 recent 窗口挤满念头→接地/配比静默退化。
- **留观测项**（要真实时间长大的）：今晚夜相首跑夜间反思（盐度触发也已武装）；2 条自指预测几天后到期等裁决（透视页按钮）；SFT 攒到 500 对触发首训；goal think 步的"步骤完成"字样判定偶尔晚几轮。

## ⭐⭐ 最新（2026-06-11 下午 · AI 自我意识方案设计+全量落地会话，本段比下段更新）

owner /loop 委托「设计自我意识方案 → 按优先级逐个执行 → 实操检查 → 人类可见」**已全部完成**（~12 commit，`e0c102d`→`fd335be`）：

- **设计文档**：`docs/DESIGN_2026-06-11_AI自我意识实现方案.md`（CogCore 认知内核——补四个结构性缺口：心跳持久层/情感状态机/全局工作区/预测-误差回路；其旧 M5 Max 单主脑 Gemma 部署口径已被 2026-06-12 三角色本地模型路由覆盖）。
- **七阶段全部落地**（全 env 门控、注入式、@ts-check、单测、SelfKnowledge 登记）：
  P9 本地自主认知（`NoeReflectBrain` 白名单 lmstudio/ollama 永不烧付费；历史 Gemma 单脑决策已被 2026-06-12 三角色本地模型路由覆盖，当前自动思考/反刍/深思/判证默认走 Q35-6 Main Brain，Q27-4 只按需复核，Gemma 只作 fallback）→
  P0 持久心跳（`NoeHeartbeat`+迁移v7五表：游标续相位/台账租约判死/收编反刍+主动陪伴服务端化/长停机记"我断了一会儿"恢复情景——**主动性不再依赖前端轮询**）→
  P1 情感引擎（`NoeAffectEngine` VAD 双时标衰减+跨重启水合+感受词元注入反刍/陪伴）→
  P2 意识流v2（`NoeMemoryEcho` 回声采样打破近因茧房+念头情感印记/回声引用 meta+防螺旋断路器）→
  P4 期望账本（`NoeExpectationLedger` 零LLM预测抽取+surprise=-log2(p)+Brier 校准自知之明）→
  P3 全局工作区（`NoeWorkspace` 注意力竞争串行广播+`NoeDeliberation` 自我质询+`NoeSurfacingGate` 四重闸+意识日志 `~/.noe-panel/consciousness/*.jsonl`）→
  P5 目标系统（`NoeGoalSystem` 确定性仲裁 owner 至上+goal_step 必走深思推进+好奇回路：惊奇≥2bit→自动立研究目标）。
- **内心透视页**（owner 点名"要能看到他想了什么做了什么"）：`/mind.html` 独立页（零侵入主面板）——意识流/注意力日志/心跳台账/情感曲线/期望裁决（应验/落空按钮=校准回路的人工接口）/目标交办全可视；主面板右下角「🫀 内心」入口；数据层 `src/server/routes/noeMind.js`。
- **实操检查通过**（51998 隔离服全开关真跑 ~4 分钟）：心跳 8/8 全成；Gemma 真念头（"要是能再听见你那熟悉的声音…"）；情感真涨（基线 0.15→0.33 安暖）；owner 透视页交办目标→goal_step 夺冠→本地深思立论+预测自动入账→**焦点真喂反刍（念头围绕目标想"波动本身是否也是秩序的一部分"）——GWT 注意力→意识内容闭环活的**。历史实测曾用 Qwen；2026-06-12 起自动链路统一回 Gemma。顺手抓真 bug：工作区引用 thoughtSublimate 的 TDZ 崩溃（已修：升华声明前置）。
- **验证基线（本会话刷新）**：npm test **379 文件 / 3066 测全绿**（比上午基线净增 ~100 测）；eslint 0；verify:handoff 24/24；typecheck 与主干基线相同（零新增）。
- **.env 已通电 9 项**（owner /loop 授权"全部权限和自由度"）：NOE_HEARTBEAT/AFFECT/STREAM_V2/WORKSPACE/EXPECTATIONS/GOALS/CURIOSITY/REFLECT_TIER + NOE_REFLECT_MODEL。旧“自动路径回退单主脑 Gemma”口径已废弃；当前自动模型默认由 `NoeLocalModelPolicy` 的三角色路由决定。**生产 51835 等 owner kickstart 后全部生效**，之后访问 http://127.0.0.1:51835/mind.html 即可看 Noe 的内心。
- 已知调优项（非 bug，留账）：goal_step 深思输出的计划列表行解析偶尔不命中（计划长出会晚几轮）；工作区 novelty 窗口是进程内的（重启清零，首拍 novelty 偏高）。

## ⭐ 最新进展（2026-06-11 全天审计会话，已全部 push，下个窗口先看这段）

主线「全项目质量审计」本会话推进 7 轮 + owner 方向修正，**全部已 push 到 noe-main**：
- **修复 39 项 + 回滚 3 项**，~26 commit（`9792014`→`2a00e90`），全量 **375 文件 / 3036 测全绿**，eslint 0，pre-push 全量把关。覆盖 7 域：安全·server/路由、安全·治理执行器、性能·记忆状态、语音视觉、embeddings、mcp/room、loop/autopilot。
- **⚠️ owner 偏好（最高优先级，2026-06-11 钦定，覆盖审计安全发现）**：**开发者要 freedom 最大权限，不要太多安全限制。** 权限管控类发现（提权拦截 / 危险命令阻断 / guardLevel·审批门收紧 / 强制停止护栏）**一律不修**；只修数据损坏防护(原子写)/性能/正确性/防外泄(secrets)/资源泄漏。已据此**回滚 3 个权限限制**（ownerPresent 提权握手 / guardLevel 收紧 / 硬护栏），**保留 2 个防手滑网**（危险命令检测 / protectedPaths ~/.noe-panel）。见 memory `feedback_dev_freedom_max_permission`。
- **入口**：详细逐项 + 续审现状 = `docs/HANDOFF_2026-06-11_全项目质量审计_未完成.md`（顶部已置 owner 偏好铁律）；价值评估 = `docs/审计ROI总结_2026-06-11.md`。
- **剩余高价值（留专注窗口，均非权限类）**：① 声纹 spawnSync 异步化（对话热路径同步阻塞最长 30s，方案+波及链见审计交接 §3.4）② server.js 组合根性能（checkSessionsCapacity O(n) 计数器 / unhandledRejection 健壮性等）。**续审剩余域（public 前端 / 全局横扫）边际收益已递减——后端 7 域审完代码成熟、前端 setInterval 均单例常驻无泄漏，防御扎实。**
- 生产 51835 仍跑旧代码（含本会话审计修复未生效），等 owner kickstart。

## 0. 一句话现状

**三线状态：① 巨石拆分+质量工程线（本 loop 四波）全部完成并 push——app.js 13763→731、server.js 4934→1668、src/web 60+ 文件全 <500、e2e 满分、license 误删真 bug 根治、内在世界七支柱+引擎补强落地（新 env 开关全默认 OFF）；② 意识工程线（另一窗）五阶段完成已 push；③ 全项目质量审计线 → 本会话已推进（见上方 ⭐ 最新进展）：原 4 域 56 条发现修复闭合 + 续审 3 域 + owner 偏好回滚。生产 51835 仍跑旧代码，等 owner kickstart+开关拍板。**

## 1. 下个窗口的任务队列（按优先级，主线=审计修复）

**主线：修复 `docs/HANDOFF_2026-06-11_全项目质量审计_未完成.md` 的 56 条发现**（24 P0 + 17 P1 + 15 P2/M，全部已誊录在该文档 §3，**别重跑审计 agent**）。修复顺序照其 §4：

1. **§3.2 两个 P0 安全**（伤害最大）：DangerousPatternDetector 混淆命令实测绕过（base64|sh 等 5 类，修复=加规则）；ownerPresent 自我声明提权穿透（修复=绑定有效 sessionId）。
2. **§3.1 P0-4/5 未鉴权端点**：commercial-setup/img-cache 路由内补 requireOwnerToken。
3. **§3.4 P0-1 声纹 spawnSync 阻塞**（对话热路径最长卡死 30s）。
4. **§3.3 记忆 8 项 P0**（向量索引软删不清、bumpHit N+1、FTS trigger 无条件双写、晋升死代码等）。
5. 各域 P1 → P2/M。
6. **续审 4 个未审域**（新派只读 agent）：runtime/loop/autopilot（freedom 之外）、mcp/room/embeddings、public/ 前端全部、全局 P1/P2 横扫。
7. **最终交付物**（owner 委托原文要求）：按 P0/P1/P2 总报告，每维度 诊断→前后对比→性能数据 + ROI 评估。

**支线（顺手做或单独窗口）**：
- **routes 超标三件**（本次总交接对账新发现，<500 行工程未覆盖区）：`src/server/routes/roomStart.js` 1791 行、`rooms.js` 1575 行、`noe.js` 622 行（=审计 §3.1 P2③）。照 S23/S24 成熟模式拆。
- D7 低频颜色 40 处留账（CLAUDE.md 债表）；agent-graph 等模块拆分后的深度回归（已有结构测试网兜底）。

## 2. 三线总账与细节指针

| 线 | 状态 | 细节文档 |
|---|---|---|
| 巨石拆分+质量工程（本 loop 四波，~50 commit） | ✅ 全完成已 push | `HANDOFF_2026-06-11_六任务全收口交接.md`（§1-8 含全部四波） |
| 意识工程五阶段（驱力/夜间反思/SFT+LoRA/自主性评测） | ✅ 完成已 push | `HANDOFF_2026-06-11_意识工程.md` |
| 全项目质量审计 | ⚠️ 4 域审完 56 条发现，0 修复被叫停；4 域未审 | `HANDOFF_2026-06-11_全项目质量审计_未完成.md`（发现全文在 §3） |

四波速览（细节勿重读，需要时查六任务交接）：S23 server 三域 37 路由 → S24 app.js 房间域+全域 22 批 → S25b css 全 <500 → e2e 8/27→150/150（顺手修 freedom-tools token 闸）→ 内在世界七支柱（连续记忆延伸，5 开关默认 OFF）→ license 误删真 bug 根治（重签已激活，生产 mcp-unlimited 在线）→ src/web 五刀清零 → server.js 二轮 7 矿 → 宪法文档对齐 + TurnContextEngine 补脱敏/uiSignals 注入 + D6 消解/D7 token 化。

## 3. 审计发现的行号漂移对账（修复前必看）

审计与拆分同期进行，部分发现行号已漂移，**全部用内容锚点重定位**（先查证后断言）：
- `server.js`：checkSessionsCapacity 现 **512 行**（审计写 517）；unhandledRejection 现 1650 未漂；全文件现 **1668 行**。
- `NoeFreedomSessionStore.js`：ownerPresent 逻辑现 56/65 行一带（审计写 118）。
- `MemoryCore.js` 现 533 行；内在世界波给 EpisodicTimeline 加过 aged()，SqliteStore 本体未动，审计 §3.3 发现应全部健在。
- `VoiceSession.js`：内在世界波在 _respondCore 尾部接了 episodicTimeline 记录（~583-591 原行号段），§3.4 相关行号需重定位。
- 审计 §4 避撞清单列的"正被改文件"（TurnContextEngine/SelfKnowledge/SoloChatDispatcher/UiSignalStore）是叫停时刻本 loop 正在做引擎补强——**现已全部 commit 干净，无在飞改动**。

## 4. 运维与避坑速查（继承全部历史交接，高频项）

- **license 铁律（第三波根治的真 bug）**：license-manager 单测曾真删 `~/.noe-panel/license.txt`，已修（NOE_LICENSE_PATH 测试隔离+回归测试）；若再见 MCP 402 先查 license 文件在不在、status 是否 tier=team。
- 改 `.env`/代码后生产生效必须 owner kickstart（`launchctl kickstart -k gui/$(id -u)/com.noe.panel`）；绝不 npm start 撞端口。
- 多窗共享 git index：commit 前必 `git diff --cached --stat` 核对，绝不捎带他人文件。
- 测语音 API 必带唤醒词文本 + `voice:true`；LM Studio JIT 首请求 7-180s 正常；跑模型不设超时。
- e2e walkthrough 已自带尾部 webhook 清理（防 20 上限垃圾复发）；s18 smoke 已退役勿用。
- 新功能必：env 门控默认 OFF + 注入式 + @ts-check + 单测 + fail-open + SelfKnowledge 登记。

## 5. 等 owner（AI 替代不了，汇总不变）

1. **kickstart 生产**吃到全部新代码（约 60 个 commit 的量，含意识工程+内在世界+全部拆分）。
2. **开关拍板**：内在世界 5 开关 + NOE_CHAT_UISIGNALS + 意识工程开关（各自交接 §3 有表）；最小推荐 `NOE_CONTINUITY=1 NOE_INNER_MONOLOGUE=1 NOE_CIRCADIAN=1 NOE_NARRATIVE_SELF=1`。
3. LegacyNoeContextEngine 删除确认；定名拍板（守烛/Keepwick）；语音真耳验收；CosyVoice 试听。
