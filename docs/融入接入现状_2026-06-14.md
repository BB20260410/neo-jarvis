# RESEARCH 报告融入·接入现状与留/删决策 2026-06-14

整夜自主执行：研究 RESEARCH 报告 60 候选 → 借理念纯 JS 重写融入 Neo。本文记录每个融入模块的
**真接入状态**与**留/删决策**，避免「融入即死代码」。原则：env 门控默认 OFF（零回归）+ ON 端到端验证真生效 + 不硬接降质。
owner 2026-06-14 授权改底层后，batch2 四模块从「待前置条件」转为真接入。

## ✅ 已真接入生效（10）——ON 端到端验证过 + OFF 零回归 + integration 起 server 绿

| 模块 | 借鉴 | 接入点 | env | 验证/诚实备注 |
|---|---|---|---|---|
| NoeMemoryDynamics | OpenMemory 双相衰减 | MemoryCore.recallFused 召回排序叠时间激活 | NOE_MEMORY_DYNAMIC_DECAY | 久远记忆衰减排后、新近排前 |
| NoeVerifiableReward | open-r1 可验证奖励 | NoeDeliberation 深思输出质量评 meta.rewardScore | NOE_VERIFIABLE_REWARD | deliberate 返回带 rewardScore |
| Instructor re-ask | Instructor | NoeStructuredCall 校验失败回喂错误重试同档 | NOE_STRUCTURED_REASK | 同档 re-ask 纠错成功 |
| BAML SAP | BAML | NoeLlmJsonExtractor 删尾逗号修补 | NOE_SAP_REPAIR | 尾逗号 JSON 修补成功 |
| NoeMemoryMarkdownMirror | Basic Memory | MemoryCore.topBySalience + mdMirrorTick 导出 ~/.noe-panel/memory-md/ | NOE_MEMORY_MD_MIRROR | 高显著记忆导出含原文 Markdown（生产已实测落地） |
| NoeReasoningSearch | GoT/ToT/llm-reasoners | NoeDeliberation 深思多温度发散→verifiableReward 打分→beam 选最优 | NOE_REASONING_SEARCH=beam | 选中 reasoningSteps 最优；**已补难题细分**（estimateTopicComplexity，简单深思不 N×chat） |
| NoeBudgetForcing | s1 测试时 scaling | NoeDeliberation 深思深度旋钮 + OpenAICompatCompletion 续写端点 | NOE_BUDGET_FORCING | 达 min 抑制结束 append 续推词、超 max 收敛；底层加 completion 续写（治 chat 不能半截续）。生产 ON 需实测 LM Studio completion 兼容 |
| NoeFisherRaoSimilarity | SuperLocalMemory Fisher-Rao | MemoryCore.recallFused 向量路名次用信息几何度量替代 cosine | NOE_MEMORY_FISHER_RANK | 底层给嵌入加方差/不确定度；ON 名次翻转端到端验证；与 NoeMemoryDynamics 正交叠加 |
| NoeCuriosityDecompose | pymdp EFE 好奇二分解 | NoeGoalSystem.harvestSurprise 拆 epistemic+pragmatic，goal 存 meta.curiosity | NOE_EFE_CURIOSITY | 底层加 DB meta 列(migration v12)；**诚实弱点：pragmatic 默认源弱**（claim×owner目标关键词重叠），ON 多偏 epistemic 主导，待接更强源(owner 近期话题/person 偏好) |
| NoeEntropyTemperature | giansha 熵驱动温度 | InnerMonologue 取念头向量→算熵→熵低升温换角度 | NOE_ENTROPY_TEMPERATURE | 熵低真升温 0.2→0.6 端到端；三重 fail-open；需 NOE_MEMORY_EMBED=ollama 提供念头向量否则退回固定温度 |

## 🟡 已融入·留作备用（3）——模块就位+测试全绿+env 门控，待前置条件/边际价值

| 模块 | 借鉴 | 不接的理由 | 决策 |
|---|---|---|---|
| NoeThoughtLoopGuard | Sibelium 去回环 | 回环检测被 InnerMonologue textSimilarity 覆盖（重叠）；bigram 抓字面抓不到语义反刍 | 留：仅 Ebbinghaus 衰减公式独特可复用，价值边际 |
| McpAggregator | MetaMCP 聚合 | 当前 Neo MCP server 少（撞名概率低），聚合边际收益小；真正发光在「越接越多 MCP server」后 | 留：前瞻基础设施，待多 server |
| Outlines-理念 | Outlines strict json_schema | strict 已在 NoeStructuredCall、json_object 已在 OllamaAdapter；'json_schema_when_possible' 是 NoeLocalModelPolicy 死标记（无人取用） | 无需接入：价值已覆盖 |

## 🗑️ 删除（0）

无删除——3 个留作备用模块均理念有据 + 测试全绿 + env 门控默认 OFF（不影响生产），删除会丢失已验证理念落地。

## 诚实纪律记录

- **不硬接降质/退化/重叠**：NoeThoughtLoopGuard（与 textSimilarity 重叠）、Outlines（已覆盖）诚实标不接而非凑数。
- **batch2 接入抓修的真问题**：workflow agent 两次漏 server.js import（NoeBudgetForcing/NoeCuriosityDecompose）致 server 启动崩 → **integration 测试（起真 server）抓到并补 import**；修了 wiring 测试共享 DB 去重 bug；NoeEntropyTemperature agent 首跑挂掉、重做时学乖（加 serverImports 字段 + 自验隔离端口启动）。
- **诚实标弱点**：NoeCuriosityDecompose 的 pragmatic 默认源弱（已记录，机制真接入但默认源待加强）。
- 修复 workflow WebFetch 核对 open-r1 公式，判定「好好好好 CJK」是测试预期算错、实现忠实公式。
- 每个真接入必 ON 端到端 + OFF 零回归 + integration + 全量；全程不碰 mind UI / docs / satellites 等 owner/codex 改动，只精确提交自己文件。

## env 点火速查表（10 个真接入 env，全部默认 OFF，显式启用，零回归）

| env | 效果 | 附加依赖 |
|---|---|---|
| NOE_MEMORY_DYNAMIC_DECAY=1 | 召回排序叠双相衰减时间激活 | — |
| NOE_VERIFIABLE_REWARD=1 | 深思输出打可验证质量分 | — |
| NOE_STRUCTURED_REASK=N | 结构化校验失败回喂错误重试 N 次（≤3） | — |
| NOE_SAP_REPAIR=1 | JSON 解析失败删尾随逗号修补 | — |
| NOE_MEMORY_MD_MIRROR=1 | 高显著记忆导出 Markdown（Obsidian 可读） | NOE_HEARTBEAT |
| NOE_REASONING_SEARCH=beam | 深思难题多候选择优（已细分仅难题触发） | 需 NOE_VERIFIABLE_REWARD=1 当 evaluate |
| NOE_BUDGET_FORCING=1 | 深思深度旋钮 + completion 续写 | 生产 ON 需实测 LM Studio completion 兼容 |
| NOE_MEMORY_FISHER_RANK=1 | 召回向量路用 Fisher-Rao 信息几何度量 | 需 semanticIndex.searchVectors（NOE_MEMORY_EMBED=ollama） |
| NOE_EFE_CURIOSITY=1 | 好奇拆 epistemic+pragmatic、goal 存可解释 meta | pragmatic 默认源弱（诚实备注） |
| NOE_ENTROPY_TEMPERATURE=1 | 意识流熵低自动升温换角度 | NOE_INNER_MONOLOGUE=1 + NOE_MEMORY_EMBED=ollama |

> 已开启生效（owner 2026-06-14 授权，生产 51835 已重启）：前 6 个纯增强（NOE_MEMORY_DYNAMIC_DECAY/NOE_VERIFIABLE_REWARD/NOE_STRUCTURED_REASK/NOE_SAP_REPAIR/NOE_MEMORY_MD_MIRROR/NOE_REASONING_SEARCH）。
> batch2 四个（BUDGET_FORCING/FISHER_RANK/EFE_CURIOSITY/ENTROPY_TEMPERATURE）已接入但默认 OFF，待 owner 决定开启（部分需 ollama 嵌入 / completion 兼容前置）。

## 下一步建议（owner 定优先级）

1. batch2 四个想生效需开 env（部分需 NOE_MEMORY_EMBED=ollama 嵌入支持）。
2. NoeCuriosityDecompose 的 pragmatic 源、NoeThoughtLoopGuard 的语义回环——若要更强可再迭代。
3. 维持现状：10 个真接入覆盖召回/深思/结构化/JSON/可读记忆/好奇/温度等核心增强，全 env 门控可控。
