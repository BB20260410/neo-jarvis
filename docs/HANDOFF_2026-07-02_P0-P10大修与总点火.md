# HANDOFF 2026-07-02 · P0–P10 大修与总点火（接手从这里开始）

> 背景：owner 指令「按 P0–P10 全部解决,然后再次全面分析」。六路只读代理体检 → 逐级 TDD 修复 → 17 项 flag 一次性点火 → 生产实机验证。本文档是唯一入口。

## 基线（2026-07-02 实测）
- 全量 `npm test`：**864 文件 / 8083 测试全绿**（本轮从 858/8037 涨,只许 ≥）
- `npm run test:e2e`：18/18 PASS；`npm run perf-check`：跑通且**结果开始落盘 output/perf-history.jsonl**（P2 新增）
- 生产 51835 已带全部新 flag 重启（PID 见 lsof;boot 后 RSS 521MB 略超 500 阈值,观察项）
- 公开仓 github.com/BB20260410/neo-jarvis：CI 全绿（856 文件/8009+3skip,非降级子集）,v0.1.0 release

## 本轮主仓 commit（noe-main,本地未 push,owner 惯例）
- a427ee5 收编飞轮产出批次（18 文件+3 测试,全量绿验证后收编）
- 8f12e2a **P0 堵外泄**：HostContext PII 门控默认关 + 验证子进程 env 白名单 + SECRET_PATTERNS 全仓单源
- 2faa89b **P1 稳底盘**：EADDRINUSE 退避 60s + tmp/损坏备份后缀唯一化 + 备份 keep 可配
- b6d04b5 **P2 飞轮观测**：evolution_outcome 带 reason 归因 + preflight 失败独立事件 + 新建文件度量修正 + MetricsStore 归档兼容 + perf 基线落盘
- c1e14e9 **P3 复核复活**：reject 一票阻断(含 advisory) + 摘 xiaomi + 反橡皮章提示词 + pack.tests 真实装配
- d213dd9 **P4 产能**：type_error 难码 deny(TS2339/2322) + test_gap 可测性预筛(>400行/重依赖不立项) + dropped 7 天冷却
- 75f7168 **P5 记忆**：KG 扩展第三路进 recallFused（图谱从只进不出→真参与推理）+ cosineSim 维度告警
- ea7ade9 **P7 心跳隔离泵**：单 job 挂起不饿死全泵（不杀 job,守不设超时红线）
- 9f5925c **P10 卫生**：server.js ≤3712 行棘轮进全量测试 + lemonsqueezy/commercial-setup 补测试 + 删 NeoEvalAcceptanceGate + 清 mcp-servers.json 10 条 e2e 污染
- 8a60d42 测试对齐 P0 单源化措辞

## 总点火（已生效,plist 备份 com.noe.panel.plist.bak.p0p10-ignite.*）
新增/恢复 17 项:`NOE_SELF_EVOLUTION=1`(恢复) `NOE_EVOLUTION_RELATIVE_BASELINE=1`(停摆根治) `NOE_DB_AUDIT_RETENTION=1` `NOE_DB_AUTORECOVER=1` `NOE_DB_BACKUP_KEEP=3` `NOE_KG_RECALL=1` `NOE_REFLECTIVE_TUNER=1`+`AUTOTICK=1`(纯shadow) `NOE_AUTONOMOUS_LEARNING=1` `NOE_CAPABILITY_ACQUISITION=1`(提议制) `NOE_WALL_GUARD=1` `NOE_SELFEVO_HOLDOUT_SHADOW=1` `NOE_DREAM=1`+`NOE_DREAM_EPISODES=1`(模型未设=零额度) `NOE_VOICE_LLM_STREAM=1` `NOE_HEARTBEAT_ISOLATE=1`。
`MAX_STUCK_TICKS` 保持既有 5(plist 已有更激进调优,未按 30 覆盖)。
**实机验证**:boot 日志确认 host-context「启用块:无(PII 默认关)」、23 个心跳 kind 注册(reflectiveTuner/capabilityTick/wallGuard 等新 kind 在列)、HTTP 200、e2e 18/18。

## PII 事件与处置（重要）
公开仓早期 26 个 commit 的 author 是 GitHub 账号显示名(中文)。已于本轮**重写公开历史**:全部压成单个干净 commit(author=Neo Maintainer),main+v0.1.0 tag force 更新,复验 author 全干净。旧 commit 成悬挂对象(知 SHA 理论可访,仓库 0 star 上线一天,实际暴露≈0)。**owner 建议**:把 GitHub 账号 profile 显示名改成笔名,一劳永逸(不改的话今后任何 Contents API 写入都会再带出)。

## owner 5 分钟事项
1. **Telegram 入站**:@BotFather 建 bot → 把 token 写进 plist `TELEGRAM_BOT_TOKEN` → bootout/bootstrap。代码全就绪。
2. **ntfy 推手机**:选私有 topic(建议自建 server 或加 access token,topic 即凭证) → plist `NOE_NTFY_TOPIC` → 手机订阅同名 topic。
3. **Gitee 镜像 + GitHub Sponsors**:照做步骤在 scratchpad `drafts_owner待办_Gitee与Sponsors.md`;社区发帖草稿在 `drafts_社区发帖草稿.md`(r/LocalLLaMA + Show HN,发布前自查提示已内嵌)。
4. **db 瘦身第二步**:AUDIT_RETENTION 已点火,等它把 noe_ticks/agent_runs 老行清完后(约 1-2 天),手动 `sqlite3 ~/.noe-panel/panel.db 'VACUUM;'` 才真回收磁盘(本轮 VACUUM 无空间可回收=1.9G 是真数据)。备份下次轮转起只留 3 份(省 ~8G)。

## 明确不做/待办（诚实清单）
- deps 大版本升级(express4→5/otel1→2/electron37→43)本轮明确不做——大修当天不叠升级风险,单独窗口做。
- 9 个「仅测试引用」疑似未接线模块(NoeCuaDriver/NoeFocusStack/NoePromptPrefix/ToolCallGuardrailController/log-ring 等)待逐个裁决接线 or 归档。
- 心跳隔离泵已点火但其「病理性>10min 挂起」场景未真实发生过,onOverdue 告警目前仅落日志(可再接 ntfy)。
- KG_RECALL 效果未做生产 before/after 模拟(fail-open 保底);建议按「真效果模拟」纪律观察一周召回质量。
- 审计遗留 4 未审域(runtime/loop/autopilot、public/ 前端、全局横扫)与 embeddings 4 项维持原状。
- 观察项:boot 后 RSS 521MB(阈值 500);6-30 式故障日若再现,现在 evolution_outcome.reason 可直接归因。
