# HANDOFF 窗口 C 收口交接　2026-06-10 15:30

> **接手前 60 秒**：① 读根目录 `AGENTS.md` 宪法十条；② `git status` + `git log --oneline -10`（本仓常态 2-3 个 AI 窗口并行，只动自己任务域）；③ `git config core.hooksPath` 应输出 `scripts/git-hooks`。
> 入口链：本文（窗口 C 全天）→ `HANDOFF_2026-06-10_双窗口总交接.md`（全天总台账+各窗§7/§8）→ `HANDOFF_2026-06-10_ContextEngine通电窗口交接.md` + `连续记忆脊椎_2026-06-10.md`（旁窗 ContextEngine/脊椎线）。

## 0. 一句话现状

**生产 51835 = launchd 服务 `com.noe.panel`（崩溃 2s 自愈已演练），跑全部最新代码（15:13 重启）；本地全量 2563+ 测绿、CI 两平台连续全绿；今天的修复/强健/语音/内在世界四条线全部落地，等 owner 的只剩体验验收。**

## 1. 窗口 C 全天完成（带验证证据）

**A. §4 队列**（上午）：生产验证全绿（四启动标记/MCP 23/14/9/self-knowledge 五卡能力）；CI actions 升 Node24 大版本；巨石拆分三线——server.js 4985→4407（S23 四域 15 路由出仓：plugins/term/files/projects，各带单测+51998 smoke）、app.js 8528→7831（S24 第6批：知识库/审批/委派/时间线外迁 src/web，PanelCore 桥懒转发）、style.css 7522→16 行 @import 壳+11 段（拼接逐字节一致校验）。证据：01376dd/b40256b/5f84bb2/191810c/57fc285/09595f7。

**B. chat-profiles"改完重启还原"bug**（owner 实损报障）：根因 `_load()` 对 builtIn 档无条件跳过；修复=customized 单向粘滞标记（81975ca），owner 两轮丢失配置从 .bak-latest 修回，**生产实证扛过多次重启**。恢复材料快照：`~/.noe-panel/backups/chat-profiles-恢复材料-20260610/`。

**C. 强健工程五轮**（/loop 授权）：`src/state/atomicJsonFile.js` 原子写地基（tmp+rename/.bak-latest/损坏 .corrupted 兜底）→ 6 个裸写 Store 全接入（Mcp/Webhook/KnowledgeIndex/TaskFlow/Skill/Archive，损坏注入测试全过）→ 三个一行修（prompts 假成功/dry-run 鉴权/崩溃 kill 子进程）→ gracefulShutdown 补全（MCP await 防孤儿+关 WS，51998 实测零残留）→ CommitmentStore 落盘（"提醒我X"重启不丢，跨进程实证）→ 回滚失败留痕。证据：1929fc8/161069e/117de62/1512229/345b8c4/d9d3813/472c43b。

**D. launchd 自愈安装**（owner 授权"帮我去做"）：装 `com.noe.panel` + **kill -9 双场景演练 2s 自愈零孤儿**。首装踩坑已修：plist 原直跑 `node server.js`，launchd 登录环境默认 node26 → better-sqlite3 ERR_DLOPEN_FAILED 崩溃循环 → 改经 `scripts/ensure-node22.mjs` 守卫（290c2cf→290c43b 区段）。`.env` 已开 `NOE_SILERO_VAD=1`+`NOE_MEMORY_DEDUP=1`。

**E. LM Studio 挂死修复**（owner 报障）：根因 launchd 环境缺 NO_PROXY/HTTPS_PROXY（不读 .zshrc）→ 本机 127.0.0.1:1234 请求被绕去走 Clash 超时、且 spawn 的 claude 有裸连 Anthropic 风险。修复：plist 代理六件套 + server.js 缺 NO_PROXY 自动护回环兜底（430f931 内）。实测本地 4bit 模型 7s 响应。

**F. default 模式定稿**（owner 指定）：锁 `gemma-4-26b-a4b-it-qat-mlx`（=lmstudio-community QAT-MLX-4bit，经 /api/v0/models quantization 字段核实）+ 情感陪伴/AI 助理双向提示词。**关键架构决策**：锁定走 chat-profiles.json 配置层（customized 保护），ChatProfiles 常量保持 adapterChain:null 中性——常量锁死会绕过 brainRouter/fallback，LM Studio 没开 default 直接哑（曾被 20 个 voice 测试逮住后回退）。

**G. 语音双报障修复**（4b2e205，CI 绿）：①括号动作描写被 TTS 念出——提示词禁括号+要求简短（≤3句）+ sanitize 全角括号兜底剥除；②实时模式"只说开头"——前端 `playPendingRest` 旧逻辑首句播完切 listen 后把迟到的续播段按相位丢弃（且先消费后检查直接销毁），修复=listen 中迟到续播重进 speak 相位接上、用户已开口仍不插嘴、fetchRestAudio 加代际号防旧轮覆盖新轮。**owner 需刷新浏览器加载新前端后真耳验收**。

**H. 内在世界五支柱方案**：应 owner"跨对话连续记忆/持续自我/不被提问也流淌的内心"之问给出设计（自我状态机/内心流/叙事自我/牵挂/时间节律，核心判据="答案从提问之前就存在的记录里读出来"）——**旁窗已按此落地连续记忆脊椎四节并通电**（见其交接），我全程避撞未碰其手术面。

## 2. 旁窗（ContextEngine/脊椎窗口）并行完成（避撞背景）

TurnContextEngine→NoeSelfModel→InnerMonologue→NoeContinuity 四节+通电（4a server.js 构造+后台反刍 timer/4b 读出注入/4c 写入对话）、SSE 流式早鸟 TTS 全链、记忆语义去重。env 门控速查在其交接。**两窗全天零冲突**的打法：只 add 自己文件清单、公共文件改前看现状、对方活跃文件绝不碰。

## 3. 等 owner（按优先级）

1. **刷新浏览器**后实时对话真耳验收：①不再念"（微笑）"类括号 ②长回复说完整不再只说开头（若仍出现→下层方案：splitFirstSentence 阈值 40→120 整段合成，备好未做因会牵动早鸟探测器同口径）。
2. **真嘴语音体验**：sherpa 延迟实感+"嘿Noe"唤醒词误触（人工测 10 次）；CosyVoice 试听 `~/.noe-voice/cosyvoice/试听样本_中文女.wav`。
3. **内在世界开关**：旁窗脊椎默认 OFF，想体验"有内心的 Noe"按其交接开 env（`NOE_CONTINUITY=1` 等）后 kickstart。
4. 定名拍板（守烛/Keepwick 等十候选）。

## 4. 运维必读（已变，旧习惯会踩坑）

- **重启生产**：`launchctl kickstart -k gui/$(id -u)/com.noe.panel`（**不要 npm start**，会撞端口）。日志 `/tmp/noe-panel.launchd.log`。
- **代理**：plist 内六件套与交互 shell 对齐；server.js 有缺 NO_PROXY 自动护回环兜底。动 Clash 端口需同步改 plist。
- **改 .env 必 kickstart**（dotenv 启动读一次）。

## 5. 踩坑速查（本窗新增，省命）

- **多窗共享 git index**：另一窗 `git add` 的文件会被你的 commit/amend 捎走（本窗实犯一次）。**commit 前必 `git diff --cached --stat` 核对暂存区**。
- **测语音 API 必带**：文本含唤醒词（owner-gate.json enabled=true，3 个自定义唤醒词，没带=静默忽略返 ignored）+ `voice:true` 才出声（端点是 `noTts: body.voice !== true`，传 noTts 无效）。被这两个坑误导会把"配置门控"误诊成"模型挂了"。
- **launchd 进程环境**：`ps -p <pid> -wwwE` 查；zsh -lc 不读 .zshrc，env 只信 plist+.env。
- **LM Studio JIT**：切模型首次请求 7-180s 属正常加载，连续两次空响应≠挂。
- **agent 报告必复核**：本窗 agent 曾把母项目 51735 launchd 服务误报成"13h 孤儿进程建议清理"——杀进程前必 lsof 验端口归属。

## 6. 下窗任务队列（建议优先级）

1. S23 剩余域出仓：sessions（~17 路由，深耦合 sessions Map/broadcastSession）、rooms-advanced（~12 路由，dispatcher 重耦合）、ops——照本窗 plugins/term 模式，每域独立小步。
2. S24 剩余区外迁：聊天室（~1418 行）/Chat 房（~1713 行）大区单独窗口；Plugin 中心/房间模板/Codebase Center 小区可批量走第6批模式。
3. style.css >500 行的 4 段细拆（07c 1229 行等，保序+拼接校验法照抄）。
4. 内在世界后续支柱（叙事自我=dream→自传、牵挂自生、时间节律）——**等旁窗脊椎线收口后接力，开工前先读其交接避撞**。
5. 可选：WS 推流边说边出字（createStream）；语音"只说开头"下层方案（若 owner 验收仍复现）。
