# HANDOFF 2026-06-18 — P4 召回地基点火（insight 召回主路修复）

> 接续 `HANDOFF_2026-06-18_P1闭环点火_深思lesson落库.md`。P4 批次1+2 修通 insight 召回**主路**（learning_lesson 写 scope=insight，P1 闭环「召回用上」依赖它）。每批走「实现→M3+codex+Claude 三方互评→点火」。

## 0. 状态
- 生产 51835 = **PID 5470**（P4 批次1+2 已点火）。母项目 51735(71420) 不受影响。
- 全量 **669 文件 5231 测全绿**。
- env 点火：`NOE_MEMORY_VECTOR_POOL=1`(.env:135)。
- ⭐ **P1 已产出 2 张真认知修正 learning_lesson**（0→2，质量好、非套话「我原以为X实际Y下次Z」含具体对象）——P1 闭环工作硬证据；hit=0（待召回用上，靠 P4+流量）。

## 1. P4 修复（批次1+2，全三方互评+点火）
- **#1** `MemoryCore.merge()` 漏删向量根因A（实测 465 孤儿/43% embedding 指向 hidden）→ 事务后清 source 向量 + warn 日志。
- **#3** 根因B：向量 search 用通道 limit=2 搜全库 → insight 被数量占优的 fact/project 挤出 → over-fetch pool(50) 融合+scope 过滤后截（flag `NOE_MEMORY_VECTOR_POOL=1`）。
- **#5** GUI 浏览记忆路由(noeCoreRoutes.js:53)刷 hit 污染度量 → `bumpHits:false`。
- **#10** codex 发现：`MemoryCore.recall/recallFused` 无 source_type 过滤 → 深思前召回 limit=8 事后过滤 learning_lesson 被淹没。加原生 `sourceTypes` 过滤（#recallFts/#recallLike/recallFused 向量路），server.js 深思前召回圈定 lesson 类。默认不传=零回归。
- **SERIOUS1**（Claude 反向探针实跑坐实）：`#recallFts` catch fallback(:707) 漏传 sourceTypes → FTS 异常时过滤失效、lesson 又被淹没 → 补传 + 回归测试。

## 2. 三方互评成果（owner 指定方法持续见效）
- **Claude 反向探针**抓到 FTS fallback 真 bug（我自己漏的）——三方互评最实在价值。
- M3+Claude+codex 一致：SQL 参数化无注入、零回归扎实、vecSourceTypes 补 slice(0,12)。
- 证伪误判：M3 「writeGate 装配 null」(批次1)、M3「NoeDeliberation 遗漏」(核实=通用召回非 lesson 专取)。

## 3. 后续（低优先，记录不阻塞）
- **#4 FTS 根因C**（ftsPhrase 长 query 整段当短语恒返0、不退 LIKE）：insight 短抽象句靠向量(#3)已覆盖，FTS 字面路边际价值有限。要做需分词 OR，**影响所有 scope FTS，高风险，单独走**。
- **#2 清存量 465 孤儿**：#3 over-fetch(pool 50)已缓解污染影响。若做须 `database.backup()`(NoeDbBackup.js:51)+dry-run count+事务+**走在线进程别开第二 sqlite 连接**+收窄 `merged_into`（三方 SERIOUS 执行警告，实测 465 全 merged_into）。
- **主对话 NoeMemoryRetriever sourceTypes**（codex/M3 提，Claude 反对硬圈）：learning_lesson 靠 scope=insight + #3 进通道；P2 实测是否被普通 insight 卡挤掉再定。
- **lesson 写入侧 topic 索引化**（M3：召回质量根因，sourceTypes 解决「能不能进」非「够不够准」）。

## 4. 基线 + 红线
- 669 文件 5231 测全绿；milestone/push 前跑全量 + perf-check。
- 红线叠加 CLAUDE.md：⚠️ **本窗教训——`pkill -f "node server.js"` 模糊匹配误杀了生产 51835 + 母项目 51735**（均 launchd/自愈拉起无损失）。清进程必先 `lsof -i:<端口>` 反查精准 PID，重启用 `launchctl kickstart -k gui/$UID/com.noe.panel`，绝不模糊 pkill（见记忆 `feedback_check_port_before_kill`）。

## 5. 下一步
- **P2 学习对话可见**：主对话召回学过的 lesson 答得更好 + 召回来源可追溯（让 owner 感受成效）。
- P3 学 owner / P5 成效证伪 / P4 批次3(#4+#2)。
