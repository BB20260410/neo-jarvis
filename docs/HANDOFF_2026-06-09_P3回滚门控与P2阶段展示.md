# HANDOFF 2026-06-09 — P3 社交回滚证据门控 + P2 发布链阶段展示增强

## 真实仓库与边界
- repo root: `/Users/hxx/Desktop/Neo 贾维斯`（不要用 `/Users/hxx/Documents/Neo 贾维斯`）
- 不读/输出 `.env` / key / token / cookie；不碰 `51735`；不碰 `games/cartoon-apocalypse/**`
- 不 commit / push 除非用户明说（本轮**未** commit）
- 真实社交平台上传/发布/删除/隐藏/回滚等副作用必须用户**再次明确授权**

## 本轮完成（P3 + P2，纯本地、零真实平台副作用）

### P3 — 社交回滚证据门控（rollback / delete / hide / recall / correct）
目标：发布错了能回退，但回退必须可审计；**当前阶段只建 evidence gate，绝不做真实删除/隐藏**。

新增文件：
- `src/runtime/NoeSocialRollbackEvidenceGate.js`（296 行）— 纯证据门控。零 `spawn`/`osascript`/`fetch`/真实删除路径；恒 `dryRunOnly:true` / `executesRealRollback:false` / `externalSideEffectPerformed:false` / `destructionPerformed:false` / `authority` 全 false。
  - 证据 gate（缺任一则 `blocked`）：目标帖 URL、目标 host 属平台帖/管理域白名单、发布后证据(url+title)、操作前证据（强证据=`domDigest`/`screenshotRef`，弱证据=`url`/`title` 降级为 warning）、`requireVerifiedEvidence` 时要求 `verified`、可信破坏性授权。
  - 破坏性授权**只**从可信 `authorization` 参数 / `deps.destructiveAuthorization` 派生，**绝不从 args payload 读**（防伪造）。
  - 产出脱敏的人工操作指引（明确声明绝不自动执行）。
- `src/runtime/NoeSocialFinalPublishRollback.js`（61 行）— 从 `NoeSocialFinalPublishExecutor` 抽出的 `buildFinalPublishRollbackEvidence`（治 <500 行债）。
- `tests/unit/noe-social-rollback-evidence-gate.test.js`（21 用例）— 含**经 adapter 端到端**：缺授权 blocked、注入可信 `deps` open 但仍不真实删除、args 伪造授权被拒、强弱证据分级、safeJson 上限、warnings。

接线（5 处一致对齐 `noe.freedom.social.rollback.evidence_gate`）：
- `NoeFreedomAdapters.js`：注册 adapter，`dryRun`/`execute` 都走**同一纯 gate**。
- `NoeFreedomManifest.js`：新增 tool 条目（`risk_level: high`，`rollback.strategy = none_gate_only_no_real_action`）。
- `NoeFreedomExecutor.js`：`socialPublishStageForChild` 加 `rollback_evidence_gate` 映射；`summarizeSocialRuntime` 透传 rollback `postUrlRef`/`postTitleRef`/`platform`。
- `NoeSocialFinalPublishExecutor.js`：`rollbackEvidence.nextFreedomActions` 追加 rollback gate step（发布后引导走 gate，而非直接删）。

### P2 — Freedom 发布链阶段展示增强
- `public/src/web/noe-freedom-stage-summary.js`（129 行）：状态徽章(✓/✗)、`blockedAt` 卡点高亮(← 卡在这里)、`domProbeSummary || domRecipeProbe` 字段名双兼容、stage `warnings`/`ledgerRef`、rollback gate 中文标签「回滚证据门控」、rollback `target`/`platform` 脱敏展示。
- `tests/unit/noe-freedom-stage-summary-ui.test.js`：补 blockedAt/徽章/warnings/domRecipeProbe/target 用例。

## 验证证据
- `npm run test:p0:unit`：**80 files / 557 tests passed**（原 536 + rollback gate 21 + stage summary 增量）
- `npm run verify:handoff`：**24/24 passed**
- `npm run verify:noe:freedom-live`：**ok 4/4**（重启后新代码）
- `git diff --check`（排除 `games/cartoon-apocalypse/**`）：PASS
- secret 扫描所有改动文件：干净；新文件无 `.env`/token 引用
- 所有改动文件 < 500 行（`NoeSocialFinalPublishExecutor.js` 501 → 470）
- 代码审查（code-reviewer agent）：**安全护栏 / 授权防伪 / 脱敏 / 门控逻辑 / 接线 全部通过**；发现项已处理（见下）

## 实机验证（live 51835，零副作用）
- **已重启 51835 加载新代码**：旧 PID `39326` → 新 PID `92089`；cwd 真实仓库；health/readiness passed。**未碰 51735（PID `54215` 健在）。** 用 `npm run restart:panel`（脚本只对 `lsof -tiTCP:51835` 的 PID 发信号，结构上不会误杀 51735）。
- 经 `POST /api/noe/freedom/dry-run` 实机调 rollback gate（owner token 仅作 header，未打印）：
  - capabilities 含 rollback gate ✓
  - 缺证据 → **HTTP 409 blocked**，4 类 blocker 齐全
  - 给齐证据 + 在 payload 伪造授权 → **仍 409 blocked**，`authApproved=false`（伪造授权被拒）
  - `executesRealRollback=false` / `dryRunOnly=true` / 零 secret 泄漏
  - **全程未上传、未发布、未删除、未隐藏任何真实内容**

## 代码审查发现与处置
- **B1（blocker，fail-safe 方向，已有意保留）**：可信 `authorization` 在生产执行链路到不了 gate（`runSpecialFreedomRuntime` 调 adapter 未透传 authorization；dry-run 路由的 `authorization` 来自**不可信** request body）。
  - **处置：有意保持当前行为** —— gate 经真实 executor 恒 `blocked`，这是 P3「只设计 gate、真实删除需明确授权」的安全默认。**不透传不可信 `body.authorization`**（会重新引入伪造漏洞）。已在 gate 代码注释 + 本 handoff 文档化。
  - **未来工作（需用户授权真实 rollback 方向）**：建可信破坏性授权基础设施（permission store / consensus ledger 注入 `deps.destructiveAuthorization`）+ 真实 rollback 执行器，再接通。adapter 端到端测试已验证「注入可信 `deps` 即可 open，且仍不真实删除」的接口正确。
- **I1（行数）**：已抽 `buildFinalPublishRollbackEvidence` 到独立文件，`NoeSocialFinalPublishExecutor.js` 501 → 470。
- **I2（gate warnings 恒空）**：已填充（弱证据 warning、pending-probe warning）。
- **S1（safeJson DoS）**：已加 64KB 上限。
- **S2（before-action 证据偏宽）**：已分强弱级（`domDigest`/`screenshotRef` 才算强证据；`url`/`title` 降级为 warning）。
- **S3（errors 可读性）**：未改（现状清晰且有测试，改动无功能收益）。

## 后续优先级（均需用户授权对应方向）
1. **P1**：真实平台 DOM E2E。先只读重跑 `media_upload` readiness gate；真实上传需用户提供 `--media-file` + 明确允许上传副作用（`--ack-upload-side-effect`），仍不要最终发布。
2. **P3 续**：接通真实 rollback 执行器 + 可信破坏性授权（需用户授权真实删除/隐藏方向）。
3. **P2 续**：独立 history 回放面板（survey 给出 `noe-freedom-run-history.js` 切入点）+ Playwright e2e 渲染测试。
4. **P4**：developer mode / tool manifest 三档产品化（survey 切入点：工具条目加 `minRequiredMode`/`sideEffects`/`allowedPaths/sites/accounts`/`evidenceRequirement` + 导出 `NOE_FREEDOM_OWNER_SUPERVISED_PROFILE` + catalog 按档分组）。

## 提醒
- server `51835` 本轮已重启，运行的是含 rollback gate 的新代码；如需回到改动前状态可再次 `npm run restart:panel`（仍是新代码，因为改动在工作区）。

## 给下个窗口的复制提示（直接粘贴到新的 Claude Code 会话）

```text
你接手的是 Noe / Neo 贾维斯项目。真实仓库只认：
/Users/hxx/Desktop/Neo 贾维斯
不要使用：/Users/hxx/Documents/Neo 贾维斯

当前状态：P3 社交回滚证据门控 + P2 发布链阶段展示已完成、实机验证、代码审查通过；
server 51835 已重启加载新代码。本轮未 commit，工作区 dirty 是预期。

硬边界：
- 不读/输出 .env、API key、token、cookie、owner token 明文
- 不碰 51735；不碰 games/cartoon-apocalypse/**
- 不 commit/push 除非我明说
- 不给模型/agent/多模型协作设人为硬超时
- 真实社交平台上传/发布/删除/隐藏/回滚等副作用必须我再次明确授权
- 不要把本地角色模拟冒充真实多模型

先核验：
pwd
git -C "/Users/hxx/Desktop/Neo 贾维斯" rev-parse --show-toplevel
git -C "/Users/hxx/Desktop/Neo 贾维斯" status --short
git -C "/Users/hxx/Desktop/Neo 贾维斯" log -5 --oneline
lsof -nP -iTCP:51835 -sTCP:LISTEN
lsof -nP -iTCP:51735 -sTCP:LISTEN || true

按顺序读：
1. /Users/hxx/Desktop/Neo 贾维斯/AGENTS.md
2. /Users/hxx/Desktop/Neo 贾维斯/CLAUDE.md
3. /Users/hxx/Desktop/Neo 贾维斯/docs/HANDOFF_2026-06-09_P3回滚门控与P2阶段展示.md
4. /Users/hxx/Desktop/Neo 贾维斯/docs/HANDOFF_2026-06-08_自由执行发布链收尾.md

已完成事实（不要重做）：
- P3 rollback evidence gate 已落地：纯 gate，零真实删除路径，恒 dryRunOnly / executesRealRollback:false
- P2 stage summary 增强已落地（徽章/卡点高亮/domRecipeProbe 修复/warnings/回滚门控标签）
- test:p0:unit 80 文件 / 557 测试全过；verify:handoff 24/24；freedom-live 4/4；git diff --check PASS；所有改动文件 < 500 行
- 实机：rollback gate dry-run 缺证据/payload 伪造授权均 409 blocked，零真实副作用
- B1（重要，别当 bug 修）：rollback gate 经真实 executor 恒 blocked 是【有意安全默认】——
  dry-run 路由的 authorization 来自不可信 body，故意不透传给 gate。接通真实授权属未来工作，
  需我明确授权真实 rollback 方向。除非重新核验失败，否则不要去"修复"这个 blocked。

下一步优先级（均需我授权对应方向）：
1. P1 真实平台 DOM E2E：先只读重跑 media_upload readiness gate；真实上传需我提供媒体文件
   + 明确允许上传副作用（--ack-upload-side-effect），仍不要最终发布
2. P3 续：接通真实 rollback 执行器 + 可信破坏性授权（需我授权真实删除/隐藏方向）
3. P2 续：独立 history 回放面板 + Playwright e2e 渲染测试
4. P4：developer mode / tool manifest 三档产品化（需先定档位语义 + 拆 992 行 manifest）

接手后以当前工作区和命令输出为权威，不要依赖旧聊天记忆。
工作区 dirty 是预期，不要擅自回滚或清理我的改动。
继续
```
