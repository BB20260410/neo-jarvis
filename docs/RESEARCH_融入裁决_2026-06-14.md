# RESEARCH 报告逐候选融入裁决 2026-06-14

研究 workflow（8 agent 逐候选对照 Neo 真实代码）裁决：共 60 候选 → 已有 14 / 值得融入 28 / 跳过 18。

> 28 个 worth 全是「借理念 + 纯 JS 重写 + env 门控默认 OFF」，逐个串行融入中（有用留/无用删）。


## 值得融入（28）

### Context7 (@upstash/context7-mcp) (ADOPT)
- 价值：主脑 qwen3.6 有知识截止日,答 SDK/库用法易编造 API;接 Context7 现取最新官方文档注入可显著降幻觉(smoke 已证拉到真实 Express 文档),与本地 unified-kb 私有知识互补
- Neo 现状：能力骨架已就位但差最后一跳。已装 @upstash/context7-mcp@^3.2.1(license MIT),已注册为 mcp-servers.json name=context7-docs(enabled,stdio 起本地 node 子进程,env 干净无 secret),已 smoke 实测(scripts/noe-p0-tool-smoke.mjs:84-90 listTools/callTool context7-docs)。MCP 客户端栈 src/mcp/McpClientManager.js 可直接消费。真实缺口:①未进默认信任名单——src/permissions/PermissionGovernance.js:415 NOE_TRUSTED_MCP 默认仅 'unified-kb,filesystem,memory,playwright',不含 context7-docs,故主脑经 govCall 调它会判 ask;②主脑聊天回路不自动调它(grep 证实 src/room/ src/cognition/ 内无 listTools/callTool/getMcpClient,MCP 仅经 REST /api/mcp/* 与 noeDo 硬编码 unified-kb 暴露)。
- 融入方案：低成本、可纯 env 门控:(1)零代码即可:把 context7-docs 加进 .env 的 NOE_TRUSTED_MCP(=unified-kb,filesystem,memory,playwright,context7-docs)让 PermissionGovernance:418 放行——这是已有 env 开关,默认行为不变(不写就 OFF),风险低。(2)若要主脑自主调用则属下条 Playwright 同款的'把 MCP 工具喂进主脑工具路由'的统一改造,建议跟 Playwright 一起做、同一 env 开关(如 NOE_BRAIN_MCP_TOOLS 默认 OFF)灰度。风险:每次查询把 query/libraryName 发第三方 Upstash 云(非本地优先),勿塞私有代码;匿名限额未知,高频可能触顶(可选填 CONTEXT7_API_KEY,非必需)。安全面已被 McpStore 命令黑名单+元字符校验+requireOwnerToken 覆盖。
- 安全性：safe_env_gated

### pymdp (inferactively-pymdp) (cognition)
- 价值：借数学公式（纯 JS 重写、零依赖）给好奇回路加 epistemic(信息增益/降不确定性)+pragmatic(贴 owner 偏好)二分解，让『好奇』从单标量升级为可解释的双因子，更像主动推断而非阈值触发
- Neo 现状：Neo 有惊奇与好奇的部分理念但形式不完整：surprise=-log2(p) 见 src/cognition/NoeExpectationLedger.js:140（纯点估计，无信念熵/变分自由能项）；好奇回路 harvestSurprise 见 src/cognition/NoeGoalSystem.js:461-470（单标量 surprise≥2bit→研究目标，未拆 epistemic/pragmatic）；driveLevel 注释自称『Active Inference 方向第一步』src/cognition/NoeGoalSystem.js:50 但只用于权重浮动，未做 EFE 二分解。Neo 的决策是 LLM 驱动+开放动作空间，与 pymdp 的离散 POMDP 控制层错配——这部分不可移植。
- 融入方案：只 borrow 标量公式不引 Python/JAX。①在 NoeGoalSystem.js harvestSurprise 旁加一个纯函数 curiosityScore(epistemicValue, pragmaticValue) 把现有单 surprise 拆成 G=risk-ambiguity 参照式的加权（约 30-50 行 JS）；②可选把 NoeExpectationLedger surprise 升级为带信念熵的变分自由能记法。均挂新 env(如 NOE_EFE_CURIOSITY)默认 OFF，配单测（遵循新文件三件套）。风险低：纯函数、可回滚、不改默认行为；离散 POMDP 控制层不要移植（层级错配）。
- 安全性：safe_env_gated

### giansha/Global-Workspace-Agents (cognition)
- 价值：两个小而真的零件：熵驱动温度=给 Neo 装『想腻了自动换角度』的新奇信号；IDLE 开口门控=把主动陪伴从『闹钟响了』升级为『忍不住想说』，精准命中 owner 要的『念头浮现感』
- Neo 现状：Neo 已有结构化 GWT（NoeWorkspace.js 确定性打分+argmax，比该项目『拼字符串黑板』强，勿降级）。但 Neo 缺两块：①熵驱动动态温度——全仓无 entropy/动态 temperature（cognition 内仅 NoeMemoryEcho.js:7 用固定温度 0.25 做联想采样，OpenAICompatChatAdapter.js:74-75 用 frequency/presence_penalty 抗复读，均非语义停滞自适应升温）；②proactive 开口质量门——proactiveTick 偏到点触发，无『只有能开启对话、真会说出口的念头才浮出外发』的 IDLE 门控判据。
- 融入方案：license MIT 全可借。①把 entropy_drive.py 约 80 行数学(EMA 聚类中心+余弦+softmax+香农熵+T_gen=T_base+α·exp(-β·H))用 JS 重写成纯函数，喂 Neo 已有向量(语义检索那套)，输出动态 temperature 接本地 qwen3.6 生成调用，挂在好奇回路/意识流，新 env 默认 OFF；②把 meta.py IDLE system prompt 那段『开启而非收尾/会说出口才浮现』翻成中文判据，插到 proactiveTick 决定是否外发的那步，新 env 默认 OFF。无新 npm 依赖。风险：单作者 7 周停更，借来代码需自补单测（其测试已删）。
- 安全性：safe_env_gated

### lealoth/Sibelium-cognitive-architecture (cognition)
- 价值：调度与稳态的可落地增量：双tick节奏基线让内心活动有快慢两档；Ebbinghaus衰减+主题回环侦测直接治意识流『复读/打转』；satiety/应激门控给好奇回路加『过载自我降速』护栏
- Neo 现状：Neo 情感已是 VAD（NoeAffectEngine.js:6,18 valence/arousal/dominance + 双时标衰减 τe90min/τm7d）——优于该项目命名情感+西语关键词规则，应保留不替换。但 Neo 缺：①双 tick + per-process 间隔表（grep `fastTick|slowTick|双tick` 零命中，心跳是单一 tick）；②意识流去回环（无 Ebbinghaus 突触强度/主题回环侦测；NoeMemoryEcho.js:4 注释承认『联想绕着昨天打转』痛点未根治）；③satiety/allostatic load 稳态门控（NoeAffectEngine.js:98 有去饱和但非认知应激降速）。
- 融入方案：license MIT，纯算法移植零依赖。①意识流 v2 移植 FlowStream 三件套(优先级线性衰减+Ebbinghaus 突触强度 tau 随 access 增大+主题回环侦测≥2关键词在≥5条近期念头)，用 Neo 已有 SQLite+语义检索算嵌入余弦≥0.85 判重复，约一两百行 JS，新 env 默认 OFF；②把间隔表(explore300/reflection450/curiosity240/simulation1200/proactive120s)作为 CogCore 各回路初始调参基线落进 .env；③satiety can_generate 冷却闸加到 proactiveTick。情感不照搬(保留 VAD)。建议存一份机制对照表在 docs。
- 安全性：safe_env_gated

### SuperLocalMemory (memory)
- 价值：借鉴 fisher.py 的 Fisher-Rao 测地相似度做向量重排,对'有方差的嵌入'更稳,可提升召回精度(约 200-300 行 JS 重写,零 Python/零许可风险)
- Neo 现状：5 路 RRF 融合 Neo 已有(NoeFusionRanker.reciprocalRankFusion k=60+weightedFusion+recallFused);矛盾检测已有(NoeMemoryConflictPolicy+sheaf 思路对应 semanticConflictSweep);实体图谱扩散对应 NoeKnowledgeGraph.oneHop。但'Fisher-Rao 测地相似度'(对带不确定度的嵌入比裸 cosine 更鲁棒)Neo 没有——NoeFusionRanker 用 RRF 名次/min-max 归一,不含信息几何度量。纯 Python(numpy/scipy),npm 包仅 python spawn 启动器,不可 in-process。
- 融入方案：理念:把 fisher.py 的 Atkinson-Mitchell 对角高斯 Fisher-Rao 闭式解 d=√2·arccosh(1+δ) 用 JS 重写成新模块 src/memory/NoeFisherRaoSimilarity.js(@ts-check+注入式纯函数+单测,遵新文件三件套),作为 NoeFusionRanker.weightedFusion 里 cosine 的可选替代度量。env 门控 NOE_MEMORY_FISHER_RANK=1 默认 OFF,不注入时 weightedFusion 行为逐字不变。风险:纯排序层加法、确定性纯函数、可单测、不改写入/不动默认召回路径,回滚=删文件+关 env;唯一注意是需嵌入带方差信号(无方差时退化回 cosine,仍安全)。
- 安全性：safe_env_gated

### OpenMemory (memory)
- 价值：补 Neo 最朴素环节=记忆随时间自然遗忘/巩固的双相衰减动力学(快遗忘λ₁+慢巩固λ₂)+检索即强化,让召回从'静态重要度'升级为'随时间演化的激活能',主观更像在回忆而非查库,直接服务 CogCore 意识感
- Neo 现状：五型 sector/salience 复合分/检索强化 Neo 部分有但'时间衰减动力学'是真缺口：NoeFusionRanker.js:72 仅 salience(1-5)×0.08 线性乘子,recency/age 完全不进排序;MemoryCore 存了 last_hit_at(:105/:469)但只用于 GC 冷热采样,不用于召回排序权重。AffectEngine.js:46-47 有双相指数衰减(τe≈90min/τmood≈7d)但只作用于情绪 VAD,不作用于记忆 salience。检索即强化部分对应 bumpHit/bumpHitMany(累加 hit_count)但无'sal←sal+η(1-sal)'式饱和强化,也无向邻接节点扩散(KnowledgeGraph.oneHop 是查询非激活传播)。跨 sector 共振矩阵 Neo 无。它硬拖 sqlite3(与 Neo better-sqlite3 冲突)+aws/azure/notion 重依赖,不可整库 adopt。
- 融入方案：理念:把 ops/dynamics.ts 的双相衰减 retention(t)=e^(-λ₁t)+θ·e^(-λ₂t) 与 hot/warm/cold 分档 λ 用 JS 重写成新模块 src/memory/NoeMemoryDynamics.js(@ts-check+注入式纯函数+单测,新文件三件套;已确认该文件不存在=纯新增)。在 NoeFusionRanker.weightedFusion 现有 salience×0.08 二级权重旁,叠一个基于(now-lastHitAt)的时间激活因子(纯函数、调用方传时间不读全局时钟)。env 门控 NOE_MEMORY_DYNAMIC_DECAY=1 默认 OFF,不开时 weightedFusion/recallFused 行为逐字不变。风险:纯排序层增强、确定性可单测、不碰写入/不改默认召回,回滚=关 env+删文件;跨 sector 共振矩阵留作后续(需先给记忆打 sector 标,非本次)。不装 npm openmemory-js(sqlite3 冲突+重依赖),不起其 MCP server。
- 安全性：safe_env_gated

### graph-of-thoughts (spcl/graph-of-thoughts) (reasoning（8）)
- 价值：给 GWT 补一个可编排思维图骨架（聚合算子+带反馈环的精炼+显式剪枝），把念头处理从隐式竞争升级为可调度可观测的图计算，深思难题质量更高。
- Neo 现状：部分已有但不完整：GoT 的 Score+KeepBestN 算子 ≈ PeerCritiqueGate.js:1-12,15-30（提案打分0-10+keep/kill剪枝）；Aggregate≈NoeLocalModelCouncil.js:435-448 synthesizer 合成。但 Neo 无【可编排思维 DAG 控制器】（GoO+拓扑队列 Controller）——当前 GWT(NoeWorkspace.js) 是广播竞争，深思(NoeDeliberation.js)是单次三段补全，不能表达 Generate→Aggregate→Improve→ValidateAndImprove→KeepBestN 的任意拓扑非线性推理。grep best-of-n/tree-of-thought/beam 在 src/ 仅命中 identity/voice 假阳性，确认无搜索控制器。
- 融入方案：理念+TS 重写（不装 Python 包/不起服务，BSD-3）。核心 ~500 行三件套：Thought(复用现有念头结构加 score/valid/solved)、Operation 基类(predecessors/successors+canBeExecuted)、GraphOfOperations+Controller(拓扑队列)。落点新建 src/cognition/got/ 模块，LLM 引擎复用 BrainRouter→qwen3.6/gemma；输出接 mind.html 可视化。env 门控 NOE_GOT_REASONING 默认 OFF，仅高风险/多步深思路径启用（普通对话走快路）。风险：每节点打 LLM 放大延迟/算力——靠开关+仅难题触发兜底；prompter/parser 须按 Neo 自身任务写（论文版是排序/计数定制）。与 graph-of-thoughts/tree-of-thought/llm-reasoners/OpenR 重叠，建议合并为一个统一 reasoning-search 模块一次落地。
- 安全性：safe_env_gated

### tree-of-thought-llm (princeton-nlp/tree-of-thought-llm) (reasoning（8）)
- 价值：教科书级最小'先想几条路再择优'审慎推理控制器；evaluator 槽位刚好把 Neo 已有但被动的期望账本(Brier)+好奇回路(新颖度)变成主动分支裁判，朝元认知/自我监控走实质一步。
- Neo 现状：部分已有但不完整：ToT 三段式的 evaluate(打分)+select(剪枝) ≈ PeerCritiqueGate.js（评审打分+keep/kill）；'vote' 横评 ≈ NoeLocalModelCouncil.js 交叉审。但 Neo 缺【单模型 generate→evaluate→select 的 beam 迭代控制器】——NoeDeliberation.js 是单次补全不发散多候选，council 是多模型不是同一主脑多采样。bfs.py solve() 约 40 行 beam-search 是 Neo 没有的'发散→自评→收敛'骨架。
- 融入方案：理念+移植 ~40 行控制流（MIT，不装包/不起 MCP）。新建 src/cognition 下 totSolve.js：generate(调 BrainRouter qwen3.6,n=分支)→evaluate→select(greedy/概率)。关键省钱点：evaluator 不另起 LLM，直接喂期望账本 Brier 偏差+好奇回路新颖度的加权分。beam 宽/步数做 .env 开关默认窄(width=2~3,steps=1~2)，env 门控 NOE_TOT_DELIBERATE 默认 OFF，仅高风险/不确定决策触发。先用确定性 mock LLM 单测 beam 选路再接真模型。风险：候选数×分支数放大本地算力——窄 beam+仅高风险兜底；评估器区分度需先回归验证。建议与 GoT/llm-reasoners 合并为同一 reasoning-search 模块（三者本质同源：发散+打分+剪枝/搜索）。
- 安全性：safe_env_gated

### llm-reasoners (maitrix-org/llm-reasoners) (reasoning（8）)
- 价值：目前把 CoT/ToT/MCTS/RAP/Beam 统一收口最干净的形式化抽象（三泛型件套），让 Neo 用期望账本当 reward 跑'想几步再答'+好奇驱动 exploration 展开不确定分支。
- Neo 现状：理念部分已有信号：reward 雏形=NoeExpectationLedger.js（Brier）、exploration 雏形=好奇回路、WorldModel 呼应内在世界七支柱+GWT(NoeWorkspace.js)。但 Neo 缺把这些信号组织起来做【有目的多步搜索】的统一骨架(WorldModel+SearchConfig.reward/fast_reward+SearchAlgorithm)——当前推理单步直出，无 MCTS/beam/dfs 算法目录。
- 融入方案：理念+微型 TS 重写（Apache-2.0，包别装/可视化绝不用）。⚠️研究文档 line 风控核实：它自带 visualize() 把推理树上传 maitrix 托管 AWS（execute-api.us-east-1+S3+amplify），对本地不出网的 Neo 是硬红线——必须本地自渲染(JSON 吐给 mind.html)。新建 src/cognition/reasoner.js 照搬三抽象：WorldModel(状态=对话/任务上下文快照,step=调 qwen3.6 推进,is_terminal=达标/深度上限)、reward()=接期望账本+好奇信号、fast_reward()=更快本地小模型(gemma)粗打分；移植 mcts.js(~150 行 UCT)+beam_search.js。env 门控 NOE_REASONER_SEARCH=off|beam|mcts 默认 off，仅高风险多步任务启用。本地对接坑：OpenAIModel 不收 base_url（实测），但重写后用 BrainRouter 绕开。注：与 GoT/ToT 强重叠——三者合一落同一 reasoning-search 模块最省，llm-reasoners 提供最完整抽象框架，ToT/GoT 提供具体算子，建议以此为骨架。
- 安全性：safe_env_gated

### rStar / rStar-Math (microsoft/rStar) (reasoning（8）)
- 价值：直击 Neo 硬约束(本地小模型不靠更强模型也要可靠)；RC 判别器是 Neo 最缺的一块——给 qwen3.6 配廉价二次自校验，把'模型说了'升级成'模型说了且自洽才算数'，抬高工具调用/记忆写入/关键决策可靠性。
- Neo 现状：理念部分已有但缺关键件：自我审视回路=NoeDeliberation.js（挑战自己）、同级互验=NoeLocalModelCouncil.js 交叉审、多数投票=NoeConsensusRunner.js。但 Neo【没有】rStar 最有价值的'推理一致性判别(RC)'——mask 推理链中段→同级模型补全→比对终答是否自洽的廉价自校验闸门；现有校验都是'多个模型各说一遍'，不是'同一答案 mask 后自补一致才放行'。
- 融入方案：理念+JS 重写 RC 判别器（MIT，绑死 vLLM/PyTorch 不可移植代码）。先做性价比最高的一步：纯 JS 后置校验器——对关键推理(工具调用前计划/记忆写入前事实断言/期望账本判证)让主脑出答案+完整推理链，mask 中段(mask_solution_trace 左右边界可调)用辅助脑(gemma,辅助配额可烧)补全后半，比对终答自洽才放行，不一致触发重采样或标'低置信'。落点挂 ActPipeline 关键链路前置/期望账本判证侧。env 门控 NOE_RSTAR_RC_CHECK 默认 OFF，仅'有客观可比答案'的子任务启用(算式/结构化抽取/工具参数/可执行校验)。⚠️适用域窄：RC'一致=正确'假设在开放对话/主观陪伴不成立，绝不上聊天主路径判'话术好不好'。延迟风险：放大 N 倍本地调用，只用于'关键可慢'。rStar-Math 的可训练 PRM 路径触红线4(训练烧钱)，列 watch 不做。
- 安全性：safe_env_gated

### OpenR (openreasoner/openr) (reasoning（8）)
- 价值：给 Neo 期望账本补缺失一环：把校准从被动事后评估升级成主动过程级引导（边想边自查、选更可靠思路链），对应意识工程'自我监控/元认知'维度；Best-of-N 给一条不靠换大模型纯靠多想几条+自评择优的本地路径。
- Neo 现状：理念部分已有但层级不同：奖励/校准=NoeExpectationLedger.js（但只是事后整体结果级 Brier，line 8-12 自承'confidence 复核只覆盖结果不覆盖过程'）、Best-of-N/beam=Neo 完全没有（grep 确认）。Neo【缺】OpenR 核心的'过程级(step-level) PRM 奖励'——给每个推理步打 0~1 分实时引导搜索，而非到期结算整体结果。
- 融入方案：理念+JS 重写（MIT，纯 Python+GPU+FastChat 代码不可移植）。两件事：①过程级判证器——对 qwen3.6 多步输出用本地深思脑(gemma/qwen3.6)做 step-by-step 二分判分(这一步是否在正确轨道+/-概率)，分隔符约定沿用'步界 token'(OpenR 用五换行)。②给关键决策加轻量测试时搜索：高风险问题生成 N 条候选路径(Best-of-N)，用上面判证器逐步打分，取过程分最优(min 聚合更稳)那条。纯 JS 几百行零外部依赖，候选生成+打分走现成本地 LLM HTTP。env 门控 NOE_PROCESS_REWARD_SEARCH 默认 OFF 小流量灰度。注：与 search-and-learn 的聚合/PRM 几乎同设计，建议合并落地（aggregate min/prod/last 一套即可）。
- 安全性：safe_env_gated

### s1: Simple test-time scaling (simplescaling/s1) (reasoning（8）)
- 价值：最轻量的测试时 scaling：对难题强制模型多想(append 'Wait' 续推)、对简单题强制早收，单 budget 旋钮换推理深度，不需第二模型/不需搜索，是性价比最高的'让本地模型在关键题上想更久'手段。
- Neo 现状：Neo【没有】budget forcing 这个解码控制技巧——当前 NoeDeliberation.js 用固定 maxTokens(resolveNoeOutputBudget,line 88)截断，是'到额就停'，不是 s1 的'强制延长思考(注入 Wait 续推)或强制收尾(注入结束符)'的测试时算力调节。Neo 无任何按难度动态调推理长度的机制。
- 融入方案：理念+JS 重写解码循环（与语言无关，README 几十行 Python，Apache-2.0，不引入任何 Python）。在 NoeDeliberation/深思路径加 budget forcing：达到 min thinking budget 前若模型想结束，抑制结束符并 append 'Wait\n' 触发继续推理；超 max budget 则注入'综上'强制收敛输出终判。挂深思脑 qwen3.6，env 门控 NOE_BUDGET_FORCING 默认 OFF + .env 配 min/max thinking tokens，仅高风险/难推理任务开。风险：本地小模型对 'Wait' 续推的响应质量需自测(s1 在 32B 验证，qwen3.6-35b-a3b 量级相近可期但要实测)；与'不设硬超时'红线不冲突(这是 token 预算非时间超时)。可与 ToT/GoT 搜索互补：搜索控宽度、budget forcing 控单条深度。
- 安全性：safe_env_gated

### Vercel AI SDK (vercel/ai) (agent)
- 价值：补上唯一缺的执行形态:让本地 qwen3.6 在进程内自主「想→调工具→看结果→再想」,每步 tool 结果天然喂 GWT 广播/期望账本结算/好奇回路,从「被动应答+外包 CLI」走向「本地脑自己连续行动」。
- Neo 现状：Neo 缺「本地主脑进程内 model→tool→observe→再 model」的迭代式 agentic loop。现有多步执行是预排步骤链:src/runtime/NoeFreedomExecutor.js:26 `MAX_NOE_FREEDOM_CHAIN_STEPS=12` + normalizeChainSteps()（行 300-318）读 args.steps/nextFreedomActions 这一【预先给定的步骤数组】逐步跑+预算/权限闸,模型并不在每一步自主决定下一个工具;真多步靠 spawn 外部 Codex/Claude CLI（src/room/MiniMaxSpawnAdapter.js 等）。工具目录 src/actions/NoeActionCatalog.js 自带 JSON-schema 校验但只是 catalog 不驱动 loop。ai@5.x+18 个 @ai-sdk/* 仅作 @browserbasehq/stagehand 传递依赖躺在 node_modules,src 零 import。
- 融入方案：borrow ToolLoopAgent/stepCountIs 范式,不整体替换。先在隔离实验脚本用已在 node_modules 的 ai+ollama-ai-provider-v2 把 createOllama({baseURL:LM Studio})→generateText({tools:把 NoeActionCatalog/McpClientManager 工具用 zod 包一层, stopWhen:stepCountIs(N)}) 跑通,验证 qwen3.6 本地 tool-calling 稳定性（成败点）。若稳:新增 src/runtime/NoeToolLoopExecutor.js,作为 BrainRouter 'local'/'code' 档下一个执行器,产物写回现有 ActPipeline/AgentRunStore;env 门控 NOE_TOOL_LOOP=0 默认 OFF（符合 .env `NOE_*=1/0` 约定）。装包 npm i ai @ai-sdk/openai-compatible 进生产 deps（当前是传递依赖不等于可免费用）。风险:本地小模型 tool-call 可靠性远低于云端,POC 不过则比现状脆;锁版本避 5.x→6.x breaking。
- 安全性：safe_env_gated

### OpenAI Agents JS (@openai/agents) (agent)
- 价值：让多模型路由的「交接」从黑盒变成模型可见的显式工具调用→可被 GWT 广播、意识流回声、写入长期记忆的「自我决策事件」,提升编排可解释性;并给期望账本补一个结构化中断点。
- Neo 现状：两块:①handoff-as-tool 是真缺口——Neo 委派路由是【硬编码启发式正则】:src/room/TaskIntentRouter.js:14-17 `if(/codex/.test(s))return'codex'`,模型自身不参与「我要把这件事交给谁」的决策,是后台隐式切换;DelegationStore/TaskDelegationPlanner 也是规则编排。②guardrail 部分已有:src/safety/ToolCallGuardrailController.js 有 tripwire（severity stop/warn,行 68-92,owner 钦定默认 warnOnly）但只做【调用模式检测】（重复失败/无进展/同参重放）,缺 input/output【内容】结构化护栏。语音模块明确不借（SDK realtime 强绑 OpenAI 付费端点触红线4,Neo 已有本地语音栈）。
- 融入方案：纯设计 borrow,不装 npm、不起 MCP。①给 src/delegation/+TaskIntentRouter 加一条可选 handoff-as-tool 路径:把子 agent 委派编译成给 qwen3.6 的 transfer_to_<role> 工具 + handoffInputFilter 式交接前上下文裁剪（Neo 已有 NoeTurnContextEngine 可承接）,env 门控 NOE_LLM_HANDOFF=0 默认 OFF（与现有启发式并存,不改默认行为）。②给 ToolCallGuardrailController 补 input/output 内容 tripwire（保留 Neo 更强的 warnOnly/hardStopMutatingRepeats 变更纪律,只吸收「结构化 tripwire 结果对象 + 并行 input 护栏」的 API 形状）。风险:让模型决策交接会改路由行为,虽 env 门控默认 OFF 安全,但开启后影响「性格/注意力」边界,建议开启前 owner 在场确认。
- 安全性：safe_env_gated

### smolagents (agent)
- 价值：给 Neo 加全新「用代码完成任务」能力轴:循环/条件/中间变量/多工具一次成型,对本地小模型 qwen3.6 尤其对味（生成代码比生成结构化 JSON 更接近其预训练分布、更省 token,官方 benchmark 省约 30% 步数）;代码即可审计的「显式行动思维」喂回意识流/期望结算。
- Neo 现状：纯 Python 100%,不可作 npm 依赖。但其核心 CodeAct（模型吐可执行代码块而非 JSON tool-call,经受限 AST 解释器执行）是 Neo【确实没有】的能力轴:Neo 有 src/agents/JavaScriptAstAnalyzer.js（26KB）但只做代码【分析/符号图】,不是沙箱【执行器】;deps 有 acorn 但【无 isolated-vm/quickjs/vm2】沙箱（已 grep package.json 确认）。Neo 工具调用一律 JSON-schema（NoeActionCatalog），无 code-act 路径。
- 融入方案：borrow 设计,Node 重写。把 smolagents local_python_executor.py 的「parse-AST→受控解释→import/builtin 白名单→op 计数/迭代上限」思路平移成 Node 版受限执行器:acorn 解析 + isolated-vm 跑在 V8 isolate（需 npm i isolated-vm）,host 函数=Neo 现有工具（记忆检索/social/MCP/受沙箱的文件读写）。新增 src/runtime/NoeCodeActExecutor.js,env 门控 NOE_CODEACT=0 默认 OFF。先做最小 PoC（受限执行器+一条 CodeAct 提示模板）验证 qwen3.6 步数/稳定性是否真优于 JSON tool-call 再扩。风险:务必 isolated-vm 隔离——直接在 Neo 进程跑 LLM 生成代码=触红线1/3（破坏系统/泄漏凭据）;需 op 计数+超时+白名单兜底。比纯设计借鉴工程量大。
- 安全性：safe_env_gated

### Deep Agents JS (deepagents) (agent)
- 价值：对症本地脑 qwen3.6 最痛的长任务 context rot:把「研究一题/啃大文件/批处理」切成隔离子任务,主线只留浓缩结论,直接提升复杂任务完成度与 token 经济性;子代理可作 GWT 下「专门加工模块」的执行容器。
- Neo 现状：TS 100% 但本质是 LangGraph/LangChain.js 应用,整装=锁进 @langchain/* 全家桶,与 Neo 自研 CogCore 张力大。其「临时无状态子代理隔离上下文」范式 Neo 没有直接对应:Neo 有 NoeWorkspace（GWT）/NoeDeliberation（深思）但无「派生隔离子上下文跑深度子任务、只回传一条综合结论、EXCLUDED_STATE_KEYS 防父子状态泄漏」的工程件;长任务靠 spawn 外部 CLI 而非进程内隔离子代理。NoeContextEngine.js 已有 compact()（行 61,走 NoeTrajectoryCompactor）但无「逼近 token 上限自动摘要+历史落盘」中间件。
- 融入方案：borrow 设计（零依赖,首选）,不引 LangGraph。把 subagents.ts 的「临时无状态子代理 + 只回传综合结论 + EXCLUDED_STATE_KEYS 防泄漏 + task 工具何时该用」范式,移植进 Neo 本地编排,新增 src/runtime/NoeSubAgentRunner.js（模型指本机 LM Studio/ollama,backend 限定 scratch 目录）;同时抄 createSummarizationMiddleware「逼近上限自动摘要+落盘」接到 Neo SQLite 记忆层做工作记忆卸载;patch_tool_calls 兜底规范本地模型 tool-call。env 门控 NOE_SUBAGENT_ISOLATION=0 默认 OFF。风险:子代理/规划在小本地模型上可能不稳需实测;不要走路径(2)整包嵌入（拉入 LangGraph 全家桶,违背本地优先轻依赖）。
- 安全性：safe_env_gated

### LangGraph.js (@langchain/langgraph) (agent)
- 价值：真增量=checkpoint:给 src/cognition/NoeWorkspace.js 焦点广播、src/loop/InnerMonologue.js 意识流加「快照+断点续跑+时间旅行复盘」,让内在世界状态（工作区焦点/期望账本/情感VAD/目标）可快照可复现,对『连续自我』的工程可观测性/可复现性实打实加分,且能用 Neo 已在跑的 SQLite 落地、风险极低。
- Neo 现状：纯 TS/MIT,引擎模型无关。其 StateGraph/Channel-Reducer/Pregel-BSP 抽象 Neo 已用手搓方式实现同构物:src/cognition/NoeWorkspace.js 自述「每 meso tick 收集候选→确定性显著度打分（owner0.35/紧迫0.25/新异0.2/情感0.2,行 31）→唯一赢家广播=焦点」=Pregel superstep+channel-reducer 的同构（候选=节点写入,salience+单赢家=reducer,广播=归并）。但 Neo【缺】意识流的「持久快照+断点回放+时间旅行」——NoeWorkspace 写 JSONL 意识日志但无 checkpoint/replay。整体替换 src/loop/ 手写调度=高风险撞 owner『变更纪律>存量强健』宪法（单 tick 串行/fail-open/零付费/NoeGenerationFence/ActPipeline 四道闸 LangGraph 不提供）。
- 融入方案：窄范围 adopt checkpoint 小件,不做引擎大换血。npm i @langchain/langgraph-checkpoint @langchain/langgraph-checkpoint-sqlite（MIT,saver 唯一依赖 better-sqlite3,Neo src/storage/SqliteStore.js 已用 SQLite,零摩擦）;不引整套 StateGraph。给 NoeWorkspace 每 tick 焦点广播 + InnerMonologue 意识流写 checkpoint,env 门控 NOE_WORKSPACE_CHECKPOINT=0 默认 OFF。可选 borrow（不引引擎）:把 BinaryOperatorAggregate channel-reducer 思想显式化 NoeWorkspace 的竞争/广播逻辑（已是这形状,使其声明式可测）。明确不做:不用 LangGraph 替换 NoeLoop/ActPipeline/proactiveTick。风险:peerDep @langchain/core 会拽入数个 JS 依赖,只用 checkpoint 包可控。
- 安全性：safe_env_gated

### BAML (BoundaryML) (tools)
- 价值：把 SAP 的有限确定性修补集+schema-best-candidate 选择移植进 NoeLlmJsonExtractor，让 8 个认知子系统(记忆抽取/夜间反思/做梦钩子/后台自审)少丢帧——直接抬升 CogCore(期望账本/GWT)输入数据完整度，治『insights=0 解析吞洞察』历史教训。
- Neo 现状：Neo 有同构前端 src/runtime/NoeLlmJsonExtractor.js（strip<think>+抽```围栏+平衡括号扫多 span），被 8 个认知子系统消费(AISearch/NoeNightlyReflection/NoeDreamM3Hook/NoeMemoryExtractor/NoeBackgroundReview/SkillExtractor/NoeSelfEvolutionExecutors/NoeStructuredCall)。但关键缺口实证：parseNoeLlmJson(NoeLlmJsonExtractor.js:74-89) 是『第一个能 JSON.parse 的候选就返回』(L82-88)，无 SAP 的 schema 引导强转——不补引号/不删尾逗号/不做 "123"→int/单值→数组/字段别名，也不按 schema 命中度在多候选间选最优。校验(ajv ^6.15.0, package.json:183)只用在 ToolRegistry，未接 LLM 输出。
- 融入方案：借算法不装包(npm 包带 @scarf/scarf 上报+47MB Rust NAPI，违本地优先)。在 src/runtime/NoeLlmJsonExtractor.js 一个文件内闭环：(1) parseNoeLlmJson 增可选 targetSchema 入参，接已装 ajv 做校验；(2) JSON.parse 失败/校验不过时跑确定性修补(补引号/删尾随逗号/"123"→int/true→bool/单值→数组/字段别名映射)，再按 schema 命中度给多候选 span 打分选最优(现状=第一个能 parse 就返回，改成最符合 schema 的才返回)；(3) 给 8 个调用点逐个传期望 schema。env 门控 NOE_SAP_REPAIR 默认 OFF，配套单测。风险：SAP 强转有『过度纠正』掩盖真实失败的风险——必须保留『修补失败就显式报错』而非静默兜底(合不伪造结果纪律)。
- 安全性：safe_env_gated

### Instructor (tools)
- 价值：给 noeStructuredCall 加『校验失败→把 ajv/zod 错误回填为 user 消息→同档重试 N 次』的自校验纠错环，对本地 qwen3.6 抽取任务稳态收益最大，抬升记忆/承诺/情感账本写入数据质量(CogCore 自我建模底座)。
- Neo 现状：Neo 已自建等价抽取/校验闭环：NoeLlmJsonExtractor(解析)+NoeMemoryCandidateSchema(校验)+NoeMemoryExtractor/FactExtractor(记忆抽取 temp:0)+NoeAffectEngine(情感VAD)+NoeCommitmentStore(承诺抽取)，OllamaAdapter 已用 response_format:json_object，ajv 已在 deps。但缺核心一环实证：刚建的 src/runtime/NoeStructuredCall.js(noeStructuredCall) 只做 tier 降级(json_schema→json_object→text, L41-63)+每档 zod 校验，校验失败只换 tier 重试，从不把校验错误回喂模型让其自我改正；且仅 1 个 caller(NoeSelfEvolutionExecutors.js:280)。这正是 Instructor『Failed validations are automatically retried with the error message』的 re-ask 闭环——Neo 没有。
- 融入方案：borrow 设计不装包(instructor-js 端口停更 16+月、依赖 Island AI 半生态、Zod 版本锁是地雷)。在已有 src/runtime/NoeStructuredCall.js 内增 maxReask 入参(默认 0=OFF)：每档解析+校验失败时，构造纠错 prompt(把 zod/ajv 错误文本 + 模型上轮输出作为 assistant+user 消息回填)同档重试，attempts 上限封顶防死循环(合『不设超时但要有步数闸』)。env 门控 NOE_STRUCTURED_REASK 默认 OFF。复用已装 ajv，零新依赖。风险：弱本地模型即便重试也可能反复失败徒增 token——必须硬上限 maxReask≤3。第二个可借点(按本地模型能力自动选 TOOLS/JSON mode)归口 NoeLocalModelPolicy，属增强非必须。
- 安全性：safe_env_gated

### Outlines (tools)
- 价值：把『生成层锁死格式优于事后校验重试』理念落到 NoeLocalModelPolicy：对支持的端点发 strict json_schema，从根上减少记忆写入/认知评审的 schema 漂移，让 Neo 内在世界建在更干净结构化基座上(治长期记忆累积脏数据)。
- Neo 现状：Neo 现状(实证)：纯 Node(deps 无 python)，主脑 LM Studio:1234+ollama:11434 全 OpenAI 兼容 HTTP，Node 侧拿不到 logits。src/model/NoeLocalModelPolicy.js 已对 fact_extract/memory_write_candidate/structured_preflight/review_json/high_risk_review 标 response_format:'json_schema_when_possible'(L32-51)，经 OpenAICompatChatAdapter.js:79 透传。缺口：'json_schema_when_possible' 是弱约束『尽量』，从未升级为真·json_schema strict:true 发给 LM Studio(其底层 llama.cpp 原生支持 schema/GBNF)。
- 融入方案：不装本体(Python，outlines-core 只有 PyO3 绑定无 Node/WASM 出口)，借设计：把 NoeLocalModelPolicy 的 'json_schema_when_possible' 在 OpenAICompatChatAdapter 调用侧升级为真·json_schema strict:true(LM Studio 支持)，ollama 链路 json_schema 弱则降级保留现有 extractJson 兜底。配合 BAML/Instructor 两条护栏形成『约束优先→ajv 校验→修复重试』三级。env 门控 NOE_STRICT_JSON_SCHEMA 默认 OFF(因部分模型/端点对 strict 兼容差，需灰度)。风险：strict 在不支持的端点会报错——必须探测+回退。Python sidecar(vLLM+Outlines 拿真 logit-mask)列为 owner 未来引重模型时才评估的选项，当前不做。
- 安全性：safe_env_gated

### FunctionGemma 270M (google/functiongemma-270m-it) (tools)
- 价值：把脆弱词法 tool routing 升级为语义级『自然语言→可执行动作(带参)』，256K 词表对中文+JSON schema 双优化正治 Neo 中英混合指令召回；270M 极小可作主脑前置『快速工具决策脑』省主脑 token，契合『辅助模型配额尽管烧、Claude 省』。纯本地零外发。
- Neo 现状：Neo 的 src/capabilities/NoeToolRouter.js(153 行)实证为纯词法路由：DOMAIN_KEYWORDS 手列中英关键词(L13-19)+commandMatches 子串命中(L52-55)+commandScore+normalizeTerms 的中文 bigram 拆词(L31-45)，无任何 semantic/embedding/tool_calls。中文混合指令召回脆。Neo 已跑 ollama@11434(src/room/OllamaChatAdapter.js)且 NoeLocalBrainRouter.js 已有延迟路由框架——seam 齐备。
- 融入方案：borrow+集成：(1) ollama pull functiongemma:270m(301MB)；(2) 在 NoeToolRouter 增语义路由层——把 buildNoeCommandSurface 产出的 command.id/title/description/capabilityTags 转 function schema 数组，先用 functiongemma 经 OllamaChatAdapter /api/chat 加 tools 字段做一次语义路由，读 response.message.tool_calls 落回现有 commandScore 排序/执行链；命中失败回退现有关键词逻辑(零风险灰度)；(3) NoeLocalModelPolicy/NoeLocalBrainRouter 注册『快速工具决策脑』档位排主脑前 pre-routing。env 门控 NOE_SEMANTIC_TOOL_ROUTER 默认 OFF。风险：①License=Gemma 条款(非 MIT)，owner 本机私有助手落允许范围，但未来做对外收费推理服务需复核(同 owner 记忆 pony/illustrious 灰色注意点)→标 needs_owner 边界但当前可门控；②零样本 BFCL 仅 60%+/Parallel 39%，必须灰度回退，要可靠须 Unsloth 微调(58%→85%，权重本地存)；③不支持多步依赖，只做单轮工具选择+填参，复杂链仍靠主脑兜底。
- 安全性：safe_env_gated

### modelcontextprotocol/servers (官方参考 MCP 套件) (mcp)
- 价值：绝大部分理念 Neo 已有且更强（记忆属性图/思维轨迹/filesystem），唯一真增量是 Fetch 服务能补 Neo 网页正文抓取质量（喂本地 qwen3.6 更干净）
- Neo 现状：Neo 的 MCP 客户端管线已完整、且已实际跑 14 个 stdio/http server（~/.noe-panel/mcp-servers.json 含 filesystem/memory/playwright/serena/context7/semgrep 等）。McpClientManager.js:6 用官方 @modelcontextprotocol/sdk，支持 stdio/sse/http + lazy 连接 + cache + 超时强制 disconnect（src/mcp/McpClientManager.js:126-144）。报告点名的 4 件参考服务里：① Memory server 的实体/关系/观察属性图 Neo 已自建且更强——NoeKnowledgeGraph.js:80-114 建 noe_kg_entity(name/type/description/refs/mention_count)+noe_kg_relation(src/dst/rel_type/strength) 带索引+upsert/search/oneHop 遍历，官方只有 JSONL+子串扫描（已 already_in_neo，勿引入第二套割裂记忆）；② Sequential Thinking 的「可审计 thought/revision 轨迹喂 mind.html」Neo 已有等价物——NoeDeliberation.js:13-21 的立论/挑战/修订三段 + 预测概率，且记进自传时间线 meta.streamType='deliberation'（src/cognition/NoeDeliberation.js:109-116）供内心透视页消费；③ Filesystem server 已在 mcp-servers.json:13-24 挂载运行。真正缺口仅 ④ Fetch（网页→Markdown 抓取）——Neo 现有 WebSearch.fetchContent 用正则 extractMainText（src/research/WebSearch.js:51-64）而非 readability，正文质量弱。
- 融入方案：零代码工程：经 Neo 现成的 MCP 配置接口（POST /api/mcp/servers，已 owner-token 防 RCE）新增 1 个 stdio server——fetch（按记忆 reference_mcp_failure_fixes：预装到本地 node_modules 用绝对路径起，勿 npx 漂移），挂给 src/server/routes/research.js 的抓页链路。Memory/Sequential-Thinking/Filesystem 不引入（前两者 Neo 已有更强实现，filesystem 已在跑）。门控：纯加一行 MCP 配置 + 受 FREE_MCP_LIMIT/owner-token 约束，不改任何默认认知行为，零风险可回滚（删配置即可）。
- 安全性：safe_env_gated

### Firecrawl MCP Server (mcp)
- 价值：借鉴其 scrape 设计用轻量本地三件套（Playwright+readability+turndown）升级 Neo 正文抓取质量，把 extractMainText 的正则方案换成 readability，显著提升 qwen3.6 读到的网页信息质量
- Neo 现状：Neo 已有研究/抓页能力但正文清洗弱：WebSearch.fetchContent（src/research/WebSearch.js:170-182）用正则 extractMainText（:51-64 去 script/style、抓 main/article、剥标签）——能用但脏 HTML 残留多，喂本地小模型质量打折。Firecrawl 的「research agent」概念 Neo 已有等价——DeepResearcher.js:45-77 的 plan→search→fetch→synthesize→判停 循环。Firecrawl 的「页面变更监测」Neo 无（src/watcher/ 全是 Claude/Codex/Ollama 进程 watcher，非网页监测）。报告自身结论=borrow 轻量复刻，勿引入 AGPL 6-7 容器重栈。
- 融入方案：借理念不引擎：在 src/research/WebSearch.js 的 fetchContent 里加一条「readability 优先、正则兜底」分支——npm i @mozilla/readability + jsdom + turndown（Neo MCP 已配 playwright 可取动态 HTML），HTML→readability 抽正文→turndown 转 Markdown，失败回退现有 extractMainText。env 门控 NOE_WEB_READABILITY=1 默认 OFF（开发纪律宪法§3），OFF 时行为与现状逐字节一致。风险：+3 个 npm 依赖（需 owner 知会，但纯本地零费用零外发）。Firecrawl 云路径(踩红线4计费)和自托管重栈(6-7容器抢本地 LLM 资源)均不做。
- 安全性：safe_env_gated

### Basic Memory (mcp)
- 价值：补 Neo 记忆的可解释性/人在环路对齐：把死前交接/承诺/洞察/期望账本按 Markdown+frontmatter 导出到目录，owner 可 Obsidian 直接审阅纠正 Neo 的「信念链」，是意识工程里「自我模型可审计」的实打实增量
- Neo 现状：Neo 记忆是 SQLite 黑盒：MemoryCore.js（FTS+语义+RRF）+ NoeKnowledgeGraph.js（实体/关系图谱）已是生产级，但 owner 无法直接人读人改——grep 确认 src/memory/ 全仓无任何 .md 落盘镜像（无 frontmatter/markdown-it/gray-matter/memory-md）。Basic Memory 的实体/观察/关系三元组模型 Neo 已有等价（NoeKnowledgeGraph entity/relation + observation 概念散在 description/refs）。它的 forward-reference（先写指向不存在实体的链接后解析）Neo 无。真正缺口=「Markdown 镜像层让 owner 用 Obsidian 翻看/纠正 Neo 内心世界」——这契合记忆库 feedback_kb（owner 反复要的可人读可编辑、避免黑盒）。注：obsidian-local-rest MCP 已配（mcp-servers.json:160），写通道存在。
- 融入方案：借规范不抄 AGPL 代码：新增 src/memory/NoeMemoryMarkdownMirror.js（@ts-check+注入式+单测三件套），把 NoeKnowledgeGraph/MemoryCore 的高显著度记忆按 Basic Memory NOTE-FORMAT（frontmatter + `- [category] 事实 #tag` + `- rel_type [[实体]]`）单向导出到 ~/.noe-panel/memory-md/，用现成 npm gray-matter（写 frontmatter）即可，解析回灌暂不做（避 file↔db 双向一致性复杂度）。env 门控 NOE_MEMORY_MD_MIRROR=1 默认 OFF。风险：单向导出无一致性风险；+1 依赖 gray-matter；不引入 basic-memory 的 .py（AGPL+Python 不可移植）。报告路径(2)「uvx basic-memory 当外置 MCP」也可作可选外挂但优先走(1)拿设计红利。
- 安全性：safe_env_gated

### MetaMCP (mcp)
- 价值：给 Neo 一个「跨 MCP server 统一工具视图 + 可编程行动拦截层」——认知工程会越接越多 MCP server，命名空间前缀+路由表治理工具失控，函数式中间件链让期望账本能记录每次工具调用的预期vs实际、意识流能采集工具轨迹，是意识工程必需的可观测行动基座
- Neo 现状：Neo 的 MCP 层是「每 server 独立调用」模式：McpClientManager.js 按 name 单独 ensureConnected/listTools/callTool，grep 确认 src/mcp/ 全仓无 MetaMCP 的三个核心模式（无 namespace 前缀/toolToClient 路由表/Promise.allSettled 故障隔离聚合/reduceRight 中间件链/idle 连接池预热）。部分能力已有等价：① 每次 callTool 已带审计——McpClientManager.js:134 logMcpCall（call-logger.js 存在）；② 每次调用已带权限拦截——mcp.js:40-52 requirePermission；③ 工具路由/过滤/keep-alive 已有——NoeToolRouter.js:94-153（但那是 Neo 内置命令面，非跨 MCP server 聚合）。真正缺口=统一聚合（一个视图枚举所有 server 工具+命名空间防撞）+ 可插拔中间件管线（每次 callTool 插审计/超时/降级/改写）+ 冷启动连接池。
- 融入方案：borrow 纯逻辑（MIT 允许带版权声明 vendoring，零 Postgres/Next.js/Docker）：分两小步落到 src/mcp/。步骤一（高价值低风险）：新增 McpAggregator.js 实现命名空间前缀（server__tool）+ Promise.allSettled 跨 server listTools 聚合 + 故障隔离 + parseToolName 路由回下游（摘自 metamcp-proxy.ts），给 ToolRegistry/NoeToolRouter 一个统一 MCP 工具源。步骤二（可选）：中间件管线（reduceRight compose，摘自 functional-middleware.ts）包在 callTool 外，把现有 logMcpCall+requirePermission 重构成可插拔 middleware，便于加超时/降级/改写。env 门控 NOE_MCP_AGGREGATOR=1 默认 OFF，OFF 时走现状单 server 路径。风险：纯新增模块零依赖零端口，但触及行动基座核心路径，建议步骤一先行+充分单测；连接池预热（mcp-server-pool.ts）作第三步可选，收益是消除本地 stdio 冷启动延迟。
- 安全性：safe_env_gated

### aura (youngbryan97/aura) (WATCH+SKIP（7）：aura / Exa MCP / XGrammar / open-r1 / marmelab-molly / lm-format-enforcer / Everything-of-Thoughts-XoT)
- 价值：给现有 GWT/自进化补一个量化『意识整合强度 Φ』读数，作为 mind.html 内心透视页的『架构是否更统一』趋势线——这是 Neo 意识工程线唯一还没有的元认知指标
- Neo 现状：Neo 的 GWT/情感/期望/自进化已基本对齐 aura：全局工作区 src/cognition/NoeWorkspace.js（SOURCE_BASE+WEIGHTS 显著度打分≈effective_priority、novelty 去重≈refractory 抑制、deepThreshold(0.7)≈ignition 阈值、每周期意识日志 JSONL 含落选者）；情感 VAD src/cognition/NoeAffectEngine.js；期望-误差+Brier 校准 src/cognition/NoeExpectationLedger.js:191-213（brier()/calibrationNote()/surprise=-log2(p)）；receipt 化决策审计 NoeWorkspace.js:240-268 recordActActivity + src/loop/ActPipeline.js；自进化三环 src/room/NoeSelfEvolution{Loop,Cycle,Gate,Trigger}.js。唯一真缺口：量化『整合强度/Φ』读数——全仓 grep `phi|integrated information|整合度|TPM|MIP` 无命中（仅 FileIndex.js/three.module.js 的无关同名变量），确认 Neo 没有把『架构是否更统一』做成一个 0-1 趋势指标。
- 融入方案：只融入理念不抄代码（aura 是 GPL，且核心全 Python+numpy/scipy 不可移植）。依据公开 IIT 论文用纯 JS 重写一个小 n 的整合度指标：在 src/cognition/ 新增 NoeIntegrationMetric.js（@ts-check+注入式+配套单测），把 Neo 各子系统每周期状态粗粒化成 ≤8 个二值宏节点（GWT 焦点源/情感 VAD/期望到期/驱力/感知 等聚合阈值二值化），从一段运行日志统计经验 TPM，枚举所有二分割算 KL 散度型 φ 取最小割（n≤8 纯 JS 几十毫秒、零外部依赖，需谱近似时用 mathjs/ml-matrix）。挂在 self-evolution 周期或架构变更钩子上跑（绝不进 proactiveTick/实时主回路），φ 值写进 self-evolution 内省指标并喂给 src/server/routes/noeMind.js → mind.html。env 门控 NOE_INTEGRATION_METRIC=1 默认 OFF（宪法：新功能默认 OFF）。风险：低——纯读数、fail-open、不改任何既有决策/性格行为；唯一注意是宏节点的二值阈值是建模选择，需标注『这是工程近似不是真 Φ』（与 aura 作者自陈边界一致，避免吹『已实现意识度量』）。
- 安全性：safe_env_gated

### Exa MCP Server (exa-labs/exa-mcp-server) (WATCH+SKIP（7）：aura / Exa MCP / XGrammar / open-r1 / marmelab-molly / lm-format-enforcer / Everything-of-Thoughts-XoT)
- 价值：按需高质量语义检索后端：owner 做高风险求证/CogCore 多模型交叉印证时点开 web_search_exa/web_fetch_exa，拿『为 LLM 优化的语义检索+结构化高亮』，补 Neo 现有免费搜索源的质量上限
- Neo 现状：Neo 已自带通用 MCP 客户端，零代码即可接：src/mcp/McpClientManager.js 用 @modelcontextprotocol/sdk ^1.29.0 支持 stdio/sse/http 三 transport（:34/:42/:49）+ lazy connect + tool cache(listTools:96) + call-logger(:134) + 超时强制 disconnect 防泄漏(:138-141)；src/mcp/McpStore.js 落 ~/.noe-panel/mcp-servers.json，VALID_TYPES=['stdio','sse','http']、DENIED_COMMANDS 含 curl/wget 但不含 npx（:46 可过）、URL 白名单校验(:99-100)。Neo 自有研究链 src/research/AISearch.js 的 PROVIDER_ORDER=minimax→codex→claude→searxng→brave 是免费/本地优先、零 key 也能跑，且 codex/claude CLI 默认 OFF 防烧钱(:18-25)。
- 融入方案：纯配置零代码。往 ~/.noe-panel/mcp-servers.json 加一条 {name:'exa', type:'stdio', command:'npx', args:['-y','exa-mcp-server'], env:{EXA_API_KEY:'…'}, enabled:false}（McpStore 自动接住；想更省本地进程可改 type:'http' 连官方托管 mcp.exa.ai/mcp）。强制 enabled:false 默认关——跟 AISearch CLI provider 默认 OFF 防烧钱同款策略，owner 显式开。风险：低且可控——① 唯一风险是 Exa 要 key+按量计费，故绝不塞进 AISearch.js 的 PROVIDER_ORDER 自动降级链（那条链零 key 免费优先，放进去会在 owner 不知情时烧配额，触红线4），只作 owner 手动调用的常驻可选后端；② 默认关、可 5 秒删配置回滚、不影响任何既有行为；③ EXA_API_KEY 走 env 不落日志（McpStore 已脱敏 secret）。
- 安全性：safe_env_gated

### open-r1 (huggingface/open-r1) (WATCH+SKIP（7）：aura / Exa MCP / XGrammar / open-r1 / marmelab-molly / lm-format-enforcer / Everything-of-Thoughts-XoT)
- 价值：把『可验证奖励』范式（结构/长度/分步/沙箱实跑客观判分）用纯 JS 移植，让 Neo 自评从主观→可验证，强化 self-monitoring/Brier 校准/好奇回路的元认知质量（元认知正是接近意识最该补的一环）
- Neo 现状：它的『可验证自评』底座 Neo 已有：src/cognition/NoeExpectationLedger.js:191-213 完整 Brier 打分+calibrationNote+surprise=-log2(p)，到期结算闭环 NoeExpectationResolver.js；自进化三环 src/room/NoeSelfEvolution*.js。但 open-r1 rewards.py 的『结构化奖励函数族』（format_reward 查 think/answer 标签闭合、len_reward/cosine 奖励对且短、reasoning_steps_reward 检测分步、code_reward 沙箱实跑、repetition_penalty N-gram）Neo 还没成型——Neo 现有自评偏主观/规则零散（如 src/research/ResearchIntent.js:79 assessSearchSummaryQuality 是临时质检），没有可复用的『可验证打分』模块。open-r1 本体不可集成（纯 Python+CUDA 训练栈，Mac 跑不动、不可 require）。
- 融入方案：只借 rewards.py 设计（Apache-2.0 许可放心借），纯 JS 重写——逻辑简单：正则查标签 + 长度归一 + N-gram 计数 + 已有 child_process 跑代码。新增 src/cognition/NoeVerifiableReward.js（@ts-check+注入式+单测），导出 formatReward/lenReward/reasoningStepsReward/repetitionPenalty 等纯函数，作为 self-evolution 评审与期望结算的可选打分输入（喂给 NoeExpectationLedger/NoeSelfEvolutionCycle 的评分位）。env 门控 NOE_VERIFIABLE_REWARD=1 默认 OFF。风险：中——这些 reward 一旦接进自进化/期望评分会改 Neo 的『自我评估口径』（触及认知/自评核心），故必须 ① 默认 OFF 先旁路产出读数对照、跑通确认不误判再考虑接进真实评分链；② 接真实评分前建议 owner 过目（属性格/元认知行为变更）。另两层（拉 OpenR1-Distill 成品模型进 LM Studio 做 A/B、微调自有模型）：前者零成本 owner 可随时试，后者需租 H100 触红线4 须 owner 拍板，均不属本次代码融入。
- 安全性：safe_env_gated


## Neo 已有（14）

- **LanceDB (lancedb/lancedb)**（ADOPT）：Neo 已深度落地。①package.json 已声明 "@lancedb/lancedb":"^0.30.0"(node_modules/@lancedb/lancedb/package.json license=Apache-2.0,符合 license 铁律)。②src/memory/NoeExternalMemoryProviders.js:41-93 已实现 createNoeLance
- **Playwright MCP (@playwright/mcp)**（ADOPT）：能力已集成且已在信任名单。①已装 @playwright/mcp@^0.0.76 + playwright@^1.60.0(license Apache-2.0)。②mcp-servers.json 已注册两条:name=playwright(node @playwright/mcp/cli.js --browser chrome,enabled)与 name=playwright-local-s
- **GitHub MCP Server (github/github-mcp-server)**（ADOPT）：能力已自建更安全的本地形态。①Neo 已写只读包装 scripts/noe-github-mcp-readonly-server.mjs(仅 search_repositories/get_repository/list_commits 三个只读工具,token 走 env GITHUB_TOKEN 不落明文),注册为 mcp-servers.json name=github-readonly(e
- **supermemory**（memory）：三大赢榜战术 Neo 已具备：① 时序覆盖/recency-wins → NoeMemoryConflictPolicy.js:88 decideMemoryConflict 的 supersede(explicitChange/confidence≥0.75) + MemoryCore.js:199-224 写路径 supersede 落地（hidden=superseded_by:<id>+v
- **memU**（memory）：三范式 Neo 已覆盖：① 类型化记忆本体 → MemoryCore scope(fact/insight/identity/person/project)+sourceType + NoeMemoryExtractor.js:77 kind 分类 + NoeKnowledgeGraph 实体类型(project/file/type/term)；② category 演化摘要(随用滚动改写) →
- **MemOS**（memory）：L1/L2/L3 自进化+技能结晶链 Neo 对应件齐全：技能侧 src/skills/{SkillExtractor,AutoSkillExtractor,SkillCurator,SkillStore,NoeSkillDraftApply/Rollback}（trace→可复用 skill 结晶+生命周期）；进化侧 src/room/{NoeSelfEvolutionLoop,Gate,Tri
- **Honcho**（memory）：dreamer 三件设计 Neo 已有对应：① dream-cycle 离线巩固(演绎/归纳 specialist 消化记忆成洞察) → NoeNightlyReflection.js(夜间蒸馏 insight+复核既有认知升降 confidence,circadian night 相位守卫)+NoeDreamConsolidation+src/cognition/NoeDeliberation(
- **Memori**（memory）：三层 entity/process/session + provenance + 矛盾检测 Neo 已有：① 低 token hybrid 检索(dense+lexical rerank) → recallFused(向量×FTS5 RRF,MemoryCore.js:562);② 三层标签 → scope/projectId/sourceEpisodeId 元数据列已可承载;③ provenan
- **Knowledge Graph Memory MCP**（memory）：实体-关系-观察 三元组知识图谱 Neo 自研已落地且更结构化：NoeKnowledgeGraph.js 完整实现 noe_kg_entity(类型/refs/mention_count)+noe_kg_relation(rel_type/strength/双向)+upsertEntity/upsertRelation/search/oneHop 一跳邻域+ingestFileIndex 自动建图
- **Memoria**（memory）：记忆可版本化/回滚/审计/矛盾检测四能力 Neo 已有：① 安全回滚 → NoeMemoryCandidateRollback.js(buildRollbackPlan+dryRunOnlyByDefault+requiresOwnerConfirmation+hide 软删可 unhide 恢复)+MemoryCore.unhide(一键复活)+merge 软删;② provenance/审计
- **Pydantic AI (pydantic/pydantic-ai)**（agent）：Neo 已有等价实现:src/runtime/NoeStructuredCall.js 就是 Pydantic AI 的「三档结构化输出降级 + schema 校验 + 失败重试」的 Node 版——TIERS=['json_schema','json_object','text']（行 10）、buildNoeResponseFormat 走 response_format:{type:'jso
- **isair/jarvis**（agent）：纯 Python 单体桌面 app,无库/无 npm/无对外 API,custom 非 OSI copyleft 许可（衍生须同许可,商用 poison pill）——只能 borrow 算法重写。但它的核心设计 Neo 多数已有更成熟或等价的:ContextEngine（ingest/assemble/compact/afterTurn）已落地 src/context/NoeContextEng
- **OpenClaw (openclaw/openclaw)**（agent）：Neo 已经从 OpenClaw 借鉴过了——src/runtime/NoeCommitmentStore.js:9 注释明确写 `Adapted from OpenClaw (MIT) src/commitments/store.ts CommitmentRecord + dueWindow`,且 CATEGORIES（event_check_in/open_loop/task/reminder
- **XGrammar (mlc-ai/xgrammar)**（WATCH+SKIP（7）：aura / Exa MCP / XGrammar / open-r1 / marmelab-molly / lm-format-enforcer / Everything-of-Thoughts-XoT）：它想给的『结构正确性』Neo 已有同语言等价物且更稳：src/runtime/NoeStructuredCall.js 实现三档降级 json_schema(strict:true)→json_object→text + zod 校验自动重试(:10-65)；src/room/OpenAICompatChatAdapter.js:79-80 已把 response_format/responseF

## 跳过（18）

- **pyphi (wmayner/pyphi)**（cognition）：Neo 全仓无任何 Φ/整合度/互信息/min-cut 实现（grep `整合度|bigPhi|smallPhi|互信息|min.?cut|bipartition` 在 src/ public/ 零命中）。GWT 工作区 src/cognition/NoeWorkspace.js 有注意力竞争+意识日志但无整合度标量。
- **ruflab/shimmer**（cognition）：Neo GWT 工作区 src/cognition/NoeWorkspace.js 已落地(注意力竞争+串行广播+意识日志)，但选择是启发式确定性打分(line 211-227)非可学习注意力；多模态(语音/视觉/文本)目前是 prompt 层拼接，全仓无 amodal 融合潜向量(grep `amodal|融合潜|onnx 融合` 零命中，onnx 仅用于 STT/VAD 不用于模态融合)。
- **NeuroConscious (EfekanSalman/NeuroConscious)**（cognition）：该项目漂亮架构大半是『未通电的陈列代码』(高级栈在 agent_initializer.py 接线但 base_agent.py 从不调用，引用了不存在的属性，零有效测试，多处真实 bug)。其所有理念 Neo 都已更成熟落地：三类记忆→Neo 有 SQLite+FTS+语义双路召回(MemoryCore.js)；情感→VAD(NoeAffectEngine.js)强于其标量启发式；目标层级→No
- **Steake/GodelOS**（cognition）：两个零件 Neo 状态：①softmax 联盟竞争+coalition_strength 进意识阈值——Neo NoeWorkspace.js:332-333 当前是确定性 argmax(sort+[0])非 softmax+temperature 竞争，确有可升级空间；②calculate_phi min-cut 互信息整合度——Neo 全仓无(同 pyphi)。Neo 的 mind.html/
- **CSC-UW/iit-pseudocode**（cognition）：纯算法规格(单 README 伪代码，已归档只读，停在 IIT 3.0)。Neo 全仓无 Φ/整合度实现(同 pyphi)。真 Φ 在 Neo 真实规模 O(n·5^3n) 不可算，实用上限 10-12 节点。license=null+README 明写『Default copyright laws apply, intended for publication』=保留所有权利非开源，伪代码原文不
- **optillm (algorithmicsuperintelligence/optillm)**（reasoning（8））：理念部分已有：optillm 的 self_consistency/cot_reflection ≈ Neo NoeDeliberation.js:13-21（苏格拉底立论-挑战-修订单次自校验）+ NoeLocalModelCouncil.js:368-486（多模型投票+交叉审）。optillm 核心卖点 best-of-N/mcts/moa 依赖 n 参数采样，Neo 主脑跑 LM Stud
- **search-and-learn (huggingface/search-and-learn)**（reasoning（8））：理念与 OpenR 高度重合：其 Best-of-N+PRM 过程打分+beam/DVTS 与 OpenR(openreasoner/openr)同设计；aggregate_scores 的 min/prod/last 聚合策略=可移植精华，但 Neo 已无此能力且已在 OpenR 词条规划落地。self-consistency(纯 Best-of-N+投票)的多数表决思想=NoeConsensu
- **Agno (agno-agi/agno, 原 Phidata)**（agent）：纯 Python 99.7%+FastAPI 运行时,与 Neo（Node/ESM,package.json type:module）异构,直接进需常驻 Python sidecar，违背本地极简。其三大原语 Neo 均有更贴 owner 的等价物:Team→src/room Squad/Arena/Debate Dispatcher+NoeLocalModelCouncil（27KB）+NoeC
- **VoltAgent (@voltagent/core)**（agent）：TS/MIT 但强绑 Vercel AI SDK v6 全家桶（几十个 @ai-sdk/* 直接依赖+OpenTelemetry+VoltOps 云观测,与 Neo 本地不出网原则冲突）。其核心可借件与本组其他候选重叠:①supervisor-subagent「工具化委派 delegate_task」=OpenAI Agents JS 的 handoff-as-tool 同一缺口（TaskInte
- **node-llama-cpp**（tools）：Neo 已有等价能力且本库架构与 Neo 冲突。Neo 本地脑是 LM Studio(qwen3.6-35b MLX, port 1234)+ollama 两个独立 HTTP server，经 src/room/OpenAICompatChatAdapter.js:79-80 已透传 response_format(json_schema/json_object)；LM Studio 底层就是同款
- **ax (DSPy for TypeScript) — @ax-llm/ax**（tools）：Neo 已有结构化输出闭环(NoeStructuredCall 三档降级+zod 校验、NoeLlmJsonExtractor、response_format 透传、Deliberation/AffectEngine/CommitmentStore)。ax 的 signature+streamingForward 字段级流式校验与 Neo 现有能力重叠；OpenAICompatChatAdapte
- **guidance (guidance-ai)**（tools）：Neo 已有等价落地通道。guidance 是 Python，无 JS/Node/WASM 绑定(llguidance Rust 内核只有 Rust/C/Python 绑定，无 npm 包)；且其 regex/CFG/fast-forward 仅在进程内后端(llama.cpp/transformers 直访 logit)生效，走 OpenAI 兼容 HTTP 时 _openai_base.py
- **instructor-js (@instructor-ai/instructor)**（tools）：与 Instructor(Python 主仓)候选同源，其精华 re-ask 自校验重试闭环已在 Instructor 条目裁为 worth_integrating(借进 src/runtime/NoeStructuredCall.js)。instructor-js 本体停更 17 个月(v1.7.0=2025-01-27)，引擎链 zod-stream/schema-stream 同冻结，pee
- **MindsDB**（mcp）：Neo 记忆/知识检索是本地 SQLite 体系：MemoryCore.js 有 FTS5+语义向量+reciprocalRankFusion 混合召回（src/memory/MemoryCore.js:4 import reciprocalRankFusion、:165 noe_memory_fts MATCH、:288-290 semanticIndex.upsert），NoeKnowledg
- **Tavily MCP**（mcp）：Neo 已有等价且本地优先的搜索能力：WebSearch.js:157-167 search 是 MiniMax→SearXNG→Brave 的 provider 降级链（自托管 SearXNG 免 key、Brave 免费额度、MiniMax 用已有 key），DeepResearcher.js 做多步研究综合。Tavily 的「为 LLM 优化的实时搜索后端」Neo 用 MiniMax/Sea
- **marmelab/molly**（WATCH+SKIP（7）：aura / Exa MCP / XGrammar / open-r1 / marmelab-molly / lm-format-enforcer / Everything-of-Thoughts-XoT）：Molly 唯一有价值的『空闲自语回路』Neo 已远超：src/loop/NoeDriveSystem.js:4 三自主循环（NoeLoop 30s / 内心反刍 15min / 主动陪伴 60s 冷却）；src/loop/InnerMonologue.js:4-14 后台自发反刍，频率由 NoeLoop 空闲 tick 控制（owner 不在/无任务才反刍），且 reflect() 回放情景流（
- **lm-format-enforcer (noamgat/lm-format-enforcer)**（WATCH+SKIP（7）：aura / Exa MCP / XGrammar / open-r1 / marmelab-molly / lm-format-enforcer / Everything-of-Thoughts-XoT）：与 XGrammar 同因架构错配：它要进程内 logits hook，Neo 经 OpenAI 兼容 HTTP 调本地后端（src/room/OpenAICompatChatAdapter.js:54 POST /chat/completions）拿不到 logits 张量、也无 Python 运行时。它提供的结构化约束 Neo 已有同语言等价物：src/runtime/NoeStructure
- **Everything-of-Thoughts-XoT (microsoft/Everything-of-Thoughts-XoT)**（WATCH+SKIP（7）：aura / Exa MCP / XGrammar / open-r1 / marmelab-molly / lm-format-enforcer / Everything-of-Thoughts-XoT）：XoT 核心前提（已知动力学的离散状态机模拟器 + 域内 self-play 训练的策略/价值网）在 Neo 场景根本不存在——Neo 的『思考』是开放域自然语言推理/内省/意识工程，没有离散状态空间、没有模拟器、没有可训练终局奖励。其本体是 Python+PyTorch、只能跑 Game of 24/8-Puzzle/魔方三个手写闭世界谜题（作者自报魔方仅 6% 准确率）。唯一可移植的『先规划骨
