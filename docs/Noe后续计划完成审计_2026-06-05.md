# Noe 后续计划完成审计 — 2026-06-05

审计对象：`docs/Noe上网搜索与移植收尾计划_2026-06-05.md`。

结论：代码侧可执行部分已经补齐，并通过单元/专项验证、cognitive Playwright 验收、live 51835、隔离端口运行态验收和自动语音证据验收；仍有用户显式触发项不能由 Codex 擅自完成，包括打开 Codex/Claude CLI 搜索开关、审批并启动派活任务、提供“图一”和 Obsidian vault/API key。

## 阶段一：AI 联网搜索

| 任务 | 当前状态 | 证据 |
|---|---|---|
| T1.1 验证真实联网能力 | MiniMax Search API 已真搜验证；Codex/Claude CLI 未实际 spawn，遵守 CLAUDE.md 红线，改为默认关闭的显式 opt-in provider | `src/research/WebSearch.js`；真实命令返回 `source=minimax` / `viaModel=MiniMax Search API` / `count=2` |
| T1.2 AISearch provider 抽象 | 完成 | `src/research/AISearch.js`；`tests/unit/ai-search.test.js` |
| T1.3 MiniMax provider 优先 | 完成 | `src/research/WebSearch.js` 优先调用 MiniMax；真实搜索验证通过 |
| T1.4 Codex/Claude 兜底 | 代码完成，运行默认关闭 | `NOE_AI_SEARCH_CODEX_CLI=1` / `NOE_AI_SEARCH_CLAUDE_CLI=1` 才启用；`tests/unit/ai-search.test.js` 覆盖 env-gated CLI runner |
| T1.5 优先级路由透明显示 | 完成 | providerOrder: `MiniMax -> Codex -> Claude -> SearXNG -> Brave`；结果含 `source` / `viaModel` |
| T1.6 DeepResearcher 切换搜索层 | 完成 | `src/server/routes/noe.js` / `src/server/routes/research.js` 使用 `createAISearch()` 注入 DeepResearcher；`tests/unit/routes/research-routes.test.js` 覆盖 deep SSE route |

## 阶段二：前端 / 语音

| 任务 | 当前状态 | 证据 |
|---|---|---|
| T2.1 cognitive 搜索/研究入口 | 完成 | `public/cognitive.html` 加载 `public/src/web/cognitive-research.js`；Playwright 390px 渲染检查无横向溢出；`tests/unit/routes/research-routes.test.js` 覆盖 `/api/noe/research/deep` SSE start/progress/result/done |
| T2.2 语音/文字搜索研究意图 | 完成 | `src/server/routes/noeDo.js`；`src/voice/VoiceSession.js`；`tests/unit/routes/noe-do-routes.test.js` 覆盖 search/deep/delegate intent |
| T2.3 文件操作接前端/语音 | 完成 | `public/cognitive.html` 先走 `/api/noe/do`，支持整理确认、撤销、文件查找、画像 |

## 阶段三：自动化 + 多 AI 协作

| 任务 | 当前状态 | 证据 |
|---|---|---|
| T3.1 SkillExtractor 自动跑 | 完成 | `src/skills/AutoSkillExtractor.js`；`server.js` 房间广播钩子；`tests/unit/auto-skill-extractor.test.js` |
| T3.2 语音派活 -> 创建房间 -> 确认后启动 | 代码链完成，真实启动需用户审批 | `src/room/TaskIntentRouter.js`；`src/server/routes/noeDelegation.js`；`src/autopilot/NoeDelegationAutostart.js`；`tests/unit/noe-delegation-autostart.test.js` |

T3.2 安全语义：`/api/noe/do` 只返回计划；`/api/noe/delegate/confirm` 只创建 idle 房。`autoStart:true` 创建 pending approval、autopilot job、agent run；审批通过后 scheduler 才走预算门并启动。

## 阶段四：Noe 自己动手

| 任务 | 当前状态 | 证据 |
|---|---|---|
| T4.1 ActPipeline 解锁 | 完成 | `src/loop/ActPipeline.js` 支持 registered executor + approved retry；destructive actions 保持 blocked_safety |
| T4.2 写文件 / 跑命令工具 | 完成安全脚手架 | `src/loop/SafeActExecutors.js`；`tests/unit/noe-act-executors.test.js`；`tests/unit/noe-act-pipeline.test.js` |

T4.2 安全语义：真实执行必须 `approvalId + realExecute:true`；写文件拒绝 `.env` / `.git` / `node_modules`；命令执行 argv-only、白名单、timeout、输出截断、密钥环境剥离。

## Noe 动手干活 + 知识库整合计划

审计对象：`docs/Noe动手干活与知识库整合计划_2026-06-05.md`。

| 任务 | 当前状态 | 证据 |
|---|---|---|
| 阶段 0 连接 + 只读看桌面 | 代码侧已完成 | `src/server/routes/mcp.js` 暴露统一 MCP call；`src/server/routes/noeDo.js` 可调 `fs_locate` / `fs_stats` |
| 阶段 1 语音整理桌面闭环 | 代码侧已完成 | `/api/noe/do` 支持整理 plan -> confirm execute -> undo；`public/cognitive.html` 接确认/撤销；`tests/unit/routes/noe-do-routes.test.js` |
| 整理后同步索引 `fs_organize_sync` | 已补 best-effort 钩子 | execute/undo 后尝试 `fs_organize_sync({batch_id,reason})`；工具不存在也不阻断移动/撤销；`tests/unit/routes/noe-do-routes.test.js` 覆盖 success/failure/undo sync |
| 阶段 2 Noe 自己动手 | 安全脚手架完成，真实执行需审批 | `ActPipeline` + `SafeActExecutors`；真实执行必须 approvalId + realExecute |
| 阶段 3 语音派活其他 AI 真执行 | 代码链完成，真实启动需用户审批 | Noe delegate plan/confirm + pending approval + scheduler handler |
| 阶段 4 闭环飞轮 | 代码侧继续补齐 | 文件整理 execute/undo 后同步索引；AutoSkillExtractor 和 Noe memory/skill routes 已接入；LLM Wiki 已有 ingest/lint/query/API，并接入 `/api/noe/do` 本地知识意图；Obsidian MCP 仍需用户 vault/key |

模型级自然语言意图识别仍未默认开启：计划文档写明“现关键词够 MVP”。默认引入 LLM 识别会增加模型调用/配额和延迟，因此当前保持规则识别 + 确认式链路；未来可在用户明确同意后加本地模型 fallback。

## 知识库方法论计划

审计对象：`docs/知识库方法论研究与落地_2026-06-05.md`。

| 任务 | 当前状态 | 证据 |
|---|---|---|
| Karpathy LLM Wiki 结构 | 已落地可用闭环 | `raw/` 4 条研究笔记；`wiki/` 4 个概念页；`templates/`；`npm run wiki:ingest`；`npm run wiki:lint` |
| LLM Wiki raw 质量门 | 已完成 | `wiki:lint` 现在检查 raw 必填字段 `concept/title/decision/priority` 和重复正文 |
| LLM Wiki 查询 | 已落地 | `npm run wiki:query -- Karpathy wiki`；`GET /api/knowledge/llm-wiki/search?q=Karpathy%20wiki`；`/api/noe/do {localWiki:true}` |
| Obsidian MCP | 条件接受，当前未接 | 用户需先安装 Obsidian + Local REST API 插件并提供 API key；`npm run obsidian:mcp:check` 可只读自检；当前不写入 placeholder 密钥或 MCP env |
| Smart Connections / Templater / haiku-ingester | 已完成取舍 | 只借鉴低负担经验；不安装插件、不引入后台 watcher、不搬整套 RAG |

## 阶段五：配置 + 端到端验证

| 任务 | 当前状态 | 说明 |
|---|---|---|
| 重启 51835 panel | 已由用户授权后执行 | `PANEL_RESTART_FORCE_DIRECT=1 PANEL_NO_OPEN=1`，当前 51835 PID 74484；51735 PID 54215 未触碰 |
| SearXNG/Brave 兜底配置 | 可选未配置 | MiniMax 已可用；兜底配置需用户提供 URL/key |
| Obsidian MCP | 可选未接，当前本机不可获取 API | `npm run obsidian:mcp:check` 只读确认：旧 vault 路径不存在，27123/27124 未监听，未注册 Obsidian MCP |
| Obsidian MCP dry-run 配置 | 已完成 | `npm run obsidian:mcp:plan` 只输出脱敏配置模板，不写 key、不注册、不安装插件 |
| 语音端到端自动验收 | 已完成 | `verify:noe:capture-evidence` 生成合成音频，经本地 whisper 转写为“帮我查今天人工智能新闻”，再走 Noe voice text route 返回当天人工智能新闻 |
| 语音 transcript 单测 | 已完成 | `tests/unit/noe-voice-session.test.js` 覆盖搜索、深度研究、派活 transcript；物理麦克风 UI 权限可按需复测 |
| 一条命令总验收 | 已完成 | `npm run verify:noe:full-current` 串起 P0、交接一致性、Wiki、Obsidian readiness、外部条件 readiness、phase5 live、cognitive；`-- --include-managed` 追加隔离服务验证 |
| cognitive 前端脚本化验收 | 已完成 | `npm run verify:cognitive` 在 390x844 和 1280x800 下验证入口按钮、无横向溢出、本地 Wiki 按钮可返回内容 |
| 派活 confirm managed 验收 | 已完成 | `npm run verify:noe:phase5 -- --managed` 会在隔离 HOME 创建临时 idle room，确认 started=false/queued=false |

新增运行态验收脚本：`npm run verify:noe:phase5`；可加 `-- --managed` 在随机非保留端口启动临时 Noe 服务做隔离验收。

默认 live 模式只验证当前已运行的 51835，不会重启 panel、创建房间、审批 job 或 spawn Codex/Claude CLI。managed 模式会用临时 HOME、临时 SQLite DB、随机非 51735/51835 端口启动当前工作树的 `server.js`，完成后关停并清理。检查项包括 owner-token、健康检查、研究状态 provider 顺序、`/api/noe/do` 搜索意图、`/api/noe/do` 派活计划、`cognitive.html` 搜索入口、LLM Wiki 查询入口。

当前 live 运行结果：通过，最新报告 `output/noe-phase5-runtime/phase5-runtime-1780642502391.json`。结果：`mode=live`、`baseUrl=http://127.0.0.1:51835`、`passed=9`、`failed=0`；`/api/noe/do` 搜索意图返回 `source=minimax` / `viaModel=MiniMax Search API`；LLM Wiki search 和 `/api/noe/do` 本地知识意图均命中 `Karpathy LLM Wiki Pattern`。

当前 managed 运行结果：通过，最新报告 `output/noe-phase5-runtime/phase5-runtime-1780643593950.json`。结果：`mode=managed`、`passed=10`、`failed=0`。报告已脱敏，未包含 owner token；临时端口已释放。

Codex/Claude CLI 搜索 fallback 已在用户授权后做真实 smoke：

- Codex：`NOE_AI_SEARCH_CODEX_CLI=1`，返回 `count=1`、`source=codex`、`hasUrl=true`。
- Claude：`NOE_AI_SEARCH_CLAUDE_CLI=1`，返回 `count=1`、`source=claude`、`hasUrl=true`。
- 两者默认仍关闭，只有显式 env opt-in 才会 spawn。

## 最新验证命令

```bash
npm run test:p0:unit
# 13 files, 58/58 passed

node scripts/ensure-node22.mjs --require-22 --exec node_modules/vitest/vitest.mjs run \
  tests/unit/ai-search.test.js \
  tests/unit/research-websearch.test.js \
  tests/unit/hwfit.test.js \
  tests/unit/skill-extractor.test.js \
  tests/unit/auto-skill-extractor.test.js \
  tests/unit/task-intent-router.test.js \
  tests/unit/noe-delegation-autostart.test.js \
  tests/unit/routes/research-routes.test.js \
  tests/unit/routes/noe-delegation-routes.test.js \
  tests/unit/routes/noe-do-routes.test.js \
  tests/unit/noe-voice-session.test.js \
  tests/unit/noe-act-executors.test.js \
  tests/unit/noe-act-pipeline.test.js \
  tests/unit/noe-act-pipeline-safety.test.js \
  tests/unit/noe-act-pipeline-failure-branches.test.js \
  tests/unit/routes/noe-act-routes-status.test.js
# 16 files, 80/80 passed

npm test -- tests/unit/obsidian-mcp-readiness.test.js tests/unit/noe-external-readiness.test.js tests/unit/llm-wiki.test.js tests/unit/routes/knowledge-evidence-routes.test.js tests/unit/routes/noe-do-routes.test.js
# 5 files, 27/27 passed

npm run wiki:ingest
# rawCount=4, pageCount=4

npm run wiki:lint
# ok=true, checked=4, issues=[]

npm run wiki:query -- Karpathy wiki
# first hit: Karpathy LLM Wiki Pattern

npm run obsidian:mcp:check
# read-only check：当前不通过；缺真实 vault / Local REST API listener / Obsidian MCP 注册

npm run obsidian:mcp:plan
# dry-run 输出内置 MCP 优先配置模板；不写入真实 MCP 配置

npm run verify:cognitive
# Playwright 验收：mobile + desktop 入口、无横向溢出、本地 Wiki 按钮通过

npm run verify:noe:phase5
# live 51835 运行态验收：9/9 passed，报告 output/noe-phase5-runtime/phase5-runtime-1780642502391.json

npm run verify:noe:phase5 -- --managed
# 隔离端口运行态验收：10/10 passed，报告 output/noe-phase5-runtime/phase5-runtime-1780643593950.json

npm run verify:noe:capture-evidence -- --voice-text "帮我查今天人工智能新闻"
# 自动语音证据与隔离派活确认通过，报告 output/noe-external-evidence/capture-report-1780645123943.json

npm run verify:noe:full-current -- --include-managed
# 总验收通过，报告 output/noe-full-current/full-current-1780645267425.json；Obsidian/external readiness 为 external_blocked
```

## 当前仍需用户侧动作的项

- Obsidian MCP：本机当前没有可用 Obsidian vault / Local REST API plugin / API key；需要用户先在 Obsidian 里准备好 vault 和插件。
- 审批 Noe 派活启动。
- 物理麦克风 UI 权限如需可再复测；自动语音链已过。

这些项不是当前代码缺口，而是外部应用或真实用户动作。默认状态保持不烧配额、不启动子 CLI。
