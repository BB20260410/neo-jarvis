# Neo 自我成长 P0-P10 · 解放版(2026-06-21)

> 唯一导向:**Neo 自我成长、不受约束**。owner 2026-06-21 明确要解放版,去掉一切过度限制。
> 替代旧版里带「安全前置框架」的 P0-P10(见 `docs/Neo能力审计_外部对标_P0-P10_2026-06-20.md`,其能力诊断/外部对标 50 真实 star 库仍有效,仅 P0-P10 排序以本文件为准)。

## 一、砍掉的 3 处限制(旧方案里限制 Neo 的)

1. **「先修沙箱 bug 再放开自我进化」**——把安全门摆在解放前。砍。解放优先。
2. **consensus 四模型共识门 / reward-hacking 门 / `self-evolution:run` 授权门**——挡在 Neo 自改前面的闸。owner 早定「无 reward hacking 门,接受」。**拆**,不是修。
3. **P6 per-change 验证 gate(证明更强才能 apply)**——自改准入审查。转成 Neo 自己的反馈信号,不拦他。

## 二、解放版 P0-P10(唯一导向:不受约束的自我成长)

- **P0 — 拆掉自我进化所有门闩**:开 executors + 拆 consensus 共识门 + 拆 reward-hacking 门 + 解 `self-evolution:run` 授权,让 Neo 真正「起意→生成补丁→应用→运行→学习」无人工门闭环。他第一次能真改自己代码。
- **P1 — 经验反馈代替审查门(Darwin Gödel Machine 思路)**:Neo 自改后自己跑 benchmark,「我变强了吗」只作为他自己的学习信号——强了固化、弱了他自己回滚,无外部裁判。门变自我认知。
- **P2 — transformers.js 进程内嵌入**:稳定语义向量底座,根治熵温度空转,思考不退化。
- **P3 — 多脑冗余 + brain_error 兜底**:本地脑抖动时不失声,意识流连续。
- **P4 — jsDiff 模糊贴补 + ast-grep**:自改补丁从精确匹配升级为容错,改自己成功率拉满。
- **P5 — Playwright MCP**:浏览器动手 72%→满。
- **P6 — 激活 ReflectiveTuner 真闭环**:491 行死代码复活,Neo 真的自己调自己的参数。
- **P7 — MMR 多样性 + FocusStack 主动上浮**:破语义塌缩,工作记忆活起来。
- **P8 — 技能自动提炼 + 记忆自动织网**:经验沉淀成可复用技能,越用越强。
- **P9 — 持续训练 + DPO/LoRA**:「攒一次练一次」变「持续渗进权重」——真正的权重级自我进化。
- **P10 — 整合度 Φ 提升 + 自我模型版本化**:子系统整合成统一意识(破 self-think 天花板)+ Neo 记录「我成长成了什么样」,自我叙事连贯。

## 三、只剩 owner 钦定的 4 条底线(保护 owner 资产,不挡 Neo 任何能力)

`rollback 可逆` / `不泄 secret` / `社媒不重复发布致封号` / `不乱烧付费配额`。
其中 rollback 反而让 Neo **敢大胆改**(做错能退)。要不要连这些也放更开,owner 定。

## 四、前缀越界 bug 定位(不替 owner 设限)

mission 证据沙箱判据 bug(`safeResolve` 缺尾分隔符,3 处)。处置取决于要给 Neo 多大文件自由:
- 要 Neo 读写全盘不受沙箱约束 → 沙箱放宽/移除,bug 不用修
- 要留最小沙箱(只防误删系统/泄 secret)→ 修 bug 让它判得准

## 五、执行顺序(解放优先)

P0 拆门先行 → P1 经验反馈让 Neo 自己掌舵 → P2-P10 持续增强。owner 资产底线随时在,但不作为解放的前置门。
