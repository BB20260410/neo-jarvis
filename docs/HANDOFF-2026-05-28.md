# Panel 项目交接文档(2026-05-28)

> **下个 Claude Code 窗口**:打开 panel 项目后,先读完这份再开工。
> **本会话耗时**:整天,57 个 commit 推到 main(从 `c350f0c` 之前的状态算起)。

---

## 🎯 你是谁:项目身份(看完确认对齐)

- **项目名**:**Panel(Noe v2.1.0+)**
- **路径**:`/Users/hxx/Desktop/00_项目/05_Claude可视化面板`
- **类型**:Electron + Node 桌面应用,**本地化多 AI 协作工作台**
- **branch**:`codex/paperclip-local-governance`(memory `noe-is-panel-project` 印证)
- **server**:`npm start`(生产)或 `npm run dev`(开发,带 --watch)
- **端口**:`127.0.0.1:51835`(memory `panel-project-location`)

**⚠️ 绝对不要混淆**:
- `/Users/hxx/Desktop/00_项目/07_Ruflo工作台` 是 Godot 游戏项目(不是 panel)
- 「易航翻译」是另一个 macOS App 项目(不是 panel)
- 用户当前工作目录可能显示 `07_Ruflo工作台`(环境变量历史遗留),但**真正在做的是 panel**

---

## 📜 本会话核心成果(57 commit 概览)

### 第一段:接续上轮 panel v2.0/2.1 收尾(早期 commit)
- v2.0.0 发布闭环、CHANGELOG、签名打包
- 报告生成 / UX 修复 / 安全补端点

### 第二段:Squad 模式 prompt 工程深化
| Commit | 内容 |
|---|---|
| `c6e07aa` | Squad 协作产出自动入证据知识库(KC) |
| `bea2865` | KC 命中 squad_* 直接跳对应房间 |
| `dbbbeda` | **Codex MCP 桥**:codex CLI 透传 panel MCP 工具(via `--profile-v2`),与 claude CLI 对齐 |
| `d6156c5` | **Dev 文件落地验证 prompt**:防 codex 多步报告 ≠ 实际幻觉,实测 T1 迭代 3→1 -67% |
| `ca69853` | PM outOfScope 字段 + fingerprint stable sort + codex 快档 model(gpt-5-mini/gpt-4o) |
| `ed29b48` | squad_done toast 显产物 cwd + 自动复制剪贴板 |

### 第三段:持久化架构 SIGKILL-safe 四层防护
| Commit | 层 | 应对 |
|---|---|---|
| `4099cef` | 防护 | PANEL_DB_PATH env 隔离 + test:e2e 默认 managed(防 e2e 污染真实 panel.db) |
| `3274c51` | L2 | squad `_runOneTaskUntilTerminal` 5 个关键节点显式持久化 |
| `b96baa4` | 性能 | ChatRoomStore active/archive 拆分文件 + fingerprint skip 写盘 |
| `5f7392e` | 测试 | SIGKILL-safe 集成单测覆盖整个 abort 路径 |
| `ab093be` | L3 | `_persistTaskList` 调 `flush()` bypass 250ms debounce |
| `1961706` | L4 | `_writeSplit` 加 `fsyncSync(fd)` + 目录 fsync,强制 page cache→disk |

→ **持久化栈完整**:SIGTERM(graceful) / SIGKILL(kill -9) / 断电 / kernel panic 都不丢中间状态。

### 第四段:**cross_verify 新模式**(用户最终原始需求 ← 重点!)

用户原话:「codex+claude 协作开发游戏/软件,在一个项目里互相完善去开发,**一起工作不要乱,每个任务要互相印证答案,一致后才进行下一步**」

| Commit | 内容 |
|---|---|
| `762828a` | 新建 `CrossVerifyDispatcher.js`,**A 方案:互相显式签字**才进下一步 |
| `3a000a1` | 全面测试 + 3 处 bug 修复(task.status / taskList 写时机 / UI 缺映射) |
| `75a4d2e` | UI 入口完成(欢迎页 CTA + 顶栏按钮 + 8 个 WS 事件 toast) |

**模式逻辑**(每个 task):
1. claude 和 codex **并行各写一版** Plan A / Plan B
2. **并行互审**:claude 看 planB 给 JSON ack,codex 看 planA 给 JSON ack
3. **双方 agree=true** → 一致达成 → 下个 task
4. **任一方 agree=false** → round 2 各自基于对方反馈修订
5. **3 round 仍不一致** → escalated 等用户裁定

**实测数据**:
- 斐波那契任务:**75 秒 1 round 一致**,双方真跑了对方代码验证
- 贪吃蛇(单 task):snake.py 94 行 py_compile OK(过程中 abort+resume 验证)

---

## 📐 当前架构地图

### 4+1 协作模式
| Mode | 角色 | 适用 |
|---|---|---|
| `chat` | 1 个 AI 1v1 | 简单聊天 |
| `debate` | 多轮提案+互评 | 头脑风暴 |
| `squad` | PM/Dev/QA 层级 | 项目落地(单向 boss-worker) |
| `arena` | 多 AI 背对背 + Judge 联网 | 事实验证 |
| **`cross_verify`** | **2 对等成员** | **双向互验,新做** |

### 8 个 Adapter
- CLI: claude / codex / gemini-cli / CCR
- HTTP: OpenAICompat / MiniMax / Ollama / GeminiChat

### 持久化栈
- **SQLite**(WAL,fsync 自动):Approval / Budget / AgentRun / Delegation / EvidenceKnowledge / GovernanceQueue / AutopilotSchedule
- **JSON 文件**:ChatRoomStore(rooms.json + rooms-archive.json 拆分 + fsync)/ McpStore / Webhook / Templates / AutopilotStore / Archive / AgentPolicy

### 代码规模
- src/ JS: 30,686 行 · server.js: 4,456 行 · public/app.js: 12,145 行
- 测试:73 文件 / 412 单测 / 144 e2e

---

## 🎮 cross_verify 模式怎么用(给下一窗口)

### A. 浏览器(用户已能用)
1. 启 panel:`cd /Users/hxx/Desktop/00_项目/05_Claude可视化面板 && npm start`
2. 打开 http://127.0.0.1:51835
3. 点「＋ 双模型互验」按钮 → 输房名 → 填 topic + cwd → 启动
4. UI 实时 toast:`cv_round_start` / `cv_propose_done` / `cv_review_done` / `cv_consensus` / `cv_escalated` / `cross_verify_done`

### B. HTTP API
```bash
TOKEN=$(cat ~/.noe-panel/owner-token.txt)
curl -X POST http://127.0.0.1:51835/api/rooms/quick \
  -H "X-Panel-Owner-Token: $TOKEN" -H "Content-Type: application/json" \
  -d '{"mode":"cross_verify","topic":"任务描述","cwd":"/tmp/xxx","startNow":true}'
```

### C. 关键源文件
- `src/room/CrossVerifyDispatcher.js`(~250 行)— 核心逻辑
- `tests/unit/cross-verify-dispatcher.test.js`(9 单测)
- `server.js` 注册 + 路由 5 处分支
- `public/app.js` UI 入口 + 8 个 WS 事件
- `public/index.html` 2 个按钮(欢迎页 CTA + 房间区顶栏)

---

## ⚠️ 必须记住的 4 个坑

1. **`npm run dev` 杀正在跑的 squad/cv**(`node --watch` 改文件硬重启)→ 实测用 `npm start`,memory `panel-dev-watch-kills-squad`
2. **e2e 曾污染 panel.db 预算策略** → 已修(test:e2e 默认 managed + PANEL_DB_PATH env),memory `panel-e2e-pollutes-budget-policies`
3. **panel.db 12.1MB**(active 房多,正常)+ **rooms.json 12.6MB / rooms-archive.json 190KB**(已拆,fingerprint+fsync)
4. **better-sqlite3 NODE_MODULE_VERSION 不匹配** → `npm rebuild better-sqlite3`(本机 Node 22),memory `panel-better-sqlite3-rebuild`

---

## 🎯 用户偏好(必须遵守)

- **中文回复**(memory 全局 CLAUDE.md)
- **自主推进不打断**,红线之外灰色地带自己做
- **默认 codex+claude**:所有协作模式优先用这对(memory `squad-default-codex-claude`)
- **不要主动推上架**(memory `panel-no-uplisting` 易航有同款,panel 也类似偏好)
- **危险操作只确认一次**,用户在含警告选项里明确选了直接做
- **不要把密码/凭据存进 memory**

---

## 📋 下一窗口可能继续的方向(用户选)

按用户最近"全做" / "依次实施"模式风格,如果用户说"继续 cross_verify",可考虑:

### A. cross_verify 深化(直接接续本会话)
1. **多 task 支持** — 目前 cv 只把整个 topic 作为单 task,可加 PM 拆解(类似 squad 第 6 步)
2. **rounds[] 在 UI 专门渲染**(目前用 squad UI 默认渲染,planA/planB/ack 没专门 view)
3. **arena/debate 也加 SIGKILL-safe**(squad 已修,cv 已修,其他可对齐)

### B. 用 cross_verify 做真任务(dogfooding)
1. 用 cv 模式修 panel 自己的某个 issue(任意挑)
2. 用 cv 做一个简单的开源工具(用户挑领域)

### C. 体验打磨(本会话提过但未做)
1. Squad/cv 任务 level 进度卡(当前 task / 阶段 / 已迭代次数 / 估时)
2. 中间产物 diff 预览(panel 内看 Dev 写的代码,不切 IDE)
3. Codex 默认 model 用户偏好(panel 记一次,所有房默认)

---

## 🗂 关键 memory(已在 MEMORY.md 索引)

新窗口启动时 MEMORY.md 会自动注入,关键条目:

- `panel-project-location` — 项目路径
- `noe-is-panel-project` — 别名 + 分支 + e2e 命令
- `squad-default-codex-claude` — 用户偏好(本会话加)
- `panel-dev-watch-kills-squad` — 避坑(本会话加)
- `panel-e2e-pollutes-budget-policies` — 已修(本会话加)
- `panel-next-improvements-2026-05-28` — P0/P1/P2 计划(本会话加,部分已实施)
- `panel-no-break-existing-on-edit` — Edit 注意
- `confirmation-once-not-thrice` — 不要重复确认
- `dont-instruct-user` — 自己做不指挥用户
- `single-viable-path-no-multiple-choice` — 唯一可行别做多选题

---

## 🟢 跨步门禁(交接时状态)

| 检查 | 状态 |
|---|---|
| ESLint(`max-warnings 0`) | ✅ 净 |
| Unit Test | ✅ **412/412** |
| e2e:managed | ✅ **144/144** |
| `git diff --check` | ✅ 净 |
| 工作树 | ✅ clean |
| panel server | 跑在 51835(PID 69856,可能已被 --watch 重启换 PID) |
| 最近 commit | `75a4d2e UI 入口:cross_verify 双模型互验加 5 处 UI 接入,浏览器一点就启` |

---

## 📞 怎么交接给下个窗口

**步骤**:
1. 关闭本会话(Ctrl+D 或 /exit)
2. 新开 Claude Code 窗口,cd 到 panel:`cd /Users/hxx/Desktop/00_项目/05_Claude可视化面板`
3. 给新 Claude 第一句话(任选一种):

   **简版**(推荐):
   > 读 docs/HANDOFF-2026-05-28.md 接手,然后等我下达任务。

   **详版**(如果用户想立即让新 Claude 接着做):
   > 读 docs/HANDOFF-2026-05-28.md。我接下来想 [continue cross_verify 多 task 支持 / 用 cv 做某个真项目 / 修某个 bug],按这份交接对齐再开工。

4. 新 Claude 会读这份 + memory MEMORY.md 自动注入,获得完整上下文。

**备选(全自动)**:用户什么都不说,新 Claude 会从 memory `panel-next-improvements-2026-05-28` 自动看到 P0/P1/P2 计划继续推进。
