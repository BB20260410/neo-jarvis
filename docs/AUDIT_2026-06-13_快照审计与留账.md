# 审计与留账 2026-06-13 · 多会话快照

## 背景

某并行全自动会话(`claude --dangerously-skip-permissions`)在 `0b565b4..becb3b0` 提交 7 个 commit / 46 文件(记忆相关性基准、目标系统拆分、提案箱+开机自检、Mind 世界可视化、观察门续跑、交接文档)。本会话对该快照做并行深度审计(6 审查 agent + adversarial 复核),并修复确认的真问题。

## 审计结论

- 0 个 high;7 个确认真问题(复核后);5 个误报被证据驳回。
- 全量 `npm test`:524 文件 / 3937 测全绿。
- `goal`(目标系统拆分)线判定 **healthy**:迁移完整、接线正确、测试验真实行为。

被驳回的误报(留证,勿据其改):
- observation「daysRemaining 误导渲染」——实际有 `禁止启动P9/R` 后缀护栏 + status=blocked,review 引用证据被截断误读。
- cross「NoeBootSelfCheck 552 行=新违规」——500 行是软约定(`以 wc -l 现场实测为准`),全仓 28 个 500+ 文件;且其修复建议指向基线既有逻辑、点名的标识符不存在。
- memory「真实库基准 1/2 被 advisory 掩盖」——advisory/required 分层是设计文档明文语义,且其证据把失败 case 张冠李戴(真正未达标的是 `extractor_fact_marker`,非它说的 `voice_note_user_scope_episode`)。

## 已修(commit 22cbd41)

| 工作线 | 修复 |
|---|---|
| 记忆基准 | isolated 自测模式不再写入未读取的 `caseFile`(消除虚假溯源证据) |
| 提案箱 | `self_model` 源对齐 `boot_self_check` 的 `latest.json` 特判,从源头消除收件箱重复堆积/重复物化 |
| Mind 世界 | 删除背景漂移失效后遗留的整套死代码(`_buildBackgroundLayer`/`setBackgroundMode`/`_updateBackgroundMotion`/`backgroundMaterials`/`SPACE_BACKGROUND`),背景仍由 `scene.background` 静态接管,渲染不变 |
| 提案 UI | `self_model_diff` 隐藏误导性通用 Materialize(仅入队不生效),引导走专属 dry-run/Apply self-model |
| 测试 | mind-world 背景断言改指活路径 `scene.background` |
| 加固 | `.gitignore` 补 `.playwright-cli`(防 playwright 调试日志含浏览器 console 误入库) |

## 留账(low / 既有债,本批未改,供后续按节奏消化)

记忆线:
- `verify:noe:memory-relevance-benchmark` 门 CLI 段回落 self-test(设计自洽,可选加 `--isolated` 表意,与 `memory-retrieval-sample` 对齐)。
- `hasLiteralAnchor` 漏判纯 hex 短 commit 哈希(如 `8fb8830`/7 位),literal 抑制对这类查询失效。
- `latestJsonReport` 按文件名字符串排序,时间戳 13→14 位时选错 latest(现实公元 5138 年才触发)。
- 真实库基准当前 `extractor_fact_marker` 在 literal-anchor 查询上 unlabeled 超阈(advisory 已知缺口,待召回调参;可在设计文档 advisory 清单补记)。

提案/自检线:
- `boot_self_check` 纯诊断项(`inspect_*`)被无差别映射进可物化的 `manual_repair` 类型(1/3 噪音;物化需 owner 双确认,无害但语义不当)。建议按 id 前缀排除诊断/重启类。
- 前端 proposal inbox 无 `NOE_*` env 门控、多文件缺 `// @ts-check`、`NoeBootSelfCheck.js` 552 行超限——均 f01b346 既有债。

Mind 世界线:
- sprite 卫星 `noeSpin` 设在 sprite 而非返回的 group,自转对 Sprite 无效(应改 material/缩放动画或显式不做)。
- `noe-mind-world-ui.test.js` 仍是 `readFileSync+toContain` 字符串体检;完整行为测试需把 `satelliteSpriteCell`/`orbitToVec3`/几何换算抽为可 import 的纯函数加真值断言(大重构,留账)。
- `noe-world-earth.js` 1244 行超 500(既有债)。

观察门线:
- `NoeWorkMapSnapshot.js` 536 行超 500;`scripts/noe-observation-status.mjs` 的 `evidenceRefs` 两处构造不一致(compactP8 无条件 vs buildP8DailyObservation 有条件)。

横向:
- `noeProposals.js` 新 endpoint 自造 `resolveRootRef` 未复用标准 `safeResolveFsPath`,且含恒为 false 的冗余条件 `resolve(rel) === rel`。
- `registerNoeProposalRoutes` 的 `selfModelDir` 为死参数(noe.js 注册未传,回退默认目录)。
- lint warning(本次 pre-commit 报,0 error):`mind.js:1123 hotspots`、`NoeProposalInbox.js:349 reportMtimeMs` 未用变量(既有)。

## 备注

- 本批修复未 push(分支 `noe-main` ahead `origin/noe-main`)。
- 全仓 28 个 500+ 行文件的拆分应作为统一规划,不单独归咎某 commit。
