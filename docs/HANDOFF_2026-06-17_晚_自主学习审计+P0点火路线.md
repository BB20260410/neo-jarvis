# HANDOFF 2026-06-17 晚 — 自主学习/主动执行审计 + P0 点火路线

> 接手先读本文件 + `CLAUDE.md`。本轮跨度大：abliterated 退役 → P1/P2 三方验证 serious 清零 → **Neo 自主学习/主动执行能力审计（B- 评级）** → P0-1 think 末步真深思修复。下窗口主线 = 接着审计的 P0 路线做 P0-2/P0-3 + owner 点火验证 + workflow 印证「直到真正解决」。

## 0. 一句话状态
- git 工作区**干净**，noe-main 本轮 13 commit（`845bfbe`→`c761637`），全量 **658 文件 5147 测全绿**。owner 惯例不 push。
- 生产 51835 跑的是**改动前**的代码（本轮改动未重启生效）；P0-1 等待 owner 隔离端口首验后点火。
- ⭐ **续报见 §6**（2026-06-17 深夜）：在本 §0 基线后又续 6 commit（多模型安全方案步骤1-5，`fe5e7f3`→`08b3df8`），全量 **660 文件 5165 测全绿**；双轮多模型对抗审查已闭环；步骤5 决定性判 FAILED 机制就绪但**生产恒不触发**（待 commitment→ledger 桥接，要 owner 拍板）。

## 1. 本轮四大块成果

### 1.1 abliterated 退役 + 前台 highspeed（owner 2026-06-17 指令，3 commit）
- `845bfbe` 前台闲聊 → `MiniMax-M2.7-highspeed`；`91c2a7c` 去本地 abliterated 残留 fallback，后台认知退 lmstudio 主脑；`b672b44` 显示端点兜底去 abliterated。
- ollama 层已 `ollama stop` abliterated 9b/4b（释放 27GB GPU，磁盘文件保留可回滚）；reload 后行为级验证 abliterated 不再被自动重载。
- 后台认知 fallback = lmstudio（model=`NOE_MAIN_BRAIN_MODEL`，主脑变它跟着变）。

### 1.2 P1/P2 觉醒路线三方验证 — serious 清零（5 batch commit）
> owner /goal：workflow 用 M3+codex+Claude4.8 全面检查 P1/P2，serious 清零才进 P3。
- 三轮 workflow 对抗审查，**serious 收敛 4→2→1→0**，每轮验证上轮修复零复现：
  - `55a4595` P1 第一批 6 问题（embed 信号泄漏 serious 等）
  - `82a3305` P2 第一批 3 问题（D4 维度丢失/brierAll 口径/source 容错）
  - `c3357e2` 第二批 8 问题（AUTONOMY 正则过宽 serious + 浏览器默认路径 serious + 6 minor）
  - `e836fd4` 第三批 serious 清零（NON_CORRECTION_RE 误杀真纠正）+ 3 minor
- 共处理 **25 真问题 + 1 误报主动撤销（P1-D，node 复现证 add 层已防）+ 1 audit pattern 适配**。
- **残留 2 窄边界 minor**（owner 拍板 serious 清零即收口，以后遇到再修）：① followup 无主题 token（同窗无关取消词误结算单条，已有 freshness 兜底挡过老）② monologue7d cutoff 未走 detectTsUnitMs（秒级 events 库 idle_rumination 误报，生产毫秒库不触发）。

### 1.3 ⭐ Neo 自主学习/主动执行能力审计（owner 明确 workflow 任务）
> workflow `neo-autonomy-learning-audit`：红队怀疑 + 代码实证双立场 × Claude 4.8 主控 × 5 维度。主控**亲查 live panel.db（740MB）+ 实跑测试**独立核验（5 个立场 agent 遇 ECONNRESET 失败，主控全成功，可信度反而更高）。

**整体评级：部分达标 / B-（5 维：1 达标·4 部分达标）**
**一句话：动手是真的，学习是「真跑·真读·学不进行为」的勤奋空转。**

| 维度 | 判定 | 核心实证 |
|---|---|---|
| D1 定义自洽性 | 🟡部分达标 | 自主起意/读取/写入真（self_learning done 219、今日仍每 ~15min 自发立项）；但内化-回写断裂 |
| D2 实现有效性 | 🟡部分达标 | 全链有实现+反向 probe 测试，但 surprise 闭环**生产从未端到端跑通一次**（wired-but-never-fired） |
| **D3 主动执行** | 🟢**达标** | 四段决策链全活体闭环，**2085 条真实非 dryRun 动作·99.8% 自发起·~4.8% 真失败率**；唯「自改代码」已具备已测·生产 0 次真跑 |
| D4 浏览器学习价值 | 🟡部分达标 | read_body 经 JXA 真取正文（254 条差异化）远超「仅打开」；但内化机制 world_model_conflict **产出=0** |
| D5 类爬虫解析 | 🟡部分达标 | 真读 SPA 首屏渲染后 DOM（星数随时刻变化为证）；但非全量（顶层/8000字截断/无 iframe/滚动） |

**那道唯一的裂缝（4 个学习维度全卡这里）——起意真·上网真·读取真·写入真，唯独"读进来的东西真回流改了行为"从未被任何生产证据坐实：**
1. think「写改进方案」步**生产 217/217=100% 被 `autoCloseTerminalThinkStep` 模板盖章，深思脑 0 次真调用**
2. 蒸馏卡 **97.9% 写入即死**（skill_distill 535 张 hit_count=0，自主学习专属 270 张总召回=1）
3. 好奇回路**供给端饿死**（judge 97.6% 判 UNKNOWN→surprise=0）+ 写入端 `NOE_LEARNING_HOOK` 默认 OFF
4. 6 主题永久轮转（distinct title 恒 6，`topicCurator.getNextTopic` 调用点不喂 dynamicConcepts）

> 完整审计裁决（每维度实证+红队+逐条核验+改进方案）原在 workflow output `/private/tmp/.../wp6224koz.output`（临时目录，可能已清）；核心结论 + 路径已落本文件 §2。

### 1.4 P0-1 think 末步真深思（commit `c761637`，本轮最后一步）
- **治审计 D1/D2 最硬根因**（think 100% 模板·深思脑 0 调用）。
- 机制定位：`NoeWorkspace.js:577` `wantsDeep = ...&&!deterministicClosure` —— autoClose(575) 一盖章就**短路 deliberate(578) 真深思**。
- 修复：`autoCloseTerminalThinkStep` 加 `NOE_THINK_DELIBERATE` 开关（默认 OFF）。ON 时 think 末步先以前序 research+act 证据调本地深思脑产【具体改进方案】（learningHook 范式：我原以为什么·实际是什么·下次怎么调整·含具体对象/数字，否则 SKIP），失败/SKIP 回退模板。**异步 fire-and-forget + 防重入 Set + fail-open（think 末步绝不卡死）**。
- `server.js` 注入 `noeDeliberateThink`（走 `noeDeliberateBrainId` 本地白名单脑，永不烧付费）。
- OFF 逐字零回归（`buildAutoCloseNote` 保留原"自动收口"措辞）；新增 ON 产改进方案/SKIP 回退 2 测试。

## 2. 下窗口主线：P0 点火路线（owner 指令「改完后 workflow 印证，直到真正解决」）

### P0-1（✅ 代码完成，待点火验证）
- **下一步**：owner 在场隔离端口 `PORT=51999 NOE_THINK_DELIBERATE=1 npm start`，跑出几个 self_learning 目标到 think 末步，查 DB 抽样。
- **验收**：think 步「自动收口模板」占比从 100% 降至 ≤20%，非模板 think note 均长 ≥80 字、含 ≥1 个来自本轮 research/rg 的具体名词/数字（脚本正则校验非套话）。

### P0-2（点火 NOE_LEARNING_HOOK，要 owner 在场）
- 代码全在（`NoeLearningHook.js` 阶段1 已落地，56 测绿），卡点 = `.env` 缺 `NOE_LEARNING_HOOK=1`（surprise→lesson→memory 写入端默认 OFF）。
- 注：`NOE_EXPECT_LOOSEN_FAIL=1` 已生效、生产已出 3 条 outcome=0，**落空判定环已活**，缺的是下游写入端点火。
- **验收**：隔离端口 ≥1h 后 `noe_memory` 出 `source_type='surprise_lesson'` ≥3 且 persisted=true（`memory.get(memId)` 命中）；对应 `noe_goals` source='surprise' status='done' ≥3。

### P0-3（治写入即死，**根因已分析，未改**）
- **根因（本窗口 grep 定位）**：`NoeDeliberation.js:90` 深思前 `recallFused({query, projectId, limit:3})` 召回**不按 sourceType 纳入 skill_distill 卡**——通用召回够不到蒸馏卡（或被 limit 3 挤掉），故 hit_count 恒 0。蒸馏卡写入：`server.js:1559 distillSkill`→`source_type='skill_distill'`。
- **改进方向**（审计建议）：act 步/深思前 `memory.recall({query:本步主题, sourceType:['skill_distill','surprise_lesson'], bumpHits:true})` 把同主题 lesson/技能卡注入执行上下文；或聊天主链 RAG 纳入 skill_distill 池。`MemoryCore.recall(561)` 已支持 bumpHits（默认 true，578 行）+ sourceType 字段（207），需在召回处加 sourceType 过滤 + 注入点。
- **验收**：接通 ≥24h 后 skill_distill hit_count>0 占比从 2.1% 升至 ≥30%、自主学习专属卡总召回从 1 升至 ≥50，人工核 10 条命中语义相关非误召回。

### P0 收尾后：owner 要 workflow 多模型（M3+codex+Claude4.8）印证
- 复用 workflow 脚本 `neo-autonomy-learning-audit-wf_387ae7c7-b8d.js`（在 session workflows/scripts 目录），或针对 P0 修复点新写聚焦印证脚本。
- **印证目标**：隔离端口点火 P0 三开关后真跑，验证「think 深思真产改进方案 / surprise_lesson 真写入 persisted / 蒸馏卡真被召回 hit_count>0」——即那道「学不进行为」裂缝真点着。没解决就继续改。
- ⚠️ **网络抖动**：本轮 4 次 workflow 都有 ECONNRESET（codex/M3 立场 agent 间歇失败），但 Claude 主控/裁决独立核验兜底。印证时 codex/M3 失败可忽略，看 Claude 主控结论。

### P1/P2 深度优化（审计路径，P0 之后）
- P1：放宽判证器固化落空回退（decisiveUnknownRate 0.976→≤0.7）/ 治 6 主题轮转（喂 dynamicConcepts）/ 点火 NOE_WORLDMODEL_CONFLICT 打开浏览器内化闸 / surprise_lesson 加行为验收。
- P2：蒸馏死卡语义去重+GC / 浏览器结构化抽取（链接/JSON-LD/表格）/ 穿透 iframe+shadow DOM+分块续读+滚动 / 7 处文案如实化 / 激活自改代码上限（生产 0 次真跑）。

## 3. 新增 env 开关（全默认 OFF，待 owner 点火）
| flag | 作用 | 点火验收 |
|---|---|---|
| `NOE_THINK_DELIBERATE` | P0-1：think 末步调深思脑产真改进方案（非模板） | 隔离端口模板占比 100%→≤20% |
| `NOE_LEARNING_HOOK` | P0-2：surprise→lesson→memory 写入端（代码已全，仅 .env 缺） | surprise_lesson 记忆 ≥3 persisted=true |
| `NOE_WORLDMODEL_CONFLICT` | P1：浏览器读到内容与认知矛盾→world_model_conflict surprise | reportback 出 world_model_conflict + 重启后世界模型仍在 |
| `NOE_CURIOSITY_ORIGIN_GATE` | 步骤2：harvestSurprise 只放行有外部锚的真负反馈(owner_*/action_failure/world_model_conflict)，拒自评/虚构/内省落空 | 噪声 origin 被拦、白名单放行（单测已钉） |
| `NOE_EXPECT_DECISIVE_FAIL` | 步骤5：承诺类(owner_pred)到期反复判不出→决定性判 FAILED | **当前点火也不触发**：owner_pred 已 verifiable=0、生产无项满足门，待接真承诺源 |

## 4. 纪律/边界（叠加 CLAUDE.md）
- **分量动作**：本轮改的 `src/cognition/NoeWorkspace.js`、`server.js` 装配属分量动作——已守单测 + flag 默认 OFF + 留 owner 点火。P0-3 改 `NoeDeliberation`/`MemoryCore` 召回同属分量，照此纪律。
- **secret**：不打印 `.env`/`room-adapters.json` 原值；commit 前自查 diff；本轮所有 commit 无 secret。
- **本地优先**：自主认知白名单仅本地 lmstudio/ollama（deliberateThink 走 noeDeliberateBrainId 本地脑，永不烧付费）；辅助模型（M3/codex/Gemini）配额可烧，Claude 自己省。
- **git**：noe-main 本地领先 origin，owner 惯例不 push（除非明确要求）；绝不 `git add -A`。
- **基线**：全量 658 文件 5147 测；新增/改动测试见各 commit。milestone/push 前跑全量 + `npm run perf-check`。

## 5. 关键文件地图（本轮触及）
- P0-1：`src/cognition/NoeWorkspace.js`（autoCloseTerminalThinkStep + deliberateThink）、`server.js`（noeDeliberateThink 注入 ~1543）、`tests/unit/noe-workspace-goals.test.js`
- P0-3 待改：`src/cognition/NoeDeliberation.js:85-94`（召回处加 sourceType 过滤）、`server.js:1559 distillSkill`、`src/memory/MemoryCore.js:561 recall`
- 审计相关：`src/cognition/NoeGoalSystem.js`（maybeSeedAutonomousLearning/topicCurator 调用 :370）、`src/cognition/NoeLearningHook.js`、`src/runtime/NoeFreedomAdapters.js`（buildBrowserDomPageScript JXA）、`src/loop/SafeActExecutors.js`（browser.observe_page）
- 审计/路线依据：本文件 §1.3/§2 + `docs/ROADMAP_Neo觉醒路线_P0-P15_2026-06-15.md`

## 6. 窗口续报（2026-06-17 深夜 — 多模型安全方案 5 步 + 双轮对抗审查，已闭环）

### 6.1 落地：自主学习「负反馈」多模型安全方案 全 5 步（6 commit）
owner 选「谨慎改判断核心，防刷假学习」后落地。核心洞=自主学习闭环此前没有「有外部锚的真负反馈」可学。
- 步骤1 `f38ea09`：钉死 judge 抗刷假学习不变量（空证据即便模型回 FAILED 也绝不 resolve）。
- 步骤2 `fe5e7f3`：接 Goodhart origin 门（`NOE_CURIOSITY_ORIGIN_GATE`，只放行 owner_*/action_failure/world_model_conflict）。
- 步骤3 `591fd30`：深思预测 dueAt 语义修复（优先 extractExpectations 解析 claim 时窗，治硬编码 now+3d）+ verifiable 可检验性列（schema **v14**：verifiable/judge_attempts/last_judged_at，PRAGMA 退化）+ isVerifiablePrediction 判据。
- 步骤4（前序）：接 owner_correction 真负反馈源（owner 否定 Neo 事实判断→surprise）。
- 步骤5 `cfd6e6d`：承诺类决定性判 FAILED 机制——四重护栏（source 白名单 + verifiable=1 + judge_attempts≥3 硬下限2 + 逾期≥24h 硬下限1h）+ `ledger.bumpAttempts` + `maybeDecisiveFail`，`NOE_EXPECT_DECISIVE_FAIL` 默认 OFF。

### 6.2 双轮多模型对抗审查（Claude4.8 主控 + codex GPT5.5 + M3 红队，外部模型均成功交叉验证）
- **一轮三方**（修 `f3d86c3`）：核心红线全 NONE——**假 FAILED 结构上无法触达 selfEvolve 改代码**（source 白名单 self_evolution + step-kind act 双屏障，codex/M3 各自穷举一致）；四重护栏端到端闭合 / schema v14 退化安全 / sweep 时序无重复 resolve / watcher 正向结算与步骤5 幂等无双重结算。
  发现 **1 SERIOUS**：owner_pred followup 赌的是 owner 言语行为，owner 沉默(under-determined)≠Noe 失败→硬判会污染 Brier+立假研究目标+失真自我认知。修=followup verifiable 1→0（owner 沉默正确归宿是 7 天 sweep 成 NULL）。+ 2 MINOR（bumpAttempts stale 返回、词表偏漏）全修。
- **二轮核验**（修 `08b3df8`）：codex+M3 均判 **seriousResolved=fully**、无回归。M3 红队抓到本次扩词表引入的新误标面（"优化我的心态"等抽象内省句被标 verifiable=1，当前 inert）→ 补 INNER_EMOTION_RE 抽象名词 + 警示注释 + 反向单测。

### 6.3 ⭐ 步骤5 现状与后续（要 owner 拍板的方向）
- **步骤5 是「就绪但生产恒不触发」的基础设施**：owner_pred 两条已 verifiable=0（followup under-determined / topic 弱信号），reflection/thought 被白名单排除，commitment 在独立 commitmentStore **不在 ledger**。故四重门生产无解，点火 `NOE_EXPECT_DECISIVE_FAIL=1` 也不触发（**非误触发风险**，机制经测试验证可在合格输入触发）。
- **真负反馈主力 = 步骤4（owner_correction）**，已落地工作。
- **真正启用步骤5 的唯一路径 = commitment→ledger 桥接**（让 Neo 答应 owner 的可交付承诺进账本、标 verifiable=1，到点没做→真负反馈）。这是**改 Neo 核心认知行为的高风险分量动作**（commitment 判失败影响面比 owner_pred 大），需 owner 明确授权再做。**未做，留 owner 决策**。
- ⚠️ 钉死：扩 `decisiveFailSources` 白名单前，必先收紧 `isVerifiablePrediction`（`NoeDeliberation.js` INNER_EMOTION_RE 仍可能误标抽象内省句）。

### 6.4 基线
全量 **5165** 测全绿（660 文件）；本窗口新增：noe-expectation-decisive-fail(9) + noe-deliberation-verifiable(6) + 步骤1/2 不变量测试。隔离 sqlite drill 端到端全过（schema v14 真迁移 / bumpAttempts 真列 / 红线 reflection 绝不判 FAILED / origin 过 Goodhart 门）。
