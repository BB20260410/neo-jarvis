# HANDOFF 2026-07-01 — 自我完善回流转正（六能力最后一块）

## 一句话
Neo 六能力里唯一没转正的「自我完善」，本 session 转正了：飞轮现在能**持续自主反思出改进方向 → 真改自己的代码逻辑 → 真通过双绿门落地**。修复后连续 **12 done / 0 dropped**（对照修复前 6 done / 4 dropped）是铁证。

## 本轮基线
- 全量 `npm test`：**7983 绿**（856 文件；本 session 起点 7912）。
- 领先 `origin/noe-main` **396 commit**（含本 session 4 个），owner 惯例未 push。
- 生产 51835 跑着本 session 全部修复 + flag（PID 80612，08:28 后多次 kickstart 到最新代码）。

## 主线：自我完善回流从「卡死」到「完全健康」
自我完善有两条回流路径，本 session 把它们都打通并验证：
- **软回流**（教训进 chat 上下文）：`lesson-pin`。
- **硬回流**（教训驱动飞轮自改方向）：`SelfDirectionSeed`——这条是主战场，连环修了 4 个问题才真正跑起来。

## 四个 commit（本地未 push，按 owner 惯例）
| commit | 做什么 | 验证 |
|---|---|---|
| `caf0cf2` | lesson-pin：learning_lesson/surprise_lesson 常驻注入每轮 chat 上下文（治「产而不用」，347 产仅 1 召回）。仿 owner-profile。flag `NOE_LESSON_PIN` | 全量 7917 绿；隔离 SQL 验真返 110 条 |
| `5752030` | half-open：修 SelfDirectionSeed reward hacking 熔断**永久死锁**（连续 3 dropped→熔断→不立新 goal→consecutiveNeutral 永≥3→永不恢复，实测卡 37h）。CircuitBreaker half-open 模式：cooldown 过后放行一次试探 | 全量 7923 绿；点火后第一次心跳就脱困（eae7daf9 done） |
| `ace9dd7` | 偏保守纠偏：DIRECTION_SYSTEM prompt 原写「首选补测试(test_only)」→ 100% test_only 只刷覆盖率。反转为「首选小 logic_changed」 | 全量 7959 绿；纠偏后首方向 c2987f86 即 logic_changed |
| `a6dbd22` | targetFile 透传：self_directed 方向 objective 只有模块名、无 src/ 完整路径 → `readTargetFileContext` 正则匹配不到 → minimax 猜路径编造 → `patch_replace_file_missing` → dropped。修：真实 meta.targetFile 透传整条链（Trigger→executor→spawnImplementer） | 全量 7983 绿；**修复后 12 done / 0 dropped** |

## 修复验证铁证（真效果，非只单测）
- 修复前（08:28 前）：6 done / 4 dropped，其中 3 dropped 是 `patch_replace_file_missing`（minimax 猜错路径，如 NoeToolRouter→src/noe-tool-router.js）。
- 修复后：**12 连 done / 0 dropped**（357e9b46/0b9039bb/2de520c9/46ec6276/2e9365ad/40a21078/b46be728/e0c57162/a6e2dd38/ee0e43d3/4de57332/a6c753f9）+ 1fea83db open。
- 连修复前 dropped 的 NoeToolRouter，现在（1fea83db）又在跑、不再 dropped；0b9039bb 的 cycle `touchedFiles=src/runtime/NoeAcuiCardStore.js`（真实文件、非编造）坐实透传生效。
- 纠偏后 **100% logic_changed**（真改逻辑）+ 修复后 **100% done**（真落地）。

## 新增 / 改动的 flag（plist 已设，生效中）
- `NOE_LESSON_PIN=1` — 软回流点火（plist 已加）。
- `NOE_SELF_DIRECTION_SEED=1` — 硬回流（**本就在 plist**，一开始我误以为没点火，其实早已 ON）。
- `NOE_HEARTBEAT=1` — 心跳（本就在 plist）。
- `NOE_SELF_DIRECTION_COOLDOWN_MS=3600000`（1h）— 启用 half-open + 失败喘息。**曾设 6h 太长**（每个 dropped 拦 6h），调回 1h。
- `NOE_EVOLUTION_RELATIVE_BASELINE`（本 session 前置 commit 6ae4126/051b38e 引入，默认 OFF）— 相对 baseline 健壮性，owner 未点火。
- plist 已备份多份（`~/Library/LaunchAgents/com.noe.panel.plist.bak.*`）。

## 关键教训（下窗口务必记住）
- **8 次差点臆断**：这一程排查 apply preflight 根因，我先后猜「op:replace 不匹配 / apply 特例 / 单例 / logic_changed 难 implement / protected 测试文件是新问题 / 日志 preflight 是 0b9039bb 的 / 0b9039bb 卡死」——**全部被自己多查一步证伪**。
  - 铁律升级：**慢 ≠ 卡；部分数据 ≠ 结论；验证效果必查数据归属（时间戳 / id / ref 匹配），别把凌晨旧 report 当新证据。**
- **cooldownMs 一个参数管两件事**（half-open 试探间隔 + 每次失败喘息），设值要兼顾；6h 对后者太长。
- **kickstart：plist flag 改动需 `bootout`+`bootstrap` reread；纯代码改用 `kickstart -k` 即可**（reload 代码、保留已加载 env）。`restart:panel` 用的是 `kickstart -k`，不 reread plist。

## 未提交说明（重要）
- git status 里 `src/agents/*`、`src/capabilities/NoeToolRouter.js`、`src/context/NoeContextBudgeter.js`、`src/evals/NoeSelfImprove*` 等一批改动，**是飞轮本 session 自主进化的产出**（就是那 12 个 done 方向真改的 targetFile），不是我手改、也不是别窗撞车。owner 决定是否 commit（惯例不 push）。
- 我手改的 4 个 commit 已固化（caf0cf2/5752030/ace9dd7/a6dbd22）。

## 残留（次要，不阻塞 done 率，留后续/fresh session）
1. **探索偏集中**：NoeContextBudgeter 被立 7 次（都 done、有价值，但探索广度可更宽）。可加 buildContext 软引导「最近已处理文件、优先拓新」（不硬拦，免误拒同文件不同角度的真改进）。
2. **另一类 preflight_blocked**：minimax patch 偶尔想改 protected 测试文件（`patch_path_policy_protected:tests/*.test.js`）或含 `patch_content_contains_secret_like_value`。凌晨旧方向暴露，不影响当前 done 率，优先级低。
3. 更多样本可继续累计确认（当前 12 done 已足够坐实）。

## owner 下一步（无强制待决策）
- 是否 push（领先 origin 396 commit）。
- 是否 commit 飞轮自主进化的产出（上面「未提交说明」那批）。
- 残留三项按优先级低处理即可。
