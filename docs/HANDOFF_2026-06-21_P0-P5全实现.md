# Neo 任务交接（2026-06-21 · P0-P5 全层级实现 + 逐阶段对立审核 + 总验收）

> 接手先读本文件 + `AGENTS.md`。本轮按 `docs/Neo_P0-P5_重编_2026-06-21.md` 把 P0-P5 全部推完，每个 P 级用子代理对立审核（多轮迭代至 PASS），末尾全面总验收。**全部改动未提交，owner 定 commit**（noe-main 惯例不 push）。

## 一、本轮成果总览

| 阶段 | 状态 | 审核轮次 → 结论 |
|---|---|---|
| P0 真自改闭环固化 | ✅ | 3 轮红队 + multimodel：r1(5攻破+M3 14项)→修→r2(2新)→修→r3 PASS |
| P1 自学习闭环 | ✅ | 2 轮红队：r1(3问题)→修→r2 PASS |
| P2 认知底座 | ✅ | 1 轮红队(P2-4 真模块 e2e 证明调制活)→ReDoS 修 → PASS |
| P3 记忆技能 | ✅ | 1 轮：P3-1/2/3/4 达标 + P3-5 缺口→补 2 模块 → PASS |
| P4 主动动手 | ✅ | 1 轮：P4-1 词表修 + P4-2/3/4 达标 → PASS |
| P5 权重进化 | ✅ | 1 轮 PASS(0 缺口) |

**总验收全绿**：`npm test` 712 文件 / **5749 测**；ESLint 0 error；`perf-check` ✅；`verify:handoff` 83/0(无 secret)；`test:e2e` 18/18；**实机隔离 boot(PORT=51999, NOE_LEARNING=1 + NOE_AFFECT_MODULATION=1)启动无崩、监听、health 鉴权门正常**。

## 二、新建/改动（11 新 src + 13 新 test，全 git 可追踪未提交）

### P0（改既有，自改链固化）
- `src/room/NoeSelfEvolutionTrigger.js`：complete→`setStatus(done)` 生命周期闭环；observe 质量闸(`requireTechnicalTarget`)；卡死解锁(`maxNonProgressTicks`，进展看 cycle 真推进/act 真成功)。
- `src/loop/NoeSelfEvolutionExecutors.js`：codex 失败重试 K=2 + 降级 lmstudio；apply 后 changedFiles 受保护文件二次核(防 reward hack)；空 operations 不算可用 patch。
- `src/runtime/mission/NoePatchTransaction.js`：软链/目录 lstat 守门(防写穿/EISDIR)；父目录软链逃逸 realpath 祖先校验(红队 r2 实锤沙箱逃逸)。

### P1 自学习（新模块）
- `NoeFailureLessons.js`(失败→type=feedback 教训,根因 unverified) / `NoeSkillDistiller.js`(goalDone→技能卡,enabled:false 注入面解耦) / `NoePreferenceCollector.js`(DPO 偏好对+质量门+**低置信隔离区**) / `NoeLearningLoop.js`(整合三器+事件钩子)。接线：`server.js` onGoalReportback(失败→教训/完成→蒸馏/失败转成功→偏好 quarantine)。

### P2 认知底座（新模块 + 既有验证）
- P2-1 嵌入维度一致性=**既有已实现**(VectorIndex ollama 失败不写 hash-128 + keep_alive=-1)，实测基准 p95=75ms<500ms。
- `NoeGwtMetrics.js`(GWT 切换率/coalition 熵/竞争广度,埋进 NoeWorkspace) / `NoeCodeIntegrationMetric.js`(图论耦合度+圈复杂度,替代不可算 IIT Φ) / `NoeAffectModulation.js`(VAD→深思阈值调制,接 NoeWorkspace,真模块 e2e 证明调制翻转深思决策)。

### P3 记忆技能（新模块 + 既有验证）
- `NoeFocusStack.js`(工作记忆栈,溢出保主线) / `NoeEntityMergeGuard.js`(防带编号/版本/频率实体误合,接 MemoryCore 双去重路) / `NoeCrossLayerSearch.js`(跨层 RRF 融合查询,接 knowledge 路由 `/api/knowledge/cross-search`) / `NoeMemoryIntegrityCheck.js`(merge 悬挂引用自检,接 noe-maintenance 启动只读检)。P3-2 Mem0/P3-4 GC=既有达标。

### P4 主动动手（新模块 + 既有/live）
- `NoeBrowserActPolicy.js`(NetworkPolicy 域白名单 opt-in + 红线5 高危分类) / `NoeFuzzyPatchMatcher.js`(内容相似度容漂移,opt-in 不接自改核心)。P4-3 证据链=既有(buildNoeActionEvidence+脱敏)达标。

### P5 权重进化（新模块 + 既有/MLX）
- `src/weights/NoeLoraVersionRegistry.js`(版本化+revert 可逆) / `src/weights/NoeWeightPromotionGate.js`(人格一致性评分+自动 revert+Shadow Mode N 连胜)。P5-1 数据门=P1-3 collector + 既有 noe-lora-gate 达标。

## 三、新增 .env 开关（owner 钦定默认 ON，2026-06-21）
- `NOE_LEARNING`：P1 自学习闭环（失败→教训/完成→蒸馏/偏好采集）。**默认 ON**，`=0/off` 可关。
- `NOE_AFFECT_MODULATION`：P2-4 VAD→深思阈值调制（需 `NOE_AFFECT` 提供 VAD，否则 no-op）。**默认 ON**，`=0/off` 可关。
- `NOE_SELF_EVOLUTION_MAX_STUCK_TICKS`：P0-4 卡死解锁阈值（默认 60，0=关）。
- `requireTechnicalTarget`：P0-4 自进化 observe 质量闸（server 对自进化 trigger 已置 true）。
- `NOE_BROWSER_ALLOWLIST`：P4-1 浏览器域白名单（逗号分隔，opt-in；不设=开放）。

## 四、kickstart 待办 → 已完成（2026-06-21 owner 授权系统级权限后真落地）
- **已提交**：3 commit（db237d0 P0-P3 / ab038b6 P4-P5 / ed7efa7 server+flags+docs），工作树 clean，领先 origin 303 未 push（owner 未要求）。
- **P4-3 cookie/DOM 脱敏 ✅**：`NoeContextScrubber` 加按 key 名整值抹除（JSON-key `"cookie":"…"` + 冒号形 `password: …`）。
- **P4-1 接线 ✅**：NetworkPolicy 域白名单接进 `SafeActExecutors.normalizeBrowserUrl`（NOE_BROWSER_ALLOWLIST opt-in；既有 ack 闸保留）。
- **P4-2 cua 绑定 ✅ 全通**：`NoeCuaDriver` 真集成；owner 授权后 `cua-driver permissions grant` 已跑成（accessibility+screen_recording=true），`get_accessibility_tree` 真返回 15 窗口 = 权限+绑定端到端全活，写控（click/type）也可用，受 `ackGuiAction` 安全闸约束。**无剩余阻塞。**
- **P5 LoRA：管线真通、训练真跑，但 adapter 不采用（2026-06-21 复核修正，原"12/12 PASS"是夸大）**：
  - 真相（多模型+子代理独立验证坐实）：① 旧 adapter-2026-06-21（rank8/400iter）**近 no-op**，边际贡献≈0，人格全靠 system prompt；当时报的"gate 12/12 PASS"是因为**旧 gate 形同虚设**（不抓"通义/Qwen"身份泄漏、裸测无 system prompt/无关 thinking、无正面人格要求）。② 重训的 adapter-2026-06-21-v2（rank16/lr1e-5/mask-prompt/1200iter，val_loss 4.50→1.17 真收敛）经**修过的 gate 实测 2/12 FAIL**，且边际对照**退化人格**（回复干瘪 9-27tok vs 纯 base 100-180tok、工程日志腔、第三人称跑题）——因 SFT 1003 对约半是项目复盘，训得越透越被带偏。**低 val_loss 在此=过拟合到坏数据，非质量信号。**
  - 结论：**persona 该挂 system prompt**（base+system+关 thinking 实测最温暖在格）；这份数据集上 LoRA 帮不上忙、v2 反而有害。`version-registry.json` 已 `active=null`（=不挂 adapter，用 base+system）。两版本均留档 + honest notes。
  - gate 已修真：注入 Noe system prompt + 关 thinking + 抓 base 身份泄漏/英文推理前缀/身份题正面要 Noe + 剥 mlx 分隔符，配 `tests/unit/noe-lora-gate.test.js`（10 测）。train 管线已加 `--mask-prompt`/rank/lr/`--tag`（质量改进留给未来更干净数据集用）。
  - 若未来要做有价值的 LoRA：先把 SFT 数据按"温暖陪伴 vs 项目复盘"分流、只用前者；或换目标（蒸馏项目知识，但那更适合走 memory/RAG 层）。base 模型路径 `~/.lmstudio/models/lmstudio-community/Qwen3.6-35B-A3B-MLX-6bit`，必须 `NOE_LORA_BASE=该路径` 绕开 HF 下载。

## 五、关键纪律（延续）
- 自改链经 PolicyFileGuard(禁改测试/退路/自身) + rollback + npm test verify；secret 不入 git/日志（verify:handoff docs_no_obvious_secrets 已验）。
- 改 plist 后 bootout/bootstrap；隔离端口 PORT=51999 验分量；kill 前 lsof 验归属。
- 生产 51835(PID 当轮查)未受本轮影响（隔离 boot 在 51999，已清理）。

## 续作记忆
`~/.claude` memory：`project_neo_self_evolution_ignition`(P0 真因链) + 本轮新增 `project_neo_p0p5_full_implementation`(P0-P5 全实现+审核)。
