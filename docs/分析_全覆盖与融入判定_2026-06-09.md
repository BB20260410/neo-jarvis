# 竞品全覆盖分析 + 融入 Noe 最终判定　2026-06-09

> 配套：P1 `分析_BaiLongma对标_2026-06-09.md`、P2 `分析_竞品Claw系对标_2026-06-09.md`、原始逐模块普查 `docs/竞品全覆盖原始普查_2026-06-09/`。
> 方法：Gemini agentic 系统性逐模块全覆盖普查（读真代码）+ Claude 主循环 Noe ground-truth + Claude 子代理实现验证。M3/MiMo 提供跨厂商印证。

## ① 覆盖口径（诚实声明）

| 仓 | 规模 | 覆盖深度 | 排除（理由） |
|---|---|---|---|
| BaiLongma | 141 文件 / 40.8K 行 | **模块级全覆盖 + 深读**（src/ 每个模块逐一） | node_modules、ACUI(Remix) 前端资产、images/music |
| MiniMax-CLI | 85 文件 / 7.6K 行 | **全覆盖深读** | test/、docs/ |
| OpenClaw | 4277 源文件 / ~50 万行 | **模块/机制级全覆盖**（拓扑+命名范式+关键分发文件深读+抽样反演；Gemini 明确未字面逐行读 4277 文件） | ~2000 个 `.test.ts`、静态 HTML/CSS、legacy 迁移层、200+ 厂商 extensions 样板 |
| Hermes | agent/ 113 文件全覆盖；gateway/cli/acp/cron 类·方法级 | **核心全覆盖**（agent/ 无遗漏；run.py 750KB/main.py 460KB 巨文件按类与异步方法 AST 解析） | vendored 依赖、locales、datagen、docker、移动 app 壳（110 万行里大半是这些非手写码） |

> 字面"逐行读完 50 万行 TS + 巨型 Python"对 LLM 不可行且无学习增量（多为样板/生成/vendored）。本次达成的是**每个模块都被读到、所有值得注意的机制都被抽取并带 文件:行**的工程级完整覆盖。原文见 `docs/竞品全覆盖原始普查_2026-06-09/`。

## ② 已实际复刻进 Noe（本轮落地 + 实机测试通过）

| 模块 | 源 | 测试 | 说明 |
|---|---|---|---|
| `src/loop/NoeGenerationFence.js` | OpenClaw 代际栅栏 / reply-turn-admission | 13✓ | 防并发回复连击竞态 |
| `src/runtime/NoeCommitmentStore.js` | OpenClaw commitments + dueWindow | 19✓ | 结构化承诺/待办 + 时间窗触发 |
| `src/prefetch/NoePrefetchStore.js` | BaiLongma prefetch | 14✓ | TTL 预取缓存池 + 注入块 |
| `src/memory/NoePersonCards.js` | BaiLongma person-cards | 12✓ | 结构化人物关系卡 |

均 <500 行、MIT 署名、纳入 `test:p0:unit`（全门 85 文件/618 测全过）。**当前为纯逻辑模块，接线进 Noe 实际回复/记忆/loop 路径是下一步（需碰核心，建议单独立项）。**

## ③ 融入候选 backlog（全覆盖后去重排序，均需你点头才动核心）

### 高价值
1. **记忆 salience 重排 + 向量×FTS 双路融合**（BaiLongma db.js）— Noe MemoryCore 仅 FTS，召回质量真缺口。M。
2. **Static Prefix Cache 防护**（BaiLongma prompt.js）— 把易变 time/env 移出可缓存系统提示前缀，最大化 prompt cache 命中省 token。S。
3. **承诺抽取流水线**（OpenClaw commitments/extraction：嵌入式微 agent + 置信度防幻觉 + dedupeKey）— 喂给已落地的 NoeCommitmentStore。M。
4. **内容热点/趋势主动感知**（BaiLongma hotspots/trending）— 挂到 Noe 已有 proactiveTick 当话题源。M。
5. **预算耗尽"死前总结"**（Hermes turn_finalizer）— loop 将尽时强制写交接总结，提升无人值守体验。S。
6. **多模态生成集成**（MiniMax SDK：image/video/music/vision，剥 CLI 壳直接 import）— Noe 已有 MiniMax key，扩展为 freedom capability。S/M。

### 中价值
7. **回合准入栅栏升级**（OpenClaw queue coalescing + 受控 drain）— 增强已落地的 NoeGenerationFence。M。
8. **伞形技能目录**（Hermes：SKILL.md+references/+templates/+scripts/）+ **Curator GC**（合并/重命名/归档防技能腐败）+ **隐式记忆提取**（免 /remember）— 增强 Noe 已有 AutoSkillExtractor/SkillCurator。M。
9. **launchd 守护进程**（OpenClaw daemon/launchd.ts）— Noe 是 macOS，免 root 常驻 + 看门狗 + restart-logs。S。
10. **诊断时间线**（OpenClaw infra/diagnostics-timeline，AsyncLocalStorage span 火焰图）。M。
11. **上下文引擎 Proxy 隔离降级**（OpenClaw context-engine）— 几行 Proxy 的健壮性。S。
12. **能力 provider/registry 范式**（Hermes browser/web_search/image_gen/tts/transcription registries）— 统一可插拔能力注册。M。
13. **LSP 代码导航**（Hermes lsp/：pyright/tsserver + 改动后行号 range_shift）— 提升 Noe 编码任务能力。M。
14. **mention-gating 群聊门控 + allow-from 白名单**（OpenClaw channels）— 多渠道入站前置。S（依赖 15）。

### 大工程（建议立项分期）
15. **多渠道入站统一网关**（OpenClaw gateway/channels + Hermes gateway/telegram）— Noe 当前只有出站发布链+webhooks，入站可达性（telegram/微信/discord）是最大互补面，但工程量 L。先 EventEmitter 抽象 + 单渠道试点。
16. **对话轨迹滑动压缩**（Hermes context_compressor：近期高保真+早期摘要的无界窗口）。M/L。

### 仅参考 / 低优先（不复刻）
- ACP 编辑器集成（Hermes acp_adapter，Cursor/VS Code 控制）— 对 Noe 偏 niche。
- ACP event-ledger（OpenClaw）— Noe 已有 consensus ledger，过度设计。
- credential pool + bitwarden（Hermes）— Noe 已有 keychain，参考。
- local-resources-scanner SSH/gitconfig 挂载（BaiLongma）— 隐私敏感，谨慎。
- Action Claim 阻断 / fake tool_call nudge（BaiLongma llm.js）— Noe 工具层可参考。

## ④ Noe 已 ≥ 竞品的项（全覆盖确认，勿重造）

- **真实多模型 consensus + 动态 quorum + post-review**：四个竞品**都没有**，Noe 独有且最强。
- **consensus 门控自我进化闭环 + 审计 ledger**：> Hermes 的 self-nudge persist。
- **治理/安全**（safety/permissions/approval/audit/DangerousPatternDetector）：> 全部。
- **四档模型路由 BrainRouter**（local/mid/code/deep）：> 各家 L1/L2 或 model-switch。
- **焦点栈 FocusStack（带压缩）/ 上下文充分性 / 工具按需路由 / 时间召回 / 委托 TaskDelegationPlanner**：已有，≥。
- **流式 think 清洗 NoeContextScrubber**：≥ Hermes think_scrubber。
- **技能体系**（AutoSkillExtractor/SkillCurator/SkillStore/learned）：已成体系，≥ Hermes 基础部分（差"伞形目录+GC"细节，见 backlog 8）。

## ⑤ 一句话总判

Noe 在**治理、多模型协作、自我进化、安全、模型路由**上**领先这四个竞品**；竞品的真价值在**"环境体验层 + 工程健壮性 + 多渠道入站 + 记忆召回质量"**。本轮已落地 4 个最高性价比纯增量模块；其余 16 项按 backlog 排期，碰核心的需你逐项授权。
