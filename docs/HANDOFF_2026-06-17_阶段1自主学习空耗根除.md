# 阶段1·自主学习空耗根除 — 交接（2026-06-17）

> 本轮主线：根除 Neo「重复打开浏览器学 6 个硬编码主题、没真学到」的空耗——接通「被现实打脸→惊奇→立目标→真学到→被读到」闭环。
> 三轮三方复盘（M3 + codex GPT5.5 + Claude 子代理，每轮真 sqlite probe / 真读代码）+ 真本地脑点火实证。**全量 5126 测绿**。

## 一、做了什么（commit 范围 9d646dc..937dc48，约 18 个）

| 模块 | 文件 | 要点 |
|---|---|---|
| **learningHook**（救活）| `src/cognition/NoeLearningHook.js` | surprise 目标 done→本地脑产【具体认知修正 lesson】→写 memory→验 recall。三方实测旧版生产 100% 死(noe_memory=0)，4 致命修复：kind:'insight'+evidenceRefs 过 gate / memId=c.memory.id / recall 用 memory.get 精确(非自证) / recall 补 projectId |
| **供给净化 + F2** | `NoeStepExpectationBridge.js` `NoeWorkspace.js` `ActPipeline.js` | classifyFailure 结构化分类(system_gate/transient/real)；act 失败被 blocked 吞补漏(按 act.status 区分)；ActPipeline 防御 executorResult.ok===false→failed；budget/quota 宽匹配；action_failure claim 改认知落差 |
| **worldModel 矛盾源** | `NoeWorldModelContradictionBridge.js` | research/browse 读到内容与认知矛盾→harvestSurprise(world_model_conflict)。WM-FATAL-1：整条 topic 子串召回恒 0→extractKeywords 关键词召回(含中文2字分段)；WM-OVERRECALL：相关性过滤(≥2关键词或1长词)防无关 belief 刷假矛盾；CONFLICT 行首锚定 |
| **owner correction 源** | `NoeOwnerCorrectionBridge.js` `NoeOwnerBehaviorPredictor.js` | owner 对话纠正 Neo 事实判断→harvestSurprise(owner_correction,最高 surprise=3)。OC-POLLUTION-1(致命)：剥离 timeline summary 里的 Neo 回复只判 owner 原话；OC-FALSEPOS-2：收紧误判(排陈述/缓和/owner 自陈) |
| **#6 接审计 + F7** | `noe-curiosity-yield-report.mjs` `NoeDeliberation.js` | learning 段(lessonsWritten/lessonsRead=hit_count 行为级)；NoeDeliberation 召回改 recallFused(lesson 语义召回面，无 semanticIndex 退回零回归) |

## 二、4 个 flag（**全默认 OFF**，分量动作待 owner 点火）

| flag | 作用 | 依赖（已默认 ON）|
|---|---|---|
| `NOE_STEP_EXPECTATION_RESOLVE` | act/research 真失败→surprise | NOE_GOALS/HEARTBEAT/EXPECTATIONS |
| `NOE_LEARNING_HOOK` | surprise 目标 done→产 lesson 写 memory | 同上 + 本地脑(NOE_INNER_BRAIN=lmstudio) |
| `NOE_WORLDMODEL_CONFLICT` | research/browse 矛盾→surprise | 同上 |
| `NOE_OWNER_CORRECTION` | owner 纠正→surprise | NOE_OWNER_PREDICTION |

**点火步骤**（owner 在场）：`PORT=51999 NOE_STEP_EXPECTATION_RESOLVE=1 NOE_LEARNING_HOOK=1 NOE_WORLDMODEL_CONFLICT=1 NOE_OWNER_CORRECTION=1 npm run start:noe`，盯 `surpriseOriginBreakdown`(应 nonNoise) + `buildCuriosityYieldReport().learning`(lessonsWritten/lessonsRead/readRate) + 抽查 noe_goals source=surprise 的 title 真伪。

## 三、真本地脑点火实证（`scripts/noe-stage1-ignition-probe.mjs`，隔离 /tmp）

`node scripts/noe-stage1-ignition-probe.mjs`（真 LM Studio 35b）——3 源全产 surprise goal(全 nonNoise)；owner_correction(HTTP 301)+world_model_conflict(GIL)产**精品 lesson**(具体/可溯源/可行动)写进 noe_memory；**行为级 readRate=100%**(lesson 写进且真被 Neo 决策 recall 读到=非盲卡)。

实证暴露的真本地脑特性（非 bug）：① lesson 产出有选择性(35b 对无具体认知的 SKIP，质量门，不硬编)；② action_failure 执行失败 35b 多判无认知修正→正确 SKIP(印证三方：执行失败是辅助源，owner纠正/worldModel 是真 epistemic 源)；③ 35b reasoning 需够 max_tokens(生产 LmStudioChatAdapter=8192+reasoning_effort=none 已正确)。

## 四、Neo 网页操控能力（owner 问，已查证）

Neo **能**操控网页且默认启用(NOE_GOAL_ACT=1)：**静默 DOM**(`browser.dom.execute`/`browser.click`/`browser.set_value`/`browser.observe_page`—osascript JXA 注入浏览器执行 element.click()/value=/innerText，不动鼠标键盘) + **真键鼠**(`macos.pointer.click` cliclick / `macos.text.type` osascript)。经 ActPipeline 完整门控(安全门/权限/审批)。

## 五、剩余（非阻断）

- **生产 kickstart**：开 4 flag 让 51835 真跑(owner 点火，分量动作红线，不自主)。
- lesson 产出随机性：35b temperature 0.3，同 topic 有时产有时 SKIP(质量门特性，生产多 goal 累积)。
- 安全约束守全程：secret 不入日志/git；隔离端口验证；不 push noe-main(owner 惯例)。
