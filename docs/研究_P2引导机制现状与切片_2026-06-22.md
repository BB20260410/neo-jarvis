# 研究：ROADMAP P2 引导机制现状与最小切片（2026-06-22）

> 子代理实测 panel.db + 源码 grep。本会话先做 P1.4(已 commit cff7a5f)；P2 切片 B(风险分级)自主落地、切片 A(advisory)待 owner 拍板。

## 前置校准：ROADMAP v3 已被自己的时间线打脸
`ROADMAP_NEXT_2026-06-22.md` 写于 P0 完成前，"complete=0 飞轮空转"已过时。实测 `~/.noe-panel/panel.db` `noe_self_evolution_cycles`：complete=1（secycle-fcd18547，Neo 首个真自改闭环）。P0 已基本满足，P2 不必再等"飞轮从 0 转起来"。

## P2.1/P2.2/P2.3 现状
- P2.1 引导≠拦截：已有 owner-seed `noeMind.js:743` + SOURCE_WEIGHT(owner=1.0) + noe_goals meta 列；缺 advisory vs directive 区分(owner seed 直接成 directive 目标)、"advisory"全是 reviewer 语境零复用、无延迟注入/re-grounding。
- P2.2 多源候选池：三源事实存在(owner/self-discovered/system_repair done×433)、owner 不独占已满足；缺"候选池"抽象(查无此物)、无统一打分中间层。
- P2.3 风险门：CandidateGate(5维工程门)+PolicyFileGuard 健壮可复用、retirement 雏形(stale>14天→paused)；缺 5 维口径(现有=size/growth/structure/tests/holdout 工程门，ROADMAP 要=blast/可逆/外部依赖/语义验证/耦合 风险分级，ROADMAP"复用现成5维"是误判)、retirement 只到 paused。

## P2 依赖 P0？是，但已通
链已通(complete=1)。真实卡点=燃料断供(self_evolution open=0)+质量天花板(唯一 complete 是玩具目标)。P2.2 候选池正是"持续供燃料"机制，接 P0.2①。

## 最小切片
### 切片 A（最高优先，改 owner 信任模型 → 必须 owner 拍板）
候选池+advisory frame。新 NoeCandidatePool.js + 表 noe_goal_candidates。owner seed 改"写候选池"(score≥阈值升格，否则记 reject_reason)。flag NOE_CANDIDATE_POOL 默认 OFF。
owner 决策点：ON 后 owner 的话从 directive(立即执行)→advisory(高权重候选可拒绝)，改变 owner 对系统控制方式。owner 可能就想要 directive。必须 owner 拍板，不可自主点火。

### 切片 B（次优，可自主，为 P3 铺路）— 本会话落地
5 维风险分级门。新 src/security/NoeRiskTiering.js(纯函数 DI)：blast/可逆/外部依赖/语义验证/耦合 → green/yellow/red。叠加非替换 CandidateGate。flag NOE_RISK_TIERING 默认 OFF。给 P3.2"只取绿档"+P3.1 信任校准铺路。新文件加 PolicyFileGuard。

### 切片 C（可选，价值小不优先）
retirement 真退役(paused→dropped+reason)。flag 默认 OFF。

## owner kickstart 必需
①新 flag 默认 OFF+owner kickstart ②切片 A 改 owner seed 语义=信任变更必须 owner 拍板 ③REAL_APPLY 点火权属 owner ④新风险门加 PolicyFileGuard(主线做)。

## 证据边界
complete=1 是 panel.db 快照未亲跑复现第二个 complete；唯一 complete 是玩具目标；advisory 零复用强断言(34处全 reviewer)证据充分；接入点行号实读准确；切片行数/表是设计估算。

## 来源
子代理 adc26269e33c6ed02；Neo 文件 noeMind.js:743、NoeGoalSystem.js:23/490、NoeEvolutionCandidateGate.js:139、NoePolicyFileGuard.js:7。
