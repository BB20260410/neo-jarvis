# HANDOFF 2026-06-16 — P2 觉醒驾驶舱 + 浏览器空转修复

> 接手先读本文件 + `CLAUDE.md`。本轮在 `/goal` 觉醒路线（`docs/ROADMAP_Neo觉醒路线_P0-P15_2026-06-15.md`）框架下完成 P1-C 复核收尾 + P2 全程 + 一个 owner 现场揪出的真 bug 修复。

## 本轮三大成果（全部多轮双代理验收 + 全量绿）

### 1. P1-C 复核收尾（origin 分桶）
- commit `138101f`：F4-REOPEN（owner_manual 加 outcome===0 守卫）+ F3 stats 接线门 b + 词界/结构化信号优化。
- 三轮验收闭环（第 2 轮 0 严重 + 第 3 轮 0 严重 = 连续 2 轮无新增高危止循）。

### 2. P2 觉醒驾驶舱（活性可观测看板）
- commit 链 `a7a42f3`（核心）→ `075b001`（盲审整改 F1-F6）→ `8014d20`（复核整改 R1-R4）。
- **5 看板**：校准曲线（Brier/ECE/MCE，与 sklearn 1e-12 一致）/ 模型存活（三脑就位+embedding后端）/ 整合度趋势 / 好奇漏斗 / 觉醒 4 维采样。
- **防 Goodhart 护栏**（盲审揪出的核心）：①撞墙信号检测（`NoeWallSignal` 整合度过度同步/独白空转）②校准 holdout 分层（`resolved_by` 区分 owner 裁决 vs 本地脑自评，前端「全自评非客观校准」caveat）。
- 新文件：`src/cognition/NoeCalibrationCurve.js` / `NoeIntegrationHistory.js` / `NoeWallSignal.js` / `NoeAwakeningSignals.js` / `src/server/services/NoeModelHealthProbe.js`。
- mind route 加 5 endpoint（calibration/curiosity-funnel/integration-history/model-health/wall-signals），前端觉醒驾驶舱折叠区（`mind.html/js/css`）。
- 真实数据实证：Brier 0.078 / ECE 0.267 揪出 Neo 系统性低估自信 / 漏斗诊断指向 NOE_EXPECT_LOOSEN_FAIL 待 soak。

### 3. Neo 浏览器空转修复（owner 现场揪出的真 bug）
- commit `663a78c`。owner 实证：Neo 近 24h 开网页 18 次、`observe_page` 自标 `pageContentReadByNoe:false`、真读内容 **0** 次，note 全是元笔记——表演「我能操控浏览器」但零信息摄入。
- 根因链 5 层叠加，逐层治本（L1-L3.5）：
  - **L1** `NoeFreedomAdapters.buildBrowserDomPageScript` 加 `read_body` 动作（JXA 读 `document.body.innerText`，优先 main/article，截断 8000 字）。
  - **L2** `pageContentReadByNoe` 据实申报（读了正文才 true）+ 包装层透传不覆盖。
  - **L3** `NoeGoalSystem` autonomousLearning 两处目标模板 observe_page 注入 read_body。
  - **L3.5** `NoeWorkspace.summarizeActOutput` 读 browser act 的 extractedText 进深思摘要（写含页面知识的笔记而非元笔记）。
- **端到端真 JXA 验证通过**：开 example.com → `pageContentReadByNoe:true` + 真读到 129 字正文。
- **L4（act 完成判据识破「只开不读」）归后续**：L1-L3.5 已让 observe_page 默认读正文、空转治本；L4 是防御性兜底（改 ActPipeline 完成判据风险高，未做）。

## 新增 .env 开关（全默认 OFF，待 owner 点火）
- `NOE_AWAKENING_SAMPLE`：觉醒 4 维采样自动调度（心跳每小时落盘 output/awakening-samples）。也可手动 `npm run noe:awakening`。
- `NOE_WALL_GUARD`：撞墙回滚执行（默认 OFF 只告警写意图 kv `noe.wall.guard.*`；真执行端待 P3 器官接，当前 ON 也只落意图态，已诚实标注）。
- `NOE_INTEGRATION_HISTORY_MAX` / `NOE_WALL_GUARD_MS` / `NOE_AWAKENING_SAMPLE_MS`：参数微调。

## schema 迁移
- v13：`noe_expectations` 加 `resolved_by`（区分 owner holdout vs auto 自评，幂等 ADD COLUMN）。resolve/calibration 对无列旧库容错降级。

## 基线
- 全量 **652 文件 5065 测全绿**；P2 相关新增约 50 测，浏览器修复新增 L1 端到端测试。

## 下窗口建议下一步（择一）
1. **P3 觉醒路线**：接三个半活器官（NOE_EFE_CURIOSITY 好奇双因子 / NOE_THOUGHT_LOOP_GUARD 防连击 / NOE_INTEGRATION_METRIC 整合度），owner 在场 PORT=51999 逐一点火（路线 P3 原文）。点火 NOE_INTEGRATION_METRIC 后整合度趋势看板 + 撞墙 over_integration 检测才有真数据。
2. **补 L4**：act 完成判据识破「只开不读」（observe act 无内容产出不算完成 → 触发反思换路），对接 P1 act 真完成判据 + P2 撞墙信号。
3. **P1-D**：预测分层（claim×证据 embedding sim 全 <0.45 标 unjudgeable_poetic 不入判证队列）。
4. **撞墙回滚执行端**：NOE_WALL_GUARD 的 noe.wall.guard.* 意图 kv 接 InnerMonologue/novelty 器官消费（随 P3 器官点火一起做）。

## 注意（接手纪律）
- 浏览器修复改了 Neo 默认行为（observe_page 默认 read_body），无 flag（修 bug 非新功能）。真生效需 Neo 运行——生产 51835 当前未跑，下次起来时查 observe_page 事件确认 pageContentReadByNoe 真变 true。
- 验证 P2/浏览器用隔离端口 `PORT=51999 npm start`（非 start:noe 硬编码 51835）。
- secret 边界、noe-main 不 push（owner 惯例）等照旧 CLAUDE.md。
