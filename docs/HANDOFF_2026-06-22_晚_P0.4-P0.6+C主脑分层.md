# HANDOFF 2026-06-22 晚 — P0.4-P0.6 + C 主脑分层（本轮全自主交付）

> 接手先读本文件 + `AGENTS.md`。上一份：`HANDOFF_2026-06-22_P0自改控制链通+Neo真改代码第一次.md`。

## 本轮完成（owner「全部按建议 + 开发者模式」全自主）

### owner 实时请求（四项主动能力实测，全真）
- **联网搜索 ✅** MiniMax Search 真返回结果；**操控电脑 ✅** 经 unified-kb MCP(:8200) `fs_stats` 真统计 33.9 万文件；
  **上网爬虫 ✅** `fetchContent` 真抓网页正文 + DeepResearcher 多轮；**写代码 ✅** Neo **首次完整自改闭环 complete**
  (cycle 02dd1364 stage=complete + goal done + 记忆 mem-86c38e68 真写 + DB complete cycle 0→1)。
- 详见 [[project_neo_self_evolution_ignition]] 记忆（已更新 complete 达成 + NO_PROXY 根因）。

### 2 个 commit（全量 6263 绿，生产重启 OK pid 浮动）
- `886d2cc` 修复 post-review 云端 reviewer 抛错崩整轮 + m3 经代理偶发失败兜底（try/catch + retry5 退避）。
- `f26bee8` P0.4 八反向 probe + P0.5 emergency stop + 核心禁区显式化 + P0.6 SLO duration + API cycle 可观测。
- `c4cd309` C 主脑分层（深思/proactive 重决策可走 cloud）。

### P0.4 八反向 probe（`tests/unit/noe-p04-*.test.js`，真调机制非 mock）
元自改拦截 / 并发顺序 / 跨耦合 blast / oracle 独立 / rollback 字节 / no_patch diagRef / 重启续跑 / blockedPath 可见。

### P0.5 emergency stop + 核心禁区
- `src/security/NoeEmergencyStop.js`（always-armed，**非默认 OFF 开关**）：信号源 = 文件 `~/.noe-panel/EMERGENCY_STOP` OR `NOE_EMERGENCY_STOP=1`；挂 `NoeHeartbeat.pumpOnce` 停所有自主 kind（保留 maintenance/wallGuard/mdMirrorTick/integration）。
- HTTP：`GET/POST /api/noe/emergency-stop {on:true/false}`（owner 一键）。
- 核心禁区显式化：`NoePolicyFileGuard` PROJECT_POLICY_FILES 补 NoeEmergencyStop/NoeHeartbeat/NoeLocalModelPolicy/NoeReflectiveTuner（防 Neo 自改关掉护栏）。

### P0.6 SLO v2 + 可观测
- `NoeSelfEvolutionExecutors.runtimeVerify` 产物补 `startedAt/durationMs` → `NoeSelfEvolutionSlo` 自动算 P50/P95（原给 null）。
- `GET /api/noe/mind/self-evolution` 加 `cycles`（open/active self_evolution goal + cycle stage，owner 实时看 Neo 进化到哪步）。
- **剩 mind.html UI**（前端实时显示 cycle stage/stuck，需 owner 验美）——未做。

### C 主脑分层（`c4cd309`）
- `NoeReflectBrain.resolveHeavyReflectBrain`：重决策 tier 白名单放开 cloud（claude/codex/minimax）；高频 `resolveReflectBrain` 仍只本地。
- 接线：`server.js` 深思(NoeDeliberation) + `noe.js` proactiveTick 优先 heavy tier。
- **点火**：`NOE_REFLECT_HEAVY_TIER=1` + `NOE_REFLECT_HEAVY_BRAIN=claude`(或 codex) → 深思/proactive 走 cloud。默认 OFF 零回归。owner 决定何时烧配额点火。

## 运维/环境（本会话变更）
- **plist** `~/Library/LaunchAgents/com.noe.panel.plist`：NO_PROXY 加 `api.minimax.chat,api.minimaxi.com`（国内服务直连，根治 m3 经境外代理偶发失败）；MINIMAX_API_KEY 在 env；reviewer = `local-qwen,m3`。已备份 `.bak.noproxy.*`。
- **kb server :8200 已加开机自启** `~/Library/LaunchAgents/com.unified-kb.plist`（`npm run kb`），重启电脑后「操控电脑·文件」自动可用。
- **run `pgrep server.js` 会匹配母项目 PID 621**（cwd=05_Claude可视化面板，占 51735）——kill 前必 `lsof` 验归属，别误杀。Neo panel 占 51835。
- `src/util/NoeEvolutionMilestone.js` 是 Neo 自产 untracked，**勿 git add**。

## 下一步 P1（接通 P5/P7/P10，substantial 新接线，非补缺）
按 `ROADMAP_NEXT_2026-06-22.md`：
- **P1.1 P5 双时态**：✅ `MemoryCore.recall/recallFused` 加 asOf 过滤（commit 96f094d，召回时间点查询 valid_from<=asOf<valid_to，#recallFts/#recallLike 同步+fallback 透传，默认零回归）。**剩**：`as_of` 图谱查询函数未建（NoeKnowledgeGraphTemporal 只有写入侧双时态列/关窗 closeStaleWindows，无 asOf 读查询函数）+ 暴露 `builtinReadonlyTools`（noe.kg.as_of）。
- **P1.2 P10 TopicDiscovery**：✅ GoalSystem 侧（commit f7c558d）：加 `discoverDynamicTopics` 注入点，maybeSeedAutonomousLearning(376) 并入静态+动态（带 evidence），fail-open，默认 null 零回归。**剩 server.js 装配**（装配顺序敏感）：① import `createTopicDiscovery`(src/cognition/NoeTopicDiscovery.js) + `collectLearningConcepts`(src/cognition/NoeLearningTopics.js) ② `let noeTopicDiscovery=null` 在 createGoalSystem(server.js:1119) 前声明 ③ createGoalSystem 加 `discoverDynamicTopics: () => { try { return noeTopicDiscovery?.discover?.()?.seeds || []; } catch { return []; } }`（**必须用 discover().seeds 保 evidence——NoeTopicDiscovery 的 discoverConcepts() 会剥成 {title,url,query} 丢 evidence，勿用**；lazy 闭包避循环依赖）④ 在 `noeCommitmentStore`(server.js:1215) **之后**装配 `noeTopicDiscovery = createTopicDiscovery({ kg: noeKnowledgeGraph(926), goalSystem: noeGoalSystem, commitmentStore: noeCommitmentStore, kv:{get:kvGet,set:kvSet}, staticConcepts: collectLearningConcepts() })`（commitmentStore 定义在 createGoalSystem 之后，故 topicDiscovery 装配必须在 1215 后）。createTopicDiscovery 的 `discover()` 内部 NOE_TOPIC_DISCOVERY 默认 OFF→seeds:[]（零回归），owner 点火产动态发现。
- **P1.3 P7 ReflectiveTuner**：✅ shadow tick 挂 heartbeat（commit d586901，autotick flag `NOE_REFLECTIVE_TUNER_AUTOTICK` 默认 OFF→owner 点火，6h，archive/recommend 不采纳，emergency stop 自动跳过）。
- **P1.4 认知链路 E2E**：证明 inner-state/persona 真进主聊天+语音 prompt（`NOE_CHAT_CONTEXT` 等首验）。未做。

### 本轮 P1 commit
96f094d(P1.1 recall asOf) · d586901(P1.3 shadow tick) · 19a83a6(owner 模型优先级 M3→codex→Claude) · f7c558d(P1.2 GoalSystem 切片)。本轮总计 8 commit（含 P0.4-0.6 f26bee8 + C c4cd309 + m3 fix 886d2cc + HANDOFF 78bddc8）。

## 验收基线
- 全量 `npm test` = **6263 绿**（745 files）；新增测试：noe-p04-*(8)+noe-emergency-stop+noe-reflect-brain heavy tier(5)+SLO duration+API。
- 生产：launchd com.noe.panel(:51835) + com.unified-kb(:8200) 均开机自启。
