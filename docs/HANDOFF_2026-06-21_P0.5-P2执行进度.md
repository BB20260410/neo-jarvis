# Neo 后续 P0-P10 执行进度（2026-06-21 · /goal 逐级实现中）

> 总路线见 `docs/Neo_后续P0-P10_2026-06-21.md`。本文件记 /goal「逐级实现 + 每级多模型&子代理审核 + 通过再下一个」的执行进度，便于续作。
> 纪律：每级 先诊断后修(数据驱动) → 实现 → 单测+反向probe → 多模型(M3+Codex)+Opus子代理双审 → 修findings → 全量绿 → 提交。

## 已完成（双审通过 + 已提交）
| P级 | 内容 | commit | 审核结论 |
|---|---|---|---|
| P0.5 | 注入契约：inner-state/persona-pin 段挂载点(DI默认null=no-op)+白名单+fail-open(复用runProvider)+keep | `76a1a82` | 子代理[合格]+多模型(测试隔离/keep语义2点已采纳) |
| P0 | verify防假绿(JSON交叉校验+fail-closed,跳冗余sha256)+NoeMissionRunner前缀/软链越界 | `7d97254` | 子代理[合格]+多模型(整数校验/success字段/软链realpath 3点已采纳) |
| P1 | 自进化诊断黑洞(attemptedCandidates)+硬网络跳重试+structuredCall DI | `a30dd8c` | 子代理[合格]+多模型(non_usable_patch_plan留因/rawReplyExcerpt/HARD_NET_RE收窄 已采纳) |
| P2-part1 | 自进化act显式灌objective(修历史空objective) | `6fb8d92` | 先诊断坐实=历史产物,当前代码已规避,仅小硬化+回归测试 |

基线：全量 **5797 测全绿**，ESLint 0。每级 commit 独立可回滚。noe-main 领先 origin，未 push。

## P2 剩余（核心 + 最高风险，下一步从这里续）
**目标**：让 Neo 在生产链路跑出第一个 `complete` 的自进化 cycle（自驱改自己代码→验证→复核→写回记忆→标 complete）。

### part-2：complete 闭环 autodrive（真能拒绝坏 cycle，非自动盖章）
- **现状**：`NoeSelfEvolutionLoop` 阶段链 post_review_required → retrospective_required → memory_writeback_ready → complete。trigger 注释明说这几段"不 propose"(留 consensus/复核驱动)，但**无人自动驱动 → cycle 永停在 post_review_required → DB complete=0**。
- **脚手架（复用）**：`NoeSelfEvolutionConsensusAutodrive.js`(autodrive 模板,tick 已用 assembleConsensus 的同款)；`NoePostReviewRunner.js`(有 approve/reject 决策 + 按 model 分角色=能真复核能拒绝)；`NoePostReviewPack.js`。
- **设计**：仿 ConsensusAutodrive 建 `makeNoeSelfEvolutionCompletionAutodrive`，tick 在 post_review_required/retrospective_required/memory_writeback_ready 阶段调它 → patch cycle → advance：
  1. post_review：用 **NoePostReviewRunner 跑非 implementer 复核**(Review 脑 qwen3.6-27b，不是写 patch 的 codex)，**ok=false 必须能让 cycle 不 complete**(防自我盖章——这是安全命脉，测试必含"坏 patch 被复核拒"用例)。
  2. retrospective：本地装配 retrospectiveRef(类 autodrive)。
  3. memory_writeback：propose memory_writeback act 写脱敏 lesson。
  4. 全程经 standing-grant(`self-evolution:run`)。
- **验证**：单测(含坏cycle被拒)+ 隔离 PORT=51999 端到端跑通一个 cycle 到 complete + 反向probe(失败patch→rollback)。
- **✅ part-2 核心已建+测+双审通过+提交(`662e4ab`+`b2338fc`)**：`NoeSelfEvolutionCompletionAutodrive`(post_review经runPostReview真复核能拒绝/无复核器/抛错/无grant/request_changes/未知词 都不盖章)+ trigger tick 接线(一拍一阶段单writer)+ 10测。Opus子代理[合格]+多模型[合格](采纳 request_changes 安全 + 删死分支)。**关键纵深防御(子代理实证)**：autodrive 叠在 complete gate 硬门之上，`computeStage` 无视 patch 的 stage 字段强制从真实证据重算 → 绕不过 gate。
- **part-2 剩余=生产接线(让它真活，具体配方)**：
  1. server.js **~L1128** 仿 consensus 装配：`const completionAutodrive = process.env.NOE_SELF_EVOLUTION_COMPLETION_AUTODRIVE === '1' ? makeNoeSelfEvolutionCompletionAutodrive({ root: process.cwd(), runPostReview }) : null`（owner钦定可默认ON，但这是分量动作，建议先隔离端口验）；trigger 装配(**~L1136**)加 `assembleCompletion: completionAutodrive`。
  2. **runPostReview 适配器**：`async ({goal,objective,patchPlanRef,applyReportRef,runtimeReportRef,dir,rootAbs}) => { const pack = buildNoePostReviewPack({...从patch/apply/runtime构, reviewers=非implementer即 ['claude','m3']}); const r = await runNoePostReviewRound({ pack, runModels:true, costAcknowledged:true, outDir:dir }, { root:rootAbs }); return { reviews: r.reviews }; }`。注意：`runNoePostReviewRound`(NoePostReviewRunner.js:278) runModels 必须配 costAcknowledged:true(否则抛 model_cost_ack_required)；pack 须过 validateNoePostReviewPack(requireReviewerOutputRefs:true)；reviewers 走 buildNoePostReviewPack(NoePostReviewPack.js:87)。
  3. 隔离端口验：真复核能跑 + 喂"坏 patch"看 reviewer 真 reject → autodrive ok:false → cycle 不 complete。

### part-3：放开 REAL_APPLY（最后一步，隔离验证后）
- `.env` `NOE_SELF_EVOLUTION_REAL_APPLY=1`(server.js:~942 装配点读它→trigger realApply→实改代码)。
- **务必先隔离 PORT=51999 端到端验**(喂强锚 objective 如"给某函数加校验"→看 apply-reports status=applied + runtime绿 + cycle到complete + noe_acts self_evolution≥1)，**反向probe**(喂会让 npm test 挂的改动→P0 verify拦+自动rollback)。
- 放开前按路线 P2 多模型段做**M3+Codex+Claude 三方共识**：①P0 verify覆盖假绿②rollback原子③post_review autodrive不会给坏cycle盖章④PolicyFileGuard行为不变。四项全过才点 live 51835(owner 已授权自主 kickstart，但这是分量动作，建议最后做)。

## P3-P10
见 `docs/Neo_后续P0-P10_2026-06-21.md`（P3 SLO+幂等事务+archive / P4 inner-state认知注入[用P0.5契约] / P5 双时态记忆 / P6 可验证基准 / P7 GEPA替LoRA / P8 召回整治 / P9 觉醒看板 / P10 自主上网升级）。

## 关键纪律提醒
- 跑模型不设超时；子代理一律 Opus+effort max；多模型走 `scripts/multimodel.mjs --mode review --task ... --in <brief> --repo <dir>`(codex 超时已改 env 可配 CODEX_TIMEOUT_MS 默认20min)。
- owner 价值观：自由>护栏，不把权限管控当 P 级；正确性/防外泄/防reward-hack/可观测照修。新 flag 默认 ON（persona-pin 等也已默认 ON）。
- secret 不入 git/日志；commit 前自查 diff。
