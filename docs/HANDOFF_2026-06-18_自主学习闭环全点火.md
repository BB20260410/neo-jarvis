# HANDOFF 2026-06-18 — 自主学习闭环全点火（P1+P4+P2）+ GPT5.5 任务

> 本窗 owner /goal 连续推进，每个 P 走「实现→M3+codex+Claude(opus) 三方互评→点火」。接手先读本文 + `CLAUDE.md` + 各 P 专项 HANDOFF。

## 0. 状态
- 生产 51835 = **PID 5152**（本窗多次 kickstart，最新加载全部改动）。母项目 51735(71420) 全程未受影响。
- 全量 **669 文件 5237 测全绿**。
- **本窗未提交**（owner 惯例不 push）；改动跨 server.js / src/memory / src/cognition / src/server/routes / public / scripts / tests / docs。
- env 点火 flags（.env）：`NOE_THINK_LESSON_PERSIST=1`(P1) · `NOE_MEMORY_VECTOR_POOL=1`(P4#3) · `NOE_MEMORY_LESSON_CHANNEL=1`(P2杠杆1)。`NOE_MEMORY_LESSON_RESERVE` 默认2。

## 1. 已交付（全三方互评+点火）
- **P1 深思 lesson 落库**（`src/cognition/NoeThinkLessonPersist.js` + server.js noeDeliberateThink）：闭合断点2（深思认知修正只进 step note 不进 noe_memory）。**实测产出 7 张真 learning_lesson**（质量好、非套话）。详见 `HANDOFF_2026-06-18_P1闭环点火_深思lesson落库.md`。
- **P4 召回地基**（`src/memory/MemoryCore.js` + NoeMemoryRetriever + noeCoreRoutes）：#1 merge 清向量(治465孤儿) + #3 向量 over-fetch(治insight被挤,flag) + #5 路由 bumpHits + #10 sourceType 过滤 + SERIOUS fallback 修复。详见 `HANDOFF_2026-06-18_P4召回地基点火.md`。
- **P2 学习对话可见**：杠杆1=lesson 专属召回通道(NoeMemoryRetriever，末尾留席治小 limit 独占) + 杠杆2=透视页召回来源可追溯(NoeMemoryStatus.recentLessons + mind.js)。
- **GPT5.5 检查任务**（owner 插入）：印证 = voice-ear P0「0字节」**误报**(完整重跑 25 cases 全 PASS，bracket restBytes=0 是不泄漏的预期)；curiosity/affect/orphan/calibration 真但是 HANDOFF 降级的后台实验/历史维护非紧急。整改 = `scripts/noe-100-readiness.mjs` 加 scope/caveat/runtimeBlockers（治「满分掩盖 runtime blocker」误导）。

## 2. 三方互评实证价值（owner 指定方法）
- Claude(opus 子代理) 用**反向探针实跑**抓到我自己漏的真 bug：P4 FTS fallback 漏 sourceTypes、P2 杠杆1 lesson 在小 totalLimit 独占挤掉 fact/user。
- codex 独有发现：timeline type 'insight' 退化、MemoryCore 无 sourceType 过滤、杠杆2 缺 hidden/expires 过滤。
- M3 红队深审 + 证伪它自己的多个误判由 Claude/codex 交叉验证（如 voice-ear 100%强塞夸大、writeGate 装配 null 误判）。

## 3. P2 成效观察（关键洞察）
- learning_lesson 7 张 hit=0：因当前 lesson 全是**技术认知修正**(系统自修复等)，owner 日常 chat 不相关 → 杠杆1 fail-safe 正确不强塞。
- **印证 P3 价值**：owner 日常对话更需召回 **owner 偏好**(user/fact scope)，而非自主学习产的技术 lesson。

## 4. 下一步
- **P3 学 owner**（进行中，子代理 opus 诊断）：让 Neo 对话主动体现 owner 偏好(语言/工作方式/项目焦点)。
- P5 成效证伪 / P4 批次3(#4 FTS分词+#2清孤儿,低优先) / lesson 写入侧 topic 索引化(召回质量根因)。

## 5. 红线教训（本窗）
⚠️ **`pkill -f "node server.js"` 模糊匹配误杀了生产 51835 + 母项目 51735**（均 launchd/自愈拉起无损失）。**清进程必先 `lsof -i:<端口>` 反查精准 PID，重启用 `launchctl kickstart -k gui/$UID/com.noe.panel`，绝不模糊 pkill**（见记忆 `feedback_check_port_before_kill`）。
