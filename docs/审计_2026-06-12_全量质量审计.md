# 审计 2026-06-12 全量质量审计

阶段：只审计、零代码修复、零 commit。
真实仓库：`/Users/hxx/Desktop/Neo 贾维斯`。
本报告仅覆盖用户指定代码范围：`server.js`、`src/**`、`public/app.js`、`public/main.js`、`public/src/**`、`scripts/**`。不审计 `node_modules/`、`_archive/`、`out-noe/`、`docs/`、`tests/**`；测试文件只作为证据引用。

## 0. 统计和 Top5

| 口径 | P0 | P1 | P2 | 合计 |
|---|---:|---:|---:|---:|
| 本报告条目 | 3 | 8 | 1 | 12 |
| 高置信 | 3 | 6 | 1 | 10 |
| 中置信 | 0 | 2 | 0 | 2 |
| 低置信 | 0 | 0 | 0 | 0 |
| 新发现 | 2 | 3 | 1 | 6 |
| 存量重复/仍相关 | 1 | 5 | 0 | 6 |

最该先修 Top5：

1. [x] #P0-01：多个会 unload/load LM Studio 的 benchmark 脚本缺少手动确认闸门，可能扰动 Gemma 常驻主脑。
2. [x] #P0-02：`network.upload` 在 developer/unrestricted 下可把任意本机文件当附件读出并上传。
3. [x] #P0-03：CAMPPlus 声纹链路仍用 `spawnSync` 阻塞 Node 事件循环，旧审计未闭合。
4. [x] #P1-01：Markdown 外链图片被改写到 `/api/img-cache` 后普通 `<img>` 无法携带 owner-token，当前链路会 401。
5. [x] #P1-02：MiniMax 音乐/视频 artifact 下载一次性 `arrayBuffer()` 入内存且无大小上限。

## 1. 文件清单和行数总账

审计清单由 `rg --files`/Node 统计生成，文本文件逐个计行；二进制/缓存文件只列入覆盖对账，不做代码语义审计。

| 范围 | 条目数 | 文本文件 | 二进制/缓存 | 文本行数 |
|---|---:|---:|---:|---:|
| `server.js` | 1 | 1 | 0 | 2,079 |
| `src/**` | 415 | 415 | 0 | 86,521 |
| `public/app.js` | 1 | 1 | 0 | 732 |
| `public/main.js` | 1 | 1 | 0 | 156 |
| `public/src/**` | 86 | 86 | 0 | 20,715 |
| `scripts/**` | 94 | 91 | 3 | 14,755 |
| 合计 | 598 | 595 | 3 | 124,958 |

二进制/缓存条目：

- `scripts/XikeLab.app/Contents/Resources/icon.icns`，1,058,705 bytes。
- `scripts/__pycache__/noe-cosyvoice-server.cpython-314.pyc`，7,524 bytes。
- `scripts/__pycache__/noe-ocr.cpython-314.pyc`，3,604 bytes。

大文件提示（>500 行，作为 P2 维护性证据）：`src/room/CrossVerifyDispatcher.js` 3305、`src/agents/AgentRunStore.js` 3282、`server.js` 2079、`src/runtime/NoeFreedomAdapters.js` 1950、`src/capabilities/NoeFreedomManifest.js` 1064、`src/server/services/rooms-core.js` 1057、`src/storage/SqliteStore.js` 998、`src/server/services/cluster-runtime.js` 941、`src/agents/AgentRunVerificationExecutor.js` 929、`scripts/noe-main-brain-absolute-benchmark-v2.mjs` 861、`src/agents/AgentSkillRegistry.js` 806、`src/room/CollaborationDispatcher.js` 796、`src/runtime/NoeFreedomExecutor.js` 784、`src/loop/SafeActExecutors.js` 782、`src/agents/SymbolGraph.js` 738、`public/app.js` 732、`src/agents/AgentRunApprovalResumeReview.js` 726、`src/autopilot/AutopilotScheduleStore.js` 721、`scripts/noe-main-brain-candidate-benchmark.mjs` 702、`src/agents/JavaScriptAstAnalyzer.js` 677、`src/cognition/NoeGoalSystem.js` 672、`src/memory/MemoryCore.js` 662、`src/room/DebateDispatcher.js` 638、`scripts/lib/noe-social-dom-live-probe-runner.mjs` 624、`src/report/RoomReporter.js` 622、`src/voice/VoiceSession.js` 622、`src/budget/BudgetPolicyStore.js` 608、`src/permissions/PermissionGovernance.js` 588、`src/server/routes/noe.js` 536、`public/src/web/noe-voice.js` 508、`scripts/noe-voice-ear-acceptance.mjs` 503。

## 2. 发现

### P0

- [x] #P0-01 | P0 | `scripts/noe-main-brain-absolute-benchmark-v2.mjs:30`, `scripts/noe-main-brain-absolute-benchmark-v2.mjs:188`, `scripts/noe-main-brain-absolute-benchmark-v2.mjs:776`, `scripts/noe-gemma-family-benchmark.mjs:86`, `scripts/noe-new-model-benchmark.mjs:109` | 会 unload/load LM Studio 的 benchmark 脚本缺少手动确认闸门。

  机理：当前项目最终决策是 Gemma 单主脑常驻，Qwen 只能 manual benchmark / explicit experiment。`absolute-benchmark-v2` 含 Qwen 候选，启动后进入 `main()`，`loadCandidate()` 先 `unloadAll()`；`gemma-family` 和 `new-model` 也会通过 `lms unload/load` 冷加载模型。与已处理的 `scripts/noe-main-brain-candidate-benchmark.mjs` 不同，这三个入口没有 `--ack-manual-benchmark` 或 `NOE_ACK_MANUAL_BENCHMARK=1` 拒跑逻辑，误运行就可能卸载当前常驻模型或改变 LM Studio loaded models。

  证据：`scripts/noe-main-brain-absolute-benchmark-v2.mjs:54-79` 列 Qwen 候选；`188-199` unload 当前 active models 与候选；`202-208` load 候选；`776-857` 直接执行 `main()`。`scripts/noe-gemma-family-benchmark.mjs:86-99` 每次 cold load 先卸载候选模型，`390-393` 再尝试 restore；`scripts/noe-new-model-benchmark.mjs:28-35` 卸载列表含恢复模型和 Qwen，`109-123` 执行卸载/加载，`431-436` 尝试 restore。对照证据：`scripts/noe-main-brain-candidate-benchmark.mjs:16` 定义确认 flag，`595-604` 无确认则拒跑。

  修复建议：给所有会 unload/load 的 benchmark 统一加同一手动确认闸门和启动提示，并在报告开头声明“manual benchmark only，不写回默认”；必要时把脚本文件名加 `manual-` 前缀。改动面 M<100 行；回归风险低。

  置信度：高。新发现；`candidate-benchmark` 已收敛，但这些入口仍裸跑。

- [x] #P0-02 | P0 | `src/runtime/NoeFreedomAdapters.js:1608`, `src/runtime/NoeFreedomAdapters.js:1649`, `src/runtime/NoeFreedomExecutor.js:687`, `src/permissions/ExecPolicyStore.js:71` | `network.upload` 在 developer/unrestricted 下可读取并上传任意本机路径。

  机理：`network.upload` 的 dry-run 和 execute 都直接 `resolve(root, filePath)`，如果 `filePath` 是绝对路径或 `../../` 逃逸路径，`resolve` 会得到 root 外文件。execute 分支随后 `readFileSync(resolved)` 并把字节作为 HTTP body 发出。常规 allowlist 中确实有路径校验，但 `NoeFreedomExecutor` 在 developer/unrestricted 模式直接把 allowlist 结果置为 ok；同时 `ExecPolicyStore` 的 developer/unrestricted preset 允许 `net.upload` 和 `secrets.read.content`。这不是限制 owner 权限的问题，而是“把本机秘密文件误当普通上传附件”的数据外泄路径。

  证据：`NoeFreedomAdapters.js:1621-1624` dry-run 用 `statSync(resolve(root,filePath))`；`1657-1663` execute 读 resolved 文件；`1675-1678` fetch 上传 body；`NoeFreedomExecutor.js:687-698` developer mode 跳过 trust/allowlist；`ExecPolicyStore.js:71-86` developer 允许 `net.upload`/`secrets.read.content`；`NoeFreedomAllowlist.js:120-127` 说明非 developer 路径本来会做 path scope 校验。

  修复建议：保留 developer 自由执行，但 `network.upload` 对 `filePath` 使用统一安全路径解析；默认拒绝明显 secret/credential 路径和 root 外路径，除非未来做“显式 secret-ref upload”能力；补文件大小上限或流式上传字节上限。改动面 M<100 行；回归风险中。

  置信度：高。新发现。

- [x] #P0-03 | P0 | `src/identity/CampPlusVoiceClient.js:47`, `src/identity/OwnerIdentityStore.js:152`, `src/identity/PersonKnowledgeStore.js:320`, `src/voice/VoiceSession.js:245` | CAMPPlus 声纹热路径仍用 `spawnSync` 阻塞事件循环。

  机理：声纹验证/录入会进入 `CampPlusVoiceClient.embedAudio()`，该函数用 `spawnSync` 启动 Python 脚本，最长 30s。调用链在 `OwnerIdentityStore._embedFrom()`、`PersonKnowledgeStore._voiceEmbeddings()`、`VoiceSession.chat()` 中同步执行；这会阻塞整个 Node 进程，影响 HTTP/WebSocket/自动循环。

  证据：`CampPlusVoiceClient.js:44-63` `spawnSync` 带输入音频和超时；`OwnerIdentityStore.js:152-160` 同步调用后失败回退 lite；`PersonKnowledgeStore.js:334-347` 同步调用并吞错误；`VoiceSession.js:245-251` 对话链路内直接 verify/identify。旧审计 `docs/HANDOFF_2026-06-11_全项目质量审计_未完成.md:71-78` 已标为 `§3.4 P0-1/P0-6` 且“留专门窗口”，当前仍未闭合。

  修复建议：`embedAudio` 改 `spawn` + Promise，调用链 `async/await` 化；保留 lite fail-open 兜底，先不做长驻 Python worker。改动面 L>=100 行；回归风险高，需覆盖“主人不被锁门外”不变量。

  置信度：高。存量重复，已收录旧 `§3.4 P0-1/P0-6`。

### P1

- [x] #P1-01 | P1 | `public/src/web/markdown-ui.js:99`, `server.js:456`, `src/server/routes/img-cache.js:136` | Markdown 外链图片走 `/api/img-cache` 后普通 `<img>` 无 owner-token 通道。

  机理：前端把外链 `<img src="https://...">` 改成 `<img src="/api/img-cache?url=...">`，但全局 `/api` owner-token guard 会拦截 `/api/img-cache`；普通 `<img>` 标签不能带 `X-Panel-Owner-Token`，因此浏览器加载图片会 401。直接给 `/api/img-cache` 放开又会削弱现有 owner-token 保护，需要改加载方式或使用有限签名。

  证据：`markdown-ui.js:99-102` 改写外图；`server.js:456-475` 全局 `/api` guard，豁免列表不含 `/api/img-cache`；`img-cache.js:136-138` endpoint 本身只接 query。旧审计 `docs/HANDOFF_2026-06-11_全项目质量审计_未完成.md:53-58` 已查证为功能债，当前仍符合机理。

  修复建议：前端改为 fetch 带 owner-token 后生成 blob URL；或实现短期、单用途、仅图片缓存可用的签名查询参数。改动面 M<100 行；回归风险中。

  置信度：高。存量重复，已收录旧 `§3.1 P0-5` 附带功能债。

- [x] #P1-02 | P1 | `src/media/NoeMediaStudio.js:76`, `src/media/NoeMediaStudio.js:128`, `src/media/NoeMediaStudio.js:154`, `src/server/routes/noeMedia.js:42` | MiniMax artifact 下载无内容长度/大小上限，一次性进内存后同步落盘。

  机理：音乐和视频成功后，`#download(url)` 对返回体直接 `await resp.arrayBuffer()`，再 `Buffer.from()`；没有检查 `content-length`，没有流式字节上限，也没有按媒体类型限制落盘大小。外部下载 URL 返回超大或异常体时会造成内存峰值和磁盘写入风险。这里不是“模型调用不设超时”的问题，而是模型返回 URL 后的 artifact 下载阶段。

  证据：`NoeMediaStudio.js:76-84` 下载实现；`98` 同步 `writeFileSync` 落盘；`130-133` 音乐走该下载；`160-163` 视频轮询成功后走该下载；`noeMedia.js:42-57` 音乐 endpoint 允许同步生成并落盘，`77-83` 视频轮询成功时落盘。测试只覆盖路由参数限制，未见 artifact 下载大小上限覆盖。性能类未实测，标记为【未实测】。

  修复建议：按 kind 设置最大 artifact bytes，先检查 `content-length`，再 stream 到 temp 文件并累计字节，超限 abort，最后原子 rename；保留生成调用无硬超时。改动面 M<100 行；回归风险中。

  置信度：高。新发现。

- [x] #P1-03 | P1 | `src/server/routes/img-cache.js:68`, `src/server/routes/img-cache.js:179`, `src/server/routes/img-cache.js:181` | `img-cache` SSRF 防护的 DNS 结果未绑定到实际 fetch 连接。

  机理：`assertPublicUrl()` 先 `dns.lookup()` 检查域名是否解析到私网，然后 `fetch(curUrl)` 让 undici/系统再次解析并连接；如果攻击域名在两次解析之间改变结果，预检公共 IP、实际 fetch 私网 IP 的 DNS rebinding 窗口仍存在。每跳 redirect 会重检 URL，但仍未绑定 IP。

  证据：`img-cache.js:82-89` 只做预检 lookup；`179-181` 预检后立即 `fetch(curUrl)`，没有自定义 lookup/dispatcher 或解析结果绑定；旧审计 `docs/HANDOFF_2026-06-11_全项目质量审计_未完成.md:133` 已列 `§3.1 P1④`。未做真实 DNS rebinding 实测，标记为【未实测】。

  修复建议：用带自定义 lookup 的 undici Agent 固定已校验地址，或获取 TTL 并拒绝低 TTL/多变解析；至少补专门单测模拟两次解析变化。改动面 M<100 行；回归风险中。

  置信度：中。存量重复，已收录旧 `§3.1 P1④`。

- [x] #P1-04 | P1 | `src/server/routes/roomsForward.js:6`, `src/server/routes/roomsForward.js:87`, `src/server/routes/roomsForward.js:119` | `rooms/forward` 的 token/context warning 块因 TDZ 永远静默失败。

  机理：`seedScope='all' && targetMode !== 'chat'` 时，代码在 token 估算块内引用 `defaultMembers`，但 `let defaultMembers` 在后面才声明；TDZ 抛出的 `ReferenceError` 被空 `catch {}` 吞掉，导致超长上下文警告永远不会出现。功能本体仍可 forward，但 owner/前端无法看到爆 context 风险提示。

  证据：文件头 `roomsForward.js:6-7` 已自述“预存 bug”；`87-101` import 后引用 `defaultMembers`；`119-143` 才声明并赋值。触发路径：POST `/api/rooms/forward`，body 中 `seedScope:'all'` 且 targetMode 为 `squad`/`debate`/`arena`。

  修复建议：把 `tm/defaultMembers` 计算提前到 warning 块之前，或先按 targetMode 临时计算 member cap；去掉空吞错或至少记录 warning 失败原因。改动面 S<20 行；回归风险低。

  置信度：高。新发现。

- [x] #P1-05 | P1 | `server.js:591`, `src/server/routes/sessions.js:31`, `src/server/routes/sessions.js:366`, `src/server/routes/sessionsContinuum.js:242` | `checkSessionsCapacity` 创建/fork/handoff 时 O(n) 全扫 session Map。

  机理：每次创建、fork、handoff 新 session 都用 `[...sessions.values()].filter()` 统计 active sessions。上限是 500/100，当前量级不至于数据损坏，但这些入口属于交互路径，且 fork/handoff 常在长对话高压时使用，O(n) + 数组复制可避免。

  证据：`server.js:591-601` 容量函数；`sessions.js:31` 创建，`sessions.js:369` fork，`sessionsContinuum.js:245` handoff 共用。旧审计 `docs/HANDOFF_2026-06-11_全项目质量审计_未完成.md:53-58` 已列为 `§3.1 P0-2`，按本轮规则降为 P1 性能项。未做压测，标记为【未实测】。

  修复建议：维护 active session 计数器或从 session store 返回汇总计数；归档/恢复/delete/set 时同步更新。改动面 M<100 行；回归风险中。

  置信度：高。存量重复，已收录旧 `§3.1 P0-2`。

- [x] #P1-06 | P1 | `public/src/web/cognitive-attachments.js:8`, `public/src/web/cognitive-attachments.js:74`, `public/src/web/cognitive-attachments.js:98`, `public/src/web/cognitive-attachments.js:224` | 认知附件在浏览器解码前缺少文件大小上限。

  机理：附件最多 8 个，但单个图片/视频没有大小门槛；`addFiles()` 直接对图片调用 `imagePayload()`，对视频调用 `videoPayload()`。图片/视频会先让浏览器解码，再 canvas downscale 到 1280。超大图片或视频仍可能在 decode/seek/canvas 前造成前端内存/卡顿。

  证据：`cognitive-attachments.js:8-11` 只有数量、上下文、边长限制；`74-91` 视频 object URL 加载、seek、canvas；`98-113` 图片解码和 canvas；`224-253` addFiles 未按 `file.size` 拒绝。对照 `public/src/web/composer-ui.js` 有 800KB 文本附件 cap，说明此处是单独缺口。未做大文件浏览器实测，标记为【未实测】。

  修复建议：加图片/视频前端阈值，超阈值只保留文件名/大小/类型 meta，不进入视觉分析；UI toast 说明跳过原因。改动面 S<20 行；回归风险低。

  置信度：中。新发现。

- [x] #P1-07 | P1 | `src/server/routes/commercial-setup.js:16`, `src/server/routes/commercial-setup.js:95`, `src/server/routes/commercial-setup.js:103` | commercial status endpoint 每次同步探测多份本地文件。

  机理：`getCommercialStatus()` 每次 GET 都 `existsSync` 多个文件，并对 webhook secret json 做同步 `readFileSync` + `JSON.parse`。这不是高频核心链路，但如果前端状态轮询或调试打开频繁，会把同步 IO 放进 `/api` 请求路径。

  证据：`commercial-setup.js:12-16` `checkFile/existsSync`；`30-38` lemon webhook 读 JSON；`43-50` polar webhook 读 JSON；`95-106` 两个 GET 每次调用。旧审计 `docs/HANDOFF_2026-06-11_全项目质量审计_未完成.md:133` 已列 `§3.1 P1②`。未做请求频率/延迟实测，标记为【未实测】。

  修复建议：加 5s TTL 内存缓存，写 webhook/license 时主动失效或接受短暂延迟。改动面 S<20 行；回归风险低。

  置信度：高。存量重复，已收录旧 `§3.1 P1②`。

- [x] #P1-08 | P1 | `src/identity/CampPlusVoiceClient.js:53` | CAMPPlus Python 子进程继承完整 `process.env`。

  机理：`spawnSync` env 展开 `...process.env`，这会把当前 Node 进程中的 API key、token 或临时凭证一起传给 Python 子进程。当前脚本是本仓库脚本，风险低于外部上传，但仍增加 secret 暴露面；修复不需要限制功能，只需给子进程传最小环境。

  证据：`CampPlusVoiceClient.js:53-61` `env:{ ...process.env, NOE_CAMPP_MODEL_DIR, PYTHONUNBUFFERED, OMP... }`。旧审计 `docs/HANDOFF_2026-06-11_全项目质量审计_未完成.md:185` 已列 `§3.4 P2②`。本报告不读取或输出任何 secret 原值。

  修复建议：白名单 `PATH`、`HOME`、Python/模型目录、线程数变量，必要时透传 `DYLD_*`/虚拟环境变量；补测试确认典型 key 名不进入 env。改动面 S<20 行；回归风险低。

  置信度：高。存量重复，按本轮 secret 外泄维度提升为 P1。

### P2

- [x] #P2-01 | P2 | `src/room/CrossVerifyDispatcher.js:1`, `src/agents/AgentRunStore.js:1`, `server.js:1`, `src/runtime/NoeFreedomAdapters.js:1` | 30 个文本文件超过 500 行，局部模块职责过宽。

  机理：大文件本身不是缺陷，但当前有 30 个 >500 行文件，其中 4 个超过 1900 行。它们集中在 dispatcher、agent run store、freedom adapters、server composition 等跨层模块；后续修 P0/P1 时容易产生隐式耦合和回归。

  证据：本报告 §1 大文件清单。典型样本：`CrossVerifyDispatcher.js` 3305 行、`AgentRunStore.js` 3282 行、`server.js` 2079 行、`NoeFreedomAdapters.js` 1950 行。

  修复建议：不做独立大重构；在触碰相关 P0/P1 时顺手按行为边界抽出小模块，例如 `networkUploadAdapter`、`benchmarkManualGate`、`voiceEmbeddingRuntime`、`sessionCapacityCounter`。改动面 L>=100 行；回归风险高。

  置信度：高。新发现，维护性项。

## 3. 旧 56 条合并去重

旧文档：`docs/HANDOFF_2026-06-11_全项目质量审计_未完成.md`。本表按旧文档结构归并，避免把已经闭合或 owner 明确不要的权限管控项重复报成新问题。

| 旧来源 | 旧项数量 | 当前归并 |
|---|---:|---|
| `§3.1` server + routes P0/P1/P2 | 14 | owner-token 同步 IO/Buffer 已修；img-cache readdir 热路径已修；commercial 行内鉴权已修；unhandledRejection 深层 try/catch 已修。仍相关：`§3.1 P0-2` 已收录 #P1-05；`§3.1 P0-5` 功能债已收录 #P1-01；`§3.1 P1②` 已收录 #P1-07；`§3.1 P1④` 已收录 #P1-03。P2“所有路由显式 requireOwnerToken”属于权限审计口径，本轮不作为修复项。 |
| `§3.2` governance / 执行器 / secrets | 10 | 危险命令检测、ownerPresent、protectedPaths、permissionDedupeKey、JSDoc 等多数已有修复记录；owner 明确不要再做提权握手/guardLevel/硬护栏类收紧。本轮只新增 #P0-02，因为它是具体文件上传外泄路径，不是权限收紧建议。 |
| `§3.3` memory / context / state | 16 | 旧文档显示 8 个 P0 全闭合，多数 P1/P2 后续也已闭合；本轮 broad scan 未发现新的 P0/P1 复活项。未对 tests 目录审计，测试缺口不作为新代码缺陷。 |
| `§3.4` voice / vision / identity | 16 | InsightFace/ScreenCapturer/VoiceVad/PersonKnowledge/VoiceSession glance 等旧 P0 已有修复记录。仍相关：`§3.4 P0-1/P0-6` 已收录 #P0-03；`§3.4 P2②` 已收录 #P1-08。 |
| 旧文档未审域：runtime/loop/autopilot、public、全局 P1/P2 | 不适用 | 本轮纳入指定范围扫过。新增 #P0-01、#P1-02、#P1-04、#P1-06、#P2-01。 |

## 4. 覆盖对账表

| 范围 | 状态 | 覆盖方式 | 未扫/限制原因 |
|---|---|---|---|
| `server.js` | 已扫 | 全文件计行；路由/auth/session/broadcast/shutdown 关键上下文读取；同步 IO、timer、model/default、owner-token、JSON stringify 模式检索 | 未逐项重构，不改代码 |
| `src/**` | 已扫 | 全文件计行；按风险模式检索 `spawnSync`、`readFileSync/writeFileSync`、`fetch/arrayBuffer`、`resolve(root,filePath)`、secret/env、allowlist、route handler、queue/cache/timer；命中文件读上下文 | 不读取 secret 文件；不审计 tests 实现 |
| `public/app.js` | 已扫 | 计行；timer/dialog/WebSocket 迁移残留模式检索 | 未发现 P0/P1 命中 |
| `public/main.js` | 已扫 | 计行；模块 import/boot/timer 模式检索 | 未发现 P0/P1 命中 |
| `public/src/**` | 已扫 | 全文件计行；Markdown 渲染、附件、fetch owner-token、object URL、file size、timer/WebSocket 模式检索；命中文件读上下文 | 浏览器大文件性能未实测 |
| `scripts/**` | 已扫 | 全文件计行；benchmark、LM Studio load/unload、secret/token、sync IO、port guard、manual ack 模式检索；benchmark 命中文件读上下文 | 未运行 benchmark，避免触碰 LM Studio loaded models |
| `tests/**` | 排除 | 仅作为证据 grep 引用 | 用户明确排除测试代码审计 |
| `docs/**` | 排除 | 只读旧 handoff/audit 作为合并去重输入 | 用户明确排除 docs 审计，但要求输出报告到 docs |
| `games/cartoon-apocalypse/**` | 未触碰 | 无 | 当前项目硬边界 |
| `51735` / `51835` | 未触碰 | 无进程操作、无 live 重启、无 API 调用 | 本阶段是代码审计，不做 live 验证 |
| LM Studio loaded models | 未触碰 | 只读脚本，不运行 benchmark | 避免扰动 Gemma 常驻主脑 |

## 5. 自检记录

- 本报告不包含 secret 原值，只引用环境变量名、文件名、代码行。
- 所有 `done/fixed` 判断均来自当前代码上下文或旧文档修复记录；本轮发现均保留未勾选。
- 性能类未做实测的条目已显式标注【未实测】。
- 阶段二修复建议：先修 #P0-01、#P0-02；#P0-03 单独开声纹异步化批次；P1 可按 `img-cache`、`media`、`roomsForward`、`sessions`、`public attachments`、`commercial/CampPlus env` 分批。

## 6. 阶段二修复记录

修复日期：2026-06-12。阶段二已按本报告 12 条全部落地，未 commit。

修复摘要：

- #P0-01：新增 `scripts/lib/noe-manual-benchmark-gate.mjs`，4 个 LM Studio benchmark 入口无 `--ack-manual-benchmark` / `NOE_ACK_MANUAL_BENCHMARK=1` 时 exit 2；结果不写回默认。
- #P0-02：`network.upload` 统一 root 内路径解析，拒绝 root 外、secret-like 路径和超大文件，失败前不读取、不 fetch。
- #P0-03 / #P1-08：CAMPPlus 改异步 `spawn`，Owner/Person/VoiceSession/HTTP route 声纹链路 `await` 化；Python 子进程只透传最小 env。
- #P1-01 / #P1-03：Markdown 外图改为 owner-token fetch 后 blob URL；`img-cache` 使用已校验 DNS 结果创建 pinned lookup。
- #P1-02：MiniMax media 下载先查 `content-length`，再按 chunk 累计大小上限。
- #P1-04：`roomsForward` 先计算 `tm/defaultMembers`，token warning 块不再 TDZ 静默失败。
- #P1-05 / #P2-01：抽出 `src/server/services/session-capacity-counter.js`，创建/fork/handoff/归档/删除用钩子维护 active count。
- #P1-06：认知附件图片/视频在浏览器 decode 前做 byte size gate，超限只保留 meta/跳过原因。
- #P1-07：commercial status / next-step 共享 5s TTL cache，避免每次 GET 同步扫文件。

验证：

- `node --check server.js && node --check src/server/services/session-capacity-counter.js`：通过。
- `node --check scripts/noe-main-brain-candidate-benchmark.mjs && node --check scripts/noe-main-brain-absolute-benchmark-v2.mjs && node --check scripts/noe-gemma-family-benchmark.mjs && node --check scripts/noe-new-model-benchmark.mjs`：通过。
- `npm run test:p0:unit`：88 files / 685 tests passed。
- `npm test`：408 files / 3333 tests passed。
- `npm run verify:handoff`：24 passed / 0 failed。
- `git diff --check -- ':!games/cartoon-apocalypse/**'`：通过。
