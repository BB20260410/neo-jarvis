# Neo 任务交接（2026-06-21 · 心跳自主真改打通 + P0-P5 重编）

> 接手先读本文件 + `AGENTS.md`。本轮两核心任务（#12 心跳自主真改链、#13 重编 P0-P5）已完成，下面是状态与**详细下一步**。

---

## 一、本轮完成（两核心任务）

### #12 心跳自主真改链 ✅（铁证）
重启后注入空 plan self_evolution goal → 生产心跳**全自动**（无人驱动）tick → 出 patch → apply → runtimeVerify → act `completed` → 真改 `NoeContextScrubber`（加 github_pat 脱敏规则）→ 全量 5598 绿。

**真因链全治（5 处，缺一不可）**：
1. **双豁免**：`NoeGoalSystem.js` nextStep + closeResolvedGoals 加 `source==='self_evolution'` 跳过——通用 goal 系统的 bootstrapEmptyGoalPlan 会给空 plan goal 填 research step → 执行 done → allDone 标 goal done → 切断 selfEvolve。
2. **objective 传递修复**（最关键）：`NoeSelfEvolutionExecutors.js` resolvePatchPlanRef 取 `ctx.objective` → 改 `ctx.objective || ctx.goal`。ctx=payload.selfEvolution 只有 `goal` 字段、无 `objective` → implementer 收空 objective → 出空 operations → apply blocked。**这正是 e2e（直接传 objective）成功、而心跳一直不成的根因差异**。
3. **readTargetFileContext**：spawnImplementer 附目标文件真实内容 → 治本地模型 chat 无文件访问、凭想象编 from（hallucinate 出文件里不存在的 pattern）。
4. **codex 失败降级 lmstudio**：spawnImplementer candidateIds=[codex, lmstudio]，codex 网络失败时降级本地（localhost 绕外网）。
5. **disableMcp**：implementer 不加载 panel MCP profile（曾致 codex 跑 12min 不出）。

（前置 task6：codex implementer no_patch_plan 真因=codex 天然出 op:replace 而系统只认 write_file → 全链路 replace 修复 + 虚拟串行应用 + finalText secret 值集合差集 + 3 轮对立审计。）

### #13 重编 P0-P5 ✅
`docs/Neo_P0-P5_重编_2026-06-21.md`：26 原子任务 × 5 字段（目标/执行逻辑带 file:line/交付物/依赖/完成判定）+ 软依赖 + 审核机制 + 第二轮审计深化段。经 2 轮 ollama 对立审计（逻辑+红队）迭代收敛（第一轮 5 必修 + 第二轮 6 深化，正确性/防污染全纳、权限管控按 owner「要自由」筛）。

---

## 二、关键代码改动（**未提交**，owner 定 commit）
- `src/runtime/mission/NoePatchTransaction.js`：replace 增量 op 全链路 + checkPreconditions 虚拟串行应用 + secret 值集合差集 + apply occ 守门。
- `src/runtime/mission/NoePatchApplyExecutor.js`：summarizeOperation replace + rollback 接 PolicyFileGuard + NUL 清除（JSON.stringify）。
- `src/runtime/NoeContextScrubber.js`：extractSecretLikeValues + **Neo 自主加的 Stripe(line17)/github_pat(line28) 脱敏规则**（drill 真改，真实安全改进，建议保留）。
- `src/loop/NoeSelfEvolutionExecutors.js`：IMPLEMENTER_SYSTEM 支持 replace + **objective 修复** + **readTargetFileContext** + **降级 lmstudio** + disableMcp。
- `src/cognition/NoeGoalSystem.js`：nextStep + closeResolvedGoals 的 self_evolution 豁免。
- `src/room/NoeSelfEvolutionTrigger.js`、`src/security/NoePolicyFileGuard.js`、`server.js`：A4/B5/realApply 注入（多为前会话）。
- `tests/unit/noe-patch-apply-executor.test.js`：replace 全套回归（多 op 漂移/拆分 secret/净持平对冲/空 to 等）。
- `docs/Neo_P0-P5_重编_2026-06-21.md` + 本交接。

全量 5598 绿 + ESLint 0。

---

## 三、当前运行状态
- 生产 51835（`lsof -i:51835` 查当前 PID）：载全部修复 + 全开关（NOE_SELF_EVOLUTION=1 / REAL_APPLY=1 / AUTOSEED=1 / HEARTBEAT=1 / TICK_MS=60000）+ 心跳运行中（任何 open self_evolution goal 会被自主真改）。
- 网络：已切**新加坡节点**（实测稳：codex→chatgpt/openai 连通 403/401、CF Worker 0.68s×3 稳定）。
- 临时产物：多个 drill self_evolution goal（github_pat 等）+ `/tmp/{restart-rebuild.sh,watch3.mjs,local-implementer.mjs,q.mjs,review-p0p5-ollama.mjs,neo-realrepo-selfheal.mjs}` 待清。

---

## 四、下一步（详细 · 按优先级）

### A. 即时收尾（~10min）
1. **commit 决定**（owner）：`git diff` 审；建议两 commit——①`修复：self-evolution 真自改链路（replace增量op+心跳自主真改+安全加固）` ②`新增：NoeContextScrubber 补 Stripe/github_pat 脱敏`。noe-main 惯例不 push，除非 owner 要。
2. **清理 drill**：把 drill 的 self_evolution goal（github_pat 等）`UPDATE noe_goals SET status='dropped'`（server 停时改防 memory 覆盖，或它们空 plan 不影响）；删 /tmp 临时脚本。
3. **生产队列复核**：确认 openSelfEvolutionGoals 无 drill 残留（否则心跳会持续真改 drill 目标）。

### B. 心跳自改持续验证（codex 已通，~30min）
1. **验证 codex implementer**（不降级）：codex 链路恢复（403/401）→ 注入一个真实小修 goal，确认心跳用 codex 出 patch→真改（codex 比本地模型出 from 更准）。
2. **长稳 + churn 复核**：挂一个真实 P0 修复 goal 跑多拍，确认稳定真改 + cycle 正常推进 + 不 churn（验 closeResolvedGoals 豁免长期 OK）。

### C. P0 剩余固化（`docs/Neo_P0-P5_重编` P0 段）
1. **P0-2 闭环**：cycle 走到 complete stage 时自动 close goal（`NoeSelfEvolutionTrigger.tick` 检测 loop.stage==='complete' → goalSystem.setStatus done），否则豁免后 goal 永 open；补降级路径单测（mock codex 失败→本地兜底出 patch）。
2. **P0-1b 深化**（第二轮审计必修）：secret 拆解语义检测（Base64/Hex/拼接绕过）+ Semgrep 静态扫描；**测试文件 Hash 校验**（防小模型改测试骗 verify = reward hack）。
3. **P0-4 AUTOSEED 质量闸**：observe 立项要技术信号 + ClarificationRequest（模糊意图问 owner 非直接丢弃）。
4. **P0-5 RUNBOOK**：bootout/bootstrap 流程 + 安全门时序 e2e 落 `docs/RUNBOOK_出事翻这页.md`。

### D. P1-P5（`docs/Neo_P0-P5_重编` 各段，软依赖可并行）
- P1 自学习：NoeFailureLessons（Root Cause 标 unverified）+ 技能蒸馏 + DPO 数据低置信度隔离。
- P2 认知底座：**优先 OLLAMA_KEEP_ALIVE=-1** 解 embedding mismatch（别上 transformers.js，会破坏心跳实时性）+ GWT + 整合度量化（耦合度/圈复杂度，非 IIT Φ）。
- P3 记忆技能：FocusStack + Mem0 + 去重类型守卫。
- P4 主动动手：Playwright MCP（高危操作守红线 5：对外发布/支付/Merge PR 确认）。
- P5 权重进化：DPO/LoRA + Shadow Mode 影子测试 + 数据质量门。

---

## 五、关键纪律 / 实战坑
- **运维**：改 plist 后必 `launchctl bootout + bootstrap`（kickstart 不重载 plist 新项）；kill 进程前 `lsof` 验归属；开发期可自主重启生产。
- **网络**：**只用新加坡节点**（非新加坡 Anthropic 封号）；codex→OpenAI、claude→CF Worker(`cl-px-h7k2nf`)；本地模型（LM Studio 1234 / ollama 11434）localhost 绕外网，是 codex 网络断时的兜底。
- **524 真因**：超长会话致 Anthropic 处理 >120s 偶发 524（反代 44 行纯透传、非源头）；治本=`/compact` 或新会话。
- **自改安全**：PolicyFileGuard 禁改测试/退路/自身；rollback + npm test verify；secret 绝不入 git/日志。
- **本地模型兜底特性**：chat 无文件访问，必须 readTargetFileContext 附目标文件内容才出准 from；本地模型偏语法级修改，复杂逻辑重构 codex 优先。
- **goal 状态机**：self_evolution goal 已豁免通用 close/nextStep；空 plan goal 免疫 closeResolvedGoals；改 goal status 在 server 停时（防 memory cache 覆盖）。

## 续作记忆
`~/.claude` memory：`project_neo_self_evolution_ignition`（含真因链 + 心跳自主真改全通）；架构背景 `project_neo_core_layers_findings`、`project_neo_full_codebase_comprehension`。
