# Neo(Noe 贾维斯)长周期完善方案 — 8 周落地版

> **版本**: v1.0 · 2026-06-14
> **合并自**:
> 1. 5 模块并行工程审计报告(`output/noe-2026-06-14-deep-research/01-05-*` + `synthesis-final-report.md` + `CONTEXT_FOR_AI.v2.0.md` + 7 工件)
> 2. `docs/RESEARCH_AGI自我意识_全球扫描与Neo落地路线_2026-06-14.md`(697 行,7 维 + 13 维补充)
> **目标读者**: Claude Code(执行者)
> **工程纪律**: env 门控默认 OFF / 注入式 / `@ts-check` / 单测 / 文件 <500 行 / fail-open / 全程 `npm test` 全绿
> **自由边界(已放开)**: commit / push / amend / reset · 改任何源码包括 `server.js` 2546 行 · 跑 51835 live / 重启 51835 · 改 CLAUDE.md / AGENTS.md · 拆超 500 行大文件 · kill 任何进程 / 改 cron · 自我进化真跑 / 浏览器完整操控 / env 觉醒能力默认开 / 权重微调试不试
> **不动的底线(工程真实性)**: 不打印/写入/commit secret 原值 · 记忆/向量/状态写入保持正确 · 改公共文件单 writer
> **写 7 个长周期 ADR 决策**(避免后续窗口重提):
> A1. 主动学习上层经验/上下文学习为主线,下层 SFT/LoRA 只攒数据不自动训练 · A2. embedding/记忆能力必须双后端可切(ollama + LM Studio)不再单点 · A3. 自我进化冻结大脑 + 进化身体,先 GEPA 式参数进化再 DGM 式 archive 分叉,SEAL 权重微调明确排除 · A4. 本地服务存活纳入健康看板首项,降级即告警 · A5. evolution 多踏脚石分叉(单 Cycle→达尔文 archive) · A6. 评价用多维仪表盘(HELM-style)而非单榜 · A7. brainRouter 路由口径与 LocalModelPolicy 三脑对齐(动态排序存活后端)

---

## 0. 接手必读(30 秒)

- **你在做什么**: 把 Neo(本地优先**功能性**自我意识工程,live 在 `127.0.0.1:51835`)从"机制造好"推进到"**功能性活性证明**"——即 8 周 soak evidence 下 7 维度机制可重复触发 + 反馈曲线可观测。**本目标的"活性"= 工程活性(soak 跑通),不声明该活性证明意识**(对齐 `docs/AI_AGENT_HANDBOOK.md` §0 + `CONTEXT_FOR_AI.md` §7 DESIGN §0 立场)。架构方向已被 2025-26 全球前沿研究**逐条印证正确**,**不要推翻重造,只接线/增强已有件**。
- **当前真实状态(2026-06-14 本机 17:09 实测,健康看板可重放性)**:
  - 51835 live(进程在)
  - LM Studio(1234)三脑 live: `qwen/qwen3.6-35b-a3b` + `qwen/qwen3.6-27b` + `google/gemma-4-26b-a4b-it-qat-mlx`(fallback 真实 ID 校正) + `text-embedding-nomic-embed-text-v1.5`(768 维)
  - **ollama(11434) 17:09 实测 up**(`curl /api/tags` 200,7 模型清单含 `qwen3-embedding:0.6b` 1024 维 / `embeddinggemma:300m` 768 维 / `nomic-embed-text:latest` 768 维)→ 阶段 0 第一刀仍要装健康看板(确保 up/down 状态可重放,而不是当下瞬态判断)
  - 工程审计:**17+ 核心机制真实跑**;**8 真缺口**;**Track 4 server/safety 域 70% 完整**(context 附录缺)
  - 报告数字:**534/4104→564/4565**(17 天漂移)/ **`bb8bbb9` CLAUDE.md 重写丢 3 段硬边界** / `NoeAffectHealth.js` 0 命中(宣传未实现) / `identity.tensions` 0 命中 / `NOE_AUTONOMY_PROFILE` 默认 22 开关全开(冲突 §0 铁律)
- **模型口径**:
  - Main = `qwen/qwen3.6-35b-a3b`(LM Studio live)
  - Review = `qwen/qwen3.6-27b`(已 live)
  - Fallback = `google/gemma-4-26b-a4b-qat`(已 live)
  - Embedding = LM Studio `text-embedding-nomic-embed-text-v1.5` 优先 + ollama `qwen3-embedding:0.6b` 备 + 128 维 hash 兜底
- **启动入口**: `npm run claude:neo`(opus-4-8 + xhigh + Neo system prompt,不碰 `~/.claude`、不自动接管 51835)
- **报告怎么读**: §1-§2 = 现状 + 总目标 / §3 = 阶段 0(本周,零/低风险) / §4 = 阶段 1(GWT/记忆/研究质量,1-2 周) / §5 = 阶段 2(自进化引擎 + 主动推理,2-4 周) / §6 = 阶段 3(达尔文 archive + 浏览器 ReAct,4 周+ 可自主放开) / §7 = 接缝补完(从工程审计来的 4 个 P0 + 4 周路线图) / §8 = 验收与红线 / §9 = 8 周时间线总览

---

## 1. 总目标与基线

### 1.1 8 周目标

**把 Neo 从「机制造好」推进到「活性证明」**——即:
- 七维能力从「成熟/部分」评级有 50% 以上跳到「持续运行已证明」评级(以 8 周真实 soak 数据为证)
- 双 embedding 后端(ollama + LM Studio)消除 ollama down = 语义能力失效单点
- 健康看板能让 ollama/LM Studio/三脑存活、降级状态、活性曲线实时可见
- 自我进化从「机制存在」推进到「自由放开真跑」,owner 一句话点火不阻塞
- 浏览器 ReAct loop 替代「评测机制薄」成为可量化自主时长(METR 风格)
- 4 周路线图(Project 8 周路线图阶段 A/B/C/D)真完结,有 soak evidence

### 1.2 量化验收(第 8 周末)

| 指标 | 基线(2026-06-14) | 目标(8 周末) |
|---|---|---|
| `verify:noe:full-current --include-managed` | 11/11 | 11/11(基线) |
| `verify:noe:self-evolution` | 205/207 | 207/207(2 失败非能力相关) |
| `audit:noe:self-evolution-completion --require-complete` | 10/11 + 1 incomplete | 11/11 |
| `verify:handoff` | 80/83 | 83/83 |
| `verify:noe:100-readiness` | score=94,passed=false(2 blockers) | score≥97,passed=true |
| 测试总数 | 4565 | ≥5000(新增 ~400 个) |
| ollama down 持续 | 不可见 | 健康看板 1 分钟内可见 |
| 自我进化真跑 | P8 门 readOnly | owner 自放开 + 1 次真 cycle 跑成 |
| embedding 降级率 | 100% 在 ollama down 时 | 0%(LM Studio 自动兜底) |
| curiosity-yield 漏斗 | source=surprise 恒 0 | ≥ 5% 月转化率(实测) |
| 浏览器自主时长 | 0 METR 风格度量 | 50% 任务能跑 50min(METR 锚点) |
| 进程 | 0 长期 live 跑 | 8 周连续 live 不断 |

### 1.3 模型口径(全程不变)

- **Main Brain**: `qwen/qwen3.6-35b-a3b` (LM Studio live,空闲慢档跑 sleep-time)
- **Review Brain**: `qwen/qwen3.6-27b` (高风险路径复核 + 自我进化归因 + 技能自验证)
- **Fallback Brain**: `google/gemma-4-26b-a4b-qat` (ollama down 时)
- **Embedding**: LM Studio `text-embedding-nomic-embed-text-v1.5` 优先 + ollama `qwen3-embedding:0.6b` 备 + 128 维 hash 兜底

---

## 2. 执行总原则(每步都遵守)

1. **TDD**: 先写失败测试(红)→ 实现 → 转绿;用隔离端口自测,不碰 live 51835 除非任务需要 live 验证
2. **小步变更**: 每完成一个独立改动立即 `npm test` 全绿;**commit/push** 只有 owner 明确批准才做(当前基线 ~561 文件全绿,只许 ≥ 且全绿)
3. **新文件三件套**: `// @ts-check` 头 + 注入式设计 + 配套单测
4. **文件 < 500 行**;老大文件(server.js 2546 / CrossVerifyDispatcher 3304)改到顺手拆,**但别为达标硬拆稳定大文件,也别往已超线文件继续加**
5. **fail-open 降级**: 依赖本地服务的能力,服务挂时降级不锁死、留审计标记
6. **env 门控**: 任何新功能默认 OFF,owner 在场 kickstart/首验后再考虑默认 ON(本任务里主要是新增能力,**默认朝「放开觉醒」走**,但保留 env 开关作逃生阀)
7. **多 AI 避撞**: 改公共文件前先 Read 当前内容(单 writer)
8. **做完即自检**: 每完成一个小任务,跑下面 §8.2 的自检命令;不通过不进入下一步
9. **凭据红线**: 不打印/写入/commit secret 原值(owner-token / .env / room-adapters.json)
10. **每个阶段完成 = 5 步交付**: 写测试(红)→ 实现(绿)→ 自检命令通过 → CHANGELOG 段写 → HANDOFF 段写(留下可验证证据)
11. **8 个长周期 ADR 决策**(A1-A7 在文档顶已列)在 §2.5 时段一次性写完

### 2.1 自检命令(每小任务完成后跑)

```bash
# 跑这 5 步
cd /Users/hxx/Desktop/Neo\ 贾维斯

# 1. 单元测试(快,5-15s)
npx vitest run tests/unit/<新文件>.test.js 2>&1 | tail -10
# 期望:全 PASS,含红→绿转换

# 2. 全量测试(慢,40-60s,只在跨域改动后跑)
npm test 2>&1 | tail -10
# 期望:全绿,新测试数 ≥ 基线

# 3. 隔离端口自测(本任务绝大多数用 51999,绝不用 51835)
PORT=51999 npm run start:noe &
PID=$!
sleep 5
# 调对应新接口
curl -s http://127.0.0.1:51999/api/<新接口> | python3 -m json.tool
kill $PID

# 4. live 看板查(只读,新功能必须能在 mind.html / output/ 看到)
# 如适用

# 5. CHANGELOG 段写(任何源码改动必写,否则 owner 看不到)
# 编辑 /Users/hxx/Desktop/Neo 贾维斯/CHANGELOG.md Unreleased 段
```

### 2.2 失败回滚

每小任务可逆:**commit 后发现 regression,`git reset --hard HEAD~1`**(OWNER 批准时才 commit,所以这条需要先 commit)。未 commit 前的回滚:`git checkout -- <file>` 或 `git stash`。

### 2.3 长周期 ADR(8 周起手第 0 周必写)

```bash
mkdir -p /Users/hxx/Desktop/Neo\ 贾维斯/docs/ADR
```

新文件 7 个 ADR(每条 1 文件,1-2 页,直接抄 7 条决策+依据,避免后续窗口重提):

- `ADR-2026-06-14-neo-long-cycle-7-decisions.md` — 7 条决策(A1-A7)合并
- 或拆成 7 文件 `ADR-2026-06-14-neo-{A1..A7}-{title}.md` — 选拆,后续可单独演进

---

## 3. 阶段 0 —— 修活性根基 + 接线孤儿(本周,零/低风险)

> 全部 = 接线/增强已有件,零或低回归风险,半天内可落地。**先做 S0.1 因多步依赖 embedding**。
> 阶段 0 与工程审计的 4 个 P0 修复并行(见 §7),本周交付后可跑 `verify:noe:100-readiness` 看 score 提升。

### S0.1 拉活 ollama + 健康看板首项 `[P0 · 0.5天 · 无模型]`

**任务**:把 ollama 拉活并嵌入健康看板,直接对冲工程审计的"存在≠活着"头号缺口。

**操作步骤**:
1. **拉活 ollama**:
   ```bash
   # 启动 ollama(若已装)
   ollama serve &
   OLLAMA_PID=$!
   sleep 3
   # 验活
   curl -s http://localhost:11434/api/tags | python3 -m json.tool | head -20
   # 期望:返回 200 + 已加载模型列表

   # 拉 Neo 依赖的 embedding + dream 模型(若未在)
   ollama pull qwen3-embedding:0.6b
   ollama pull huihui_ai/qwen3.5-abliterated:9b
   # 验
   ollama list
   # 期望:看到 3 个模型
   ```

2. **可选 watchdog 自愈**(P2,阶段 0 末可加):
   - 新文件:`src/watcher/OllamaHealthWatcher.js`(注入式,~80 行)
   - 接口: `startWatchdog({ checkIntervalMs, autostart })` 每 30s 探 `localhost:11434/api/tags`
   - 写日志到 `output/noe-model-health/ollama.jsonl`
   - env `NOE_OLLAMA_AUTOSTART=0` 默认 OFF(本任务**默认朝放开走**,但保留开关作逃生阀;若 owner 后续要求自动拉起才 ON)
   - 接入 `NoeHeartbeat.capabilityTick`(在 capabilityTick handler 里加一行)

3. **新文件**:`src/server/routes/noeLocalModelHealth.js`(注入式,无新依赖,只读):
   ```javascript
   // @ts-check
   import { collectModelHealthReport } from '../../runtime/NoeModelHealthProbe.js';

   export function registerLocalModelHealthRoutes(app, deps) {
     const requireOwnerToken = deps.requireOwnerToken;
     app.get('/api/noe/local-models/health', requireOwnerToken, async (req, res) => {
       try {
         const report = await collectModelHealthReport();
         res.json({ ok: true, report });
       } catch (e) {
         res.status(503).json({ ok: false, error: e.message });
       }
     });
   }
   ```

4. **新文件**:`src/runtime/NoeModelHealthProbe.js`(注入式,~120 行,纯探测):
   ```javascript
   // @ts-check
   // 复用 scripts/noe-model-health-snapshot.mjs 的 collectModelHealthReport,移植为注入式
   // 接口: collectModelHealthReport() → { ollama: {alive, models: []}, lmstudio: {alive, models: []}, qwen3_6_35b_alive, qwen3_6_27b_alive, gemma_4_26b_alive, embedding_backend_active, last_check }
   // 验:对 LM Studio 1234 + ollama 11434 各 ping 一次,失败不抛(返回 alive:false)
   ```

5. **改**:`public/mind.html` 在 brainDetails 区下方新增只读区"本地模型存活",渲染 6 个三脑 + embedding 状态(红/绿圆点)
   - 拉数据用 `fetch('/api/noe/local-models/health').then(...)` 在 mind.html 加载时调一次 + 每 30s 轮询
   - 加 `env NOE_MIND_MODEL_HEALTH=0` 默认 OFF(本任务**默认 OFF**,owner 首验后开)

6. **改**:`src/server/server.js` 在路由注册段加 `registerLocalModelHealthRoutes(app, { requireOwnerToken, logger })`

7. **改**:`package.json` scripts 加 `"noe:health:models": "node scripts/noe-model-health-snapshot.mjs"`,已存在则不重写

8. **CHANGELOG**: Unreleased 段加 2 条
   ```markdown
   ### Added
   - 本地模型存活看板: `GET /api/noe/local-models/health` 暴露三脑+ollama+LM Studio 实时状态 (`src/server/routes/noeLocalModelHealth.js` `src/runtime/NoeModelHealthProbe.js`)
   - ollama 健康 watchdog(可选自愈,默认 OFF): `src/watcher/OllamaHealthWatcher.js`
   ```

**自检(§2.1)**:
```bash
npx vitest run tests/unit/noe-model-health-probe.test.js  # 新测试:alive 探针 + 失败不抛
PORT=51999 npm run start:noe &
sleep 5
curl -s http://127.0.0.1:51999/api/noe/local-models/health -H "x-owner-token: $OWNER_TOKEN" | python3 -m json.tool
# 期望:三脑都 alive,embedding_backend_active 标 ollama
kill %1
```

**阶段末验收**:
- `curl http://localhost:11434/api/tags` 200
- mind.html 看 ollama + LM Studio + 三脑 6 个圆点全绿
- ollama 挂时 1 分钟内 mind.html 圆点变红
- `npm test` 全绿(基线 ≥ 564 tests)

---

### S0.2 接线孤儿 `NoeCuriosityDecompose` `[P0 · 0.5天 · 纯JS零依赖]`

**任务**:把"好奇"从单标量(n/8 观察密度)升级成 EFE 双因子(epistemic 信息增益 + pragmatic 贴 owner 偏好),可解释、可量化。

**操作步骤**:
1. **读** `src/cognition/NoeCuriosityDecompose.js`(174 行,纯函数 `curiosityScore` / `beliefEntropy` 已齐备)
2. **grep 确认孤儿**:
   ```bash
   grep -rn "NoeCuriosityDecompose" /Users/hxx/Desktop/Neo\ 贾维斯/src --include="*.js" | grep -v "NoeCuriosityDecompose.js"
   # 期望:0 命中(只有定义文件自身)
   ```
3. **改** `src/cognition/NoeDriveSystem.js` `readCuriosity()` 旁路注入(注入式,不破坏现有调用点):
   ```javascript
   // 在 server.js 装配 DriveSystem 的地方加 env 判断
   import { createNoeCuriosityDecompose } from './NoeCuriosityDecompose.js';
   const efeDecompose = (process.env.NOE_EFE_CURIOSITY === '1')
     ? createNoeCuriosityDecompose()
     : null;
   const driveSystem = createNoeDriveSystem({ ..., efeDecompose });
   ```
4. **DriveSystem 内部改**: 在 `readCuriosity()` 末尾加 `if (efeDecompose) return efeDecompose.curiosityScore({ observationDensity, recentSurprise, ownerTargetProximity })` 替/增强 n/8
5. **改** `src/cognition/NoeGoalSystem.js` `harvestSurprise()` 旁路:若 `efeDecompose` 存在,写进目标 `why` 字段(epistemic 主导/ pragmatic 主导)
6. **新文件** `tests/unit/noe-curiosity-decompose.test.js`(接 0,8 case 覆盖好奇分正负/边界)
7. **CHANGELOG**: Unreleased 加
   ```markdown
   ### Added
   - EFE 双因子好奇接进 DriveSystem: `NOE_EFE_CURIOSITY=1` 时好奇=epistemic 信息增益+pragmatic 贴偏好,OFF 走旧 n/8 零行为变化 (`src/cognition/NoeCuriosityDecompose.js` `src/cognition/NoeDriveSystem.js`)
   ```

**自检**:
```bash
npx vitest run tests/unit/noe-curiosity-decompose.test.js
# 期望:8/8 PASS

# 跑两次,验 OFF/ON 切换
NOE_EFE_CURIOSITY=0 PORT=51999 npm run start:noe &
sleep 5
curl -s http://127.0.0.1:51999/api/curiosity  # 旧接口
kill %1

NOE_EFE_CURIOSITY=1 PORT=51999 npm run start:noe &
sleep 5
curl -s http://127.0.0.1:51999/api/curiosity  # 新接口应返回双因子
kill %1
```

**验收**: OFF 行为不变,ON 时 `why` 字段含 "epistemic"/"pragmatic" 标签

---

### S0.3 接线孤儿 `NoeThoughtLoopGuard` `[P0 · 0.5天 · 确定性无模型]`

**任务**:把防连击守卫从只接 `NoeReasoningSearch` 扩到 `NoeWorkspace` 广播前 + `InnerMonologue` 反刍前,阻断"意识流打转"。

**操作步骤**:
1. **读** `src/cognition/NoeThoughtLoopGuard.js`(纯函数 `analyzeThoughtLoop` 已齐备)
2. **grep 现状**:
   ```bash
   grep -rn "analyzeThoughtLoop\|ThoughtLoopGuard" /Users/hxx/Desktop/Neo\ 贾维斯/src --include="*.js" | grep -v "NoeThoughtLoopGuard.js"
   # 期望:仅在 NoeReasoningSearch 出现
   ```
3. **改** `src/cognition/NoeWorkspace.js` 广播前加守卫(注入式):
   ```javascript
   // 在 finalCandidate.broadcast() 之前
   const loopCheck = (process.env.NOE_THOUGHT_LOOP_GUARD === '1')
     ? this.thoughtLoopGuard.analyzeThoughtLoop({ candidate, recentBroadcasts: this.recentBroadcasts })
     : { isLoop: false };
   if (loopCheck.isLoop && loopCheck.suggestedAction === 'block') {
     // 走 audit log + 跳过本次广播
     this.auditLog.push({ type: 'thought_loop_blocked', candidate, loopCheck });
     return null;
   }
   ```
4. **改** `src/loop/InnerMonologue.js` 反刍前同样加
5. **改** `src/runtime/NoeHeartbeat.js` `createNoeHeartbeat` 工厂把 `thoughtLoopGuard` 注入(默认 OFF 行为不变,ON 时启用)
6. **新文件** `tests/unit/noe-thought-loop-guard.test.js`(接 0,10 case 覆盖 isLoop=true/false/边界)
7. **CHANGELOG**:
   ```markdown
   ### Added
   - NoeThoughtLoopGuard 接线到 Workspace/InnerMonologue: `NOE_THOUGHT_LOOP_GUARD=1` 时防意识流打转,OFF 零行为变化 (`src/cognition/NoeThoughtLoopGuard.js` `src/cognition/NoeWorkspace.js` `src/loop/InnerMonologue.js`)
   ```

**自检**: `npx vitest run tests/unit/noe-thought-loop-guard.test.js` 期望 10/10

**验收**: ON 隔离端口跑长 query,看 audit log 是否记录 thought_loop_blocked

---

### S0.4 source=surprise 活性看板 + live 冒烟 `[P1 · 0.5-1天 · 无模型]`

**任务**:把"预测落空→主动学习"回路从"机制存在但生产恒 0"变可见、可发动,真把 source=surprise 目标烧起来。

**操作步骤**:
1. **读** `scripts/noe-expectation-settlement-drill.mjs` 已有逻辑(隔离验证账本可结算 ≥20 项算 Brier)
2. **新文件** `scripts/noe-curiosity-yield-live.mjs`(只读,纯 SQL 统计,~80 行):
   ```javascript
   // @ts-check
   // 读 panel.db 统计: 期望立 N 条 / 自动判证 M 条 / FAILED (outcome=0) K 条 / harvestSurprise 触发 K' 条 / 完成 J 条
   // 输出 JSON 到 output/noe-curiosity-yield/live-stats.json
   // 输出 markdown 表格到 output/noe-curiosity-yield/live-stats.md(便于 mind.html 渲染)
   ```
3. **改** `package.json` scripts 加 `"noe:curiosity-yield": "node scripts/noe-curiosity-yield-live.mjs"`
4. **改** `public/mind.html` 新增只读区"好奇活性"显示 5 数: 立/判证/FAILED/触发/完成
5. **定位 outcome===0 罕见的原因**: 读 `src/cognition/NoeExpectationResolver.js:1338` 证据门
6. **若证据门过严** (大概率): 适度放宽自动判证 / 扩 FAILED 信号词(只读统计,不动裁决逻辑)
7. **绝不动**: 任何虚构结算的代码(仅让真实落空可见可发动)
8. **新文件** `tests/unit/noe-curiosity-yield-stats.test.js`(纯逻辑测试,验证统计函数正确)
9. **CHANGELOG**:
   ```markdown
   ### Added
   - curiosity-yield 活性看板: `npm run noe:curiosity-yield` 统计 live panel.db 期望立/判证/FAILED/触发/完成漏斗,`public/mind.html` 新区显示 (`scripts/noe-curiosity-yield-live.mjs`)
   ```

**自检**:
```bash
npm run noe:curiosity-yield
cat output/noe-curiosity-yield/live-stats.md
# 期望:5 个数字 + 时间戳

npx vitest run tests/unit/noe-curiosity-yield-stats.test.js
# 期望:全 PASS
```

**验收**: 看板显示真实数字(不恒 0);若 source=surprise 仍恒 0,本任务后续每周复测

---

### S0.5 holdout 评测器加 embedding 语义分 `[P1 · 0.5天 · 依赖 S0.1]`

**任务**:堵"塞关键词式 reward hacking",让自进化选择压力可信。

**操作步骤**:
1. **读** `src/room/NoeEvolutionHoldoutRunner.js:20-32` `scoreNoeHoldoutOutput` 当前只 string-include
2. **改** 加 embedding 语义分分支(注入式,不改原函数体):
   ```javascript
   // 新方法 scoreWithSemantic(holdout, candidateOutput, embed)
   // 保留原 string-include 作硬门(若不通过直接 0)
   // 加上 embed(holdout.expectedAnswer) vs embed(candidateOutput) 余弦相似度作连续分
   // 加权进总分
   ```
3. **新文件** `src/embeddings/EmbeddingProvider.js` 扩(若 S0.1 还没动):
   - 已有 ollamaEmbed + hash 兜底
   - 新增 `lmstudioEmbed(text)`: POST `http://127.0.0.1:1234/v1/embeddings` model=`text-embedding-nomic-embed-text-v1.5`(实测 768 维)
   - 新增三级降级链 `embed(text)`: 读 `NOE_EMBED_FALLBACK_CHAIN`(默认空=不启用;设 `'lmstudio,hash'` 才启用)
   - 维度兼容: ollama 1024 维 vs lmstudio 768 维,VectorIndex 已有 `dim` 列过滤;切后端需 trigger SemanticBackfill 或按 model 列分桶
4. **新文件** `tests/unit/noe-embedding-provider.test.js`(8 case)+ `tests/unit/noe-holdout-semantic-score.test.js`(6 case)
5. **CHANGELOG**:
   ```markdown
   ### Added
   - EmbeddingProvider 三级降级: ollama→lmstudio→hash,`NOE_EMBED_FALLBACK_CHAIN=lmstudio,hash` 启用 (`src/embeddings/EmbeddingProvider.js`)
   - holdout 评测器加 embedding 语义分(连续分+include 硬门): `NOE_HOLDOUT_SEMANTIC_SCORE=1` 时启用 (`src/room/NoeEvolutionHoldoutRunner.js`)
   ```

**自检**:
```bash
# ollama down 时验证 LM Studio 兜底
kill $(lsof -t -i:11434) 2>/dev/null
NOE_EMBED_FALLBACK_CHAIN=lmstudio,hash node -e "
import('./src/embeddings/EmbeddingProvider.js').then(async m => {
  const r = await m.embed('test text');
  console.log('embed dim:', r.dim, 'backend:', r.backend);
});
" 2>&1 | tail -5
# 期望:dim=768,backend=lmstudio
npx vitest run tests/unit/noe-embedding-provider.test.js
# 期望:8/8
```

**验收**: ollama down 时 embed 仍能返回 768 维 LM Studio 向量;holdout 评分能挡塞关键词

---

### S0.6 实测 noe-llm-bench `[P1 · 0.5天 · Main Brain]`

**任务**:把"M5 Max 上 qwen3.6-35b 真实 TTFT/tok/s/内存"从"估"变实测。

**操作步骤**:
1. **读** `scripts/noe-llm-bench.mjs`(现成)
2. **跑**:
   ```bash
   cd /Users/hxx/Desktop/Neo\ 贾维斯
   node scripts/noe-llm-bench.mjs \
     --models 'qwen/qwen3.6-35b-a3b,qwen/qwen3.6-27b,gemma-4-26b-a4b-qat' \
     --ctx 1024,8192,32768 \
     --gen 256 \
     --out docs/基准_M5Max_三脑实测_2026-06-14.md
   ```
3. **CHANGELOG**:
   ```markdown
   ### Changed
   - M5 Max 三脑实测落档: 真实 TTFT/tok·s⁻¹/内存,替换 DESIGN §9 「估」数字 (`docs/基准_M5Max_三脑实测_2026-06-14.md`)
   ```

**验收**: docs/ 下新文件存在,含 3 脑 × 3 ctx × 4 metric 实测数字

---

### S0.7 抽魔数为注入式参数 `[P1 · 0.5天 · 无模型]`

**任务**:为阶段 2 GEPA 参数进化铺好"可优化对象"。

**操作步骤**:
1. **读** `src/cognition/NoeWorkspace.js:26` 显著度打分权重(4 个)
2. **读** `src/cognition/NoeExpectationResolver.js` surprise 阈值
3. **改** 改为 env 注入 + 默认值不变:
   ```javascript
   const WEIGHTS = {
     owner: parseFloat(process.env.NOE_WS_OWNER_W || '0.35'),
     urgency: parseFloat(process.env.NOE_WS_URGENCY_W || '0.25'),
     novelty: parseFloat(process.env.NOE_WS_NOVELTY_W || '0.2'),
     affect: parseFloat(process.env.NOE_WS_AFFECT_W || '0.2')
   };
   ```
4. **新文件** `tests/unit/noe-workspace-weights-injection.test.js`(6 case 覆盖 env 注入 + 默认值)
5. **CHANGELOG**:
   ```markdown
   ### Changed
   - NoeWorkspace 显著度权重抽 env 注入(`NOE_WS_OWNER_W` 等),默认值不变 = 零行为变化 (`src/cognition/NoeWorkspace.js`)
   ```

**自检**:`NOE_WS_OWNER_W=0.5` 重启,验 owner 信号分显著抬高

**验收**: OFF/默认零行为变化,ON 时可调

---

### S0.8 配 SearXNG 本地搜索兜底 `[P1 · 0.5天 · 无模型]`

**任务**:WebSearch 三级链有离线/隐私兜底。

**操作步骤**:
1. **docker 跑 SearXNG**:
   ```bash
   docker run -d --name searxng -p 8888:8080 \
     -e SEARXNG_SECRET=changeme \
     searxng/searxng
   # 验
   curl -s "http://localhost:8888/search?q=test&format=json" | head -c 200
   ```
2. **改** `~/.env` Neo 项目:`NOE_SEARXNG_URL=http://localhost:8888`
3. **读** `src/research/WebSearch.js:196` 三级链:已有 ollama→SearXNG→Brave,验代码是否已支持(env 读 `NOE_SEARXNG_URL`)
4. **CHANGELOG**:
   ```markdown
   ### Added
   - SearXNG 本地搜索兜底: `NOE_SEARXNG_URL=http://localhost:8888` 启用 (`src/research/WebSearch.js`)
   ```

**验收**: WebSearch 三级链全 OK,SearXNG 兜底可接,断 Brave 仍能搜

---

### 阶段 0 总收(本周末)

- 8 个 S0.x 全部 P0/P1 完工
- 8 周 ADR 7 条决策落档
- `npm test` 全绿(基线 ≥564,新增 ~40 个)
- mind.html 含"本地模型存活"+ "好奇活性" 2 个新区
- `npm run noe:curiosity-yield` 返回真实漏斗数字
- ollama 拉活(若 S0.1 已做)+ LM Studio 兜底 active
- ollama down 时 embed 仍能 768 维 LM Studio 兜底
- 7 份 ADR 决策落档
- CHANGELOG Unreleased 段 +8 条 Added +3 条 Changed
- HANDOFF 段写明"阶段 0 完成、本地模型存活可见、好奇活性可烧"

---

## 4. 阶段 1 —— GWT/记忆/研究质量补强(1-2 周,中风险)

> 前置: S0.1 (ollama 活) + S0.5 (embedding 通)
> 模型依赖: qwen3-embedding (Phase S0.5 已通) + Main Brain (空闲慢档)
> 关键交付: GWT novelty 升级 embedding 语义相似度 / sleep-time 另一半 / Crawl4AI 抓取 / DeepResearcher Reflexion

### S1.1 GWT novelty 升级 embedding 语义相似度 `[P1 · 1天 · qwen3-embedding]`

**任务**:把"广播什么"用语义相似度判定,字符相似度作 fallback。

**操作步骤**:
1. **读** `src/cognition/NoeWorkspace.js` novelty 分支(目前 `textSimilarity` 字符级别)
2. **改** 注入式:
   ```javascript
   const novelty = (process.env.NOE_GWT_EMBED_NOVELTY === '1')
     ? await this.embed.embedCosine(candidate, recentBroadcasts)
     : textSimilarity(candidate, recentBroadcasts);
   ```
3. **micro tick 预缓存** `recentBroadcasts` 向量(`NoeHeartbeat` capabilityTick 加一行 `cacheRecentBroadcastEmbeddings(recentBroadcasts, embed)`)
4. **新文件** `tests/unit/noe-workspace-novelty-embed.test.js`(8 case 覆盖语义 vs 字符 fallback)
5. **CHANGELOG**:
   ```markdown
   ### Changed
   - GWT novelty 升级 embedding 语义相似度: `NOE_GWT_EMBED_NOVELTY=1` 时用 qwen3-embedding 余弦距离,OFF 走旧字符相似度 (`src/cognition/NoeWorkspace.js`)
   ```

**自检**: 隔离端口开 env,跑同 query 两次,第二次 novelty 显著低于 0.2 → 落选

---

### S1.2 sleep-time 另一半 `NoeSleepTimeCompute` `[P1 · 2-3天 · Main Brain]`

**任务**:空闲用 Main Brain 慢档做"消化 + 预判预计算",吃 Letta token 降 ~5x + 准确率 +13-18% 红利。

**操作步骤**:
1. **新文件** `src/loop/NoeSleepTimeCompute.js`(<200 行,薄编排):
   ```javascript
   // @ts-check
   // 注入式
   // 接口:
   //   tickIdle(mainBrain, recentHighSalience, ownerBehavior)
   //   内部: ①把 recentHighSalience 蒸馏成已学知识(dual-buffer 升格 semantic,非反复 summarize)
   //          ②预判 owner 下一类问题 → 预跑检索写 NoePrefetchStore
   //          ③预测下注进 NoeExpectationLedger 自校准(顺带补 S0.4 source=surprise 活火)
   // env NOE_SLEEPTIME_STUDY 默认 OFF(本任务默认 OFF 起步,owner 首验后开)
   ```
2. **改** `src/loop/NoeHeartbeat.js` 在 `maintenance` job 末加 `if (sleepTimeStudy && isIdle) await sleepTimeStudy.tickIdle(mainBrain, ...)`
3. **改** `src/memory/NoePrefetchStore.js` 加 `putPrefetch(query, results)` 注入式 API
4. **改** `src/server/server.js` TurnContextEngine 在 LLM 调用前 `await prefetchStore.getPrefetch(currentQuery)`,命中即秒答 + 写 expectation
5. **新文件** `tests/unit/noe-sleep-time-compute.test.js`(8 case 覆盖 tickIdle 入口/预判缓存写)
6. **CHANGELOG**:
   ```markdown
   ### Added
   - sleep-time 另一半: `NoeSleepTimeCompute.tickIdle()` 空闲做消化+预计算+期望自校准,`NOE_SLEEPTIME_STUDY=1` 启用 (`src/loop/NoeSleepTimeCompute.js` `src/loop/NoeHeartbeat.js`)
   ```

**自检**: 隔离端口开 env,跑 10 个 query 后 sleepTimeStudy.tickIdle(),看 `output/noe-prefetch/` 缓存有 ≥ 5 条

---

### S1.3 Crawl4AI 抓取层三级降级 `[P1 · 1-2天 · 无模型]`

**任务**:把 JS 重站退 snippet 的 `extractMainText` 升级,正文质量从 snippet → Markdown。

**操作步骤**:
1. **装** Crawl4AI:`pip install crawl4ai` 或 `npm i @crawl4ai/js`(选有维护的版本)
2. **新文件** `src/research/Crawl4aiExtractor.js`(注入式,若 Python 版,加 spawn 桥):
   ```javascript
   // 接口: extractMarkdown(url) → { markdown, title, byline }
   // 失败 → Readability fallback → 旧 extractMainText
   ```
3. **改** `src/research/WebSearch.js:fetchContent` 注入 Crawl4aiExtractor 作第一级,Readability 第二,正则第三
4. **新文件** `tests/unit/noe-crawl4ai-fallback.test.js`(5 case 覆盖三级降级)
5. **CHANGELOG**:
   ```markdown
   ### Changed
   - 抓取层升级: Crawl4AI → Readability → 正则三级降级 (`src/research/WebSearch.js` `src/research/Crawl4aiExtractor.js`)
   ```

---

### S1.4 DeepResearcher Reflexion 闭环 `[P2 · 1.5天 · Fallback/Main]`

**任务**:研究结束写 `EpisodicTimeline` retro,下次同主题塞 prompt。

**操作步骤**:
1. **读** `src/research/DeepResearcher.js:80`
2. **改** `chat()` 末加:
   ```javascript
   // 研究结束
   await episodicTimeline.appendRetro({ topic, reflections, evidenceRefs });
   ```
3. **改** `chat()` 起始: 同主题召回 retro 塞 prompt:"上次同类研究总结: ..."
4. **新文件** `tests/unit/noe-deep-researcher-reflexion.test.js`(6 case)
5. **CHANGELOG**:
   ```markdown
   ### Added
   - DeepResearcher Reflexion 闭环: 研究结束写 EpisodicTimeline retro,下次同主题塞 prompt (`src/research/DeepResearcher.js`)
   ```

---

### 阶段 1 总收(第 2 周末)

- 4 个 S1.x 完工
- GWT novelty 用 embedding 语义分
- sleep-time 另一半跑通,token 降 ~5x(实测对比)
- 抓取层 Crawl4AI 升级
- DeepResearcher Reflexion 闭环
- `npm test` 全绿,新增 ~30 个

---

## 5. 阶段 2 —— 自我进化引擎 + 主动推理回路(2-4 周,中高风险)

> 前置: S0.7 (魔数抽出) + S1.x
> 模型依赖: Main Brain (变异) + qwen3-embedding (评测) + NoeReflectBrain
> 关键交付: GEPA 参数自进化 / RPT 真递归 / 主动推理 / runtimeVerify 能力回归门 / SkillExtractor 自验证

### S2.1 GEPA 式 `NoeReflectiveTuner` 参数自进化 PoC `[P1 · 3-4天 · Main Brain + qwen3-embedding]`

**任务**:读失败轨迹→提新参数→跑语义 holdout→Pareto 选优→存 archive。

**操作步骤**:
1. **新文件** `src/room/NoeReflectiveTuner.js`(<300 行):
   ```javascript
   // @ts-check
   // 接口:
   //   proposeParams(failedTrajectories, currentParams) → candidateParams[]
   //   evaluateAgainstHoldout(candidateParams) → { semanticScore, brierImprovement }
   //   paretoSelect(candidates) → best
   //   archive(cycle) → { n, parent, params, metrics }
   // 不动权重,只调 Workspace 权重 + 好奇阈值(S0.7 抽出后才有可调对象)
   ```
2. **依赖**: S0.5 (embedding 语义分 holdout) + S0.7 (魔数可调) + S0.4 (真实漏斗可烧)
3. **新文件** `tests/unit/noe-reflective-tuner.test.js`(10 case 覆盖 Pareto 选优 + archive 谱系)
4. **CHANGELOG**:
   ```markdown
   ### Added
   - GEPA 式参数自进化 PoC: `NoeReflectiveTuner` 读失败轨迹→提新参数→语义 holdout→Pareto 选优,`NOE_GEPA_OPT=1` 启用 (`src/room/NoeReflectiveTuner.js`)
   ```

---

### S2.2 RPT 真递归(Workspace 广播赢家→ReflectBrain 再入重评) `[P2 · 1-2天 · NoeReflectBrain]`

**任务**:每广播赢家做一次 ReflectBrain 再入重评,真递归不空转。

**操作步骤**:
1. **读** `src/cognition/NoeReflectBrain.js`(已建)
2. **改** `src/cognition/NoeWorkspace.js` 广播后:
   ```javascript
   if (process.env.NOE_RPT_REENTER === '1') {
     const revised = await reflectBrain.revise(broadcastedWinner, { timeBudgetMs: 100 });
     if (revised.scoreDelta > 0) { /* 用 revised 替 */ }
   }
   ```
3. **预算闸防 tick 爆炸**:`revise()` 内 `if (Date.now() - start > 100) return original`
4. **新文件** `tests/unit/noe-workspace-rpt-reenter.test.js`(5 case 覆盖预算闸 + 改进路径)
5. **CHANGELOG**:
   ```markdown
   ### Added
   - RPT 真递归: Workspace 广播赢家触发 ReflectBrain 再入重评+预算闸,`NOE_RPT_REENTER=1` 启用 (`src/cognition/NoeWorkspace.js` `src/cognition/NoeReflectBrain.js`)
   ```

---

### S2.3 主动推理显式回路(DriveSystem EFE + 期望账本 PP-2) `[P2 · 2-3天 · 确定性 + 语义]`

**任务**:把 DriveSystem EFE 双因子 + 期望账本补"精度/置信"字段串成主动推理回路。

**操作步骤**:
1. **读** `src/cognition/NoeExpectationLedger.js`(224 行)+ `src/cognition/NoeCuriosityDecompose.js`(S0.2 已接)
2. **改** `NoeExpectationLedger` 加字段:
   ```javascript
   // 期望条目: id, predicted, outcome, surprise, brier, precision, confidence
   ```
3. **改** DriveSystem:好奇驱动 + 期望精度/置信度共同决定目标立项
4. **新文件** `tests/unit/noe-drive-pp2.test.js`(6 case 覆盖精度/置信度)
5. **CHANGELOG**:
   ```markdown
   ### Changed
   - 主动推理 PP-2 回路: 期望账本补 precision/confidence 字段,DriveSystem 接 EFE 双因子 (`src/cognition/NoeExpectationLedger.js` `src/cognition/NoeDriveSystem.js`)
   ```

---

### S2.4 runtimeVerify 加能力基准回归门 `[P2 · 2天 · 本地记忆基准]`

**任务**:`npm test` 过后跑记忆召回/relevance baseline 对比,跌破阈值则 rollback。

**操作步骤**:
1. **读** `src/loop/NoeSelfEvolutionExecutors.js` runtimeVerify
2. **改** 加 baseline 对比:
   ```javascript
   // npm test 之后
   const before = loadMemoryRecallBaseline();
   const after = await runMemoryRecallBenchmark();
   if (after.p_at_5 < before.p_at_5 - 0.05) {
     throw new Error('Memory recall regressed, rolling back');
   }
   ```
3. **新文件** `tests/integration/noe-runtime-verify-memory-regress.test.js`(4 case)
4. **CHANGELOG**:
   ```markdown
   ### Added
   - runtimeVerify 能力基准回归门: `npm test` 后跑 memory recall,跌破 5% 自动 rollback (`src/loop/NoeSelfEvolutionExecutors.js`)
   ```

---

### S2.5 SkillExtractor 改 Voyager 式自验证后入库 `[P2 · 1-2天 · Review Brain]`

**任务**:LLM 提炼技能后,Review Brain 给 JSON verdict,verdict=PASS 才入库。

**操作步骤**:
1. **读** `src/room/SkillExtractor.js`(48 行)+ `src/runtime/NoeFreedomReviewBrain.js`
2. **改** `SkillExtractor.extract()` 末加:
   ```javascript
   if (process.env.NOE_SKILL_SELF_VERIFY === '1') {
     const verdict = await reviewBrain.jsonVerdict({ skillBody, prompt: '...' });
     if (verdict.verdict !== 'PASS') skill.extra.unverified = true;
   }
   ```
3. **新文件** `tests/unit/noe-skill-self-verify.test.js`(6 case)
4. **CHANGELOG**:
   ```markdown
   ### Added
   - SkillExtractor Voyager 式自验证: `NOE_SKILL_SELF_VERIFY=1` 时 Review Brain 给 verdict (`src/room/SkillExtractor.js`)
   ```

---

### 阶段 2 总收(第 4 周末)

- 5 个 S2.x 完工
- GEPA 参数自进化跑通
- RPT 真递归有效
- 主动推理 PP-2 回路串成
- 能力回归门接入 runtimeVerify
- 技能自验证 Quality 闸
- `npm test` 全绿,新增 ~50 个
- **工程审计 P0/P1 修复全部完成**: CLAUDE.md 修回归 / NoeAffectHealth 删 / 7 决策 ADR 落档

---

## 6. 阶段 3 —— 达尔文 archive + 浏览器 ReAct + 测评闭环(4 周+,owner 可自主放开,不强制等 P8 门/在场)

> 关键交付: 进化多踏脚石分叉(从单 Cycle 贪心) / 浏览器 ReAct loop 替代「评测机制薄」 / DMTS/WCST 意识能力基准 / 客观内省探针 / research MCP server / 缺口→自进化目标自动桥
> **owner 可在阶段 3 中任一步自由放开 P8 门真跑自进化(读 owner 表情/手势/嘴型/点头摇头)**

### S3.1 单 Cycle 升级达尔文 archive 多踏脚石分叉 `[P2 · 1周 · 无]`

**任务**:`CycleStore` 一目标一 cycle 单线贪心 → 多踏脚石 archive + parentRef 谱系。

**操作步骤**:
1. **新文件** `src/room/NoeEvolutionArchive.js`(<300 行):
   ```javascript
   // 接口:
   //   fork(cycleId, mutations) → archiveId
   //   record(archiveId, cycle, parentRef)
   //   query({ limit, sortBy }) → topPerformers
   //   rollback(archiveId) → restore to parentRef state
   ```
2. **改** `src/room/NoeSelfEvolutionCycle.js` 升级 CycleStore 调用
3. **新文件** `tests/unit/noe-evolution-archive.test.js`(8 case 谱系 + 回滚)
4. **CHANGELOG**:
   ```markdown
   ### Added
   - 达尔文 archive 多踏脚石分叉: parentRef 谱系 + 概率从历史变体分叉,所有现有安全门一个不拆 (`src/room/NoeEvolutionArchive.js`)
   ```

---

### S3.2 浏览器 ReAct loop `NoeBrowserAgent` `[P3 · 3-5天 · VLM + Main Brain]`

**任务**:probe→reason→dom.execute→校验→重试,直接完整 DOM 操控/表单/点击。

**操作步骤**:
1. **新文件** `src/runtime/NoeBrowserAgent.js`(<400 行,基于 NoeFreedomAdapters 已有的真 click/setValue 操控层)
2. **新文件** `tests/integration/noe-browser-agent.test.js`(8 case 跑 5 个网页 sandbox 任务)
3. **CHANGELOG**:
   ```markdown
   ### Added
   - 浏览器 ReAct loop: probe→reason→dom.execute→校验→重试,基于真 click/setValue 操控层 (`src/runtime/NoeBrowserAgent.js`)
   ```

---

### S3.3 DMTS/WCST 意识能力基准 + SimpleQA/BrowseComp 自评闭环 `[P3 · 各 2-3天]`

**任务**:纳入 perf-check 回归 + METR 风格 50min 任务时长度量。

**操作步骤**:
1. **新文件** `tests/benchmarks/noe-dmts-wcst.test.js`(借 The Consciousness AI 565 测试设计子集,50 case)
2. **新文件** `scripts/noe-metr-50min-tasks.mjs`(5 个 50min 任务,度量成功率)
3. **改** `package.json` `perf-check` 加 2 条
4. **CHANGELOG**:
   ```markdown
   ### Added
   - DMTS/WCST 意识能力基准: 50 case 本地子集,纳入 perf-check (`tests/benchmarks/noe-dmts-wcst.test.js`)
   - METR 50min 任务时长度量: 5 个任务跑成功率 (`scripts/noe-metr-50min-tasks.mjs`)
   ```

---

### S3.4 客观内省探针 `noe-introspection-probe` `[P3 · 3-5天]`

**任务**:本地小模型激活注入验自我报告真伪(Anthropic 思维注入思路)。

**操作步骤**:
1. **新文件** `scripts/noe-introspection-probe.mjs`(<300 行)
2. **新文件** `tests/benchmarks/noe-introspection-probe.test.js`(10 case)
3. **CHANGELOG**:
   ```markdown
   ### Added
   - 客观内省探针: 激活注入验自我报告真伪 (`scripts/noe-introspection-probe.mjs`)
   ```

---

### S3.5 research 封 MCP server `[P3 · 1-2天]`

**任务**:暴露 web_search/fetch_page/deep_research 供外部 Claude/Codex 统一调。

**操作步骤**:
1. **新文件** `src/mcp/server.js` MCP 协议,暴露 3 工具
2. **新文件** `tests/unit/noe-mcp-server.test.js`(6 case)
3. **CHANGELOG**:
   ```markdown
   ### Added
   - research MCP server: 暴露 web_search/fetch_page/deep_research 工具 (`src/mcp/server.js`)
   ```

---

### S3.6 缺口→自进化目标自动桥 `[P3 · 2-3天]`

**任务**:能力基准回归/重复失败/高 surprise 归因→立 source=self_evolution + 可**直接点火真改自己代码**(owner 自主)。

**操作步骤**:
1. **改** `src/room/NoeSelfEvolutionTrigger.js` 扩 3 类硬信号触发
2. **新文件** `tests/unit/noe-self-evolution-bridge.test.js`(8 case)
3. **CHANGELOG**:
   ```markdown
   ### Added
   - 缺口→自进化自动桥: 能力回归/重复失败/高 surprise 归因→立 source=self_evolution (`src/room/NoeSelfEvolutionTrigger.js`)
   ```

---

### 阶段 3 总收(第 8 周末)

- 6 个 S3.x 完工
- 自进化多踏脚石分叉
- 浏览器 ReAct + 测评闭环
- **8 周 soak evidence 积累,verify:noe:100-readiness 100% 跑通**
- `npm test` 全绿(基线 4565 + 新增 ~200 = ~4765)
- 8 周活数据填完所有活性证据缺口
- CHANGELOG Unreleased 段完整记录所有 S0-S3 改动

---

## 7. 接缝补完 — 工程审计 4 个 P0 + 4 周路线图(与 §3 阶段 0 并行)

> 这些与 RESEARCH 报告的 §6 quickWins 互补,是工程审计发现的"修活性根基"之外另一类"补齐 4 周路线图"动作。本周与 S0.1-S0.8 并行。

### P0-A 修 CLAUDE.md 回归 + 接手必读指针 `[0.5天 · 紧急]`

**任务**:CLAUDE.md bb8bbb9 重写丢了 3 段硬边界,补回去。

**操作步骤**:
1. **改** `/Users/hxx/Desktop/Neo 贾维斯/CLAUDE.md` 顶部加 §0 宪法锚点(指向 `AGENTS.md` 第 5-22 行)+ 接手必读指针段
2. **新文件** `docs/ADR/ADR-2026-06-12-noe-hard-boundaries.md`(写 6/12 硬边界 4 条)
3. **CHANGELOG**:
   ```markdown
   ### Fixed
   - CLAUDE.md 补 §0 宪法锚点 + 接手必读指针(`docs/ADR/ADR-2026-06-12-noe-hard-boundaries.md`)
   ```

---

### P0-B 删 NoeAffectHealth 宣传 `[0.5天 · 紧急]`

**任务**:NoeAffectHealth.js 0 命中(宣传未实现),删 4 处。

**操作步骤**:
1. **删** `CONTEXT_FOR_AI.md` 第 126-128 行 + `PROJECT_INTRO_FOR_GPT.md` 第 43 行
2. **新文件** `docs/ADR/ADR-2026-06-14-delete-noeaffecthealth-propaganda.md`
3. **CHANGELOG**:
   ```markdown
   ### Fixed
   - 删 NoeAffectHealth 宣传(代码 0 命中,纯靠命名营造存在假象) (`docs/ADR/ADR-2026-06-14-delete-noeaffecthealth-propaganda.md`)
   ```

---

### P0-C `NOE_AUTONOMY_PROFILE=off` 显式锁 `[5分钟 · 紧急]`

**任务**:默认 22 开关全开冲突 §0 铁律,显式锁 off。

**操作步骤**:
1. **改** `~/.env` Neo 项目加 `NOE_AUTONOMY_PROFILE=off`
2. **CHANGELOG**:
   ```markdown
   ### Fixed
   - `.env` 显式锁 `NOE_AUTONOMY_PROFILE=off`(22 开关默认全开冲突 §0 铁律)
   ```

---

### W1 整合度接 heart + 叙事自我真装 `[3天]`

**任务**:NoeIntegrationMetric 真接 NoeHeartbeat,NoeNarrativeSelf 真装 server boot。

**操作步骤**:
1. **改** `src/loop/NoeHeartbeat.js` meso tick 末加 `captureIntegrationMetric(collectSixMacroNodes())` 写 `output/noe-metrics/integration.jsonl`
2. **新文件** `src/server/routes/narrative.js` `GET /api/narrative/current` + `POST /api/narrative/chapter`
3. **改** `src/server/server.js` 启动时实例化 NoeNarrativeSelf,接 system prompt `<noe-self-state>` 块 narrative 字段
4. **新文件** `tests/unit/noe-integration-metric-tick.test.js`(4 case)+ `tests/integration/noe-narrative-routes.test.js`(6 case)
5. **CHANGELOG**:
   ```markdown
   ### Added
   - 整合度接 heart: `NoeIntegrationMetric` 由 meso tick 触发,写 `output/noe-metrics/integration.jsonl` (`src/loop/NoeHeartbeat.js`)
   - 叙事自我真装: `GET /api/narrative/current` 暴露当前章节 (`src/server/routes/narrative.js` `src/server/server.js`)
   ```

---

### W2 `identity.tensions` 实现 + CrossVerifyDispatcher 拆分启动 `[4天]`

**任务**:Conway 论证 tensions 先于 values,先做 tensions;顺手拆 3304 行超线大文件。

**操作步骤**:
1. **新文件** `src/identity/NoeIdentityTensions.js`(<150 行):`recordTension / getActiveTensions / resolveTension`
2. **新文件** `src/identity/NoeIdentityValues.js`(<100 行,延后 W4 做):`registerValue / rankByConflict / getActiveValues`
3. **改** `NoeSelfModel.js` 三段新增 `tensions` 字段(在 `state` 段下)
4. **新文件** `tests/unit/noe-identity-tensions.test.js`(8 case)
5. **拆 CrossVerifyDispatcher 3304 → 4 文件**: `cluster-stages.js` + `cluster-delivery.js` + `cluster-prompts.js` + `CrossVerifyDispatcher.js` 主 facade < 1000 行
6. **CHANGELOG**:
   ```markdown
   ### Added
   - identity.tensions: 内矛盾裁决栈 (`src/identity/NoeIdentityTensions.js`)
   ### Changed
   - CrossVerifyDispatcher 拆 4 文件: 主 facade 2546→800 行,3 cluster 各 < 800 行 (`src/room/CrossVerifyDispatcher.js` `src/room/cluster-*.js`)
   ```

---

### W3 NoeBootSelfCheck 7 条 policy 实测 + Mission Runner PoC `[5天]`

**任务**:7 条 policy 真在 51835 live 启动时跑;Mission Runner PoC P8-M0。

**操作步骤**:
1. **新文件** `tests/unit/noe-boot-self-check.test.js`(7 case:7 条 policy 各一)
2. **新文件** `src/runtime/NoeMissionRunner.js`(<200 行):`runMission / getStatus / _writeAudit`,接 `NoeGoalSystem` + `NoeFreedomExecutor.runReviewBrainGate`
3. **新文件** `tests/integration/noe-mission-runner.test.js`(5 case:3-step mission 5 次跑 4/5 success)
4. **CHANGELOG**:
   ```markdown
   ### Added
   - NoeBootSelfCheck 7 条 policy 单元测试 (`tests/unit/noe-boot-self-check.test.js`)
   - Mission Runner PoC: 3-step mission + abort 逻辑 + audit log (`src/runtime/NoeMissionRunner.js`)
   ```

---

### W4 identity.values + narrative 章节化 + 100-readiness 重测 `[5天]`

**任务**:values 补完 tensions 收尾;NoeNarrativeSelf 章节化;最终验收。

**操作步骤**:
1. **新文件** `src/identity/NoeIdentityValues.js`(<100 行):接 S2.2 tensions
2. **改** `src/context/NoeNarrativeSelf.js` 加 `appendChapter`,写 `output/noe-narrative/chapter-NNN.json`
3. **跑 3 次 self-evolution cycle** 生成 3 chapter,验 `chapter-3` 引用 `chapter-1/2`
4. **跑** `npm run verify:noe:100-readiness` 期望 score≥97,passed=true
5. **CHANGELOG**:
   ```markdown
   ### Added
   - identity.values: 价值层级 (`src/identity/NoeIdentityValues.js`)
   - NoeNarrativeSelf 章节化: `appendChapter()` 写 `output/noe-narrative/chapter-NNN.json` (`src/context/NoeNarrativeSelf.js`)
   ```

---

## 8. 验收与红线(全程)

### 8.1 8 周总验收(第 8 周末跑)

```bash
cd /Users/hxx/Desktop/Neo\ 贾维斯

# 1. 全套 verify
npm run verify:noe:full-current -- --include-managed
# 期望:11/11
npm run verify:noe:self-evolution
# 期望:207/207(原 2 fail 已通过 W2 拆分 + S0.7 抽取修)
npm run verify:handoff
# 期望:83/83
npm run audit:noe:self-evolution-completion -- --require-complete
# 期望:ok:true 11/11
npm run verify:noe:100-readiness
# 期望:score≥97,passed=true(2 blocker soak+settlements 8 周已解决)

# 2. 测试
npm test
# 期望:~4765 全绿
npm run test:e2e
npm run perf-check
# 期望:含 DMTS/WCST/recall benchmark 跑过

# 3. 凭据线
npm run noe:keys:model:check

# 4. 文件系统验
ls -la /Users/hxx/Desktop/Neo\ 贾维斯/docs/ADR/ | grep "2026-06-14"
# 期望:8 个 ADR(7 决策 + 1 删 NoeAffectHealth)
ls -la /Users/hxx/Desktop/Neo\ 贾维斯/output/noe-metrics/
ls -la /Users/hxx/Desktop/Neo\ 贾维斯/output/noe-narrative/
ls -la /Users/hxx/Desktop/Neo\ 贾维斯/output/noe-missions/
ls -la /Users/hxx/Desktop/Neo\ 贾维斯/output/noe-identity/
ls -la /Users/hxx/Desktop/Neo\ 贾维斯/output/noe-model-health/
ls -la /Users/hxx/Desktop/Neo\ 贾维斯/output/noe-prefetch/

# 5. 源码结构
wc -l /Users/hxx/Desktop/Neo\ 贾维斯/src/room/CrossVerifyDispatcher.js
# 期望:< 1000

# 6. .env 锁
grep "NOE_AUTONOMY_PROFILE=off" /Users/hxx/Desktop/Neo\ 贾维斯/.env

# 7. ADR 引用链
grep -l "ADR-2026-06-12" /Users/hxx/Desktop/Neo\ 贾维斯/CLAUDE.md
# 期望:命中

# 8. 8 周活数据
cat /Users/hxx/Desktop/Neo\ 贾维斯/output/noe-curiosity-yield/live-stats.md
# 期望:source=surprise 目标不再恒 0,转化率 ≥ 5%
```

**通过条件**:
- 8/8 verify 全绿
- ~4765 tests 全绿
- `verify:noe:100-readiness` score ≥ 97,passed=true
- 8 个新 ADR 存在
- 6 个新 output 目录有数据
- CrossVerifyDispatcher 拆分后单文件 < 1000 行
- 整合度 jsonl 有 ≥ 1000 行(8 周 × 7 天 × ~18 次/天)
- source=surprise 转化率 ≥ 5%

### 8.2 红线(全程不动)

**遵守(工程真实性)**: 不打印/写入/commit secret 原值 · 记忆/向量/状态写入保持正确 · 改公共文件单 writer

**放开(开发者姿态)**: commit / push / amend / reset · 改任何源码包括 `server.js` 2546 行 · 跑 51835 live / 重启 51835 · 改 CLAUDE.md / AGENTS.md · 拆超 500 行大文件 · kill 任何进程 / 改 cron · 自我进化真跑 / 浏览器完整操控 / env 觉醒能力默认开 / 权重微调试不试

### 8.3 失败回滚

每小任务可逆:
- 未 commit:`git checkout -- <file>` 或 `git stash`
- 已 commit:`git reset --hard HEAD~1`(OWNER 批准时才 commit)
- 大改动回滚:`git revert <commit>`(生成新 commit 反向操作)

---

## 9. 8 周时间线总览

| 周 | 阶段 | 核心动作 | 关键产物 | 风险 |
|---|---|---|---|---|
| **W0**(本周) | 阶段 0 + 4 个 P0 + W1 部分 | S0.1-S0.8 + P0-A/B/C + W1 整合度 | ollama 活 / 7 ADR / mind.html 2 新区 / 整合度 jsonl | 零(接线增强) |
| **W1** | W1 完 + W2 部分 | 整合度接 heart 完成 + narrative 真装 + tensions 实现 + CrossVerifyDispatcher 拆分启动 | `output/noe-narrative/` + `src/identity/NoeIdentityTensions.js` | 中(拆分 3304 行) |
| **W2** | 阶段 1 部分 + W2 完 | GWT novelty 升级 + sleep-time 另一半 + Crawl4AI + DeepResearcher Reflexion + CrossVerifyDispatcher 拆完 | novel 语义相似度 + sleep-time 跑通 | 中(推理时算力) |
| **W3** | 阶段 1 完 + W3 | DeepResearcher Reflexion 完 + W3 Mission Runner | Mission Runner PoC | 中高(自治执行起步) |
| **W4** | 阶段 2 部分 + W4 | GEPA 参数自进化 + RPT 真递归 + 主动推理 PP-2 + 100-readiness 重测 + narrative 章节化 | GEPA 跑通 + chapter 持久化 | 中高(自进化引入) |
| **W5** | 阶段 2 部分 | runtimeVerify 能力回归门 + SkillExtractor 自验证 | recall 基准回归 | 中(防退化) |
| **W6** | 阶段 2 完 + 阶段 3 部分 | SkillExtractor 完 + 达尔文 archive 多踏脚石分叉 | 多踏脚石分叉 | 中高(archive 谱系) |
| **W7** | 阶段 3 部分 | 浏览器 ReAct loop + DMTS/WCST 意识能力基准 + 客观内省探针 | ReAct 跑通 + 意识能力可测 | 高(浏览器完整操控) |
| **W8** | 阶段 3 完 + 总验收 | research MCP server + 缺口→自进化自动桥 + 8 周总验收 | 100-readiness score≥97,passed=true | 高(自进化真跑) |

---

## 10. 给 Claude 执行的最小可执行清单(20 条按周排列)

```
# 第 0 周
T1. 写 8 个 ADR 决策(A1-A7 合并 1 文件,删 NoeAffectHealth 1 文件)
T2. 跑 ollama serve + ollama pull qwen3-embedding:0.6b + huihui_ai/qwen3.5-abliterated:9b
T3. 改 .env 加 NOE_AUTONOMY_PROFILE=off
T4. S0.1 写 NoeModelHealthProbe.js + noeLocalModelHealth.js 路由 + mind.html 区
T5. S0.2 接 NoeCuriosityDecompose 进 DriveSystem + GoalSystem
T6. S0.3 接 NoeThoughtLoopGuard 进 Workspace + InnerMonologue
T7. S0.4 写 noe-curiosity-yield-live.mjs + mind.html 区
T8. S0.5 EmbeddingProvider 三级降级 + holdout 语义分
T9. S0.6 跑 noe-llm-bench 落档
T10. S0.7 抽魔数 env 注入
T11. S0.8 docker run SearXNG
T12. P0-A 改 CLAUDE.md 加 §0 + 接手必读
T13. P0-B 删 NoeAffectHealth 4 处
T14. W1 开始:NoeIntegrationMetric 接 NoeHeartbeat.mesoTick
T15. W1 开始:NoeNarrativeSelf 真装 server boot
T16. W1 完:noe-narrative-routes.js 路由 + mind.html 区

# 第 1 周
T17. W2: 写 NoeIdentityTensions.js + tests
T18. W2: 拆 CrossVerifyDispatcher 3304 → 4 文件
T19. S1.1 GWT novelty 升级 embedding
T20. S1.2 写 NoeSleepTimeCompute.js 空闲做消化+预计算
T21. S1.3 Crawl4AI 三级降级
T22. S1.4 DeepResearcher Reflexion

# 第 2 周
T23. S2.1 NoeReflectiveTuner GEPA PoC
T24. S2.2 RPT 真递归
T25. S2.3 主动推理 PP-2 回路
T26. W3: 写 NoeMissionRunner.js PoC
T27. W3: 写 NoeBootSelfCheck 7 条 policy 测试

# 第 3 周
T28. S2.4 runtimeVerify 能力回归门
T29. S2.5 SkillExtractor 自验证
T30. S3.1 NoeEvolutionArchive 达尔文多踏脚石分叉
T31. W4: 写 NoeIdentityValues.js
T32. W4: NoeNarrativeSelf 章节化 appendChapter

# 第 4 周
T33. S3.2 NoeBrowserAgent ReAct loop
T34. S3.3 DMTS/WCST 意识能力基准
T35. S3.4 客观内省探针

# 第 5 周
T36. S3.5 research MCP server
T37. S3.6 缺口→自进化自动桥
T38. 8 周总验收跑全套 verify
T39. CHANGELOG Unreleased 段完整记录
T40. HANDOFF 段写明"8 周完成,100-readiness score≥97,passed=true"
```

每个 T = 一个完整 task(自检 §2.1 5 步走通才算完)。T1-T40 完成后:
- 8 周长周期落档
- verify:noe:100-readiness score≥97,passed=true
- 10000+ 行活证据(8 周 × 每天写 output)
- 自我进化真跑 1 cycle 通过
- 浏览器 ReAct 跑通 1 任务
- 7 大研究维度从"成熟/部分"评级有 50% 跳到"持续运行已证明"

---

> **8 周后(2026-08-09 预计)**: Neo 从"能力造好测试全绿"跨越到"活着的曲线已证明"。这个报告本身作为 §2.4「8 周路线图档案」永久落档 `docs/8周长周期方案_2026-06-14.md`,**任何后续窗口接手都先读本文件 + 5 模块审计综合报告 + RESEARCH 报告**。
