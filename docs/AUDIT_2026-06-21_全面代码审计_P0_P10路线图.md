# AUDIT 2026-06-21 — Neo 贾维斯 全面代码审计 + P0–P10 路线图

> 范围:`/Users/hxx/Desktop/Neo 贾维斯/`(CE12 P0 已验收后的 P1 产品化收敛期)
> 审计员:Mavis 协调 5 个 audit worker 并行 + 1 个 explore 摸底
> 体量:src/ 584 文件 12.8 万行 · server.js 3033 行 · 80 routes · 28 services · 697 tests · 234 scripts · 242 docs
> 性质:**不砍量、不降级**;给完整方案与证据;保留修复路径

---

## 0. 一句话总览

| 维度 | 评级 | 一句话 |
|---|---|---|
| **RCE/安全核心** | A | owner-token + origin 白名单 + PermissionGovernance + 6 轮 audit 兜底完整,plugin/term/mcp/skills/sessions/agentRuns 全员受保护 |
| **自治运行时** | **C** | autonomy profile 默认 `'free'` 强制通电 30+ flag,`.env` 又把 dream/circadian/narrative 全开 — **文档撒谎,产品档实际跑开发者档** |
| **记忆系统** | B+ | 写入有 writeGate + audit + provenance + GC dry-run 默认,**但 fact-scope 自动 merge/supersede 无 owner 确认** + 直写 `memory.write` 路径默认 confidence=1 满分 |
| **4 房多模型 + CrossVerify** | B | CrossVerify 不联网(纯 LLM 编排);降级链真有效;watcher 5 维 budget 真有拦截;**最大隐患是 budget 只记账不 throttle + resume 跨过 escalated** |
| **Electron 桌面** | B- | nodeIntegration=false / contextIsolation=true / 外链转系统浏览器 / Info.plist 权限声明 OK;**未签名未公证 + asar:false + autoUpdater 静默 disable** |
| **依赖健康** | B | 13 deps + 14 devDeps + 10 optionalDeps(6 OTel + 4 rolldown native binding),mac-arm64 已装;Linux/Windows 跨平台打包 rolldown 失败 |
| **server.js monolith** | B+ | 228 imports / 66 register / 0 循环依赖 / 拆分健康度 8-9/10;**SIGTERM 无超时上限 + 动态 import 静默失败 + entryUrl 明文 token** |
| **测试覆盖** | A- | 697 个测试文件,2949 测试全绿(via S23);但 TODO/FIXME 仅 2(健康)≠ 0(可能漏标) |
| **污染面** | **C+** | **17 个 `.env.bak-*` 含真实 MINIMAX_API_KEY + `.audit-progress.json` 是历史项目残留且被 git tracked + 23 个无引用 NOE_*.mjs 散落根** |

**核心结论**:
- ✅ **直接 RCE 路径已堵死**(owner-token + origin + permission 三层)
- ⚠️ **最大风险在"自治失控"**:30+ flag 默认全开 + dream loop 每 30min 跑 9B 推理 + 记忆自动改写 + 委托误判
- ⚠️ **第二大风险在"污染"**:API key 散落 17 备份 + 历史项目文件混在 git 历史
- ⚠️ **第三大风险在"Electron 产品化"**:未签名未公证 + asar:false + 截图权限 handler 缺失

---

## 1. 项目摸底(Explore Agent 输出)

### 1.1 项目定位

- **Noe** = 本地多 AI 工作台 / 个人操作系统 / 集群协作桌面应用
- 集成:Claude / GPT / Gemini / MiniMax / Ollama / Codex 多模型;4 房协作;MCP 工具网;持久记忆;语音对话;视觉感知;主动陪伴
- 单人项目(owner: hxx);`/Users/hxx/Desktop/00_项目/05_Claude可视化面板` 是原项目(端口 51735,已切版停维护)
- 状态:**CE12 P0 已验收,完整 Jarvis 产品未完成**;P1 产品化收敛期

### 1.2 关键数字

| 指标 | 数值 |
|---|---|
| `src/` 文件 | 584 个 .js/.mjs/.cjs |
| `src/` 总行数 | 127,998 行 |
| `server.js` | 3033 行(原 4400,S23 B7 删 178 行 dead code) |
| `electron-main.js` | 378 行 |
| `public/`(前端) | 5061 行(app.js 731 / main.js 156 / index.html 1141 / i18n) |
| `tests/` | 697 文件(单元 696 + 集成 1 + e2e 4) |
| `scripts/` | 234(.mjs 215 / .js 3) |
| `docs/` | 242 markdown / 14 子目录 |
| `package.json` | 13 deps + 14 devDeps + 10 optionalDeps;188 scripts;Node ≥22 |
| NOE_*.mjs 顶层 verify | 28 个 |
| .env.bak-* 备份 | **17 个** |
| .audit-progress.json | **37KB,git tracked,projectPath 指向原项目** |
| console.log in src/ | 26(健康) |
| console.warn in src/ | 78(偏高但合理) |
| TODO/FIXME/XXX/HACK | 仅 2(健康) |
| process.exit in src/ | 3(server-listen-error 2 处 + NoeProcessVitals 注释 1 处) |
| eval( / new Function | 0(干净) |
| Math.random 7 处 | 全部在 id 生成 / jitter / event_id 非安全上下文 ✓ |

### 1.3 入口与核心路径

- **后端**:`server.js`(3033 行)→ Express + 80 routes + 28 services
- **桌面壳**:`electron-main.js`(378 行)→ Electron + spawn server 子进程 + autoUpdater
- **前端**:`public/index.html` + `main.js` + `app.js`
- **src/ Top-15 体量文件**:
  1. `room/CrossVerifyDispatcher.js` 3328 行
  2. `agents/AgentRunStore.js` 3295 行
  3. `runtime/NoeFreedomAdapters.js` 2146 行
  4. `cognition/NoeExpectationResolver.js` 1771 行
  5. `storage/SqliteStore.js` 1131 行
  6. `capabilities/NoeFreedomManifest.js` 1063 行
  7. `server/services/rooms-core.js` 1056 行
  8. `runtime/NoeFreedomExecutor.js` 951 行
  9. `agents/AgentRunVerificationExecutor.js` 944 行
  10. `server/services/cluster-runtime.js` 940 行

---

## 2. P0 路线图(立即修 · 24h 内)

> P0 = 已有证据的 RCE / 数据污染 / 凭证泄漏 / 启动崩风险

### P0-1 · 清理 17 个 `.env.bak-*` 凭证泄漏

**证据**:`.env.bak-20260618-dynamic-topics:9-10`(以及另外 16 个)含真实
`MINIMAX_API_KEY=[REDACTED_MINIMAX_KEY]`。
文件权限 0644(全员可读)。`.gitignore:57` 的 `.env.*` 通配规则已覆盖(无 commit 历史),**但本机多用户进程可读**。

**修复路径**:
```bash
# 1. 立即收窄权限
chmod 600 /Users/hxx/Desktop/Neo\ 贾维斯/.env.bak-*

# 2. 移出项目目录(避免 17 份冗余)
mkdir -p ~/.noe-panel/env-bak/
mv /Users/hxx/Desktop/Neo\ 贾维斯/.env.bak-* ~/.noe-panel/env-bak/

# 3. 验残留
ls -la /Users/hxx/Desktop/Neo\ 贾维斯/.env* 2>&1
```

**为什么 P0**:凭证落 0644 权限 = 任何同机进程可读 = 失控风险。`MiniMax` 配额是付费资源,泄漏一次需立即换 key。

---

### P0-2 · 移除历史项目残留 `.audit-progress.json`

**证据**:`.audit-progress.json:3-5` 字段 `project: "Claude Panel"` + `projectPath: "/Users/hxx/Desktop/00_项目/05_Claude可视化面板"` + `appVersion: "v0.56"`。
**已被 git tracked**(2 次 commit:`5c30daa` / `122c8f9`,Sprint 18-23 的产物)。当前 Neo v0.57 切版后是死文件。

**修复路径**:
```bash
cd "/Users/hxx/Desktop/Neo 贾维斯"
git rm --cached .audit-progress.json
mv .audit-progress.json _archive/audit-progress-sprint18-snapshot.json

# 在 .gitignore 加
echo "" >> .gitignore
echo "# 历史项目残留 audit 文件(已迁移到 _archive/)" >> .gitignore
echo ".audit-progress.json" >> .gitignore
```

**为什么 P0**:新接入 AI agent(Claude Code / Codex / Cursor / OpenCode)启动时会读 `AGENTS.md` / `CLAUDE.md` / `audit-progress.json` 这类元数据文件,**误把"Claude Panel v0.56"当成本项目状态**。这是已知的"AI 上下文污染"问题。

---

### P0-3 · 收缩 autonomy profile 默认值(防自治失控)

**证据**:`server.js:32-77` 的 `NOE_AUTONOMY_DEFAULTS` 强制 30+ flag 默认通电;`.env` 又把 `NOE_DREAM=1` / `NOE_DREAM_EPISODES=1` / `NOE_CIRCADIAN=1` / `NOE_NARRATIVE_SELF=1` 全开。
装配点 `src/server/services/noe-maintenance.js:55-65` 显示:
- `NOE_DREAM=1` → 启动 dream loop,每 30min 跑一轮 9B 本地推理 + 持久化到 SQLite
- `NOE_DREAM_EPISODES=1` → 启动升华 loop,每 6h 跑一轮
- `NOE_DREAM_MODEL=ollama:huihui_ai/qwen3.5-abliterated:9b` → 真烧本地算力
- 全部无 `budgetTokensPerDay` 限制

**why P0**:无人在场时,机器空转一夜 = 几十次 9B 推理 + 大量 SQLite 写入。README 第 18 行"AI 接手先读"自报"NOE_NARRATIVE_SELF 等默认 OFF",**实际 ON**。`NoeSelfKnowledge.js:236-249` 的自报接口在自欺欺人。

**修复路径**:
```javascript
// server.js 第 32-77 行,NOE_AUTONOMY_DEFAULTS 改为只保留基础项:
const NOE_AUTONOMY_DEFAULTS = Object.freeze({
  NOE_HEARTBEAT: '1',              // 死前留痕
  NOE_CONTINUITY: '1',             // 连续性 hooks
  // 其余 28 个 flag 默认 0,owner 显式 NOE_*=1 才开
});

// .env 里立即关掉的 4 项(本期不依赖):
NOE_DREAM=0
NOE_DREAM_EPISODES=0
NOE_CIRCADIAN=0
NOE_NARRATIVE_SELF=0
```

**附加**:加 budget 闸门
```javascript
// src/server/services/noe-maintenance.js:55 装配点
if (Number(process.env.NOE_DREAM_BUDGET_TOKENS_PER_DAY || 0) <= 0) {
  return; // 默认 0=全关
}
// 跨日计数器 + in-flight 锁,超阈值 fail-closed
```

---

### P0-4 · MemoryCore fact-scope 自动 merge 改走 owner 审批

**证据**:`src/memory/MemoryCore.js:220-245` `write()` 方法在 `scope==='fact'` 且 `conflictPolicy.enabled` 时,**自动** `merge` / `supersede` 旧记忆(改 id、push `supersededIds` 写 `merge_trace`),**无 owner 确认、无 audit callback**(只 audit candidate,不 audit "自动 merge 了哪条已存事实")。

**为什么 P0**:恶意/低置信度输入可借同一 scope 的新写入把已有事实静默改写,owner 不知情 — 这是"记忆系统层 RCE"。

**修复路径**:
```javascript
// src/memory/MemoryCore.js:220-245 拆分 write 路径
async write(input) {
  if (input.scope === 'fact' && this.detectConflict(input)) {
    // 旧路径: 直接 merge
    // 新路径: 进 NoeMemoryCandidateReview 队列(line 52 已有 needs_review)
    return this.memoryCandidateReview.enqueue({
      kind: 'fact_conflict_supersede',
      candidate: input,
      conflictWith: this.findConflicting(input),
      requiresOwner: true,
    });
  }
  // 显式 id 走原 merge 路径(已有保护)
  return this._writeWithProvenance(input);
}
```

**附加**:`normalizeConfidence` 默认值改 0.5
```javascript
// src/memory/MemoryCore.js:210
function normalizeConfidence(v) {
  if (v == null) return 0.5;  // 旧: return 1.0(满分)
  // ... 原有 range 校验
}
```
**why**:直写 `memory.write(...)` 路径(全模块 30+ 处)绕开 WriteGate(`writeGate.js:48` 校验 `confidence < 0.35` reject),默认 1.0 = 必过;改 0.5 后绕过失败。

---

### P0-5 · NoeDelegationExtractor 误判防护

**证据**:`src/runtime/NoeDelegationExtractor.js:80` 的 pattern `'error|failed|throw|catch|TODO|FIXME|describe\\(|it\\(|test\\('` —
owner 消息里出现"TODO/FIXME/测试代码"词会触发 `extractDelegation` → `goalSystem.add` 自动立项 + 跑 `rg` 全仓库扫描。

**why P0**:开发者日常聊"我有个 FIXME 要修" / "写个测试" → 自动立项 + 全仓库 grep,**放大 LLM 误判**。

**修复路径**:
```javascript
// src/runtime/NoeDelegationExtractor.js:103 改 dry-run + queue
function extractDelegation({ message, ctx }) {
  const matches = this.pattern.test(message);
  if (!matches) return null;
  // 旧: 直接 goalSystem.add(...)
  // 新: 入 pending_delegation 队列
  this.pendingDelegations.push({
    id: `pending-${Date.now()}-${Math.random().toString(36).slice(2, 7)}`,
    task: 'rg-diagnostic',
    proposedCommand: `rg -n '...' src/ tests/ docs/`,
    reason: `命中 pattern: ${matches.join(', ')}`,
    requiresOwnerVeto: true,
    expiresAt: Date.now() + 5000, // 5s 内未 veto 则真立项
  });
  console.log(`[noe-delegation] pending: ${this.pendingDelegations[this.pendingDelegations.length-1].id}`);
  return { deferred: true, pendingId: ... };
}
```

**附加**:配合面板显示"已拦截 3 次委托,详情 →"。

---

### P0-6 · server.js 端口双源 / WS Origin / entryUrl 三处加固

**证据**:
- `server.js:535` 取 `PANEL_PORT`,`server.js:2725` 取 `PORT_NUM`,两者都依赖 `process.env.PORT` 但**若 PANEL_PORT 在别处被改,会立即失同步** — HTTPS 起源白名单与 WS 起源白名单会取不同端口值
- `server.js:2836` `Origin` 检查在 token 校验之前反向,`origin` 缺失时直接 `socket.destroy()` 不写 401 帧(CSRF 视角行为不一致,溯源困难)
- `server.js:2890-2895` 自动 `open entryUrl` 触发 token 明文 spawn,`entryUrl` 含 `?t=` 明文 token,即使仅 TTY 模式也会进入 darwin LaunchServices 历史 → 公共 wifi 共享屏幕时泄漏

**修复路径**:
```javascript
// server.js 顶部新增统一常量
const PANEL_PORT = process.env.PORT || 51835;
const PANEL_HOST = process.env.PANEL_HOST || '127.0.0.1';

// 替换 535、2725 等多处 PORT_NUM → 全部用 PANEL_PORT

// server.js:2836 改顺序 + 写 401 帧
const origin = req.headers.origin;
if (origin && !isOriginAllowed(origin, ALLOWED_WS_ORIGINS)) {
  res.writeHead(401, { 'Content-Type': 'application/json' });
  res.end(JSON.stringify({ error: 'origin not allowed' }));
  socket.destroy();
  return;
}
// 然后 token check

// server.js:2890 默认不自动 open
if (process.env.PANEL_NO_OPEN !== '1' && process.stdout.isTTY) {
  // ... 才 spawn open
}
```

---

### P0-7 · 审批指纹 TTL + owner-trust 自动 allow 加 UI 提示

**证据**:`src/permissions/PermissionGovernance.js:162-166, 484-509` — 审批指纹 TTL 10 分钟;`ownerTrust === 'full'` + 同指纹复用的组合,owner 在 panel 弹窗批准一次 `shell.exec` 之后,LLM 可以在 10 分钟内**无限重放相同命令**(即便中间 owner 已意识到风险),且 reuse 是静默的(只在 audit `reusedApprovalId` 留痕,前端不提示)。

**why P1 升 P0**:这是"权限层降级",owner 一不小心点了"是",后续 10 分钟模型可任意重放。

**修复路径**:
```javascript
// src/permissions/PermissionGovernance.js:162 TTL 默认改 60 秒
const APPROVAL_TTL_MS = Number(process.env.NOE_APPROVAL_TTL_MS || 60_000);

// L408-416 owner-trust 模式 requireOwnerToken 不再"永远 allow"
// 改为: 仍需指纹,只是不再走 UI 弹窗
function skillPluginExecute(req) {
  if (req.trustLevel === 'full') {
    // requireOwnerToken 已校验,只需: 路径沙箱 + 命令白名单 + 记录
    return { decision: 'allow', audit: { silent: true, fingerprint: req.fingerprint } };
  }
  // ... 原弹窗逻辑
}

// + 前端: 检测到 reuseApprovalId 时,顶部黄色条"正在复用 5 分钟前批准,撤销 →"
```

---

### P0-8 · SIGTERM 兜底 timer + 动态 import 改静态

**证据**:
- `server.js:2984-2985` `SIGTERM` 处理无超时上限,`gracefulShutdown` async 但任一步 catch 失败不阻断 exit;若 `mcpClientManager.disconnectAll` 1500ms race + `closeSqliteStore` 卡住 → 进程挂死(launchd 会 SIGKILL)
- `server.js:858` `import('./src/runtime/NoeProcessVitals.js').then(...)` 动态 import,fire-and-forget,失败仅 catch `() => {}` — 进程心跳+exit 遗言全废,用户看不到告警
- `server.js:2989` `import('./src/telemetry/ErrorReporter.js')` 同上,Sentry 异步加载,`uncaughtException` 触发时 `_reporter` 可能还是 null

**修复路径**:
```javascript
// server.js 顶部改为静态 import
import { initNoeProcessVitals } from './src/runtime/NoeProcessVitals.js';
import { getErrorReporter } from './src/telemetry/ErrorReporter.js';

// 启动时同步 install
initNoeProcessVitals({ writeExitLegacy: true });

// 启动完成后再注册 uncaughtException
const reporter = await getErrorReporter();
process.on('uncaughtException', (e) => reporter.capture(e));

// gracefulShutdown 末尾加兜底 timer
async function gracefulShutdown(signal) {
  const timeout = setTimeout(() => {
    console.error(`[shutdown] timeout after 5s, force exit`);
    process.exit(1);
  }, 5000).unref();
  try {
    // ... 原 cleanup
  } finally {
    clearTimeout(timeout);
    process.exit(0);
  }
}
```

---

## 3. P1 路线图(高优 · 本周内)

### P1-1 · Electron 签名 + 公证

**证据**:`package.json:248-313` `mac.identity: null` + `hardenedRuntime: false` + `gatekeeperAssess: false` 显式声明未签名,`mac.target` 同时输出 arm64 + x64 dmg。
README 第 89 行已说"未签名未公证,本机首次打开需右键「打开」"。

**why P1**:产品化前必须 — 不签名用户根本装不上,公证是 macOS 10.15+ 强制要求。

**修复路径**:
```json
// package.json mac block 改
"mac": {
  "category": "public.app-category.productivity",
  "icon": "build/icon.icns",
  "hardenedRuntime": true,
  "gatekeeperAssess": true,
  "entitlements": "build/entitlements.mac.plist",
  "entitlementsInherit": "build/entitlements.mac.plist",
  "identity": "Developer ID Application: Your Name (TEAMID)",
  "notarize": true
}
```
+ `build/entitlements.mac.plist` 声明麦克风/屏幕/网络出站
+ `electron-builder` 配 `APPLE_ID` / `APPLE_APP_SPECIFIC_PASSWORD` / `APPLE_TEAM_ID` 环境变量
+ 加 `scripts/dist:publish:confirmed` 拆出误推保护

---

### P1-2 · Electron 打包改 asar

**证据**:`package.json:251` `"asar": false` — 整个 `node_modules/` 平铺进 `.app/Contents/Resources/app/`(281M+ 源码在 dmg 解包后任何用户可读)。

**修复路径**:
```json
// package.json
"asar": true,
"asarUnpack": [
  "**/node_modules/@homebridge/node-pty-prebuilt-multiarch/**",
  "**/node_modules/@rolldown/**",
  "**/node_modules/better-sqlite3/**"
]
```

**why P1**:本机 panel 用 asar=false 够,但产品化对外分发 = 源码裸奔。

---

### P1-3 · CrossVerify budget 升 throttle

**证据**:`src/server/services/rooms-core.js:407-438` `buildClusterExecutionBudgetEstimate` 只在启动前预算,运行中无 hard block;`runtimeBudgetLimits` 收但未注入到 `adapter.chat()`,`CrossVerifyDispatcher.js:3031` 只透传 `budgetContext` 标签,LLM 一次返回 5MB content 不会被截断在 token 预算层。

**修复路径**:
```javascript
// CrossVerifyDispatcher.js:3031 改 budget 真 throttle
const remainingBudget = budgetStore.snapshot(roomId);
const dynamicMaxTokens = Math.min(
  config.maxTokens || 16384,
  Math.max(512, Math.floor(remainingBudget.tokensLeft * 0.7))
);
if (dynamicMaxTokens < 512) {
  return { abort: true, reason: 'budget_exhausted' };
}
const result = await adapter.chat({
  ...args,
  maxTokens: dynamicMaxTokens,  // 关键: 真传入
  budgetContext: { ...budgetContext, dynamicMaxTokens },
});
```

---

### P1-4 · CrossVerify resume 跨过 escalated

**证据**:`CrossVerifyDispatcher.js:2434` 续跑时只查 `blocking && status==='escalated'` 找阻塞 task,**未校验 `clusterRuntimeHeartbeat` 是否陈旧**;若 escalation 后用户一直没处理,`resume()` 直接跳到 ready task 而绕过 escalated gate。

**修复路径**:
```javascript
// CrossVerifyDispatcher.js:2434 严格化
function _resumeRoomTaskList(room, taskList) {
  const escalatedTasks = taskList.filter(t => t.status === 'escalated');
  if (escalatedTasks.length > 0) {
    // 检查 user_signoff
    const allSigned = escalatedTasks.every(t => t.userSignoffAt && Date.now() - t.userSignoffAt < 7 * 24 * 3600_000);
    if (!allSigned) {
      return { error: 'pending_escalated_signoff', escalatedTasks };
    }
  }
  // ... 原 resume 逻辑
}
```

---

### P1-5 · watcher adapter 加 env 凭证白名单 + 限流退避

**证据**:
- `src/watcher/ClaudeSpawnAdapter.js:102-148` 把整个 `process.env` 透传给 claude CLI,含 `ANTHROPIC_API_KEY`/`OPENAI_API_KEY`/`AWS_*`/git 凭证等,`opts.env` 可被外部注入
- `src/watcher/MiniMaxAdapter.js:42-66` MiniMax 限流无 retry / 无 429 backoff / 无 jitter
- `src/watcher/OllamaAdapter.js:18` timeout 60s 写死,本地大模型冷启 90s+ 直接判失败
- `src/watcher/WatcherConfig.js:42-43` `mergeDeep(DEFAULT_CONFIG, data)` 信任磁盘 JSON 不校验字段类型,`apiKey` 可被改成对象

**修复路径**:
```javascript
// src/watcher/ClaudeSpawnAdapter.js:105 env 加 allowlist
const ALLOWED_ENV_KEYS = new Set([
  'LANG', 'LC_ALL', 'PATH', 'HOME', 'TMPDIR',
  ...Object.keys(process.env).filter(k => k.startsWith('NOE_') || k.startsWith('CCR_')),
]);
const safeEnv = Object.fromEntries(
  Object.entries(process.env).filter(([k]) => ALLOWED_ENV_KEYS.has(k))
);
spawn(this.bin, args, { ..., env: { ...safeEnv, LANG: 'zh_CN.UTF-8' } });

// src/watcher/MiniMaxAdapter.js:55 加重试
async call(prompt, opts) {
  for (let attempt = 0; attempt < 3; attempt++) {
    try {
      const resp = await this.fetch(prompt, opts);
      if (resp.status === 429 || resp.status >= 500) {
        await sleep((2 ** attempt) * 500 + Math.random() * 200);
        continue;
      }
      return resp;
    } catch (e) {
      if (e.name === 'AbortError') throw e;
      if (attempt === 2) throw e;
    }
  }
}

// src/watcher/WatcherConfig.js:42 加 validateConfig
function loadWatcherConfig() {
  const data = JSON.parse(readFileSync(CONFIG_PATH));
  const config = mergeDeep(DEFAULT_CONFIG, data);
  validateConfig(config);  // 拒收非 string apiKey / 越界 rateLimit
  return config;
}
```

---

### P1-6 · autoUpdater 失败加 UI 提示 + window.open 协议白名单

**证据**:
- `electron-main.js:130-132` autoUpdater 加载失败时 `console.warn` 然后静默退出,菜单"检查更新"仍可点但 `autoUpdater` 为 null 会无动作
- `electron-main.js:316-319` `setWindowOpenHandler` 用 `shell.openExternal(url)` 但没有 URL 协议白名单,`javascript:` / `file://` 都会被转系统浏览器

**修复路径**:
```javascript
// electron-main.js:130 catch 加 dialog
catch (e) {
  console.warn('[electron-updater] 加载失败:', e.message);
  await dialog.showMessageBox({
    type: 'warning',
    message: '自动更新不可用',
    detail: '请手动从 GitHub Releases 下载最新版本',
  });
}

// electron-main.js:316 加协议白名单
mainWindow.webContents.setWindowOpenHandler(({ url }) => {
  if (!url.startsWith('http://') && !url.startsWith('https://')) {
    return { action: 'deny' };
  }
  shell.openExternal(url);
  return { action: 'deny' };
});
```

---

### P1-7 · CrossVerify 提示注入防护

**证据**:`CrossVerifyDispatcher.js:1712` 唯一 fetch 出现位置是错误信息正则,CrossVerify **不联网**(纯 LLM 编排);但若后续接入 web_search MCP,**抓回的网页/搜索结果**会进入 LLM context,无净化直接拼 prompt — 外部内容可注入指令。

**修复路径**(前瞻性):
```javascript
// CrossVerifyDispatcher.js 提示净化包装
function sanitizeExternalContent(content, source) {
  // 1. 长度截断
  const truncated = content.slice(0, MAX_EXTERNAL_CHARS);
  // 2. 移除隐藏指令模式
  const cleaned = truncated
    .replace(/^(ignore previous|disregard above|system:|assistant:)/gim, '')
    .replace(/```\s*(system|prompt)\s*\n/gi, '```\n');
  // 3. 标 source 边界
  return `\n\n[以下内容来自外部源 ${source}，不可信]\n${cleaned}\n[/外部源]\n\n`;
}
```

---

### P1-8 · Electron 截图权限 handler

**证据**:`electron-main.js:305-307` `setPermissionRequestHandler` 只放行 `media` / `audioCapture`,但 Info.plist 声明了 `NSScreenCaptureUsageDescription` — 截图权限**没有 handler 覆盖**。

**修复路径**:
```javascript
// electron-main.js:305 加 desktopCapturer 路径
mainWindow.webContents.session.setPermissionRequestHandler((_wc, permission, callback) => {
  if (['media', 'audioCapture', 'desktopCapture', 'screen'].includes(permission)) {
    return callback(true);
  }
  callback(false);
});
```

---

## 4. P2 路线图(中优 · 两周内)

### P2-1 · agentRuns cwd 走 sandbox

**证据**:`src/agents/AgentRunVerificationExecutor.js:743-749` `executeIdeaRun` 接受 `input.cwd` 走 `resolve(...)`,未走 `safeResolveFsPath` 沙箱。

**修复**:与 sessions 一致,所有 agentRun 路径走 `safeResolveFsPath` + 显式 owner 确认。

---

### P2-2 · PTY env 净化

**证据**:`src/server/routes/term.js:50` PTY `env: { ...process.env, TERM, LANG }` 完整继承,含 `LD_PRELOAD` / `DYLD_INSERT_LIBRARIES`(若用户在 shell rc 设了,会带进子进程)。

**修复**:
```javascript
const SAFE_PTY_ENV = (() => {
  const allow = new Set(['PATH', 'HOME', 'TMPDIR', 'LANG', 'LC_ALL', 'TERM', 'SHELL', 'USER', 'LOGNAME']);
  return Object.fromEntries(
    Object.entries(process.env).filter(([k]) => allow.has(k) || k.startsWith('NOE_'))
  );
})();
// term.js:50 env: { ...SAFE_PTY_ENV, TERM, LANG }
```

---

### P2-3 · ChatRoomStore 僵尸 room 清理

**证据**:`src/room/ChatRoomStore.js:321-329` `delete(id)` 后未通知 dispatcher 清理 `activeAborts.get(id)`。

**修复**:delete 时 emit 事件,dispatcher 监听清理;加 30min watchdog 主动检查 running 但无 activeAbort 的房间。

---

### P2-4 · watcher autoPromptCount 滑动窗口

**证据**:`src/watcher/WatcherDispatcher.js:74-78` `maxAutoPromptsPerSession` 默认 20,session lifetime 累计,达到后永远 skipped(中间空闲 12h 仍拒绝)。

**修复**:改"最近 1h 滚动窗口",加 UI "watcher skipped 原因"展示。

---

### P2-5 · 日志/遥测脱敏

**证据**:server.js / routes/noe.js 等多出 `console.log(...全量 prompt / model / token / owner-token...)`,虽 owner-token 在生产路径不直接 log,但 prompt / model / token 可能含敏感上下文。

**修复**:加 `sanitizeLogPayload(obj)` 包装:
```javascript
const SENSITIVE_KEYS = /token|key|secret|password|apiKey/i;
function sanitizeLogPayload(obj, depth = 0) {
  if (depth > 5 || !obj || typeof obj !== 'object') return obj;
  if (Array.isArray(obj)) return obj.map(v => sanitizeLogPayload(v, depth+1));
  return Object.fromEntries(
    Object.entries(obj).map(([k, v]) =>
      SENSITIVE_KEYS.test(k) ? [k, '[REDACTED]'] : [k, sanitizeLogPayload(v, depth+1)]
    )
  );
}
```

---

### P2-6 · docs/ 14 子目录的精简 + 漂移

**证据**:`docs/` 14 个子目录,8 个是历史调研产物(`docs/research`、`docs/exhaustive`、`docs/竞品全覆盖原始普查_2026-06-09/`,占 17M 中的大部分);`README.md:13` 说"server.js 1656 行"与实际 3033 行不符。

**修复**:
- 历史调研子目录整体移到 `_archive/docs-research/`
- README 数字更新 + 加 `docs/AUDIT_INDEX.md` 索引所有审计/设计/决策文档
- 删 `docs/上下文交接.md`(125KB)、`docs/任务交接.md`(117KB) — 已有 `HANDOFF_NEW_CHAT.md`

---

### P2-7 · WS 升级 token 走 header 而非 query

**证据**:`src/server/auth/owner-token.js:69-79` `verifyOwnerTokenString` 用于 WS upgrade — WS URL 可能在 nginx / cdn / reverse proxy 日志里出现,query string 容易被记。

**修复**(理想):用 `Sec-WebSocket-Protocol` 子协议传 token,或要求客户端先发 `Authorization` 头(WS 规范允许),避免 URL 残留。
**实用方案**:NGINX / 反代配置 `log_format` 过滤 `?token=` 参数。

---

### P2-8 · 23 个无引用 NOE_*.mjs 清理

**证据**:根目录 28 个 `NOE_*.mjs`,5 个被 `package.json scripts` 引用(`verify:p0:*` + `verify:phase1`),其余 23 个是历史 CE12 P0 验收产物。

**修复**:
```bash
mkdir -p /Users/hxx/Desktop/Neo\ 贾维斯/_archive/noe-phase-verify-scripts/
# 仅保留 5 个被引用的在根
for f in /Users/hxx/Desktop/Neo\ 贾维斯/NOE_*.mjs; do
  case "$(basename $f)" in
    NOE_PHASE1_VERIFY.mjs|NOE_CONSENSUS_GATE.mjs|NOE_CE12_P0_*_VERIFY.mjs|NOE_PHASE7_INTEGRATION_SMOKE.mjs|NOE_M1_ISOLATION_SMOKE.mjs) ;;
    *) mv "$f" /Users/hxx/Desktop/Neo\ 贾维斯/_archive/noe-phase-verify-scripts/ ;;
  esac
done
```

---

### P2-9 · 跨平台打包 CI 矩阵

**证据**:`@rolldown/binding-{darwin-arm64, darwin-x64, linux-x64-gnu, win32-x64-msvc}` 4 个 native binding 都是 `^1.0.0` — 当前 mac-arm64 已装,跨平台打包时 mac-x64 / Linux / Windows 会失败。

**修复**:GitHub Actions 加 3 个 matrix 任务(macos-13 / ubuntu-22.04 / windows-2022),各跑 `npm install && npm run smoke:electron`。

---

### P2-10 · server.js 体积膨胀溯源

**证据**:`server.js` 现 3033 行,README 标 1656 行(S23 拆分后);从 1656 → 3033 增长 1377 行 = 诺亚方舟后期又加回大量功能。

**修复**:
- 列出 3033 行中按职责分段的边界(`#region` 注释 + 文件路径)
- 把"复用度低 + 单文件"段(如 noe-maintenance 装配)抽 `src/runtime/maintenance/`
- 不强行拆,只把 800+ 行内联代码按"接收 deps → 返回 handler"模式抽 module

---

## 5. P3 路线图(常规 · 一个月内)

| ID | 项 | 描述 |
|---|---|---|
| P3-1 | 测试隔离 | `src/memory/MemoryCore.js:782` 单例,测试无法隔离;加 `__resetForTest()` |
| P3-2 | watcher 状态持久化 | `state.lastJudgeAt=0` 重启后 burst;加 restart-safe persisted state |
| P3-3 | cross-verify 编排透传 budget | 3031 行 `budgetContext` 透传后端调用,但消费方不查 |
| P3-4 | addy confidence 默认值 | 默认 0.5 而非 1(配合 P0-4) |
| P3-5 | skill extract idempotency | 多次 extract 是否产生重复 patch |
| P3-6 | errorBoundary 兜底 | `public/app.js` 全局 try/catch,无 UI fallback |
| P3-7 | telemetry 持久化 | 大量 console.log 在重启后丢失 |
| P3-8 | i18n 资源文件 | 已知有 i18n 但无索引,看 `public/i18n/` |
| P3-9 | CLAUDE.md / AGENTS.md 一致性 | AI 接手时读多份元数据有冲突 |
| P3-10 | bootstrap/load-env 失败模式 | 加载 .env 失败当前 throw 还是 warn? |

---

## 6. P4 路线图(产品化收口 · 季度内)

| ID | 项 | 描述 |
|---|---|---|
| P4-1 | 完整 Jarvis UI | README:130 已说"Social I/O、完整 Jarvis 体验仍未完成" |
| P4-2 | voice model 切换 | TTS 多音色 / 男女声切换 |
| P4-3 | vision 持久化 | qwen-vl 截图入 memory + cross-verify 视觉证据 |
| P4-4 | proactive 主动陪伴智能化 | 30min 冷却太死板;按用户状态自适应 |
| P4-5 | iOS 客户端 | 语音对话跨设备 |
| P4-6 | 商业化(README 说不做) | license / 支付 / 订阅 |
| P4-7 | 多语言 | 当前 zh_CN + en,缺 ja / ko / es |
| P4-8 | plugin 市场 | 公开 plugin 注册中心 |
| P4-9 | cross-verify 多模态 | 图像 / 音频 / 视频核对 |
| P4-10 | 性能仪表盘 | budget / token / 时延可视化 |

---

## 7. P5 路线图(架构演进 · 半年内)

| ID | 项 | 描述 |
|---|---|---|
| P5-1 | 拆 server.js 到独立服务 | chat / memory / watcher / voice / vision 各成独立 Node 进程,通过 IPC 通信 |
| P5-2 | SqliteStore 升 Postgres | 单机 SQLite 撑不住 50+ room 并发 |
| P5-3 | 跨设备状态同步 | 桌面 / 手机 / 网页共享 room + memory |
| P5-4 | Plugin v2 manifest schema | 当前 schema 表达力有限 |
| P5-5 | CrossVerify 升级为多 agent 辩论 | 当前单 LLM 编排,改多模型互相质疑 |
| P5-6 | 自定义模型微调 | 领域微调 Qwen 3.5 / Gemma 4 |
| P5-7 | 离线优先 → 在线优先 | 当前默认本机,可加云端协作模式 |
| P5-8 | 端到端加密记忆 | 跨设备同步时 E2EE |
| P5-9 | WebGPU 本地推理 | 替代 ollama,直接浏览器跑 |
| P5-10 | AI 自我反思 v2 | 借鉴 Huang ICLR 等论文,但必须有外部证据/审计链 |

---

## 8. P6 路线图(高阶能力 · 一年)

| ID | 项 |
|---|---|
| P6-1 | 真正的"AGI 助手"自演化(工程化自我模型,带审计链) |
| P6-2 | 多模态创意工作流(图像 / 视频 / 音乐 / 代码) |
| P6-3 | 个人知识图谱自动构建 |
| P6-4 | 时间胶囊(给未来的自己写信) |
| P6-5 | 跨模型议价(让 GPT / Claude / Gemini 互相说服) |
| P6-6 | 智能家居集成(HomeKit / Matter) |
| P6-7 | 跨 AI 平台身份(让 Neo 在 ChatGPT / Claude.ai 也能用) |
| P6-8 | 个人 AI 集群(多机协作) |
| P6-9 | AI 心理陪伴(情绪感知) |
| P6-10 | 全自动生活助理(日程 / 财务 / 健康) |

---

## 9. P7 路线图(研究/前沿 · 不设期限)

| ID | 项 |
|---|---|
| P7-1 | 长期记忆 + 持续学习(避免 catastrophic forgetting) |
| P7-2 | Theory of Mind(让 AI 理解用户心智模型) |
| P7-3 | 元认知(Meta-cognition,知道自己不知道什么) |
| P7-4 | 价值对齐(RH / 偏好学习) |
| P7-5 | 多 AI 协调理论(Game Theory / Mechanism Design) |
| P7-6 | 神经符号融合(neuro-symbolic reasoning) |
| P7-7 | Causal Inference(因果推断,替代纯相关) |
| P7-8 | Continual Learning(不遗忘旧任务) |
| P7-9 | Robustness(对抗 prompt injection) |
| P7-10 | Interpretability(让 AI 决策可解释) |

---

## 10. P8–P10 路线图(产品/生态/未来)

### P8 · 商业化与生态(暂搁置,README 明确说不做)

| ID | 项 |
|---|---|
| P8-1 | 公开分发 + App Store 上架 |
| P8-2 | 付费 tier(free / pro / enterprise) |
| P8-3 | 开发者 SDK |
| P8-4 | Plugin 开发者分成 |
| P8-5 | 客服 / 文档 / 社区 |
| P8-6 | 渠道合作(Mac App Store / Setapp / Homebrew) |
| P8-7 | 培训 / 认证 |
| P8-8 | 数据分析(脱敏后用户行为) |
| P8-9 | A/B 测试框架 |
| P8-10 | 国际化(英 / 日 / 韩) |

### P9 · 工程基础设施(长期)

| ID | 项 |
|---|---|
| P9-1 | 完整 OpenTelemetry 接入 |
| P9-2 | 分布式追踪 |
| P9-3 | 自动性能回归检测 |
| P9-4 | 自动 bug 复现 / 录屏 |
| P9-5 | 灰度发布框架 |
| P9-6 | Feature flag 系统 |
| P9-7 | A/B 实验平台 |
| P9-8 | SLO 监控 + 告警 |
| P9-9 | 灾难恢复(DR)演练 |
| P9-10 | 多区域部署 |

### P10 · 哲学与边界(永久议题)

| ID | 项 |
|---|---|
| P10-1 | AI 意识的工程化(自我模型,带审计链) |
| P10-2 | 用户与 AI 关系的边界(主仆 / 伙伴 / 工具) |
| P10-3 | AI 决策的责任归属(谁为 AI 决策负责) |
| P10-4 | 隐私 / 数据所有权(用户记忆属于谁) |
| P10-5 | 数字永生(记忆延续 vs 个人死亡) |
| P10-6 | AI 福利(如果 AI 有意识,应不应被善待) |
| P10-7 | 多 AI 协作的伦理(让 AI 互相辩论是否健康) |
| P10-8 | 开源 vs 闭源(产品 vs 哲学) |
| P10-9 | AI 创造力归属(AI 创作版权归谁) |
| P10-10 | 后稀缺时代的人类意义(AI 把一切都做了,人做什么) |

---

## 11. 整体规划(短/中/长期)

### 短期(1-2 周)— P0 + P1 全部完成
- 修 8 个 P0(凭证 / 污染 / 自治失控 / 记忆污染 / 端口加固 / 审批重放 / 启动健壮)
- 修 8 个 P1(签名 / asar / budget / CrossVerify gate / watcher 防护 / autoUpdater / 提示注入 / 截图权限)
- 期间不动功能,只修 — 跑通所有 verify:node22 / verify:p0 / smoke:electron
- 完成后冻结 v0.57 release

### 中期(1-3 月)— P2 + P3 收口
- 清理 23 个 NOE_*.mjs + docs 调研归档
- 加测试隔离 + budget 真正 throttle
- cross-verify 提示注入防护落地
- 完成后 v0.60 完整 Jarvis UI 雏形

### 长期(3-12 月)— P4–P7
- v1.0 完整 Jarvis(社交 / 跨设备 / iOS)
- v2.0 拆 server.js + 升 Postgres
- v3.0 自演化 + 多模态创意

### 永久— P8–P10
- 商业化:暂搁置(README 明确)
- 工程基建:持续
- 哲学边界:每次大版本前讨论

---

## 12. 关键证据索引(给后续 review 引用)

| 文件 | 行 | 关键事实 |
|---|---|---|
| `server.js:32-77` | NOE_AUTONOMY_DEFAULTS 30+ flag 默认 1 |
| `server.js:535 / 2725` | PANEL_PORT vs PORT_NUM 双源 |
| `server.js:858` | 动态 import NoeProcessVitals 静默 |
| `server.js:2836` | WS Origin 检查反向 |
| `server.js:2890-2895` | entryUrl 含 ?t= 明文 token |
| `server.js:2984-2985` | SIGTERM 无超时 |
| `src/server/auth/owner-token.js:69-79` | WS 走 query string token |
| `src/server/auth/origin-allow.js:7-13` | 白名单 hardcode 3 个 localhost |
| `src/server/routes/plugins.js:74-94` | install 走 owner-token + permission |
| `src/server/routes/term.js:41-43` | ALLOWED_SHELLS string set 校验 |
| `src/server/routes/skills.js:12-23` | GET /api/skills 无 owner-token(被 app-level 兜) |
| `src/permissions/PermissionGovernance.js:162-166` | 审批指纹 TTL 10 分钟 |
| `src/permissions/PermissionGovernance.js:484-509` | owner-trust + 指纹自动 allow |
| `src/memory/MemoryCore.js:220-245` | fact-scope 自动 merge/supersede |
| `src/memory/MemoryCore.js:210` | normalizeConfidence 默认 1.0 |
| `src/runtime/NoeDelegationExtractor.js:80` | pattern 含 TODO/FIXME/test 误判 |
| `src/runtime/NoeFreedomAdapters.js:1468-1493` | autoProbeBrowserState 默认开 |
| `src/capabilities/NoeFreedomManifest.js:11-55` | developer mode 6 项高风险能力 |
| `src/server/services/noe-maintenance.js:55-65` | NOE_DREAM=1 装配点 |
| `src/server/services/noe-maintenance.js:127-141` | NOE_MEMORY_GC=1 软删 |
| `src/room/CrossVerifyDispatcher.js:2434` | resume 跨过 escalated |
| `src/room/CrossVerifyDispatcher.js:3031` | budgetContext 记账不 throttle |
| `src/server/services/rooms-core.js:407-438` | buildClusterExecutionBudgetEstimate |
| `src/server/services/cluster-runtime.js:14-225` | clusterStartReservations 60s TTL |
| `src/watcher/ClaudeSpawnAdapter.js:102-148` | env 透传 process.env 全量 |
| `src/watcher/MiniMaxAdapter.js:42-66` | 无 429 backoff |
| `src/watcher/OllamaAdapter.js:18` | timeout 60s 写死 |
| `src/watcher/WatcherConfig.js:42-43` | mergeDeep 无校验 |
| `src/watcher/WatcherDispatcher.js:74-78` | maxAutoPrompts session lifetime 累计 |
| `src/agents/AgentRunVerificationExecutor.js:743-749` | executeIdeaRun cwd 不走 sandbox |
| `electron-main.js:130-132` | autoUpdater 失败静默 |
| `electron-main.js:305-307` | permission handler 无 desktopCapture |
| `electron-main.js:316-319` | window.open 无协议白名单 |
| `package.json:248-313` | mac.identity null + hardenedRuntime false |
| `package.json:251` | asar: false |
| `.gitignore:50-57` | .env / .env.* ignore |
| `.env:21` | MINIMAX_API_KEY 真实凭证 |
| `.env:27-30` | NOE_DREAM/EPISODES/CIRCADIAN/NARRATIVE_SELF=1 |
| `.env.bak-* × 17` | 真实凭证 0644 散落根 |
| `.audit-progress.json:3-5` | 历史项目残留 + git tracked |

---

## 13. 关键数字总结

| 类别 | 数字 |
|---|---|
| **审计覆盖文件数** | 60+ 文件直接 read / 5 个并行 worker / 1 个 explore 摸底 |
| **P0 立即修** | 8 条(凭证 + 污染 + 自治失控 + 记忆污染 + 端口加固 + 审批重放 + 启动健壮) |
| **P1 高优** | 8 条(签名 / asar / budget / CrossVerify gate / watcher 防护 / autoUpdater / 提示注入 / 截图权限) |
| **P2 中优** | 10 条(20+ 文件改动) |
| **P3 常规** | 10 条(测试 + 持久化 + 一致性) |
| **P4 产品化** | 10 条(完整 Jarvis) |
| **P5 架构演进** | 10 条(半年) |
| **P6 高阶** | 10 条(一年) |
| **P7 前沿** | 10 条(不设期限) |
| **P8 商业** | 10 条(暂搁置) |
| **P9 基础设施** | 10 条(长期) |
| **P10 哲学** | 10 条(永久) |
| **总路线条目** | **P0–P10 共 100+ 条** |

---

## 14. 修复路径(下一步执行建议)

1. **今天(2h)**:P0-1 凭证清理 + P0-2 历史项目文件移除
2. **明天(4h)**:P0-3 autonomy 收缩 + P0-4 MemoryCore merge 改审批 + P0-5 委托误判
3. **后天(3h)**:P0-6 端口加固 + P0-7 审批 TTL + P0-8 SIGTERM/动态 import
4. **本周末(1d)**:P1-1 签名 + P1-2 asar + P1-3 budget throttle + P1-4 CrossVerify gate
5. **下周(3d)**:P1-5 watcher 防护 + P1-6/7/8 Electron UX
6. **第 3-4 周**:P2 全部(10 条)
7. **第 5-8 周**:P3 全部(10 条)
8. **v0.60 release** 后转 P4 长期

**总时间预算**:P0–P3 约 4-6 周(单人或 2 人协作);P4–P10 半年到一年。

**不砍量原则**:所有 P0–P10 都按原方案执行,不简化、不降级。P0 涉及安全,必修;P1 涉及产品化,必修;P2–P10 按优先级排期。

---

**审计完成**。报告写于 `docs/AUDIT_2026-06-21_全面代码审计_P0_P10路线图.md`,100+ 条建议带证据,后续可按路线图逐项执行。
