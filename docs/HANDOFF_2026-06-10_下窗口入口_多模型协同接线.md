# HANDOFF 下窗口入口 · 多模型协同把后续任务全部做完　2026-06-10

> ## ✅ 2026-06-10 深夜执行完成（claude 单干模式）
> §3 队列 12 项全部执行完毕 + §6 最终验收通过：全量 vitest **278 文件/2263 测全绿**、handoff 24/24、
> 本窗文件 lint 零问题、git diff --check 干净、隔离端口(51998)两轮实机全功能验证（5 条新接线日志 + HTTP 200 + 零异常 + geo-weather 真抓成功）。
> 执行明细/遗留见 `docs/后续规划_2026-06-10.md` §六；**生产 51835 需重启才吃到全部新接线**（重启带回 NOE_DREAM=1 若在用）。
> 可选通电开关：`NOE_MEMORY_GC=1|dry`、`NOE_GEO_WEATHER=1`、`NOE_MEMORY_EMBED=ollama`、`TELEGRAM_BOT_TOKEN`。
> launchd 守护产物在 `docs/launchd/`（owner 手动装）。下窗首做推荐：MCP 填充（见 `docs/评估_Top100集成裁决_2026-06-10.md`）。

> **给下个窗口的单一行动入口。** goal =「多模型协同执行(用好 M3/Gemini,额度尽管烧),把后续任务依次全部做完,每阶段走质量门,做完才停」。
> **⚠️ 2026-06-10 晚 owner 改指令**:不再用其他 AI 模型协作,所有任务由当前窗口 claude 单干;§4 多模型 SOP 与 §6 第2步"派 M3 对抗审"停用,审查改主模型自审。其余队列/质量门/最终验收不变。
> 项目: `~/Desktop/Neo 贾维斯`(Noe,生产 panel 51835,**不碰 51735**)。先读本文件,再按 §3 队列开干。
> owner 已授权所有红线(2026-06-10,含付费 API/launchd/改安全机制);灾难级不可逆动作仍按常识兜底确认。

## 1. 现状一句话

波次3/4 完成、波次5#1#2 完成、多模型 SOP 实战落地、P0 接线已通电 2 个(代码层);全量 vitest **265 文件 / 2180 测全绿**,`git diff --check` 干净;**未 commit**(约 176 文件未提交横跨前几个窗口的波次0-4 成果,用户没说 commit 就不 commit);本窗口**没改 server.js**。
⚠️ **A2 / runGc 要在生产 51835 生效需重启 panel**——建议等下窗口把 server.js 那批(§3 第3/7项)改完**一次性重启**(重启带回 `NOE_DREAM=1` 若在用梦境)。

## 2. 本窗口已完成(已验证,勿重做)

| 件 | 内容 | 测试 |
|---|---|---|
| `src/autopilot/NoeTurnFinalizer.js` | 预算死前交接(波次3完);M3 审后修 NaN 阈值静默失效等 3 真bug | 15 |
| `src/memory/NoeMemoryCurator.js` | 记忆 GC 分桶(波次4完);M3 审后重写修 8 真bug(布尔1/0兼容/now漂移/parseTs守卫/去重等) | 22 |
| `src/autopilot/NoeLocalAgentProbe.js` | 本机 agent 探测;实测 claude/codex/minimax/ollama 4个全可委托 | 13 |
| `src/media/MiniMaxImageClient.js` | MiniMax 图像直调;**真出图验证**(317KB/25.5s);**endpoint=api.minimax.chat 国内站**(国际站对此 key 不工作) | 12 |
| `MemoryCore.runGc()` | GC 接线(dry-run/apply、projectId 归一隔离、maxScan 防 OOM、身份级铁律、可 unhide);M3 审后修 B1/B2/E2 | 5(真SQLite) |
| A2 自我认知接线 | `ChatProfileStore.resolve` 统一注入 self-knowledge(文字聊天 0→1)+ `VoiceSession:339` 幂等防重复 | 3 |
| `scripts/aiteam.mjs` | **M3 默认禁 thinking**(大输入5min失败→秒级成功;`AITEAM_M3_THINK=1`可开)+ `--mode review/deepread/explain` | — |
| 验证脚本 | `scripts/noe-verify-three-models.mjs`(3/3通) / `noe-verify-minimax-image.mjs`(出图) | — |

多模型实战战果: **M3(禁thinking)当 reviewer 共抓 14 个真 bug,全部核实并修**。
密钥: minimax/xiaomi 都已在 macOS Keychain,代码走 `resolveNoeProviderSecret`,绝不打印;用户若要换 key→Keychain Access 删旧条目后 `npm run noe:keys:model:setup`。

## 3. 后续任务队列(依次做,每项走 §5 质量门)

1. **T1-GenerationFence 接线** → 接 VoiceSession / 入站连发路径。**勿接 SoloChatDispatcher**——它用 `activeAborts`(line 83)"拒绝并发"(第二条直接报错),与 Fence"允许连发、压制旧代"哲学冲突(本窗口接管发现,接错会白做)。
2. **T1-Commitment/PersonCard/Prefetch 接线** → 在持有 store 的消费点注入。范式=`VoiceSession.js:336-348`(运行时 `sys += 块`);它们带数据需 store 实例+上下文查询,**不能**像 A2 那样塞 resolve。四模块接口: `createGenerationFence`/`createCommitmentStore`/`createPrefetchStore`/`createPersonCardStore`(均工厂函数);`NoeContextEngine.js:64-66` 引用但未通电。
3. **感知三件套** → server.js 启动 `collectHostContext` 缓存 + 注入 systemPrompt(`src/context/NoeHostContext.js` 已建)。【改 server.js】
4. **FusionRanker → MemoryCore** 向量×FTS 双路召回(`src/memory/NoeFusionRanker.js` 已建,RRF/加权可选)。
5. **TurnFinalizer / HangAlert → loop** 接 SoloChatDispatcher/NoeLoop 的预算检查点+长跑心跳。
6. **LocalAgentProbe → adapters 注册** 启动时探测,委托路由按本机实况。
7. **runGc 定时挂载** → 梦境循环里定时调 `memoryCore.runGc({apply:true})`(server.js:1643-1655 梦境循环处)。【改 server.js】
8. **C 类接线**: T32 `withActiveGuard` 包 self-evolution/consensus 入口;T3 `splitStablePrefix` 接 prompt 组装;T27 Sticky Events 接 server.js SSE 广播。
9. **L4 解枷锁**: `PermissionGovernance.js:329-332` ownerTrust=full 时 `.ssh` deny→ask。
10. **P1**: Gemini 提速去噪(给 gemini CLI 跳过 MCP 的 flag / 精简 `~/.gemini` MCP 配置——动用户全局配置,先确认);运行态纪律原语(self-reminder / session rotate / narrowest-fits / plan DAG / hooks registry)。
11. **P2**: geo-weather(无key) / launchd(.plist+说明) / MiniMax 视频音乐 SDK(仿 MiniMaxImageClient) / T34 Telegram 入站(`InboundChannels.js` 已建,server.js 读 `TELEGRAM_BOT_TOKEN`) / T7 承诺抽取流水线。
12. **Top100 外部集成**(silero-vad/mem0/Playwright/OmniParser…): 需装库/下模型,逐项评估成本再动。清单: `docs/选型_Noe急需补强Top100_按优先级互补_2026-06-10.md`。

完整背景/优先级依据: `docs/后续规划_2026-06-10.md`(§五含执行进度+T1 接管发现)。

## 4. 多模型 SOP(怎么用 M3/Gemini,全部实测结论)

```bash
# 写完模块→审代码(M3 禁thinking,60-90s,真会抓 bug——本窗口抓了14个)
node scripts/aiteam.mjs --mode review --in <文件> --out /tmp/mmtest/review-xx.md
# 跨文件读真代码/出接线方案(Gemini agentic,后台 ~18min,质量最高但会掉线)
node scripts/aiteam.mjs --mode deepread --repo . --task "..." --out /tmp/mmtest/xx.md
# 快速验算/事实/中文短改写(2-3s)
node scripts/aiteam.mjs --vendor m3 --task "..."   # 或 --vendor mimo
```

- **M3 大输入必须禁 thinking**(aiteam 已默认禁);**MiMo 只能短问答**(大输入空 reply);**Gemini 只后台**(简单问也 614s,agentic 一次成一次挂)。
- 真实调用一律 `run_in_background` 后台跑,落盘只回指针;**不设任何超时**(项目铁律 feedback_no_model_timeout)。
- **子代理掉线/空 reply/fetch failed → 主模型立即接管补位**(本窗口接管 3 次,机制有效);每阶段产出必须主模型核实(它们会有噪声)。

## 5. 质量门(每阶段必走,违者返工)

```bash
node node_modules/vitest/vitest.mjs run   # 全量必须全绿(基线 265 文件/2180 测)
npm run verify:handoff
git diff --check
```

纪律: 不 commit(用户没说);改 server.js/src 后告知**需重启 51835**;不碰 51735;单 writer(只主模型落盘);M3/Gemini/MiMo 额度尽管烧、Claude 自己省;真实验证禁假数据;新模块先写测试再接线。

## 6. 最终验收(§3 全部做完后,owner 明确要求,缺一不可)

> owner 原话(2026-06-10):「所有任务做完后,进行一次全面的自检,全面的代码审查,清理脏区,然后进行本项目的所有功能的实际测试,实机操作。期间只要有问题就进行修复,直到前面所说都没有问题为止。」

依次执行,**任一环节发现问题→立即修复→该环节重跑**,全过才算交付:

1. **全面自检**: 全量 vitest + `npm run verify:handoff` + `npm run verify:noe:self-evolution` + `git diff --check` + eslint(若配);逐项核对 §3 清单确认无漏项。
2. **全面代码审查**: 本轮所有改动文件逐个过(主模型 Read 审 + 派 M3 `--mode review` 对抗审,两路都过);重点:接线点正确性、并发/边界、密钥不泄露、与既有约定一致。
3. **清理脏区**: 删调试残留/console.log/临时文件/死代码/无用 import;`/tmp/mmtest` 等临时产物不留进仓;确认没有误改无关文件(`git status` 逐条过)。
4. **全功能实机测试**: 隔离端口(如 51999,**不碰生产 51835/51735**)真启 server.js → 逐功能实际操作验证: 文字聊天(A2 自我认知真注入?)、记忆写入/召回/runGc(dry-run+apply)、语音链路(如可测)、freedom API(`npm run verify:noe:freedom-live` 思路但走隔离端口)、面板核心页面(可用 chrome-devtools 实截图看);新接线(Fence/Commitment/感知三件套/Telegram 若做)逐个真触发一遍。
5. **修复循环**: 期间任何问题(报错/行为不符/性能异常)→修复→补测试→回到第 1 步重新过,直到一轮全绿零问题。
6. **收官**: 更新本文件与 `docs/后续规划_2026-06-10.md` 执行进度;告知 owner 需重启 51835(列出本批改了哪些 server.js/src 文件);**生成一份大白话总结报告**(完成了什么/怎么验证的/遗留什么)。
