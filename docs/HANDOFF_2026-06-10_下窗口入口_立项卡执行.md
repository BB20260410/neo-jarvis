# HANDOFF 下窗口入口 · 立项卡执行　2026-06-10 07:47

<!-- ═══ 接手提示词（Codex / Claude 通用；以后每份新 HANDOFF 照抄这一块放开头） ═══ -->
> **你（接手的 AI）开工前 60 秒做三件事：**
> ① 读根目录 `AGENTS.md` 的「开发纪律宪法」（10 条，Codex 与 Claude 共同遵守，与任何旧文档冲突时以它为准）；
> ② `git status` + `git log --oneline -5` 看清当前态——**哪些文件可能正被另一个 AI 的窗口编辑（本项目常态是 Codex 和 Claude 并行开发），只动你任务域的文件**；
> ③ 确认质量门活着：`git config core.hooksPath` 应输出 `scripts/git-hooks`（空则跑 `git config core.hooksPath scripts/git-hooks` 激活）。
>
> **干活节奏**：领任务 → 小步改（新文件带 `// @ts-check`+注试+env 门控默认 OFF）→ 每个独立改动立即 commit（中文消息）→ 改完实机自测（隔离端口 51998，绝不碰 51835/51735）→ 里程碑 push（hook 自动全量测试把门）→ 更新 HANDOFF（已完成带证据/进行中/下一步/动过哪些公共文件）。
> **绝不**：凭印象断言机制存在（先 grep/实测）、让 owner 当测试员、绕过 hook 后不补测、在另一个 AI 活跃的文件上动刀。
<!-- ═══════════════════════════════════════════════════ -->


> **⛳ 2026-06-10 上午执行完毕：§2 队列五张卡全部做完**（明细见 §7 执行记录；生产 51835 重启后全部生效）。
> **给下个窗口：读完本文件直接从 §2 第 0 项开始干，不用问。** 单 writer、claude 单干（不派外部模型）。
> **owner 已授权**（2026-06-10）：所有红线放行，含 npm/pip 装新依赖、下模型——装前一句话报体积即可，不用等批。

## 1. 现状一句话（前两轮已完成，勿重做）

波次0-6 接线全部通电完毕：11 项功能接线（栅栏防连击/三store注入/感知三件套/双路召回/死前交接/卡死告警/agent探测/记忆GC/StickyEvents/L4/Gemini去噪）+ T34 Telegram + geo-weather + T7承诺抽取 + MiniMax 图像/视频/音乐三 client + ntfy 推手机 + MCP 配置填充（filesystem/memory/playwright 三 server，`~/.noe-panel/mcp-servers.json`，已备份 .bak-20260610）+ launchd 产物（docs/launchd/）。
**全量 vitest 280 文件 / 2275 测全绿**；两轮隔离端口实机验证通过。明细：`docs/后续规划_2026-06-10.md` §六。
⚠️ **生产 51835 还没重启**——重启后这些才在生产生效（带回 `NOE_DREAM=1` 若在用）。

## 2. 任务队列（依次做完；每项走 §4 质量门）

**第 0 项 · 生产验证**（条件触发：owner 重启 51835 后才做；没重启就提醒一句然后跳过先做第 1 项）
- 看启动日志含：`[noe-host-context]` `[noe-agent-probe]`（+开了开关才有的 `[noe-memory-gc]` 等）
- MCP 真连：调一次 panel 的 MCP 工具列表（`/api/mcp` 相关路由或 McpClientManager.listTools），确认 filesystem/memory/playwright 三个 server 首连成功、工具数 >0（lazy connect，首调才连；npx 首跑要下包，慢属正常）
- 文字聊天问一句"你有哪些能力/本机环境怎样"，验证 self-knowledge + host-context 注入生效

**第 1 项 · 卡① sherpa-onnx 语音实时化**（最高价值：治 3-7s 断顿）
**第 2 项 · 卡③ RapidOCR 屏幕读字**（最轻：pip ~60MB 含模型）
**第 3 项 · 卡④ Playwright**（先验证已挂的 playwright-mcp 形态够不够，不够再装完整包）
**第 4 项 · 卡② CosyVoice 本地中文 TTS**（最重 ~2.5GB，放后）
**第 5 项 · 卡⑤ session rotate 强制交接**（复用已建 TurnFinalizer/Compactor，前后端各半窗）

→ 每张卡的目标/步骤/验收/成本已写好：**`docs/立项卡_重依赖集成_2026-06-10.md`**（照卡执行）。
→ 全做完后执行 §5 终检，然后出大白话总结。

## 3. 关键认知（接手必读，省你踩坑）

- **VoiceSession 接新 STT/TTS 的契约**：`sttClient.transcribe(wavBuffer)→text`、`ttsClient.synthesize(text, opts)→{audioBuffer, format}`（仿 `LocalSttClient.js` / `MiniMaxTtsClient.js`）；`_pickTts` 在 `VoiceSession.js:165` 按语言分档，链尾加兜底即可。
- **新能力要登记 self-knowledge**：`src/context/NoeSelfKnowledge.js` 的 `noeCapabilities()` 加条目（只列真落地的），不然 Noe 不知道自己有这能力。
- **测试范式**：纯逻辑+注入式（fetch/spawn/时钟全可注入），真 SQLite 用 `initSqlite(tmp)`+`close()`（beforeEach/afterEach，见 `tests/unit/noe-memory-core-gc.test.js`）。**initSqlite 无参不会再切库**（已修+防回归测试，背景见 `sqlite-store-init-guard.test.js`）。
- **Python 件的接法**：venv 独立 + Node spawn 调 stdin/stdout（参考 `scripts/insightface-embed.py` 既有模式），不污染全局。
- **改 server.js 后**：`node --check` + 隔离端口 smoke（`PORT=51998 node server.js`，看日志杀进程，**别碰 51835/51735**），并在收尾告知 owner 需重启。
- ~~不 commit（owner 没说）~~ → 2026-06-10 宪法改小步 commit。多窗累积改动已由强健窗口统一收口提交（7b03d4d 含五张立项卡全部文件）；立项卡窗口补了尾巴 e977f53（sherpa 关 VAD 预处理防裁尾音）。树已干净。

## 4. 质量门（每项做完必走）

```bash
node node_modules/vitest/vitest.mjs run        # 全量必须全绿（基线 280 文件/2275 测）
npx eslint <本次新增/改动文件>                  # 零 error
npm run verify:handoff                          # 24/24
git diff --check
```
实机验收按各卡"验收"栏真测（语音延迟真听、OCR 真截屏识、TTS 真出声）——**自测过再汇报，绝不让 owner 当测试员**。

## 5. 终检（队列全空后）

全面自检（上面四连）→ 改动文件全自审 → 清脏（无调试残留/临时文件不进仓）→ 隔离端口全功能实机一轮 → 有问题修到零 → 更新本文件与 `后续规划_2026-06-10.md` 执行进度 → **大白话总结报告**。

## 6. 背景文档索引（要细节再读，平时不用）

- 两轮执行明细：`docs/后续规划_2026-06-10.md` §五§六
- Top100 逐簇裁决：`docs/评估_Top100集成裁决_2026-06-10.md`
- 多模型 SOP（已停用，owner 要求单干）：`docs/HANDOFF_2026-06-10_下窗口入口_多模型协同接线.md` §4
- 可选通电开关速查：`NOE_MEMORY_GC=1|dry` / `NOE_GEO_WEATHER=1` / `NOE_MEMORY_EMBED=ollama` / `TELEGRAM_BOT_TOKEN` / `NOE_NTFY_TOPIC`
