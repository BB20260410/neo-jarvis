# HANDOFF 2026-06-13 长期任务断点续跑交接

更新时间: 2026-06-13T18:59:04+08:00

## 1. 真实仓库与边界

- 真实仓库: `/Users/hxx/Desktop/Neo 贾维斯`
- 当前启动目录可能是 `/Users/hxx/Documents/Neo 2`，不要在该目录做 Neo 工作。
- 不读取、不打印、不复制 `.env`、API key、token、cookie、OAuth、owner-token 或 `~/.noe-panel/room-adapters.json`。
- 不碰 `51735`。`51835` 本轮只读确认，不重启、不 kill。
- 不 commit / push / reset / clean，不清理 dirty worktree。
- 不碰 `games/cartoon-apocalypse/**`。

建议下个窗口先执行:

```bash
pwd
git -C "/Users/hxx/Desktop/Neo 贾维斯" rev-parse --show-toplevel
git -C "/Users/hxx/Desktop/Neo 贾维斯" status --short --untracked-files=all | sed -n '1,120p'
git -C "/Users/hxx/Desktop/Neo 贾维斯" log -5 --oneline
cd "/Users/hxx/Desktop/Neo 贾维斯"
sed -n '1,220p' output/noe-long-task-followup/latest.md
```

## 2. 当前长期任务状态

权威断点文件:

- JSON: `output/noe-long-task-followup/latest.json`
- Markdown: `output/noe-long-task-followup/latest.md`
- run ledger: `output/noe-long-task-followup/runs.jsonl`

latest 已吸收的状态:

- generated local: `2026-06-13T18:54:10+08:00`
- status: `waiting_for_natural_judgement`
- action: `observe_next_natural_judgement_then_rerun_observation_status`
- launchd label: `com.noe.long-task-followup`
- launchd path: `/Users/hxx/Library/LaunchAgents/com.noe.long-task-followup.plist`
- launchd command: `cd "$HOME/Desktop/Neo 贾维斯" && exec npm run noe:long-task-followup`
- launchd stdout: `/tmp/noe-long-task-followup.launchd.log`
- launchd stderr: `/tmp/noe-long-task-followup.launchd.err.log`
- runs: `31`
- lastExitCode: `0`
- run interval: `900` seconds
- next expected run local: `2026-06-13T19:09:10+08:00`
- stale if no run after local: `2026-06-13T19:24:10+08:00`

完成门当前未通过:

- `canMarkComplete: false`
- `readyForNextStageReview: false`
- expectation: `11/20`，剩余 `9`
- soak: `4/7d`，剩余 `3d`
- HERMES: `16.44/24h`，剩余约 `7.56h`

当前 blockers:

- `expectation_calibration_pending`
- `hermes_background_audit_pending`
- `observation_window_not_elapsed`
- `not_enough_soak_evidence`
- `expectation_settlements_below_20`
- `insufficient_observation_window:16.44/24`

## 3. 本轮已做的推进

本轮确认长期任务是可断点续跑状态:

- `launchd` 自然 run 已从前序 `31` 之前连续增长到 `31`。
- `output/noe-long-task-followup/latest.md` 明确写出 safe resume 和下一窗口指令。
- 中断当前等待前，未手动运行 `npm run verify:noe:long-task-followup`，没有用手动 timestamp 代替自然 run。

本轮在 `latest` 之后又生成了待吸收证据:

- `npm run verify:noe:memory-provider-poc` 通过。
  - `NOE_LANCEDB_MEMORY` unset
  - `NOE_WIKI_MEMORY_PROVIDER` unset
  - `defaultChangesNoeBehavior: false`
- `npm run verify:noe:mission-runtime` 通过。
  - mission id: `p8-smoke-1781348265316`
  - finalization ref: `output/noe-missions/p8-smoke-1781348265316/artifacts/finalization-000003.json`
- `npm run verify:noe:hermes-background-audit` 通过。
  - latest generated: `2026-06-13T10:57:52.635Z`
  - observedHours: `16.704950849677193`
  - recordCount: `38`
  - status: `blocked`
  - blocker: `insufficient_observation_window:16.7/24`

注意: 上面 `16.70h` 是 HERMES audit 的最新值，但还没有被 `output/noe-long-task-followup/latest.json` 吸收。需要等下一次 launchd 自然 run。

## 4. 本轮验证结果

已通过:

```bash
npm run verify:noe:memory-provider-poc
npm run verify:noe:mission-runtime
npm run verify:noe:hermes-background-audit
npm run verify:handoff
npm run verify:noe:self-evolution
npm run test:p0:unit
git diff --check -- ':!games/cartoon-apocalypse/**'
```

关键结果:

- `verify:handoff`: `83 passed, 0 failed`
- `verify:noe:self-evolution`: `198 passed, 0 failed`
- `test:p0:unit`: `138` test files / `959` tests passed
- `git diff --check`: passed

## 5. 代码与工作区状态

相关源码 dirty 文件:

- `scripts/noe-long-task-followup.mjs`
- `tests/unit/noe-long-task-followup.test.js`
- `src/room/NoeConsensusRunner.js`

`src/room/NoeConsensusRunner.js` 本轮做过最小清理:

- 原因: `npm run verify:noe:self-evolution` 曾失败在 `runner_under_500_lines`。
- 处理: 只压缩空行/格式，把文件降到 `497` 行以内。
- 结果: `verify:noe:self-evolution` 复跑通过。

工作区还有大量其他 dirty 文件，视为既有改动；不要清理或回滚。

## 6. live / 端口 / 模型状态

本轮只读确认:

- `51835`: 有 node 进程监听，未重启、未 kill。
- `51735`: 有 node 进程监听，未触碰。
- `lms ps`: `qwen/qwen3.6-35b-a3b` loaded；本轮未 load/unload。

注意: 这些状态会漂移，下个窗口要重新只读确认。

## 7. 下个窗口执行顺序

1. 先读本文件和 `output/noe-long-task-followup/latest.md`。
2. 如果当前时间早于 `2026-06-13T19:09:10+08:00`，等待 launchd 自然 run；不要手动跑 follow-up。
3. 如果当前时间在 `19:09:10` 到 `19:24:10` 之间，先检查是否已自然 run 到 `runs: 32`。
4. 如果 `19:24:10` 后仍未增长到 `runs: 32`，再排查:

```bash
launchctl print gui/501/com.noe.long-task-followup 2>/dev/null | sed -n '1,120p'
stat -f '%Sm %z %N' -t '%Y-%m-%dT%H:%M:%S%z' /tmp/noe-long-task-followup.launchd.log /tmp/noe-long-task-followup.launchd.err.log
tail -n 5 output/noe-long-task-followup/runs.jsonl
```

5. 自然 run 成功后，期望看到:

- `runs: 32`
- `lastExitCode: 0`
- `completionGate.current.hermesObservedHours` 从 `16.44` 吸收到约 `16.70`

6. 再补一轮安全证据:

```bash
npm run verify:noe:memory-provider-poc
npm run verify:noe:mission-runtime
npm run verify:noe:hermes-background-audit
```

7. 等下一次 launchd 自然吸收。不要把 HERMES 未满 24h、soak 未满 7d、expectation 未满 20 的状态标 done。

## 8. 完成标准

只有同时满足以下条件，才能把长期任务标完成:

- `completionGate.canMarkComplete === true`
- `readyForNextStageReview === true`
- natural expectation resolved >= `20`
- soak active days >= `7`
- HERMES observed hours >= `24`
- `npm run test:p0:unit` 通过
- `npm run verify:handoff` 通过
- `npm run verify:noe:self-evolution` 通过
- `git diff --check -- ':!games/cartoon-apocalypse/**'` 通过

当前不满足完成标准，状态是继续观察与断点续跑。
